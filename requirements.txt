PyYAML>=6.0
tree-sitter>=0.22
tree-sitter-rust>=0.21
tree-sitter-move==0.0.2
tree-sitter-language-pack>=0.7

# Optional classifier helpers. Core audit/bootstrap flows do not require these,
# but the rejection-classifier training scripts use scikit-learn when present.
scikit-learn>=1.4
