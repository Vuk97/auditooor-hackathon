[package]
name = "reth-evm-fixture"
version = "0.0.1"
edition = "2021"

[dependencies]
# Mention reth so the workspace heuristic fires.
# (No real dep needed — fixture is text-only.)

[lib]
path = "src/lib.rs"
