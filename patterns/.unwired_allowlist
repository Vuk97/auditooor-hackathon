# Patterns that are documented in reference/patterns.dsl* but intentionally
# NOT wired into any runnable detector.
#
# Format: one pattern ID per line (matching the YAML filename stem).
# Lines starting with # are comments.  Blank lines are ignored.
#
# Add a pattern here when:
#   - status: documentation-only AND the text-pattern has no Solidity surface
#     (e.g. Rust-only patterns that cannot fire on Solidity targets)
#   - The pattern is superceded by a more general detector (note the reason)
#   - The pattern is in a DSL round marked as deprecated / held
#
# Do NOT add patterns here to hide wiring gaps — the allowlist is audited in
# the 47-test gate (test_detector_registry_completeness.py).
#
# ── Rust-only / non-Solidity patterns (r94_solodit_rust round) ─────────────
# These were mined from Rust findings and have no executable Solidity detector.
# They are documentation-only entries kept for pattern-library completeness.
asymmetric-liquidity-enables-flat-oracle-purchase-on-geometric-pool
