<!--
Skeleton: R46 Trusted-Infrastructure Tabulation (Rule 46, pre-submit Check #95).
REQUIRED for any HIGH+ draft whose exploit chain requires compromise of a
TRUSTED infrastructure component (oracle sidecar, proposer / sequencer
infra, validator-set node, signer node, RPC provider, off-chain dispatcher,
off-chain prover, MEV-share dispatcher, batcher, collator). R46 sits AFTER
R42 because R42 asks WHAT configured component the impact flows through,
R46 asks WHO holds the trusted role and whether that role's compromise is
OOS under program rules.

Placeholder syntax: <<key|default|description>>. Fill each <<...>> in.
-->

## Trusted Infrastructure Tabulation

Trusted Infrastructure Tabulation:
- Trusted component + protocol role: <<trusted_component_role|<component verbatim + role>|name the component verbatim and the role it plays - e.g. "Slinky oracle sidecar / price input", "op-batcher / state-root submission", "validator-set leader / block proposal">>
- Program OOS clause (verbatim quote + SEVERITY.md/SCOPE.md file:line): <<oos_clause_verbatim|<quote the exact OOS clause + file:line>|quote the exact OOS clause that covers compromise of this component. If no OOS clause covers it, state "no OOS clause" and the finding may be fileable>>
- Defense layer classification: <<defense_layer_class|primary|primary | defense-in-depth - is the trusted component the SOLE guard between attacker and impact, or one of several stacked guards?>>
- Non-trusted-compromise trigger present? <<non_trusted_trigger|no|yes | no - cite file:line evidence. Does the exploit also fire without the trusted-comp compromise? if yes, reframe around the non-trusted variant>>
- Verdict: <<r46_verdict|trusted-infra-walk-back|non-trusted-trigger-exists | trusted-infra-walk-back | OOS-as-cited | contested-OOS-with-rebuttal>>

<!-- WALK-BACK / OVERRIDE GUIDANCE (delete this comment block before paste):
- If trusted-infra is primary defense AND compromise is OOS: REQUIRED to
  walk back severity (Informative / Low), reframe around a non-trusted
  trigger, OR add `<!-- r46-rebuttal: <reason up to 200 chars> -->`
  citing why the OOS clause does not actually cover the contested behavior.
-->
