<!--
Skeleton: R52 Rubric Row Mapping (Rule 52 / L17 verbatim-or-drop).
REQUIRED for every submission. The severity rubric is fixed and pasted
into the workspace tooling (SEVERITY.md). It is the only severity
authority. There is NO out-of-band channel to negotiate exceptions.
This section pins the verbatim rubric row the finding maps to and
states the evidence-class match for the chosen tier.

Placeholder syntax: <<key|default|description>>. Fill each <<...>> in.
-->

## Rubric Row Mapping

Rubric Row Mapping:
- Workspace SEVERITY.md path: <<severity_md_path|<workspace>/SEVERITY.md|absolute path to the workspace's SEVERITY.md / scope-and-severity doc>>
- Selected tier: <<selected_tier|HIGH|LOW | MEDIUM | HIGH | CRITICAL - must match a tier listed verbatim in the rubric. Spark has CRIT-1, CRIT-2, HIGH-1 only - no Medium.>>
- Selected rubric row id (workspace-defined): <<rubric_row_id|<e.g. CRIT-1>|the internal label of the rubric row, e.g. CRIT-1 / HIGH-1 / sev-3 / Critical-direct-loss>>
- Selected impact (verbatim quote): <<selected_impact_verbatim|<quote the impact line verbatim>|quote the impact-line text EXACTLY as it appears in the workspace SEVERITY.md. Do not paraphrase. Verbatim grep must match.>>
- Rubric-row file:line: <<rubric_row_fileline|SEVERITY.md:<line>|cite the SEVERITY.md line number where the selected row sits>>
- Evidence-class match check: <<evidence_class_match|executed_poc - matches rubric requirement|state explicitly that the evidence class (executed_poc / source_only / hypothetical / modeled) is sufficient for the selected tier. If the rubric demands network-level evidence and the PoC is in-process only, the match FAILS - walk back tier or build the evidence.>>
- Build-or-drop disposition: <<build_or_drop|build-and-match|build-and-match | drop-no-match | hold-pending-additional-evidence. Per Rule 1 (L17), there is no fileable middle ground.>>

<!-- OVERRIDE: <!-- r52-rebuttal: <reason up to 200 chars> --> -->
