{
  "verdict": "INVALID (gated path)",
  "rule": "Sink is only reachable through onlyOwner; trusted_relayer modifier blocks the attacker.",
  "proof": "The caller must be the owner, so an external attacker cannot reach the sink."
}
