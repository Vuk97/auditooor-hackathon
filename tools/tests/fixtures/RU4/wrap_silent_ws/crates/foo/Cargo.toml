[package]
name = "foo"
version = "0.1.0"
edition = "2021"

# A member [profile.release] here is IGNORED by Cargo (root-only). If the
# resolver wrongly honored this it would mis-report the crate as panic-safe.
[profile.release]
overflow-checks = true
