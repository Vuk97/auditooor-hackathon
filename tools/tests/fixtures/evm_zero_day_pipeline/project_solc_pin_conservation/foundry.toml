[profile.default]
src = 'src'
test = 'test'
# Authoritative solc pin: differs from the source `^0.8.0` (which would resolve to
# the highest installed minor) AND from the svm highest. The authored test pragma
# MUST be pinned to this version so forge compiles under it.
solc = '0.8.21'
