// SPDX-License-Identifier: MIT
pragma solidity ^0.8.19;

/// @notice EXTERNAL-DEPENDENCY mock only (Rule 40: mocks replace external deps,
/// never the protocol-owned vulnerable path). The vault under test is real;
/// only the underlying token is mocked. Standard minimal ERC20 surface used by
/// the vault: balanceOf, approve, transfer, transferFrom, plus a test-only mint.
contract MockERC20 {
    mapping(address => uint256) public balanceOf;
    mapping(address => mapping(address => uint256)) public allowance;

    function mint(address to, uint256 amount) external {
        balanceOf[to] += amount;
    }

    function approve(address spender, uint256 amount) external returns (bool) {
        allowance[msg.sender][spender] = amount;
        return true;
    }

    function transfer(address to, uint256 amount) external returns (bool) {
        balanceOf[msg.sender] -= amount;
        balanceOf[to] += amount;
        return true;
    }

    function transferFrom(address from, address to, uint256 amount) external returns (bool) {
        if (allowance[from][msg.sender] != type(uint256).max) {
            allowance[from][msg.sender] -= amount;
        }
        balanceOf[from] -= amount;
        balanceOf[to] += amount;
        return true;
    }
}
