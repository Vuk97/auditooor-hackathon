skeleton: "name_match_missing_require"
name: "aave-v3-flashloan-callback-executeoperation-lacks-sender-validation"
class_name: "AaveV3FlashloanCallbackExecuteoperationLacksSenderValidation"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: aave-v3-flashloan-callback-execute-operation-lacks"
help: "Aave V3 Flashloan callback ExecuteOperation Lacks Sender Validation"
wiki_title: "Aave V3 Flashloan callback ExecuteOperation Lacks Sender Validation"
wiki_description: |
    The `executeOperation` function in some Aave V3's flashloan receiver contracts is vulnerable because it **does not verify** that the caller (`msg.sender`) is the legitimate Aave Pool contract. This critical missing check allows **any external address** to directly call `executeOperation` on a receiv
wiki_exploit_scenario: |
    Per Glider query tags [Aave, flashLoan, lack-of-access-control]: The `executeOperation` function in some Aave V3's flashloan receiver contracts is vulnerable because it **does not verify** that the caller (`msg.sender`) is the legitimate Aave Pool contract. This cr
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "AaveV3FlashloanCallbackExecuteoperationLacksSenderValidation"
fn_name_regex: ".*(executeOperation).*"
write_var_regex: ".*(executeope).*"
guard_var_regex: ".*(executeope).*"
vuln_fn_name: "executeOperation"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "executeope = newVal;"
guard_require_line: "require(newVal <= 10000);"
state_decl: "uint256 internal executeope;"
