skeleton: "name_match_missing_require"
name: "malformed-equate-statement"
class_name: "MalformedEquateStatement"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: malformed-equate-statement-fails-to-assign-state-c"
help: "Malformed Equate Statement"
wiki_title: "Malformed Equate Statement"
wiki_description: |
    A common pitfall is when a protocol utilizes == when = was intended, this can often cause severe logic bugs as the intended state change does not occur as x == y; does not perform any action (the bool return is not captured in a call), similarly if a condition such as msg.sender == owner is missing 
wiki_exploit_scenario: |
    Per Glider query tags [Logic Bug]: A common pitfall is when a protocol utilizes == when = was intended, this can often cause severe logic bugs as the intended state change does not occur as x == y; does not perform any action (the bool
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "MalformedEquateStatement"
fn_name_regex: ".*(malformed).*"
write_var_regex: ".*(malformed).*"
guard_var_regex: ".*(malformed).*"
vuln_fn_name: "setParam"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "malformed = newVal;"
guard_require_line: "require(newVal <= 10000);"
state_decl: "uint256 internal malformed;"
