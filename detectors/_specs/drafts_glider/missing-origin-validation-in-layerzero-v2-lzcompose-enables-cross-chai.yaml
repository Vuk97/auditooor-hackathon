skeleton: "highlevelcall_missing_sibling"
name: "missing-origin-validation-in-layerzero-v2-lzcompose-enables-cross-chai"
class_name: "MissingOriginValidationInLayerzeroV2LzcomposeEnablesCrossChai"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: missing-message-origin-validation-in-layer-zero-v2"
help: "Missing Origin Validation in LayerZero V2 lzCompose Enables Cross-Chain Message Spoofing"
wiki_title: "Missing Origin Validation in LayerZero V2 lzCompose Enables Cross-Chain Message Spoofing"
wiki_description: |
    This query detects **LayerZero V2 `lzCompose` implementations that fail to fully validate critical trust boundaries**, specifically: - Missing validation of the `from` parameter (expected to be the originating OFT / OApp contract) - Missing or incomplete enforcement of the trusted LayerZero Endpoint
wiki_exploit_scenario: |
    Per Glider query tags [layerzero, cross-chain, message-spoofing, lzcompose, access-control]: This query detects **LayerZero V2 `lzCompose` implementations that fail to fully validate critical trust boundaries**, specifically: - Missing validation of the `from` parameter (expected to be the or
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "MissingOriginValidationInLayerzeroV2LzcomposeEnablesCrossChai"
trigger_sig_regex: ".*(lzCompose).*"
required_sibling_regex: ".*(validate|check|verify|assert).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "lzCompose"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
state_decl: "mapping(address => uint256) internal balances;"
