skeleton: "name_match_missing_call"
name: "state-variable-shadowing"
class_name: "StateVariableShadowing"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: inherited-state-variables-shadowed"
help: "State Variable Shadowing"
wiki_title: "State Variable Shadowing"
wiki_description: |
    Detects when a contract declares a state variable with the same name as a base contract. Uses source code validation to filter out inherited variables (bypassing Glider's inheritance flattening behavior), ensuring only explicitly declared variables are checked.
wiki_exploit_scenario: |
    Per Glider query tags [shadowing, logic-error, inheritance, swc-119]: Detects when a contract declares a state variable with the same name as a base contract. Uses source code validation to filter out inherited variables (bypassing Glider's inheritance flattening behavi
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "StateVariableShadowing"
fn_name_regex: ".*(state).*"
read_var_regex: ".*(state).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "stateFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return state > 0;"
state_decl: "uint256 internal state;"
