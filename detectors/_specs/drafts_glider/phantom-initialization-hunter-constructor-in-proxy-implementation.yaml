skeleton: "name_match_missing_call"
name: "phantom-initialization-hunter-constructor-in-proxy-implementation"
class_name: "PhantomInitializationHunterConstructorInProxyImplementation"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: logic-contract-takeover-via-unprotected-initialize"
help: "Phantom Initialization Hunter (Constructor in Proxy Implementation)"
wiki_title: "Phantom Initialization Hunter (Constructor in Proxy Implementation)"
wiki_description: |
    Detects Proxy Implementation contracts (identified by 'initialize' functions) that erroneously use a 'constructor' to set state variables.
wiki_exploit_scenario: |
    Per Glider query tags [proxy, initialization, constructor, uups, logic-error]: Detects Proxy Implementation contracts (identified by 'initialize' functions) that erroneously use a 'constructor' to set state variables.
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "PhantomInitializationHunterConstructorInProxyImplementation"
fn_name_regex: ".*(phantom).*"
read_var_regex: ".*(phantom).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "phantomFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return phantom > 0;"
state_decl: "uint256 internal phantom;"
