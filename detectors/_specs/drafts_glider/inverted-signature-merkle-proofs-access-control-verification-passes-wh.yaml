skeleton: "name_match_missing_require"
name: "inverted-signature-merkle-proofs-access-control-verification-passes-wh"
class_name: "InvertedSignatureMerkleProofsAccessControlVerificationPassesWh"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: inverted-signaturemerkle-proofsaccess-control-veri"
help: "Inverted Signature|Merkle Proofs|Access Control Verification Passes When it Should Fail"
wiki_title: "Inverted Signature|Merkle Proofs|Access Control Verification Passes When it Should Fail"
wiki_description: |
    A common mistake is preceding a require/assert condition with NOT (!) which flips the condition, such as require(!_verify(...)) rather than require(_verify(...)). Thus when a verification/access control is meant to fail, it actually passes. This bypasses security controls and can allow malicious use
wiki_exploit_scenario: |
    Per Glider query tags [Signature, Logic]: A common mistake is preceding a require/assert condition with NOT (!) which flips the condition, such as require(!_verify(...)) rather than require(_verify(...)). Thus when a verification/access contr
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "InvertedSignatureMerkleProofsAccessControlVerificationPassesWh"
fn_name_regex: ".*(inverted).*"
write_var_regex: ".*(inverted).*"
guard_var_regex: ".*(inverted).*"
vuln_fn_name: "setParam"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "inverted = newVal;"
guard_require_line: "require(newVal <= 10000);"
state_decl: "uint256 internal inverted;"
