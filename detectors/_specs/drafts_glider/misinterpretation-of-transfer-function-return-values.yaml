skeleton: "name_match_missing_require"
name: "misinterpretation-of-transfer-function-return-values"
class_name: "MisinterpretationOfTransferFunctionReturnValues"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: misinterpretation-of-transfer-function-return-valu"
help: "Misinterpretation of transfer function return values"
wiki_title: "Misinterpretation of transfer function return values"
wiki_description: |
    Some ERC-20 tokens do not return a boolean value indicating success. In cases like this, the contract should not rely on require/assert statements to validate if the transfer succeeded.
wiki_exploit_scenario: |
    Per Glider query tags [ERC-20, ERC-721, safeTransfer, call patterns]: Some ERC-20 tokens do not return a boolean value indicating success. In cases like this, the contract should not rely on require/assert statements to validate if the transfer succeeded.
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "MisinterpretationOfTransferFunctionReturnValues"
fn_name_regex: ".*(misinterpretation).*"
write_var_regex: ".*(misinterpr).*"
guard_var_regex: ".*(misinterpr).*"
vuln_fn_name: "setParam"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "misinterpr = newVal;"
guard_require_line: "require(newVal <= 10000);"
state_decl: "uint256 internal misinterpr;"
