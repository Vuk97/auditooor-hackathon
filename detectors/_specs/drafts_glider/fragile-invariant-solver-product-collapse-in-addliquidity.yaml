skeleton: "name_match_missing_call"
name: "fragile-invariant-solver-product-collapse-in-addliquidity"
class_name: "FragileInvariantSolverProductCollapseInAddliquidity"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: y-eth-product-collapse-exploits"
help: "Fragile invariant solver (product collapse) in addLiquidity"
wiki_title: "Fragile invariant solver (product collapse) in addLiquidity"
wiki_description: |
    Finds pools whose supply/invariant solver (calcD/calcSupply/get_d/get_y, etc.) iterates on Π/Σ terms and is reachable from addLiquidity/join, matching the yETH Pi -> 0 collapse + bootstrap underflow pattern (pow_up imbalance, prev_supply==0, unsafe_sub on A*Σ - D*Π).
wiki_exploit_scenario: |
    Per Glider query tags [invariant, stableswap, product, addLiquidity, overmint, underflow]: Finds pools whose supply/invariant solver (calcD/calcSupply/get_d/get_y, etc.) iterates on Π/Σ terms and is reachable from addLiquidity/join, matching the yETH Pi -> 0 collapse + bootstrap underflow p
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "FragileInvariantSolverProductCollapseInAddliquidity"
fn_name_regex: ".*(addLiquidity).*"
read_var_regex: ".*(addliquidi).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "addLiquidity"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return addliquidi > 0;"
state_decl: "uint256 internal addliquidi;"
