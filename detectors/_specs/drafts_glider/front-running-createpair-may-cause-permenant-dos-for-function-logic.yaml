skeleton: "name_match_missing_call"
name: "front-running-createpair-may-cause-permenant-dos-for-function-logic"
class_name: "FrontRunningCreatepairMayCausePermenantDosForFunctionLogic"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: create-pair-do-s"
help: "Front-running createPair may cause permenant DoS for function logic"
wiki_title: "Front-running createPair may cause permenant DoS for function logic"
wiki_description: |
    An attacker can frontrun the UniswapV2 Factory `createPair` call, causing the target pair to be pre-created and resulting in a permanent DoS for the victim’s intended deployment/business flow.
wiki_exploit_scenario: |
    Per Glider query tags [DOS, ERC20.]: An attacker can frontrun the UniswapV2 Factory `createPair` call, causing the target pair to be pre-created and resulting in a permanent DoS for the victim’s intended deployment/business flow.
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "FrontRunningCreatepairMayCausePermenantDosForFunctionLogic"
fn_name_regex: ".*(createPair).*"
read_var_regex: ".*(createpair).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "createPair"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return createpair > 0;"
state_decl: "uint256 internal createpair;"
