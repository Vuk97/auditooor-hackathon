skeleton: "state_write_without_paired_write"
name: "rounding-asymmetry-in-share-debt-conversion-flows"
class_name: "RoundingAsymmetryInShareDebtConversionFlows"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: rounding-asymmetry-in-share-debt-conversion-flows"
help: "Rounding asymmetry in share/debt conversion flows"
wiki_title: "Rounding asymmetry in share/debt conversion flows"
wiki_description: |
    Flags contracts that mix rounding-down and rounding-up math across convert/preview/borrow/repay paths, a drainage vector where users can loop mint/redeem or borrow/repay to extract rounding dust.
wiki_exploit_scenario: |
    Per Glider query tags [rounding, conversion, debt, shares, preview]: Flags contracts that mix rounding-down and rounding-up math across convert/preview/borrow/repay paths, a drainage vector where users can loop mint/redeem or borrow/repay to extract rounding dust.
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "RoundingAsymmetryInShareDebtConversionFlows"
fn_name_regex: ".*(rounding).*"
primary_var_regex: ".*(balance|amount|position|stake).*"
paired_var_regex: ".*(flag|status|cleared|isLong|reset).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
