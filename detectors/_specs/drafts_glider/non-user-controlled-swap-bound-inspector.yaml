skeleton: "name_match_missing_require"
name: "non-user-controlled-swap-bound-inspector"
class_name: "NonUserControlledSwapBoundInspector"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: non-user-controlled-swap-bound-inspector"
help: "Non-User-Controlled Swap Bound Inspector"
wiki_title: "Non-User-Controlled Swap Bound Inspector"
wiki_description: |
    Detects external swap calls whose boundary amount parameters (e.g. min/max/slippage/tolerance-style arguments) may not provide meaningful user protection. This query is intended to surface swap flows where the boundary parameter is present syntactically, but its value may on-chain derived or hardcod
wiki_exploit_scenario: |
    Per Glider query tags [slippage, swap]: Detects external swap calls whose boundary amount parameters (e.g. min/max/slippage/tolerance-style arguments) may not provide meaningful user protection. This query is intended to surface swap flows 
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "NonUserControlledSwapBoundInspector"
fn_name_regex: ".*(False).*"
write_var_regex: ".*(false).*"
guard_var_regex: ".*(false).*"
vuln_fn_name: "False"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "false = newVal;"
guard_require_line: "require(newVal <= 10000);"
state_decl: "uint256 internal false;"
