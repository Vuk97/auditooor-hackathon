skeleton: "name_match_missing_call"
name: "rounding-to-zero-solvency-bypass"
class_name: "RoundingToZeroSolvencyBypass"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: rounding-to-zero-solvency-bypass"
help: "Rounding-to-Zero Solvency Bypass"
wiki_title: "Rounding-to-Zero Solvency Bypass"
wiki_description: "Detects pattern where division by external call result can round to zero, bypassing guard checks."
wiki_exploit_scenario: |
    Per Glider query tags [rounding, oracle, solvency, division-by-large-value]: Detects pattern where division by external call result can round to zero, bypassing guard checks.
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "RoundingToZeroSolvencyBypass"
fn_name_regex: ".*(rounding).*"
read_var_regex: ".*(rounding).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "roundingFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return rounding > 0;"
state_decl: "uint256 internal rounding;"
