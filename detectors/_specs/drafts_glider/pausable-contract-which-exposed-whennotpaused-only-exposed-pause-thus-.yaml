skeleton: "name_match_missing_call"
name: "pausable-contract-which-exposed-whennotpaused-only-exposed-pause-thus-"
class_name: "PausableContractWhichExposedWhennotpausedOnlyExposedPauseThus"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: pausable-contract-cant-be-unpaused"
help: "Pausable contract which exposed whenNotPaused only exposed Pause thus contract can be bricked"
wiki_title: "Pausable contract which exposed whenNotPaused only exposed Pause thus contract can be bricked"
wiki_description: |
    Pausable is a very common library used by contracts, functions with the whenNotPaused modifier will not run if the contract is in a 'paused' state. There are two key functions which need to be exposed for this pause/unpause functionality, _pause and _unpause. These are not exposed by default, and mu
wiki_exploit_scenario: |
    Per Glider query tags [Pausable]: Pausable is a very common library used by contracts, functions with the whenNotPaused modifier will not run if the contract is in a 'paused' state. There are two key functions which need to be exposed
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "PausableContractWhichExposedWhennotpausedOnlyExposedPauseThus"
fn_name_regex: ".*(whenNotPaused).*"
read_var_regex: ".*(whennotpau).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "whenNotPaused"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return whennotpau > 0;"
state_decl: "uint256 internal whennotpau;"
