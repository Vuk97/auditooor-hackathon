skeleton: "name_match_missing_call"
name: "contracts-should-never-grant-excessive-privileges-to-users-such-as-wit"
class_name: "ContractsShouldNeverGrantExcessivePrivilegesToUsersSuchAsWit"
wave: "13"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider query: excessive-erc-20-token-withdrawal"
help: |
    Contracts should never grant excessive privileges to users, such as withdrawing all tokens from a contract.
wiki_title: |
    Contracts should never grant excessive privileges to users, such as withdrawing all tokens from a contract.
wiki_description: |
    Contracts that hold ERC-20 tokens shouldn't allow arbitrary addresses to withdraw all tokens from the contract. These arbitrary addresses include both privileged roles and arbitrary addresses. Contracts allowing full withdrawals will cause the contract to lose all funds.
wiki_exploit_scenario: |
    Per Glider query tags [ERC-20, access control]: Contracts that hold ERC-20 tokens shouldn't allow arbitrary addresses to withdraw all tokens from the contract. These arbitrary addresses include both privileged roles and arbitrary addresses. Contrac
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "ContractsShouldNeverGrantExcessivePrivilegesToUsersSuchAsWit"
fn_name_regex: ".*(contracts).*"
read_var_regex: ".*(contracts).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "contractsFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return contracts > 0;"
state_decl: "uint256 internal contracts;"
