skeleton: "name_match_missing_call"
name: "missing-access-control-on-authorizeupgrade"
class_name: "MissingAccessControlOnAuthorizeupgrade"
wave: "13"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider query: missing-access-control-in-uupss-authorize-upgrade"
help: "Missing Access Control on _authorizeUpgrade"
wiki_title: "Missing Access Control on _authorizeUpgrade"
wiki_description: |
    Detects contracts implementing the UUPSUpgradeable pattern without proper access control on the `_authorizeUpgrade(address)` function.
wiki_exploit_scenario: |
    Per Glider query tags [upgradeability, access-control, uups]: Detects contracts implementing the UUPSUpgradeable pattern without proper access control on the `_authorizeUpgrade(address)` function.
wiki_recommendation: "Apply the guard described by the Glider query; cross-reference the canonical Hexens doc."
contract_name: "MissingAccessControlOnAuthorizeupgrade"
fn_name_regex: ".*(missing).*"
read_var_regex: ".*(missing).*"
required_call_regex: ".*(accrue|update|sync|validate|check).*"
guarded_helper_name: "_guard"
vuln_fn_name: "missingFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return missing > 0;"
state_decl: "uint256 internal missing;"
