id: systemic-lack-of-authentication-on-privileged-entry-points
title: |
  SYSTEMIC LACK OF AUTHENTICATION ON PRIVILEGED ENTRY POINTS
severity: Critical
language: rust
platform: soroban
source: halborn-k2-2025-09
source_id: "7.1"
bug_class: authorization
indicators:
  - 'text-pattern: systemic lack of authentication on privileged entry points'
victim: tbd
exploit_precondition: tbd
real_world_example: |
  ENABLES PROTOCOL COMPROMISE // CRITICAL Description Multiple entry points trust an unauthenticated caller parameter instead of binding to the transaction signer, enabling arbitrary users to seize privileged powers: Pool tokens: aToken and debt-token pool-only functions mint_scaled , burn_scaled , transfers) just compare caller to the stored pool address and never call require_auth , letting anyone spoof the pool to mint/burn or drain underlying reserves. Price oracle: admin entry points accept caller: Address , check it against the stored admin, but never require_auth , so anyone can spoof the...
suggested_remediation: |
  See source report 7.1 (halborn-k2-2025-09).
cross_refs: []
