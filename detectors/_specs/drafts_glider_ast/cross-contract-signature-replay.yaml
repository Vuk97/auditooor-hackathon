skeleton: "name_match_missing_call"
name: "cross-contract-signature-replay"
class_name: "CrossContractSignatureReplay"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Hexens Glider AST: signatures-lack-addressthis-allowing-for-cross-con"
help: "Cross-contract Signature replay"
wiki_title: "Cross-contract Signature replay"
wiki_description: |
    Flags externally callable functions where the signature digest is hashed without including 'address(this)' Logic: - Traces through "Transparent" layers (ECDSA helpers, EIP-191 prefixing). - Stops at "Root" sources (Parameters or Raw Hashing). - Flags ONLY if a Root Hash is created locally without ad
wiki_exploit_scenario: |
    Tags [signatures, replay, ecrecover, security]: Flags externally callable functions where the signature digest is hashed without including 'address(this)' Logic: - Traces through "Transparent" layers (ECDSA helpers, EIP-191 prefixing). - Stops at "
wiki_recommendation: "Apply the access check implied by the Glider query filter."
contract_name: "CrossContractSignatureReplay"
fn_name_regex: ".*"
read_var_regex: ".*cross.*"
required_call_regex: ".*(validate|check|verify|require).*"
guarded_helper_name: "_validateGuard"
vuln_fn_name: "crossFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return cross > 0;"
state_decl: "uint256 internal cross;"
