skeleton: "name_match_missing_call"
name: "openzeppelin-s-timelockcontroller-contains-operation-ready-check"
class_name: "OpenzeppelinSTimelockcontrollerContainsOperationReadyCheck"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Hexens Glider AST: timelock-contracts-doesnt-contain-operation-ready"
help: "OpenZeppelin's TimelockController contains operation ready check"
wiki_title: "OpenZeppelin's TimelockController contains operation ready check"
wiki_description: |
    OZ's TimelockController < 4.3.1 and < 3.4.2 contains a vulnerability where the _beforeCall function does not check if the operation is ready before executing it. Without this check, this vulnerability allows an actor with the executor role to escalate privileges.
wiki_exploit_scenario: |
    Tags [OpenZeppelin, access control]: OZ's TimelockController < 4.3.1 and < 3.4.2 contains a vulnerability where the _beforeCall function does not check if the operation is ready before executing it. Without this check, this vulnerability
wiki_recommendation: "Apply the access check implied by the Glider query filter."
contract_name: "OpenzeppelinSTimelockcontrollerContainsOperationReadyCheck"
fn_name_regex: ".*"
read_var_regex: ".*openzeppelin.*"
required_call_regex: ".*(validate|check|verify|require).*"
guarded_helper_name: "_validateGuard"
vuln_fn_name: "openzeppelinFn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return openzeppelin > 0;"
state_decl: "uint256 internal openzeppelin;"
