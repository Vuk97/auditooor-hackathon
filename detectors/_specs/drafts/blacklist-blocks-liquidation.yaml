skeleton: "name_match_missing_call"
name: "blacklist-blocks-liquidation"
class_name: "BlacklistBlocksLiquidation"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ah"
help: |
    `repayBorrowAllowed` checks blacklist but `liquidateBorrowAllowed` does not; liquidation calls repay internally causing revert on blacklisted positions.
wiki_title: "Blacklist-Blocks-Liquidation"
wiki_description: |
    `repayBorrowAllowed` checks blacklist but `liquidateBorrowAllowed` does not; liquidation calls repay internally causing revert on blacklisted positions.
wiki_exploit_scenario: |
    Attacker exploits Blacklist-Blocks-Liquidation as described: `repayBorrowAllowed` checks blacklist but `liquidateBorrowAllowed` does not; liquidation calls repay internally causing revert on blacklisted positions.
wiki_recommendation: |
    lending fork where `liquidateBorrowFresh` calls `repayBorrowAllowed` which checks a blacklist not excluded in liquidation path.
contract_name: "BlacklistBlocksLiquidation"
state_decl: "uint256 internal liquidateborrowallowed;"
fn_name_regex: ".*(repayBorrowAllowed|liquidateBorrowAllowed|liquidateBorrowFresh).*"
read_var_regex: ".*(liquidateBorrowAllowed|liquidateBorrowFresh|repayBorrowAllowed).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "repayBorrowAllowed"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return liquidateborrowallowed > 0;"
