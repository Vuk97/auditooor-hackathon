skeleton: "name_match_missing_require"
name: "fee-uncapped-in-constructor"
class_name: "FeeUncappedInConstructor"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    Constructor sets fees without checking against `MAX_*_FEE` cap; `setFees` function has the cap but constructor skips it.
wiki_title: "fee-uncapped-in-constructor"
wiki_description: |
    Constructor sets fees without checking against `MAX_*_FEE` cap; `setFees` function has the cap but constructor skips it.
wiki_exploit_scenario: |
    Attacker exploits fee-uncapped-in-constructor as described: Constructor sets fees without checking against `MAX_*_FEE` cap; `setFees` function has the cap but constructor skips it.
wiki_recommendation: |
    compare `setFee*` function with matching field assignment in `constructor` — check whether same bounds are applied.
contract_name: "FeeUncappedInConstructor"
state_decl: "mapping(address => uint256) internal balances;"
fn_name_regex: ".*(setFees|constructor).*"
write_var_regex: ".*(constructor|setFees).*"
guard_var_regex: ".*(constructor|setFees).*"
vuln_fn_name: "setParam"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balances[msg.sender] = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
