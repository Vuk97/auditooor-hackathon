skeleton: "name_match_missing_call"
name: "fee-signature-covers-only-feedata"
class_name: "FeeSignatureCoversOnlyFeedata"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ab"
help: |
    ed25519 fee signature hashes only `FeeData` struct, not full intent hash; valid fee sig reusable across different intents.
wiki_title: "Fee-Signature-Covers-Only-FeeData"
wiki_description: |
    ed25519 fee signature hashes only `FeeData` struct, not full intent hash; valid fee sig reusable across different intents.
wiki_exploit_scenario: |
    Attacker exploits Fee-Signature-Covers-Only-FeeData as described: ed25519 fee signature hashes only `FeeData` struct, not full intent hash; valid fee sig reusable across different intents.
wiki_recommendation: |
    signature scheme where signed payload does not bind to a containing parent object (intent/order) identifier.
contract_name: "FeeSignatureCoversOnlyFeedata"
state_decl: "uint256 internal tracked;"
fn_name_regex: ".*(FeeData).*"
read_var_regex: ".*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "FeeData"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return tracked > 0;"
