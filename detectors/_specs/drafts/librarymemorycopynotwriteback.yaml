skeleton: "name_match_missing_call"
name: "librarymemorycopynotwriteback"
class_name: "Librarymemorycopynotwriteback"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ad"
help: |
    `emergencyRemoveStrategy` passes `protectStrategy` as `address` (memory copy) to library; library clears local copy but state variable unchanged.
wiki_title: "LibraryMemoryCopyNotWriteback"
wiki_description: |
    `emergencyRemoveStrategy` passes `protectStrategy` as `address` (memory copy) to library; library clears local copy but state variable unchanged.
wiki_exploit_scenario: |
    Attacker exploits LibraryMemoryCopyNotWriteback as described: `emergencyRemoveStrategy` passes `protectStrategy` as `address` (memory copy) to library; library clears local copy but state variable unchanged.
wiki_recommendation: "library function receiving value-type storage state as non-`storage` param and modifying it."
contract_name: "Librarymemorycopynotwriteback"
state_decl: "uint256 internal address;"
fn_name_regex: ".*(emergencyRemoveStrategy|protectStrategy|address).*"
read_var_regex: ".*(address|emergencyRemoveStrategy|protectStrategy).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "emergencyRemoveStrategy"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return address > 0;"
