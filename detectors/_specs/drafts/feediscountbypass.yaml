skeleton: "name_match_missing_call"
name: "feediscountbypass"
class_name: "Feediscountbypass"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ac"
help: |
    `swap()` accepts user-supplied `actualUser` address for discount eligibility check; caller can pass any discount-eligible address as `actualUser` without restriction.
wiki_title: "FeeDiscountBypass"
wiki_description: |
    `swap()` accepts user-supplied `actualUser` address for discount eligibility check; caller can pass any discount-eligible address as `actualUser` without restriction.
wiki_exploit_scenario: |
    Attacker exploits FeeDiscountBypass as described: `swap()` accepts user-supplied `actualUser` address for discount eligibility check; caller can pass any discount-eligible address as `actualUser` without restriction.
wiki_recommendation: |
    function taking `actualUser` address param where discount eligibility checked against `actualUser` not `msg.sender`, and no whitelist enforcement.
contract_name: "Feediscountbypass"
state_decl: "uint256 internal actualuser;"
fn_name_regex: ".*(swap|actualUser).*"
read_var_regex: ".*(actualUser).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "swap"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return actualuser > 0;"
