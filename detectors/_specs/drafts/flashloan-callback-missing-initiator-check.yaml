skeleton: "name_match_missing_call"
name: "flashloan-callback-missing-initiator-check"
class_name: "FlashloanCallbackMissingInitiatorCheck"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    `LeverageStrategy.receiveFlashLoan` checks `msg.sender == flashLoanModule` but not that contract itself initiated the loan; attacker initiates flash loan to contract with arbitrary
wiki_title: "flashloan-callback-missing-initiator-check"
wiki_description: |
    `LeverageStrategy.receiveFlashLoan` checks `msg.sender == flashLoanModule` but not that contract itself initiated the loan; attacker initiates flash loan to contract with arbitrary `userData`.
wiki_exploit_scenario: |
    Attacker exploits flashloan-callback-missing-initiator-check as described: `LeverageStrategy.receiveFlashLoan` checks `msg.sender == flashLoanModule` but not that contract itself initiated the loan; attacker initiates flash loan to contract with arbitrary `userData`.
wiki_recommendation: |
    flash-loan callback function that checks `msg.sender == loanProvider` but does NOT check `initiator == address(this)`.
contract_name: "FlashloanCallbackMissingInitiatorCheck"
state_decl: "uint256 internal userdata;"
fn_name_regex: ".*(userData).*"
read_var_regex: ".*(userData).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "userData"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return userdata > 0;"
