skeleton: "name_match_missing_call"
name: "escrow-counterparty-msg-sender-vs-signer"
class_name: "EscrowCounterpartyMsgSenderVsSigner"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ah"
help: |
    `signDealAndPay` passes `msg.sender` instead of validated `signer` to `updateEscrow`; front-runner can claim corp assets using victim's signature.
wiki_title: "Escrow-Counterparty-Msg-Sender-vs-Signer"
wiki_description: |
    `signDealAndPay` passes `msg.sender` instead of validated `signer` to `updateEscrow`; front-runner can claim corp assets using victim's signature.
wiki_exploit_scenario: |
    Attacker exploits Escrow-Counterparty-Msg-Sender-vs-Signer as described: `signDealAndPay` passes `msg.sender` instead of validated `signer` to `updateEscrow`; front-runner can claim corp assets using victim's signature.
wiki_recommendation: "escrow function accepting `signer` param but setting `counterParty = msg.sender` for asset delivery."
contract_name: "EscrowCounterpartyMsgSenderVsSigner"
state_decl: "uint256 internal signdealandpay;"
fn_name_regex: ".*(signDealAndPay|signer|updateEscrow).*"
read_var_regex: ".*(signDealAndPay|signer|updateEscrow).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "signDealAndPay"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return signdealandpay > 0;"
