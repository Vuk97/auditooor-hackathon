skeleton: "name_match_missing_call"
name: "reentrancy-swap-executor-callback"
class_name: "ReentrancySwapExecutorCallback"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    `BaseRouterSingleOutput.execute()` sends tokens to `SWAP_EXECUTOR` then calls `executeSwap()` as callback; attacker's router calls `extractRequests()` with victim's Permit2 request
wiki_title: "reentrancy-swap-executor-callback"
wiki_description: |
    `BaseRouterSingleOutput.execute()` sends tokens to `SWAP_EXECUTOR` then calls `executeSwap()` as callback; attacker's router calls `extractRequests()` with victim's Permit2 request, stealing `bridgeAmount` while satisfying `minSwapOutput`.
wiki_exploit_scenario: |
    Attacker exploits reentrancy-swap-executor-callback as described: `BaseRouterSingleOutput.execute()` sends tokens to `SWAP_EXECUTOR` then calls `executeSwap()` as callback; attacker's router calls `extractRequests()` with victim's Permit2 request, stealing `bridgeAm
wiki_recommendation: "`SWAP_EXECUTOR.executeSwap(...)` callback before `initialBalance` check with no reentrancy guard."
contract_name: "ReentrancySwapExecutorCallback"
state_decl: "uint256 internal bridgeamount;"
fn_name_regex: ".*(SWAP_EXECUTOR|executeSwap|extractRequests).*"
read_var_regex: ".*(bridgeAmount|initialBalance|minSwapOutput).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "SWAP_EXECUTOR"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return bridgeamount > 0;"
