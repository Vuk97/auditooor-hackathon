skeleton: "name_match_missing_call"
name: "auction-failure-not-handled-protocol-permanently-stalled"
class_name: "AuctionFailureNotHandledProtocolPermanentlyStalled"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ag"
help: |
    Failed auction (`FAILED_UNDERSOLD`/`FAILED_LIQUIDATION`) leaves `auctions[currentPeriod]` non-zero; `currentPeriod` can never increment; new auctions impossible; bidder funds locke
wiki_title: "Auction Failure Not Handled: Protocol Permanently Stalled"
wiki_description: |
    Failed auction (`FAILED_UNDERSOLD`/`FAILED_LIQUIDATION`) leaves `auctions[currentPeriod]` non-zero; `currentPeriod` can never increment; new auctions impossible; bidder funds locked.
wiki_exploit_scenario: |
    Attacker exploits Auction Failure Not Handled: Protocol Permanently Stalled as described: Failed auction (`FAILED_UNDERSOLD`/`FAILED_LIQUIDATION`) leaves `auctions[currentPeriod]` non-zero; `currentPeriod` can never increment; new auctions impossible; bidder funds locked.
wiki_recommendation: "auction state machine without `FAILED` path to increment period."
contract_name: "AuctionFailureNotHandledProtocolPermanentlyStalled"
state_decl: "uint256 internal currentperiod;"
fn_name_regex: ".*(FAILED_UNDERSOLD|FAILED_LIQUIDATION|currentPeriod).*"
read_var_regex: ".*(currentPeriod).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "FAILED_UNDERSOLD"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return currentperiod > 0;"
