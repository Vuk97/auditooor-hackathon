skeleton: "name_match_missing_call"
name: "first-deposit-totalsupply-zero"
class_name: "FirstDepositTotalsupplyZero"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    Permissionless `processNextOracleRecords` + 1 wei donation sets `totalControlled() > 0` while `totalSupply == 0`, causing `ethToMntETH` to return 0 shares for all subsequent deposi
wiki_title: "first-deposit-totalSupply-zero"
wiki_description: |
    Permissionless `processNextOracleRecords` + 1 wei donation sets `totalControlled() > 0` while `totalSupply == 0`, causing `ethToMntETH` to return 0 shares for all subsequent deposits.
wiki_exploit_scenario: |
    Attacker exploits first-deposit-totalSupply-zero as described: Permissionless `processNextOracleRecords` + 1 wei donation sets `totalControlled() > 0` while `totalSupply == 0`, causing `ethToMntETH` to return 0 shares for all subsequent deposits.
wiki_recommendation: |
    ERC4626-style vault where the `previewDeposit`/`convertToShares` path has a division where denominator can be 0 via external donation.
contract_name: "FirstDepositTotalsupplyZero"
state_decl: "uint256 internal converttoshares;"
fn_name_regex: ".*(processNextOracleRecords|ethToMntETH|previewDeposit).*"
read_var_regex: ".*(convertToShares|ethToMntETH|previewDeposit).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "processNextOracleRecords"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return converttoshares > 0;"
