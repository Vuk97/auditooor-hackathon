skeleton: "name_match_missing_require"
name: "oracle-timestamp-unchecked"
class_name: "OracleTimestampUnchecked"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    DIA oracle `getValue` returns `(price, timestamp)` but timestamp is ignored; stale prices used for collateral/liquidation.
wiki_title: "oracle-timestamp-unchecked"
wiki_description: |
    DIA oracle `getValue` returns `(price, timestamp)` but timestamp is ignored; stale prices used for collateral/liquidation.
wiki_exploit_scenario: |
    Attacker exploits oracle-timestamp-unchecked as described: DIA oracle `getValue` returns `(price, timestamp)` but timestamp is ignored; stale prices used for collateral/liquidation.
wiki_recommendation: |
    `HighLevelCall` to DIA/Band oracle where return value tuple has second element (timestamp) discarded (tuple unpacking with `(price, )` pattern).
contract_name: "OracleTimestampUnchecked"
state_decl: "mapping(address => uint256) internal balances;"
fn_name_regex: ".*(getValue|HighLevelCall).*"
write_var_regex: ".*(getValue).*"
guard_var_regex: ".*(getValue).*"
vuln_fn_name: "setParam"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balances[msg.sender] = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
