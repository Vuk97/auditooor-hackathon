skeleton: "name_match_missing_call"
name: "dustaccounting"
class_name: "Dustaccounting"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ac"
help: |
    Round-down share conversion creates dust remainders that inflate `totalAssetsValue()`; phantom USDC rewards lock stakes.
wiki_title: "DustAccounting"
wiki_description: |
    Round-down share conversion creates dust remainders that inflate `totalAssetsValue()`; phantom USDC rewards lock stakes.
wiki_exploit_scenario: |
    Attacker exploits DustAccounting as described: Round-down share conversion creates dust remainders that inflate `totalAssetsValue()`; phantom USDC rewards lock stakes.
wiki_recommendation: |
    `convertToShares` uses `Math.Rounding.Floor` without tracking remainder; state var `totalStakedAssets` diverges from share math.
contract_name: "Dustaccounting"
state_decl: "uint256 internal converttoshares;"
fn_name_regex: ".*(totalAssetsValue|convertToShares|totalStakedAssets).*"
read_var_regex: ".*(convertToShares|totalStakedAssets).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "totalAssetsValue"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return converttoshares > 0;"
