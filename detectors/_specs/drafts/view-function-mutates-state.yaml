skeleton: "name_match_missing_call"
name: "view-function-mutates-state"
class_name: "ViewFunctionMutatesState"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ab"
help: |
    `checkClaimPayoutBatch()` is declared view but internally calls `oneifyPayloadHash()` which writes state; reverts on-chain, silently corrupts off-chain simulation.
wiki_title: "View-Function-Mutates-State"
wiki_description: |
    `checkClaimPayoutBatch()` is declared view but internally calls `oneifyPayloadHash()` which writes state; reverts on-chain, silently corrupts off-chain simulation.
wiki_exploit_scenario: |
    Attacker exploits View-Function-Mutates-State as described: `checkClaimPayoutBatch()` is declared view but internally calls `oneifyPayloadHash()` which writes state; reverts on-chain, silently corrupts off-chain simulation.
wiki_recommendation: "`view` function that calls an internal or external function that writes to storage."
contract_name: "ViewFunctionMutatesState"
state_decl: "uint256 internal view;"
fn_name_regex: ".*(checkClaimPayoutBatch|oneifyPayloadHash|view).*"
read_var_regex: ".*(view).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "checkClaimPayoutBatch"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return view > 0;"
