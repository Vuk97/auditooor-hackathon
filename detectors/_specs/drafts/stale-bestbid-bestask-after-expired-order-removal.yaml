skeleton: "name_match_missing_call"
name: "stale-bestbid-bestask-after-expired-order-removal"
class_name: "StaleBestbidBestaskAfterExpiredOrderRemoval"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ae"
help: |
    _matchIncomingAsk continues loop after removing expired order without refreshing bestBid; next iteration uses stale price, causes revert on empty order.
wiki_title: "Stale bestBid/bestAsk After Expired-Order Removal"
wiki_description: |
    _matchIncomingAsk continues loop after removing expired order without refreshing bestBid; next iteration uses stale price, causes revert on empty order.
wiki_exploit_scenario: |
    Attacker exploits Stale bestBid/bestAsk After Expired-Order Removal as described: _matchIncomingAsk continues loop after removing expired order without refreshing bestBid; next iteration uses stale price, causes revert on empty order.
wiki_recommendation: "order-book matching loops with continue-after-remove that don't refresh best price."
contract_name: "StaleBestbidBestaskAfterExpiredOrderRemoval"
state_decl: "uint256 internal tracked;"
fn_name_regex: ".*(stale|bestbid|bestask).*"
read_var_regex: ".*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "stale"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return tracked > 0;"
