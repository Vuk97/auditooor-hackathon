skeleton: "name_match_missing_call"
name: "extraneous-approval-in-withdrawal-allows-double-withdrawal"
class_name: "ExtraneousApprovalInWithdrawalAllowsDoubleWithdrawal"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ag"
help: |
    `withdraw()` calls `approve(user, amount)` then `transfer(user, amount)`; approval not spent by direct transfer; user can call `transferFrom` to withdraw again if contract holds fu
wiki_title: "Extraneous Approval in Withdrawal Allows Double-Withdrawal"
wiki_description: |
    `withdraw()` calls `approve(user, amount)` then `transfer(user, amount)`; approval not spent by direct transfer; user can call `transferFrom` to withdraw again if contract holds funds.
wiki_exploit_scenario: |
    Attacker exploits Extraneous Approval in Withdrawal Allows Double-Withdrawal as described: `withdraw()` calls `approve(user, amount)` then `transfer(user, amount)`; approval not spent by direct transfer; user can call `transferFrom` to withdraw again if contract holds funds.
wiki_recommendation: "`approve` followed by `transfer` (not `transferFrom`) to same recipient."
contract_name: "ExtraneousApprovalInWithdrawalAllowsDoubleWithdrawal"
state_decl: "uint256 internal approve;"
fn_name_regex: ".*(withdraw|approve|transfer).*"
read_var_regex: ".*(approve|transfer|transferFrom).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "withdraw"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return approve > 0;"
