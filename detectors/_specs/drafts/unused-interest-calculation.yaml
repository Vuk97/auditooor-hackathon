skeleton: "highlevelcall_missing_sibling"
name: "unused-interest-calculation"
class_name: "UnusedInterestCalculation"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ah"
help: |
    `_closePositionInternal` recomputes `_interest` via `_computeInterest` but discards the result; no on-chain enforcement of interest amount.
wiki_title: "Unused-Interest-Calculation"
wiki_description: |
    `_closePositionInternal` recomputes `_interest` via `_computeInterest` but discards the result; no on-chain enforcement of interest amount.
wiki_exploit_scenario: |
    Attacker exploits Unused-Interest-Calculation as described: `_closePositionInternal` recomputes `_interest` via `_computeInterest` but discards the result; no on-chain enforcement of interest amount.
wiki_recommendation: |
    function that calls `_computeInterest()` and assigns result to local `_interest` without using it in validation.
contract_name: "UnusedInterestCalculation"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(unused|interest|calculation).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
