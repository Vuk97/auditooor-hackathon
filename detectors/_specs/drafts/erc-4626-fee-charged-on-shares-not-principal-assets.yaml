skeleton: "name_match_missing_call"
name: "erc-4626-fee-charged-on-shares-not-principal-assets"
class_name: "Erc4626FeeChargedOnSharesNotPrincipalAssets"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ag"
help: |
    `_calculateRewardsFee(assetsWithdrawn, shares)` passes `shares` as principal when shares != assets over time; users overcharged as share:asset ratio grows.
wiki_title: "ERC-4626 Fee Charged on shares (Not Principal Assets)"
wiki_description: |
    `_calculateRewardsFee(assetsWithdrawn, shares)` passes `shares` as principal when shares != assets over time; users overcharged as share:asset ratio grows.
wiki_exploit_scenario: |
    Attacker exploits ERC-4626 Fee Charged on shares (Not Principal Assets) as described: `_calculateRewardsFee(assetsWithdrawn, shares)` passes `shares` as principal when shares != assets over time; users overcharged as share:asset ratio grows.
wiki_recommendation: "`_calculateRewardsFee` or similar using `shares` as if it were `assets` denominated."
contract_name: "Erc4626FeeChargedOnSharesNotPrincipalAssets"
state_decl: "uint256 internal assets;"
fn_name_regex: ".*(shares|assets).*"
read_var_regex: ".*(assets|shares).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "shares"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return assets > 0;"
