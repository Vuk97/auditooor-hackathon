skeleton: "name_match_missing_call"
name: "frontruntransfer"
class_name: "Frontruntransfer"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ac"
help: |
    `transferUser(to)` checks `_userGraduals[to].lastUpdatedTrancheIndex == 0`; attacker front-runs to initialize target address, permanently blocking victim's account transfer.
wiki_title: "FrontRunTransfer"
wiki_description: |
    `transferUser(to)` checks `_userGraduals[to].lastUpdatedTrancheIndex == 0`; attacker front-runs to initialize target address, permanently blocking victim's account transfer.
wiki_exploit_scenario: |
    Attacker exploits FrontRunTransfer as described: `transferUser(to)` checks `_userGraduals[to].lastUpdatedTrancheIndex == 0`; attacker front-runs to initialize target address, permanently blocking victim's account transfer.
wiki_recommendation: |
    account transfer function with `require(state[to] == 0)` and no access restriction on initializer for `to`.
contract_name: "Frontruntransfer"
state_decl: "uint256 internal to;"
fn_name_regex: ".*(transferUser|require).*"
read_var_regex: ".*(to).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transferUser"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return to > 0;"
