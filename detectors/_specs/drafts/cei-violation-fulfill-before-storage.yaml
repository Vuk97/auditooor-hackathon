skeleton: "name_match_missing_call"
name: "cei-violation-fulfill-before-storage"
class_name: "CeiViolationFulfillBeforeStorage"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    `fulfilRequests` triggers receiver callback at line 324 before marking `fulfilledRequests[requestHash]` at line 334; receiver can cancel request in callback, refunding input token
wiki_title: "CEI-violation-fulfill-before-storage"
wiki_description: |
    `fulfilRequests` triggers receiver callback at line 324 before marking `fulfilledRequests[requestHash]` at line 334; receiver can cancel request in callback, refunding input token on source chain while keeping output tokens.
wiki_exploit_scenario: |
    Attacker exploits CEI-violation-fulfill-before-storage as described: `fulfilRequests` triggers receiver callback at line 324 before marking `fulfilledRequests[requestHash]` at line 334; receiver can cancel request in callback, refunding input token on source chain whil
wiki_recommendation: |
    `_executeCalldata`/`callback` call before `$.fulfilledRequests[hash] = ...` storage write — wave2 `nonreentrant_self_call` is related; CEI pattern violation specific to cross-chain fulfillment.
contract_name: "CeiViolationFulfillBeforeStorage"
state_decl: "uint256 internal callback;"
fn_name_regex: ".*(fulfilRequests|callback|nonreentrant_self_call).*"
read_var_regex: ".*(callback|fulfilRequests|nonreentrant_self_call).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "fulfilRequests"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return callback > 0;"
