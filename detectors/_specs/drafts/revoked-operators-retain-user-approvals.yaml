skeleton: "name_match_missing_call"
name: "revoked-operators-retain-user-approvals"
class_name: "RevokedOperatorsRetainUserApprovals"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ae"
help: |
    disallowOperator clears allowedOperators but doesn't invalidate existing per-user operatorApprovals; revoked operators still act on behalf of users.
wiki_title: "Revoked Operators Retain User Approvals"
wiki_description: |
    disallowOperator clears allowedOperators but doesn't invalidate existing per-user operatorApprovals; revoked operators still act on behalf of users.
wiki_exploit_scenario: |
    Attacker exploits Revoked Operators Retain User Approvals as described: disallowOperator clears allowedOperators but doesn't invalidate existing per-user operatorApprovals; revoked operators still act on behalf of users.
wiki_recommendation: "operator-revocation flows that don't sweep or gate on-the-fly in `onlySenderOrOperator`."
contract_name: "RevokedOperatorsRetainUserApprovals"
state_decl: "uint256 internal onlysenderoroperator;"
fn_name_regex: ".*(onlySenderOrOperator).*"
read_var_regex: ".*(onlySenderOrOperator).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "onlySenderOrOperator"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return onlysenderoroperator > 0;"
