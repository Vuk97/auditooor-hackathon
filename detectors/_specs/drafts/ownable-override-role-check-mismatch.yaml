skeleton: "name_match_missing_call"
name: "ownable-override-role-check-mismatch"
class_name: "OwnableOverrideRoleCheckMismatch"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ah"
help: |
    `NameToken` overrides `_requireCallerIsContractOwner` to use `DEFAULT_ADMIN_ROLE`; `transferOwnership` does not grant role to new owner causing permanent admin lockout.
wiki_title: "Ownable-Override-Role-Check-Mismatch"
wiki_description: |
    `NameToken` overrides `_requireCallerIsContractOwner` to use `DEFAULT_ADMIN_ROLE`; `transferOwnership` does not grant role to new owner causing permanent admin lockout.
wiki_exploit_scenario: |
    Attacker exploits Ownable-Override-Role-Check-Mismatch as described: `NameToken` overrides `_requireCallerIsContractOwner` to use `DEFAULT_ADMIN_ROLE`; `transferOwnership` does not grant role to new owner causing permanent admin lockout.
wiki_recommendation: |
    contract overriding `_requireCallerIsContractOwner` or `_checkOwner` to use `hasRole` without overriding `transferOwnership` to sync the role.
contract_name: "OwnableOverrideRoleCheckMismatch"
state_decl: "uint256 internal hasrole;"
fn_name_regex: ".*(NameToken|DEFAULT_ADMIN_ROLE|transferOwnership).*"
read_var_regex: ".*(hasRole|transferOwnership).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "NameToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return hasrole > 0;"
