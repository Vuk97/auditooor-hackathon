skeleton: "name_match_missing_call"
name: "decimal-truncation-price-check"
class_name: "DecimalTruncationPriceCheck"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    `checkSwapOraclePrice()` computes `fromPrice / toPrice` with integer division that always truncates to 0 or 1, making `priceChangeLimit` check trivially satisfied.
wiki_title: "decimal-truncation-price-check"
wiki_description: |
    `checkSwapOraclePrice()` computes `fromPrice / toPrice` with integer division that always truncates to 0 or 1, making `priceChangeLimit` check trivially satisfied.
wiki_exploit_scenario: |
    Attacker exploits decimal-truncation-price-check as described: `checkSwapOraclePrice()` computes `fromPrice / toPrice` with integer division that always truncates to 0 or 1, making `priceChangeLimit` check trivially satisfied.
wiki_recommendation: |
    `BinaryOperation(DIV)` on two oracle prices where result is compared to a constant limit without scaling by 1e18.
contract_name: "DecimalTruncationPriceCheck"
state_decl: "uint256 internal pricechangelimit;"
fn_name_regex: ".*(checkSwapOraclePrice|priceChangeLimit|BinaryOperation).*"
read_var_regex: ".*(priceChangeLimit).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "checkSwapOraclePrice"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return pricechangelimit > 0;"
