skeleton: "name_match_missing_call"
name: "redemption-pool-fee-minting-amg-accounting-mismatch"
class_name: "RedemptionPoolFeeMintingAmgAccountingMismatch"
wave: "12"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "corpus slice slice_ae"
help: |
    minted FAsset uses `poolFeeUBA` (full value) but recorded AMG uses `convertUBAToAmg(poolFeeUBA)` (floor); fractional dust causes agent obligation undercount.
wiki_title: "Redemption Pool Fee Minting AMG Accounting Mismatch"
wiki_description: |
    minted FAsset uses `poolFeeUBA` (full value) but recorded AMG uses `convertUBAToAmg(poolFeeUBA)` (floor); fractional dust causes agent obligation undercount.
wiki_exploit_scenario: |
    Attacker exploits Redemption Pool Fee Minting AMG Accounting Mismatch as described: minted FAsset uses `poolFeeUBA` (full value) but recorded AMG uses `convertUBAToAmg(poolFeeUBA)` (floor); fractional dust causes agent obligation undercount.
wiki_recommendation: "contracts that mint tokens then record a rounded-down accounting value."
contract_name: "RedemptionPoolFeeMintingAmgAccountingMismatch"
state_decl: "uint256 internal poolfeeuba;"
fn_name_regex: ".*(poolFeeUBA|convertUBAToAmg).*"
read_var_regex: ".*(poolFeeUBA).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "poolFeeUBA"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return poolfeeuba > 0;"
