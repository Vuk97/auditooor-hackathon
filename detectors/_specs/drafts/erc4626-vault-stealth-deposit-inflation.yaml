skeleton: "name_match_missing_call"
name: "erc4626-vault-stealth-deposit-inflation"
class_name: "Erc4626VaultStealthDepositInflation"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_ah"
help: |
    `MoneyBrinter` vault allows `depositFor(address(vault))` via third-party plugin, bypassing internal balance tracking; first depositor inflates denominator to steal subsequent depos
wiki_title: "ERC4626-Vault-Stealth-Deposit-Inflation"
wiki_description: |
    `MoneyBrinter` vault allows `depositFor(address(vault))` via third-party plugin, bypassing internal balance tracking; first depositor inflates denominator to steal subsequent deposits.
wiki_exploit_scenario: |
    Attacker exploits ERC4626-Vault-Stealth-Deposit-Inflation as described: `MoneyBrinter` vault allows `depositFor(address(vault))` via third-party plugin, bypassing internal balance tracking; first depositor inflates denominator to steal subsequent deposits.
wiki_recommendation: |
    ERC-4626 vault where `totalAssets()` delegates to external plugin that accepts `depositFor` from anyone.
contract_name: "Erc4626VaultStealthDepositInflation"
state_decl: "uint256 internal depositfor;"
fn_name_regex: ".*(MoneyBrinter|totalAssets|depositFor).*"
read_var_regex: ".*(depositFor).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "MoneyBrinter"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return depositfor > 0;"
