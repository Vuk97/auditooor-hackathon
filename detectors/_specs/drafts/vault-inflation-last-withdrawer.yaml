skeleton: "name_match_missing_call"
name: "vault-inflation-last-withdrawer"
class_name: "VaultInflationLastWithdrawer"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    Last withdrawer can transfer assets to vault + deposit 1 wei + take second depositor's funds via `isLast` branch.
wiki_title: "vault-inflation-last-withdrawer"
wiki_description: |
    Last withdrawer can transfer assets to vault + deposit 1 wei + take second depositor's funds via `isLast` branch.
wiki_exploit_scenario: |
    Attacker exploits vault-inflation-last-withdrawer as described: Last withdrawer can transfer assets to vault + deposit 1 wei + take second depositor's funds via `isLast` branch.
wiki_recommendation: "ERC4626 `deposit` function where `totalSupply == 0` path does not enforce `shares > 0` minimum."
contract_name: "VaultInflationLastWithdrawer"
state_decl: "uint256 internal deposit;"
fn_name_regex: ".*(isLast|deposit).*"
read_var_regex: ".*(deposit|isLast).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isLast"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return deposit > 0;"
