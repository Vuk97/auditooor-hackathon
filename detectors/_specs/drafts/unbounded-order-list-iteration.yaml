skeleton: "name_match_missing_call"
name: "unbounded-order-list-iteration"
class_name: "UnboundedOrderListIteration"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    `IterableMapping` never shrinks on delete (sets flag only), causing gas costs to grow indefinitely and eventually exceeding block limit.
wiki_title: "unbounded-order-list-iteration"
wiki_description: |
    `IterableMapping` never shrinks on delete (sets flag only), causing gas costs to grow indefinitely and eventually exceeding block limit.
wiki_exploit_scenario: |
    Attacker exploits unbounded-order-list-iteration as described: `IterableMapping` never shrinks on delete (sets flag only), causing gas costs to grow indefinitely and eventually exceeding block limit.
wiki_recommendation: |
    `EnumerableMap` or custom iterable mapping where `remove` uses a deletion flag rather than array shrink.
contract_name: "UnboundedOrderListIteration"
state_decl: "uint256 internal remove;"
fn_name_regex: ".*(IterableMapping|EnumerableMap|remove).*"
read_var_regex: ".*(remove).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "IterableMapping"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return remove > 0;"
