skeleton: "name_match_missing_call"
name: "interest-index-not-updated-on-transfer"
class_name: "InterestIndexNotUpdatedOnTransfer"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    `CErc721` interest reward calculation multiplies balance by `(supplyIndex - interestIndex)`; transfers don't reset `interestIndex` for receiver, allowing instant reward fabrication
wiki_title: "interest-index-not-updated-on-transfer"
wiki_description: |
    `CErc721` interest reward calculation multiplies balance by `(supplyIndex - interestIndex)`; transfers don't reset `interestIndex` for receiver, allowing instant reward fabrication via balance inflation.
wiki_exploit_scenario: |
    Attacker exploits interest-index-not-updated-on-transfer as described: `CErc721` interest reward calculation multiplies balance by `(supplyIndex - interestIndex)`; transfers don't reset `interestIndex` for receiver, allowing instant reward fabrication via balance inflati
wiki_recommendation: |
    ERC20 `transfer`/`transferFrom` path that does NOT call `_updateInterestIndex(to)` before balance increase, where interest is calculated as `balance * indexDiff`.
contract_name: "InterestIndexNotUpdatedOnTransfer"
state_decl: "uint256 internal interestindex;"
fn_name_regex: ".*(CErc721|interestIndex|transfer).*"
read_var_regex: ".*(interestIndex|transfer|transferFrom).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "CErc721"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return interestindex > 0;"
