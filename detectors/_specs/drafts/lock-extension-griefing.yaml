skeleton: "name_match_missing_call"
name: "lock-extension-griefing"
class_name: "LockExtensionGriefing"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    Any user can call `depositFor(victim, 0, MAX_LOCK)` to extend victim's lock without depositing tokens, blocking `withdraw`.
wiki_title: "lock-extension-griefing"
wiki_description: |
    Any user can call `depositFor(victim, 0, MAX_LOCK)` to extend victim's lock without depositing tokens, blocking `withdraw`.
wiki_exploit_scenario: |
    Attacker exploits lock-extension-griefing as described: Any user can call `depositFor(victim, 0, MAX_LOCK)` to extend victim's lock without depositing tokens, blocking `withdraw`.
wiki_recommendation: |
    `_deposit` function path where `duration` is accepted from caller and added to existing lock even when `amount == 0`.
contract_name: "LockExtensionGriefing"
state_decl: "uint256 internal tracked;"
fn_name_regex: ".*(depositFor|withdraw|duration).*"
read_var_regex: ".*(_deposit|duration|withdraw).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "depositFor"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return tracked > 0;"
