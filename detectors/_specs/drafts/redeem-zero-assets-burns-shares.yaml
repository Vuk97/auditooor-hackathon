skeleton: "name_match_missing_call"
name: "redeem-zero-assets-burns-shares"
class_name: "RedeemZeroAssetsBurnsShares"
wave: "12"
severity: "HIGH"
confidence: "MEDIUM"
source: "corpus slice slice_aa"
help: |
    When `previewRedeem(shares) > liquidAssets`, function returns 0 but `_processExit` still burns shares, silently destroying user funds.
wiki_title: "redeem-zero-assets-burns-shares"
wiki_description: |
    When `previewRedeem(shares) > liquidAssets`, function returns 0 but `_processExit` still burns shares, silently destroying user funds.
wiki_exploit_scenario: |
    Attacker exploits redeem-zero-assets-burns-shares as described: When `previewRedeem(shares) > liquidAssets`, function returns 0 but `_processExit` still burns shares, silently destroying user funds.
wiki_recommendation: "ERC4626 `redeem` where `_processExit(shares)` is called unconditionally even when `assets == 0`."
contract_name: "RedeemZeroAssetsBurnsShares"
state_decl: "uint256 internal redeem;"
fn_name_regex: ".*(redeem).*"
read_var_regex: ".*(redeem).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "redeem"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return redeem > 0;"
