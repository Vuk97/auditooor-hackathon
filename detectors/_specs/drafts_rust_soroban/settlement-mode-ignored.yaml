id: settlement-mode-ignored
title: "Settlement mode argument silently ignored"
severity: Medium
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44421"
bug_class: input-validation
indicators:
  - "function accepts `settlement_mode: SettlementMode` but always executes one branch"
  - "match arms all collapse to same behavior"
victim: "Users: expected mode differences don't exist."
exploit_precondition: "Caller uses non-default mode."
real_world_example: |
  V12 #44421 (Medium): Settlement mode always ignored.
suggested_remediation: "Either implement the distinct branches or drop the parameter."
cross_refs:
  - liquidator-always-receives-underlying
