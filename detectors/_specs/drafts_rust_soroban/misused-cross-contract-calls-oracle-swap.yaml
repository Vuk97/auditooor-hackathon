id: misused-cross-contract-calls-oracle-swap
title: "Misused cross-contract calls break oracle and swap flows"
severity: Low
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44829"
bug_class: abi-mismatch
indicators:
  - "wrong method name or arg order on cross-contract call"
  - "runtime revert masks specific failure mode"
victim: "Protocol: broken flows."
exploit_precondition: "Flow exercised."
real_world_example: |
  V12 #44829 (Low): Misused cross-contract calls break oracle and swap flows.
suggested_remediation: "Generate clients from target wasm; integration test each call."
cross_refs:
  - cross-contract-abi-mismatch-reverts
