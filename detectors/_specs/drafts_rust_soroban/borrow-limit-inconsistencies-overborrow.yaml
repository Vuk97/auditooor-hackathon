id: borrow-limit-inconsistencies-overborrow
title: "Borrow-limit inconsistencies allow over-borrowing"
severity: Medium
language: rust
platform: soroban
source: "halborn-2025-09"
source_ids:
  - "7.15"
bug_class: input-validation
indicators:
  - "`borrow_cap` read in one path but not in another (e.g. flash-loan vs direct borrow)"
  - "cap check uses pre-transfer balance but post-check used in validation"
victim: "Protocol: attacker borrows past intended cap."
exploit_precondition: "Inconsistent cap-check paths."
real_world_example: |
  Halborn 7.15 (MEDIUM): Borrow-limit inconsistencies allow over-borrowing.
suggested_remediation: "Centralize cap check in one helper invoked by every borrow path."
cross_refs:
  - reserve-cap-bypass-swap-collateral
