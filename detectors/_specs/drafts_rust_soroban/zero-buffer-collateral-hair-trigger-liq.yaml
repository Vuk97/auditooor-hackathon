id: zero-buffer-collateral-hair-trigger-liq
title: "Zero-buffer collateral config triggers hair-trigger liquidations"
severity: Info
language: rust
platform: soroban
source: "halborn-2025-09"
source_ids:
  - "7.37"
bug_class: input-validation
indicators:
  - "reserve config allows `ltv == liquidation_threshold`"
  - "no minimum buffer between the two"
victim: "Borrowers: any price tick liquidates them."
exploit_precondition: "Admin sets params with zero buffer."
real_world_example: |
  Halborn 7.37 (INFO): Zero-buffer collateral config triggers hair-trigger liquidations.
suggested_remediation: "Enforce `liquidation_threshold - ltv >= MIN_BUFFER` on setter."
cross_refs:
  - missing-cross-param-validation-threshold-bonus
