id: broken-asset-list-invariant-dos
title: "Broken asset-list invariant causes DoS and orphaned whitelist"
severity: Medium
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44867"
bug_class: input-validation
indicators:
  - "asset-list ordering invariant (sorted/unique) not enforced on every write"
  - "binary-search lookup fails when invariant is violated"
victim: "Protocol: related lookups revert."
exploit_precondition: "Admin add with duplicates or wrong order."
real_world_example: |
  V12 #44867 (Medium): Broken asset-list invariant causes DoS.
suggested_remediation: "Enforce sort+unique in every mutator."
cross_refs:
  - ttl-desynced-registries
  - ordered-reserve-list-drop-leaves-bitmap
