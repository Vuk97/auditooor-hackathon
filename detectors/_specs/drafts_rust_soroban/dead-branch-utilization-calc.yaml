id: dead-branch-utilization-calc
title: "Dead branch in utilization calculation obscures intent"
severity: Info
language: rust
platform: soroban
source: "halborn-2025-09"
source_ids:
  - "7.53"
bug_class: error-handling
indicators:
  - "branch like `if total_supply == 0 { return Err(DivideByZero) }` unreachable given `total_debt` check"
  - "dead code path"
victim: "Developers: misreads dead branch as safety check."
exploit_precondition: "N/A."
real_world_example: |
  Halborn 7.53 (INFO): Dead branch in utilization calculation obscures intent.
suggested_remediation: "Remove or document."
cross_refs: []
