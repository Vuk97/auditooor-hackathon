id: wrong-active-slot-check
title: "Wrong active-slot check in reserve enumeration"
severity: Medium
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44494"
bug_class: input-validation
indicators:
  - "iteration over reserve bitmap checks wrong bit (off-by-one or wrong mask)"
  - "active reserves silently skipped"
victim: "Users: positions on skipped reserves miscounted."
exploit_precondition: "Depends on which slot the off-by-one affects."
real_world_example: |
  V12 #44494 (Medium): Wrong active-slot check.
suggested_remediation: "Use a single canonical bitmap iterator helper; unit-test across all slots."
cross_refs:
  - acl-membership-first-64-only
