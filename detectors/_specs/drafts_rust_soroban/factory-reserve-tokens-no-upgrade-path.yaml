id: factory-reserve-tokens-no-upgrade-path
title: "Factory reserve tokens cannot be upgraded through intended path"
severity: Medium
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44842"
bug_class: authorization
indicators:
  - "factory-deployed aToken/debtToken upgrade requires admin auth on the token itself, but factory holds upgrade-admin key"
  - "no proxy/pass-through upgrade helper"
victim: "Protocol: cannot patch reserve token bugs via intended ops flow."
exploit_precondition: "Upgrade needed on factory-deployed token."
real_world_example: |
  V12 #44842 (Medium): Factory reserve tokens cannot be upgraded through intended path.
suggested_remediation: "Add a factory-mediated upgrade helper that forwards auth."
cross_refs:
  - wrong-actor-forwarding-bricks-upgrade
