id: misnamed-debt-token-views
title: "Misnamed debt-token views misstate borrow context"
severity: Low
language: rust
platform: soroban
source: "halborn-2025-09"
source_ids:
  - "7.24"
bug_class: error-handling
indicators:
  - "view named `balance()` returns scaled debt while callers expect underlying"
  - "view name doesn't match return semantics"
victim: "Integrators."
exploit_precondition: "Caller trusts the name."
real_world_example: |
  Halborn 7.24 (LOW): Misnamed debt-token views misstate borrow context.
suggested_remediation: "Rename to `scaled_balance()` / `balance_with_interest()`."
cross_refs:
  - repay-function-doc-misalignment
