id: treasury-not-upgradable
title: "Treasury cannot be upgraded after deployment"
severity: High
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44872"
bug_class: authorization
indicators:
  - "no `upgrade(wasm_hash)` entry point on treasury contract"
  - "treasury code frozen at deploy"
victim: "Protocol: cannot patch treasury bugs."
exploit_precondition: "A bug is found in treasury code."
real_world_example: |
  V12 #44872 (High): Treasury cannot be upgraded after deployment.
suggested_remediation: "Add an admin-gated `upgrade(new_wasm)` with require_auth."
cross_refs:
  - upgrade-admin-permanently-immutable
