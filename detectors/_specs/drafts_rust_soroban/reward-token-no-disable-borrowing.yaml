id: reward-token-no-disable-borrowing
title: "Missing functionality to disable borrowing on a reserve"
severity: Low
language: rust
platform: soroban
source: "k2-v12"
source_ids:
  - "44374"
bug_class: error-handling
indicators:
  - "no `set_borrow_disabled(reserve, true)` admin setter"
  - "only full freeze available, which blocks more than just borrow"
victim: "Protocol: cannot granularly throttle borrowing during stress."
exploit_precondition: "Market stress requires borrow-only pause."
real_world_example: |
  V12 #44374 (Low): Missing functionality to disable borrowing on a reserve.
suggested_remediation: "Add per-action disable flags."
cross_refs:
  - reserve-pause-ignored-in-core-ops
