skeleton: "name_match_missing_require"
name: "m-15-check-to-is-not-empty"
class_name: "M15CheckToIsNotEmpty"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #1293 (Code4rena/Sandclock)"
help: "[M-15] Check _to is not empty"
wiki_title: "[M-15] Check _to is not empty"
wiki_description: |
    _Submitted by pauliax_
    
    functions `claimYield`, `\_withdraw`, and `\_unsponsor` should validate that `\_to` is not an empty 0x0 address to prevent accidental burns.
    
    #### Recommended Mitigation Steps
    
    Consider implementing the proposed validation:
    require `\_to != address(0)`
    
    **[gabrielpoca (Sandcl
wiki_exploit_scenario: |
    Per Solodit #1293: _Submitted by pauliax_
    
    functions `claimYield`, `\_withdraw`, and `\_unsponsor` should validate that `\_to` is not an empty 0x0 address to prevent accidental burns.
    
    #### Recommended Mitigation Steps
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M15CheckToIsNotEmpty"
solodit_id: "1293"
solodit_slug: "m-15-check-_to-is-not-empty-code4rena-sandclock-sandclock-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal claimyield;"
fn_name_regex: ".*(claimYield).*"
write_var_regex: ".*(claimYield).*"
guard_var_regex: ".*(claimYield).*"
vuln_fn_name: "claimYield"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "claimyield = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
