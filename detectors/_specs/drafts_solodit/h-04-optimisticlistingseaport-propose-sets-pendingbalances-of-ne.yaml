skeleton: "name_match_missing_call"
name: "h-04-optimisticlistingseaport-propose-sets-pendingbalances-of-ne"
class_name: "H04OptimisticlistingseaportProposeSetsPendingbalancesOfNe"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #12198 (Code4rena/Tessera)"
help: |
    [H-04] OptimisticListingSeaport.propose sets pendingBalances of newly added proposer instead of previous one
wiki_title: |
    [H-04] OptimisticListingSeaport.propose sets pendingBalances of newly added proposer instead of previous one
wiki_description: |
    In `OptimisticListingSeaport.propose`, `pendingBalances` is set to the collateral. The purpose of this is that the proposer of a previous proposal can withdraw his collateral afterwards. However, this is done on the storage variable `proposedListing` after the new listing is already set:
    
    ```solidi
wiki_exploit_scenario: |
    Per Solodit #12198: In `OptimisticListingSeaport.propose`, `pendingBalances` is set to the collateral. The purpose of this is that the proposer of a previous proposal can withdraw his collateral afterwards. However, thi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H04OptimisticlistingseaportProposeSetsPendingbalancesOfNe"
solodit_id: "12198"
solodit_slug: |
    h-04-optimisticlistingseaportpropose-sets-pendingbalances-of-newly-added-proposer-instead-of-previous-one-code4rena-tessera-tessera-versus-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal pendingbalances;"
fn_name_regex: ".*(pendingBalances|proposedListing).*"
read_var_regex: ".*(pendingBalances|proposedListing).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "pendingBalances"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return pendingbalances > 0;"
