skeleton: "name_match_missing_call"
name: "m-09-token-vault-mapping-can-be-overwritten"
class_name: "M09TokenVaultMappingCanBeOverwritten"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #780 (Code4rena/yAxis)"
help: "[M-09] token -> vault mapping can be overwritten"
wiki_title: "[M-09] token -> vault mapping can be overwritten"
wiki_description: |
    _Submitted by cmichel_
    
    One vault can have many tokens, but each token should only be assigned to a single vault.
    The `Manager` contract keeps a mapping of tokens to vaults in the `vaults[_token] => _vault` map, and a mapping of vault to tokens in `tokens[vault] => token[]`.
    
    The `addToken` function
wiki_exploit_scenario: |
    Per Solodit #780: _Submitted by cmichel_
    
    One vault can have many tokens, but each token should only be assigned to a single vault.
    The `Manager` contract keeps a mapping of tokens to vaults in the `vaults[_token] => _
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M09TokenVaultMappingCanBeOverwritten"
solodit_id: "780"
solodit_slug: "m-09-token-vault-mapping-can-be-overwritten-code4rena-yaxis-yaxis-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal addtoken;"
fn_name_regex: ".*(Manager|addToken|token|onlyVault).*"
read_var_regex: ".*(addToken|onlyVault|removeToken).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "Manager"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return addtoken > 0;"
