skeleton: "name_match_missing_require"
name: "integer-underflow"
class_name: "IntegerUnderflow"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #50267 (Halborn/BnbX)"
help: "INTEGER UNDERFLOW"
wiki_title: "INTEGER UNDERFLOW"
wiki_description: |
    ##### Description
    
    Calling the `increaseTotalRedelegated` function is causing an integer underflow in the `startUndelegation` function.
    
    If the `totalRedelegated` amount is increased by calling `increaseTotalRedelegated`, the `startUndelegation` transaction will revert with: `Arithmetic operation un
wiki_exploit_scenario: |
    Per Solodit #50267: ##### Description
    
    Calling the `increaseTotalRedelegated` function is causing an integer underflow in the `startUndelegation` function.
    
    If the `totalRedelegated` amount is increased by calling `incre
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IntegerUnderflow"
solodit_id: "50267"
solodit_slug: "integer-underflow-halborn-stader-labs-bnbx-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal increasetotalredelegated;"
fn_name_regex: ".*(increaseTotalRedelegated|startUndelegation|totalRedelegated|totalDeposited).*"
write_var_regex: ".*(increaseTotalRedelegated|requestWithdraw|startUndelegation).*"
guard_var_regex: ".*(increaseTotalRedelegated|requestWithdraw|startUndelegation).*"
vuln_fn_name: "increaseTotalRedelegated"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "increasetotalredelegated = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
