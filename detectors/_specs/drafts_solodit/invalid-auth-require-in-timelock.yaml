skeleton: "name_match_missing_call"
name: "invalid-auth-require-in-timelock"
class_name: "InvalidAuthRequireInTimelock"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #57506 (Zokyo/Fractal)"
help: "Invalid auth require in Timelock."
wiki_title: "Invalid auth require in Timelock."
wiki_description: |
    **Description**
    
    Timelock.sol
    
    46: function updateDelay(uint256 newDelay) external virtual authorized {
    
    47:
    
    require(
    
    48:
    
    msg.sender == address(this),
    
    49:
    
    "TimelockController: caller must be timelock"
    
    50:
    
    );
    
    51:
    
    emit MinDelayChange(minDelay, newDelay);
    
    52:
    
    minDelay = newDelay;
    
    53: }
    
    Thi
wiki_exploit_scenario: |
    Per Solodit #57506: **Description**
    
    Timelock.sol
    
    46: function updateDelay(uint256 newDelay) external virtual authorized {
    
    47:
    
    require(
    
    48:
    
    msg.sender == address(this),
    
    49:
    
    "TimelockController: caller must be time
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InvalidAuthRequireInTimelock"
solodit_id: "57506"
solodit_slug: "invalid-auth-require-in-timelock-zokyo-none-fractal-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(updateDelay|newDelay|minDelay).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "updateDelay"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
