skeleton: "name_match_missing_require"
name: "m-25-sptknminimaloracle-calculatesptknperbase-does-not-return-0-"
class_name: "M25SptknminimaloracleCalculatesptknperbaseDoesNotReturn0"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #52777 (Sherlock/Peapods)"
help: |
    M-25: spTKNMinimalOracle `_calculateSpTknPerBase()` does not return 0, which breaks the feature of having a fallback oracle.
wiki_title: |
    M-25: spTKNMinimalOracle `_calculateSpTknPerBase()` does not return 0, which breaks the feature of having a fallback oracle.
wiki_description: |
    Source: https://github.com/sherlock-audit/2025-01-peapods-finance-judging/issues/476 
    
    ## Found by 
    X77, pkqs90
    
    
    ### Summary
    
    spTKNMinimalOracle `_calculateSpTknPerBase()` does not return 0, which breaks the feature of having a fallback oracle.
    
    ### Root Cause
    
    The spTKNMinimalOracle is a dual orac
wiki_exploit_scenario: |
    Per Solodit #52777: Source: https://github.com/sherlock-audit/2025-01-peapods-finance-judging/issues/476 
    
    ## Found by 
    X77, pkqs90
    
    
    ### Summary
    
    spTKNMinimalOracle `_calculateSpTknPerBase()` does not return 0, which br
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M25SptknminimaloracleCalculatesptknperbaseDoesNotReturn0"
solodit_id: "52777"
solodit_slug: |
    m-25-sptknminimaloracle-_calculatesptknperbase-does-not-return-0-which-breaks-the-feature-of-having-a-fallback-oracle-sherlock-peapods-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(spTKNMinimalOracle|getPrices).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "spTKNMinimalOracle"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
