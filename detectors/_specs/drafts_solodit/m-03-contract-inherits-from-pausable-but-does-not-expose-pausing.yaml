skeleton: "name_match_missing_require"
name: "m-03-contract-inherits-from-pausable-but-does-not-expose-pausing"
class_name: "M03ContractInheritsFromPausableButDoesNotExposePausing"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20513 (Pashov Audit Group/Parcel Payroll)"
help: "[M-03] Contract inherits from `Pausable` but does not expose pausing/unpausing functionality"
wiki_title: "[M-03] Contract inherits from `Pausable` but does not expose pausing/unpausing functionality"
wiki_description: |
    **Impact:**
    Low, as methods do not have `whenNotPaused` modifier
    
    **Likelihood:**
    High, as it is certain that contract can't be paused at all
    
    **Description**
    
    The `Organizer` smart contract inherits from OpenZeppelin's `Pausable` contract, but the `_pause` and `_unpause` methods are not exposed ext
wiki_exploit_scenario: |
    Per Solodit #20513: **Impact:**
    Low, as methods do not have `whenNotPaused` modifier
    
    **Likelihood:**
    High, as it is certain that contract can't be paused at all
    
    **Description**
    
    The `Organizer` smart contract inherits
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03ContractInheritsFromPausableButDoesNotExposePausing"
solodit_id: "20513"
solodit_slug: |
    m-03-contract-inherits-from-pausable-but-does-not-expose-pausingunpausing-functionality-pashov-none-parcel-payroll-markdown
solodit_quality: "5"
solodit_rarity: "1"
solodit_tags: "Missing Check, Pause"
state_decl: "uint256 internal whennotpaused;"
fn_name_regex: ".*(whenNotPaused|Organizer|Pausable).*"
write_var_regex: ".*(whenNotPaused).*"
guard_var_regex: ".*(whenNotPaused).*"
vuln_fn_name: "whenNotPaused"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "whennotpaused = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
