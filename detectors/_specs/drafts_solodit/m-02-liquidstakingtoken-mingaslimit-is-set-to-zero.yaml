skeleton: "name_match_missing_call"
name: "m-02-liquidstakingtoken-mingaslimit-is-set-to-zero"
class_name: "M02LiquidstakingtokenMingaslimitIsSetToZero"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #44032 (Shieldify/Dinero Supereth)"
help: "[M-02] `LiquidStakingToken._minGasLimit()` Is Set to Zero"
wiki_title: "[M-02] `LiquidStakingToken._minGasLimit()` Is Set to Zero"
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    When sending slow messages using `_sendSlowSyncMessage()`, `_minGasLimit()` is used:
    
    ```solidity
    function _sendSlowSyncMessage(
        address,
        uint256 _value,
        uint256,
        bytes memory _data
    ) internal override {
        bytes memory message = abi.encodeC
wiki_exploit_scenario: |
    Per Solodit #44032: ## Severity
    
    Medium Risk
    
    ## Description
    
    When sending slow messages using `_sendSlowSyncMessage()`, `_minGasLimit()` is used:
    
    ```solidity
    function _sendSlowSyncMessage(
        address,
        uint256 _val
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02LiquidstakingtokenMingaslimitIsSetToZero"
solodit_id: "44032"
solodit_slug: "m-02-liquidstakingtoken_mingaslimit-is-set-to-zero-shieldify-none-dinero-supereth-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal mingaslimit;"
fn_name_regex: ".*(minGasLimit|withdraw).*"
read_var_regex: ".*(minGasLimit|withdraw).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "minGasLimit"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return mingaslimit > 0;"
