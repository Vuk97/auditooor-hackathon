skeleton: "name_match_missing_call"
name: "wrong-poolsize-increment-in-goldilend-repay"
class_name: "WrongPoolsizeIncrementInGoldilendRepay"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #30922 (Cyfrin/Goldilocks)"
help: "Wrong `PoolSize` increment in `Goldilend.repay()`"
wiki_title: "Wrong `PoolSize` increment in `Goldilend.repay()`"
wiki_description: |
    **Severity:** High
    
    **Description:** When a user repays his loan using `repay()`, it increases `poolSize` with the repaid interest. During the increment, it uses the wrong amount.
    
    ```solidity
      function repay(uint256 repayAmount, uint256 _userLoanId) external {
        Loan memory userLoan = loans[msg.
wiki_exploit_scenario: |
    Per Solodit #30922: **Severity:** High
    
    **Description:** When a user repays his loan using `repay()`, it increases `poolSize` with the repaid interest. During the increment, it uses the wrong amount.
    
    ```solidity
      funct
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "WrongPoolsizeIncrementInGoldilendRepay"
solodit_id: "30922"
solodit_slug: "wrong-poolsize-increment-in-goldilendrepay-cyfrin-none-cyfrin-goldilocks-v11-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal interest;"
fn_name_regex: ".*(repay|poolSize|interest|PoolSize).*"
read_var_regex: ".*(interest|poolSize|repay).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "repay"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return interest > 0;"
