skeleton: "name_match_missing_require"
name: "m-03-the-attacker-can-prevent-the-deposit-from-reaching-maxraise"
class_name: "M03TheAttackerCanPreventTheDepositFromReachingMaxraise"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #49909 (Shieldify/H4Shfund)"
help: "[M-03] The Attacker Can Prevent The Deposit From Reaching `maxRaiseAmount`"
wiki_title: "[M-03] The Attacker Can Prevent The Deposit From Reaching `maxRaiseAmount`"
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    In `Leap.depositEth()`, if the current deposit exceeds `maxRaiseAmount`, it will revert:
    
    ```solidity
    //uncapped if set to 0 initially.
    if (launchParams.maxRaiseAmount != 0) {
        if (totalRaised + _value > launchParams.maxRaiseAmount)
            //cannot raise
wiki_exploit_scenario: |
    Per Solodit #49909: ## Severity
    
    Medium Risk
    
    ## Description
    
    In `Leap.depositEth()`, if the current deposit exceeds `maxRaiseAmount`, it will revert:
    
    ```solidity
    //uncapped if set to 0 initially.
    if (launchParams.maxRa
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03TheAttackerCanPreventTheDepositFromReachingMaxraise"
solodit_id: "49909"
solodit_slug: |
    m-03-the-attacker-can-prevent-the-deposit-from-reaching-maxraiseamount-shieldify-none-h4shfund-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal maxallocation;"
fn_name_regex: ".*(maxRaiseAmount|PHASE_DEPOSIT|maxAllocation).*"
write_var_regex: ".*(maxAllocation|maxRaiseAmount).*"
guard_var_regex: ".*(maxAllocation|maxRaiseAmount).*"
vuln_fn_name: "maxRaiseAmount"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "maxallocation = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
