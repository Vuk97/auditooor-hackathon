skeleton: "name_match_missing_call"
name: "ticket-duplication"
class_name: "TicketDuplication"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #13583 (ConsenSys/PoolTogether - LootBox and MultipleWinners Strategy)"
help: "Ticket duplication"
wiki_title: "Ticket duplication"
wiki_description: |
    #### Description
    
    
    `Ticket._beforeTokenTransfer()` contains logic to update the `SortitionSumTree` from which prize winners are drawn. In the case where the `from` address is the same as the `to` address, tickets are duplicated rather than left unchanged. This allows any attacker to duplicate their
wiki_exploit_scenario: |
    Per Solodit #13583: #### Description
    
    
    `Ticket._beforeTokenTransfer()` contains logic to update the `SortitionSumTree` from which prize winners are drawn. In the case where the `from` address is the same as the `to` addr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TicketDuplication"
solodit_id: "13583"
solodit_slug: "ticket-duplication-consensys-pooltogether-lootbox-and-multiplewinners-strategy-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal from;"
fn_name_regex: ".*(SortitionSumTree|from).*"
read_var_regex: ".*(from).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "SortitionSumTree"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return from > 0;"
