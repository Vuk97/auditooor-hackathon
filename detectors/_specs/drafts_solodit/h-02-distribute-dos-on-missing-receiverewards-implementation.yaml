skeleton: "name_match_missing_call"
name: "h-02-distribute-dos-on-missing-receiverewards-implementation"
class_name: "H02DistributeDosOnMissingReceiverewardsImplementation"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #42181 (Code4rena/NFTX)"
help: "[H-02] `distribute` DoS on missing `receiveRewards` implementation"
wiki_title: "[H-02] `distribute` DoS on missing `receiveRewards` implementation"
wiki_description: |
    `NFTXEligiblityManager._sendForReceiver` should check `returnData.length == 1` before decoding. Otherwise, if it returns no return data, the `abi.decode` call will revert and with it the whole `distribute` function .
    
    A single poorly implemented `feeReceiver` can break the whole `distribute` functi
wiki_exploit_scenario: |
    Per Solodit #42181: `NFTXEligiblityManager._sendForReceiver` should check `returnData.length == 1` before decoding. Otherwise, if it returns no return data, the `abi.decode` call will revert and with it the whole `distr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02DistributeDosOnMissingReceiverewardsImplementation"
solodit_id: "42181"
solodit_slug: "h-02-distribute-dos-on-missing-receiverewards-implementation-code4rena-nftx-nftx-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal distribute;"
fn_name_regex: ".*(distribute|feeReceiver|receiveRewards).*"
read_var_regex: ".*(distribute|feeReceiver).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "distribute"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return distribute > 0;"
