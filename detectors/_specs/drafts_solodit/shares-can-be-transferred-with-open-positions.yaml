skeleton: "name_match_missing_call"
name: "shares-can-be-transferred-with-open-positions"
class_name: "SharesCanBeTransferredWithOpenPositions"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #33813 (OpenZeppelin/Panoptic Audit)"
help: "Shares Can Be Transferred With Open Positions"
wiki_title: "Shares Can Be Transferred With Open Positions"
wiki_description: |
    Shares can be transferred or redeemed from an owner's account while they have open positions. In several cases, `msg.sender` is used where it should be `from` or `owner`:
    
    
    * `transferFrom`
    * `maxWithdraw` used in `withdraw`
    * `maxRedeem` used in `redeem`
    
    
    By approving a second account with no open
wiki_exploit_scenario: |
    Per Solodit #33813: Shares can be transferred or redeemed from an owner's account while they have open positions. In several cases, `msg.sender` is used where it should be `from` or `owner`:
    
    
    * `transferFrom`
    * `maxWith
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SharesCanBeTransferredWithOpenPositions"
solodit_id: "33813"
solodit_slug: "shares-can-be-transferred-with-open-positions-openzeppelin-none-panoptic-audit-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal from;"
fn_name_regex: ".*(from|owner|transferFrom|maxWithdraw).*"
read_var_regex: ".*(from|maxRedeem|maxWithdraw).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "from"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return from > 0;"
