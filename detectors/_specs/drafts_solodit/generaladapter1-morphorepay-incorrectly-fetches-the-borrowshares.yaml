skeleton: "name_match_missing_require"
name: "generaladapter1-morphorepay-incorrectly-fetches-the-borrowshares"
class_name: "Generaladapter1MorphorepayIncorrectlyFetchesTheBorrowshares"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #44974 (Spearbit/Morpho Bundler v3)"
help: "GeneralAdapter1.morphoRepay() incorrectly fetches the borrowShares of initiator instead of onBe-"
wiki_title: "GeneralAdapter1.morphoRepay() incorrectly fetches the borrowShares of initiator instead of onBe-"
wiki_description: |
    ## Half
    
    **Severity:** Medium Risk  
    **Context:** GeneralAdapter1.sol#L309-L312, GeneralAdapter1.sol#L316  
    
    **Description:**  
    When `GeneralAdapter1.morphoRepay()` is called with `shares == type(uint256).max`, `MorphoLib.borrowShares()` is called to set shares to the amount of borrowShares held by
wiki_exploit_scenario: |
    Per Solodit #44974: ## Half
    
    **Severity:** Medium Risk  
    **Context:** GeneralAdapter1.sol#L309-L312, GeneralAdapter1.sol#L316  
    
    **Description:**  
    When `GeneralAdapter1.morphoRepay()` is called with `shares == type(uint
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "Generaladapter1MorphorepayIncorrectlyFetchesTheBorrowshares"
solodit_id: "44974"
solodit_slug: |
    generaladapter1morphorepay-incorrectly-fetches-the-borrowshares-of-initiator-instead-of-onbe-spearbit-none-morpho-bundler-v3-pdf
solodit_quality: "5"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal initiator;"
fn_name_regex: ".*(onBehalf|initiator).*"
write_var_regex: ".*(initiator|onBehalf).*"
guard_var_regex: ".*(initiator|onBehalf).*"
vuln_fn_name: "onBehalf"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "initiator = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
