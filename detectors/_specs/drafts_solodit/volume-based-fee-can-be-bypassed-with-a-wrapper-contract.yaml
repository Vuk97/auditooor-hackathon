skeleton: "paired_function_divergence"
name: "volume-based-fee-can-be-bypassed-with-a-wrapper-contract"
class_name: "VolumeBasedFeeCanBeBypassedWithAWrapperContract"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #61602 (Cyfrin/Octodefi)"
help: "Volume-based fee can be bypassed with a wrapper contract"
wiki_title: "Volume-based fee can be bypassed with a wrapper contract"
wiki_description: |
    **Description:** `_executeAction()` levies a percentage fee only when `FeeController.getTokenForAction()` can map the `(target, selector)` pair to a tracked token. If the mapping is absent it falls back to `minFeeInUSD`.
    An attacker can therefore wrap any high-value call inside a helper contract tha
wiki_exploit_scenario: |
    Per Solodit #61602: **Description:** `_executeAction()` levies a percentage fee only when `FeeController.getTokenForAction()` can map the `(target, selector)` pair to a tracked token. If the mapping is absent it falls ba
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "VolumeBasedFeeCanBeBypassedWithAWrapperContract"
solodit_id: "61602"
solodit_slug: "volume-based-fee-can-be-bypassed-with-a-wrapper-contract-cyfrin-none-octodefi-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(getTokenForAction|minFeeInUSD).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
