skeleton: "name_match_missing_call"
name: "m-12-missing-maxnumberofkeys-checks-in-sharekey-and-grantkey"
class_name: "M12MissingMaxnumberofkeysChecksInSharekeyAndGrantkey"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #1088 (Code4rena/Unlock Protocol)"
help: "[M-12] Missing maxNumberOfKeys checks in shareKey and grantKey"
wiki_title: "[M-12] Missing maxNumberOfKeys checks in shareKey and grantKey"
wiki_description: |
    _Submitted by kenzo_
    
    More keys can be minted than maxNumberOfKeys since `shareKey` and `grantKey` do not check if the lock is sold out.
    
    #### Impact
    
    More keys can be minted than intended.
    
    #### Proof of Concept
    
    In both `shareKey` and `grantKey`, if minting a new token, a new token is simply minte
wiki_exploit_scenario: |
    Per Solodit #1088: _Submitted by kenzo_
    
    More keys can be minted than maxNumberOfKeys since `shareKey` and `grantKey` do not check if the lock is sold out.
    
    #### Impact
    
    More keys can be minted than intended.
    
    #### Proo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M12MissingMaxnumberofkeysChecksInSharekeyAndGrantkey"
solodit_id: "1088"
solodit_slug: |
    m-12-missing-maxnumberofkeys-checks-in-sharekey-and-grantkey-code4rena-unlock-protocol-unlock-protocol-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal grantkey;"
fn_name_regex: ".*(shareKey|grantKey|maxNumberOfKeys|purchase).*"
read_var_regex: ".*(grantKey|maxNumberOfKeys|notSoldOut).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "shareKey"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return grantkey > 0;"
