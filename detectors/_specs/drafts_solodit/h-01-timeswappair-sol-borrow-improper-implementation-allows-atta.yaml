skeleton: "paired_function_divergence"
name: "h-01-timeswappair-sol-borrow-improper-implementation-allows-atta"
class_name: "H01TimeswappairSolBorrowImproperImplementationAllowsAtta"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #25631 (Code4rena/Timeswap)"
help: |
    [H-01] `TimeswapPair.sol#borrow()` Improper implementation allows attacker to increase `pool.state.z` to a large value
wiki_title: |
    [H-01] `TimeswapPair.sol#borrow()` Improper implementation allows attacker to increase `pool.state.z` to a large value
wiki_description: |
    _Submitted by WatchPug_
    
    In the current implementation, `borrow()` takes a user input value of `zIncrease`, while the actual collateral asset transferred in is calculated at L319, the state of `pool.state.z` still increased by the value of the user's input at L332.
    
    Even though a large number of `zI
wiki_exploit_scenario: |
    Per Solodit #25631: _Submitted by WatchPug_
    
    In the current implementation, `borrow()` takes a user input value of `zIncrease`, while the actual collateral asset transferred in is calculated at L319, the state of `pool.s
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01TimeswappairSolBorrowImproperImplementationAllowsAtta"
solodit_id: "25631"
solodit_slug: |
    h-01-timeswappairsolborrow-improper-implementation-allows-attacker-to-increase-poolstatez-to-a-large-value-code4rena-timeswap-timeswap-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(borrow|pay|xDecrease).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
