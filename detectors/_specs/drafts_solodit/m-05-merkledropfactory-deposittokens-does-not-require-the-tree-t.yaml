skeleton: "name_match_missing_require"
name: "m-05-merkledropfactory-deposittokens-does-not-require-the-tree-t"
class_name: "M05MerkledropfactoryDeposittokensDoesNotRequireTheTreeT"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42553 (Code4rena/FactoryDAO)"
help: "[M-05] `MerkleDropFactory.depositTokens()`  does not require the tree to exist"
wiki_title: "[M-05] `MerkleDropFactory.depositTokens()`  does not require the tree to exist"
wiki_description: |
    _Submitted by AuditsAreUS_
    
    The function `depositTokens()` does not first check to ensure that the `treeIndex` exists.
    
    The impact is that we will attempt to transfer from the zero address to this address. If the transfer succeeds (which it currently does not since we use `IERC20.transferFrom()`)  t
wiki_exploit_scenario: |
    Per Solodit #42553: _Submitted by AuditsAreUS_
    
    The function `depositTokens()` does not first check to ensure that the `treeIndex` exists.
    
    The impact is that we will attempt to transfer from the zero address to this add
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M05MerkledropfactoryDeposittokensDoesNotRequireTheTreeT"
solodit_id: "42553"
solodit_slug: |
    m-05-merkledropfactorydeposittokens-does-not-require-the-tree-to-exist-code4rena-factorydao-factorydao-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal deposittokens;"
fn_name_regex: ".*(depositTokens|treeIndex|tokenBalance|safeTransferFrom).*"
write_var_regex: ".*(depositTokens|safeTransferFrom|tokenBalance).*"
guard_var_regex: ".*(depositTokens|safeTransferFrom|tokenBalance).*"
vuln_fn_name: "depositTokens"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "deposittokens = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
