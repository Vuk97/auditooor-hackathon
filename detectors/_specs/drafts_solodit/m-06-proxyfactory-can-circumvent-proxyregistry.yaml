skeleton: "name_match_missing_call"
name: "m-06-proxyfactory-can-circumvent-proxyregistry"
class_name: "M06ProxyfactoryCanCircumventProxyregistry"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #3148 (Code4rena/Mimo DeFi)"
help: "[M-06] ProxyFactory can circumvent ProxyRegistry"
wiki_title: "[M-06] ProxyFactory can circumvent ProxyRegistry"
wiki_description: |
    _Submitted by 0xDjango_
    
    The `deployFor()` function in `MIMOProxyFactory.sol` can be called directly instead of being called within `MIMOProxyRegistry.sol`. This results in the ability to create many MIMOProxies that are not registered within the registry. The proxies deployed directly through the f
wiki_exploit_scenario: |
    Per Solodit #3148: _Submitted by 0xDjango_
    
    The `deployFor()` function in `MIMOProxyFactory.sol` can be called directly instead of being called within `MIMOProxyRegistry.sol`. This results in the ability to create many
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M06ProxyfactoryCanCircumventProxyregistry"
solodit_id: "3148"
solodit_slug: "m-06-proxyfactory-can-circumvent-proxyregistry-code4rena-mimo-defi-mimo-august-2022-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal deployfor;"
fn_name_regex: ".*(deployFor|onlyRegistry).*"
read_var_regex: ".*(deployFor|onlyRegistry).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "deployFor"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return deployfor > 0;"
