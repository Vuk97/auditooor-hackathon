skeleton: "name_match_missing_require"
name: "c-01-split-can-be-abused-to-create-locked-data-with-an-arbitrary"
class_name: "C01SplitCanBeAbusedToCreateLockedDataWithAnArbitrary"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #58150 (Pashov Audit Group/KittenSwap_2025-05-07)"
help: "[C-01] `split` can be abused to create `locked` data with an arbitrary `amount`"
wiki_title: "[C-01] `split` can be abused to create `locked` data with an arbitrary `amount`"
wiki_description: |
    ## Severity
    
    **Impact:** High
    
    **Likelihood:** High
    
    ## Description
    
    Due to the lack of a check on the split amount inside `split`, a user can provide an arbitrary `_amount` to the function. Since `locked.amount` is stored as an `int128`, this can result in one of the split tokens having a negative
wiki_exploit_scenario: |
    Per Solodit #58150: ## Severity
    
    **Impact:** High
    
    **Likelihood:** High
    
    ## Description
    
    Due to the lack of a check on the split amount inside `split`, a user can provide an arbitrary `_amount` to the function. Since `lo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "C01SplitCanBeAbusedToCreateLockedDataWithAnArbitrary"
solodit_id: "58150"
solodit_slug: |
    c-01-split-can-be-abused-to-create-locked-data-with-an-arbitrary-amount-pashov-audit-group-none-kittenswap_2025-05-07-markdown_
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal int128;"
fn_name_regex: ".*(split|int128|locked|amount).*"
write_var_regex: ".*(int128|locked|split).*"
guard_var_regex: ".*(int128|locked|split).*"
vuln_fn_name: "split"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "int128 = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
