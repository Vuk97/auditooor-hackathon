skeleton: "paired_function_divergence"
name: "m-09-createpair-expects-zero-slippage"
class_name: "M09CreatepairExpectsZeroSlippage"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42468 (Code4rena/Trader Joe)"
help: "[M-09] `createPair()` expects zero slippage"
wiki_title: "[M-09] `createPair()` expects zero slippage"
wiki_description: |
    _Submitted by sirhashalot_
    
    The LaunchEvent.sol `createPair()` function calls router.addLiquidity() with a amountADesired == amountAMin and amountBDesired == amountBMin. Because there is no allowance for slippage, if the zero slippage requirement is not met then the addLiquidity() function [will rev
wiki_exploit_scenario: |
    Per Solodit #42468: _Submitted by sirhashalot_
    
    The LaunchEvent.sol `createPair()` function calls router.addLiquidity() with a amountADesired == amountAMin and amountBDesired == amountBMin. Because there is no allowance
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M09CreatepairExpectsZeroSlippage"
solodit_id: "42468"
solodit_slug: "m-09-createpair-expects-zero-slippage-code4rena-trader-joe-trader-joe-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(createPair).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
