skeleton: "name_match_missing_call"
name: "m-09-repository-removecontract-removes-the-contract-wrongly"
class_name: "M09RepositoryRemovecontractRemovesTheContractWrongly"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #16087 (Code4rena/Malt Protocol)"
help: "[M-09] `Repository._removeContract()` removes the contract wrongly."
wiki_title: "[M-09] `Repository._removeContract()` removes the contract wrongly."
wiki_description: |
    After removing the contract, the `contracts` array would contain the wrong contract names.
    
    ### Proof of Concept
    
    `Repository._removeContract()` removes the contract name from `contracts` array.
    
    ```solidity
    File: 2023-02-malt\contracts\Repository.sol
    223:   function _removeContract(string memory _
wiki_exploit_scenario: |
    Per Solodit #16087: After removing the contract, the `contracts` array would contain the wrong contract names.
    
    ### Proof of Concept
    
    `Repository._removeContract()` removes the contract name from `contracts` array.
    
    ```
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M09RepositoryRemovecontractRemovesTheContractWrongly"
solodit_id: "16087"
solodit_slug: |
    m-09-repository_removecontract-removes-the-contract-wrongly-code4rena-malt-protocol-malt-protocol-versus-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal contracts;"
fn_name_regex: ".*(contracts).*"
read_var_regex: ".*(contracts).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "contracts"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return contracts > 0;"
