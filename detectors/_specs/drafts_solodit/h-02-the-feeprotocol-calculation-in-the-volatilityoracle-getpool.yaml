skeleton: "name_match_missing_require"
name: "h-02-the-feeprotocol-calculation-in-the-volatilityoracle-getpool"
class_name: "H02TheFeeprotocolCalculationInTheVolatilityoracleGetpool"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #52788 (Shieldify/Stackingsalmon)"
help: "[H-02] The `feeProtocol` Calculation in the `VolatilityOracle::_getPoolMetadata()` Is Flawed"
wiki_title: "[H-02] The `feeProtocol` Calculation in the `VolatilityOracle::_getPoolMetadata()` Is Flawed"
wiki_description: |
    ## Severity
    
    High Risk
    
    ## Description
    
    The `feeProtocol` calculation in the `VolatilityOracle::_getPoolMetadata()` is erroneous:
    
    ```solidity
    unchecked {
      if (feeProtocol % 16 != 0) metadata.gamma0 -= uint24(fee / (feeProtocol % 16));
      if (feeProtocol >> 4 != 0) metadata.gamma1 -= uint24(fee / (f
wiki_exploit_scenario: |
    Per Solodit #52788: ## Severity
    
    High Risk
    
    ## Description
    
    The `feeProtocol` calculation in the `VolatilityOracle::_getPoolMetadata()` is erroneous:
    
    ```solidity
    unchecked {
      if (feeProtocol % 16 != 0) metadata.gamma0
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02TheFeeprotocolCalculationInTheVolatilityoracleGetpool"
solodit_id: "52788"
solodit_slug: |
    h-02-the-feeprotocol-calculation-in-the-volatilityoracle_getpoolmetadata-is-flawed-shieldify-none-stackingsalmon-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal clear;"
fn_name_regex: ".*(feeProtocol|uint32|token1|token0).*"
write_var_regex: ".*(clear|defaultFeeProtocol|denominator).*"
guard_var_regex: ".*(clear|defaultFeeProtocol|denominator).*"
vuln_fn_name: "feeProtocol"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "clear = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
