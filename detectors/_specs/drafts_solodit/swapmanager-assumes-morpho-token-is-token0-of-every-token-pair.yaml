skeleton: "name_match_missing_require"
name: "swapmanager-assumes-morpho-token-is-token0-of-every-token-pair"
class_name: "SwapmanagerAssumesMorphoTokenIsToken0OfEveryTokenPair"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #6925 (Spearbit/Morpho)"
help: "SwapManager assumes Morpho token is token0 of every token pair"
wiki_title: "SwapManager assumes Morpho token is token0 of every token pair"
wiki_description: |
    ## Vulnerability Report
    
    ## Severity
    **High Risk**
    
    ## Context
    `SwapManagerUniV2.sol#L106`
    
    ## Description
    The `consult` function wrongly assumes that the Morpho token is always the first token (`token0`) in the Morpho <> Reward token token pair. This could lead to inverted prices and a denial of se
wiki_exploit_scenario: |
    Per Solodit #6925: ## Vulnerability Report
    
    ## Severity
    **High Risk**
    
    ## Context
    `SwapManagerUniV2.sol#L106`
    
    ## Description
    The `consult` function wrongly assumes that the Morpho token is always the first token (`toke
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SwapmanagerAssumesMorphoTokenIsToken0OfEveryTokenPair"
solodit_id: "6925"
solodit_slug: "swapmanager-assumes-morpho-token-is-token0-of-every-token-pair-spearbit-morpho-pdf"
solodit_quality: "5"
solodit_rarity: "4"
solodit_tags: "Validation, Swap"
state_decl: "uint256 internal amountout;"
fn_name_regex: ".*(consult|token0|amountOut).*"
write_var_regex: ".*(amountOut|consult|token0).*"
guard_var_regex: ".*(amountOut|consult|token0).*"
vuln_fn_name: "consult"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "amountout = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
