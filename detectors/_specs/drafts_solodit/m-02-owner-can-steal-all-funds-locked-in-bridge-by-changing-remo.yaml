skeleton: "name_match_missing_call"
name: "m-02-owner-can-steal-all-funds-locked-in-bridge-by-changing-remo"
class_name: "M02OwnerCanStealAllFundsLockedInBridgeByChangingRemo"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #31768 (ZachObront/Alongside)"
help: "[M-02] Owner can steal all funds locked in bridge by changing `REMOTE_TOKEN` value"
wiki_title: "[M-02] Owner can steal all funds locked in bridge by changing `REMOTE_TOKEN` value"
wiki_description: |
    In the new `BridgedIndexToken.sol`, which the L1 contract will be upgraded to, there is a `setBridge()` function used to set the `REMOTE_TOKEN` and the `BRIDGE` addresses.
    ```solidity
    function setBridge(
        address _remoteToken,
        address _bridge
    ) external onlyOwner {
        REMOTE_TOKEN = _remoteT
wiki_exploit_scenario: |
    Per Solodit #31768: In the new `BridgedIndexToken.sol`, which the L1 contract will be upgraded to, there is a `setBridge()` function used to set the `REMOTE_TOKEN` and the `BRIDGE` addresses.
    ```solidity
    function setBrid
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02OwnerCanStealAllFundsLockedInBridgeByChangingRemo"
solodit_id: "31768"
solodit_slug: |
    m-02-owner-can-steal-all-funds-locked-in-bridge-by-changing-remote_token-value-zachobront-none-alongside-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal burn;"
fn_name_regex: ".*(setBridge|REMOTE_TOKEN|BRIDGE|remoteToken).*"
read_var_regex: ".*(burn|mint|remoteToken).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "setBridge"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return burn > 0;"
