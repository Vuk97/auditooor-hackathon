skeleton: "name_match_missing_call"
name: "hash-collision-in-the-mimc-library-fixed"
class_name: "HashCollisionInTheMimcLibraryFixed"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #34966 (ConsenSys/Linea ENS)"
help: "Hash Collision in the MiMC Library ✓ Fixed"
wiki_title: "Hash Collision in the MiMC Library ✓ Fixed"
wiki_description: |
    #### Resolution
    
    
    
     The problem has been fixed in [PR\#178](https://github.com/Consensys/linea-resolver/pull/178).
     
    
    #### Description
    
    
    In the `hash` function of the `Mimc` library, because of the following block of code:
    
    
    
    ```
                    lastChunk := shr(mul(sub(0x20, remaining), 0x8), lastC
wiki_exploit_scenario: |
    Per Solodit #34966: #### Resolution
    
    
    
     The problem has been fixed in [PR\#178](https://github.com/Consensys/linea-resolver/pull/178).
     
    
    #### Description
    
    
    In the `hash` function of the `Mimc` library, because of the fo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "HashCollisionInTheMimcLibraryFixed"
solodit_id: "34966"
solodit_slug: "hash-collision-in-the-mimc-library-fixed-consensys-none-linea-ens-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal calldata;"
fn_name_regex: ".*(hash|Mimc|calldata|mimcHash).*"
read_var_regex: ".*(calldata|hash|mimcHash).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "hash"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return calldata > 0;"
