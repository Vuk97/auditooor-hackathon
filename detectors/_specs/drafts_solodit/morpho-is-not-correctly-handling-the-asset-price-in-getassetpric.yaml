skeleton: "name_match_missing_call"
name: "morpho-is-not-correctly-handling-the-asset-price-in-getassetpric"
class_name: "MorphoIsNotCorrectlyHandlingTheAssetPriceInGetassetpric"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #16218 (Spearbit/Morpho)"
help: |
    Morpho is not correctly handling the asset price in _getAssetPrice when isInEMode == true but priceSource isaddres(0)
wiki_title: |
    Morpho is not correctly handling the asset price in _getAssetPrice when isInEMode == true but priceSource isaddres(0)
wiki_description: |
    ## Severity: Critical Risk
    
    ## Context
    `MorphoInternal.sol#L524-L536`
    
    ## Description
    The current implementation of `_getAssetPrice` returns the asset's price based on the value of `isInEMode`.
    
    ```solidity
    function _getAssetPrice(address underlying, IAaveOracle oracle, bool isInEMode, address price
wiki_exploit_scenario: |
    Per Solodit #16218: ## Severity: Critical Risk
    
    ## Context
    `MorphoInternal.sol#L524-L536`
    
    ## Description
    The current implementation of `_getAssetPrice` returns the asset's price based on the value of `isInEMode`.
    
    ```so
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MorphoIsNotCorrectlyHandlingTheAssetPriceInGetassetpric"
solodit_id: "16218"
solodit_slug: |
    morpho-is-not-correctly-handling-the-asset-price-in-_getassetprice-when-isinemode-true-but-spearbit-morpho-pdf
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal emodeprice;"
fn_name_regex: ".*(isInEMode|priceSource|eModePrice).*"
read_var_regex: ".*(eModePrice|isInEMode|priceSource).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isInEMode"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return emodeprice > 0;"
