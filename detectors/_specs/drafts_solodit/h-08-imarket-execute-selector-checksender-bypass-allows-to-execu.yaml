skeleton: "name_match_missing_call"
name: "h-08-imarket-execute-selector-checksender-bypass-allows-to-execu"
class_name: "H08ImarketExecuteSelectorChecksenderBypassAllowsToExecu"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #32319 (Code4rena/Tapioca DAO)"
help: "[H-08] `IMarket.execute.selector`, `_checkSender` bypass allows to execute arbitrary operations"
wiki_title: "[H-08] `IMarket.execute.selector`, `_checkSender` bypass allows to execute arbitrary operations"
wiki_description: |
    Because of an incorrect interpretation of `calldata` for the `execute` signature, we are able to bypass the `_checkSender` and perform arbitrary `execute` operations as Magnetar.
    
    ### Impact
    
    `Market.execute` uses the following signature:
    
    ```solidity
        function execute(Module[] calldata modules,
wiki_exploit_scenario: |
    Per Solodit #32319: Because of an incorrect interpretation of `calldata` for the `execute` signature, we are able to bypass the `_checkSender` and perform arbitrary `execute` operations as Magnetar.
    
    ### Impact
    
    `Market
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H08ImarketExecuteSelectorChecksenderBypassAllowsToExecu"
solodit_id: "32319"
solodit_slug: |
    h-08-imarketexecuteselector-_checksender-bypass-allows-to-execute-arbitrary-operations-code4rena-tapioca-dao-tapioca-dao-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal calldata;"
fn_name_regex: ".*(calldata|execute|length).*"
read_var_regex: ".*(calldata|execute|length).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "calldata"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return calldata > 0;"
