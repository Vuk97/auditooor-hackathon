skeleton: "state_write_without_paired_write"
name: "c-01-missing-session-key-uniqueness-validation-in-resourcelock-d"
class_name: "C01MissingSessionKeyUniquenessValidationInResourcelockD"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #61403 (Shieldify/Etherspot Credibleaccountmodule)"
help: "[C-01] Missing Session Key Uniqueness Validation in `ResourceLock` due to Key Data Overwrite"
wiki_title: "[C-01] Missing Session Key Uniqueness Validation in `ResourceLock` due to Key Data Overwrite"
wiki_description: |
    ## Severity
    
    Critical Risk
    
    ## Description
    
    The `enableSessionKey()` function accepts a `ResourceLock` input (`_resourceLock`) but does not validate whether the `sessionKey` is already in use for the given `msg.sender`. This leads to silent overwrites of existing session data when the same `session
wiki_exploit_scenario: |
    Per Solodit #61403: ## Severity
    
    Critical Risk
    
    ## Description
    
    The `enableSessionKey()` function accepts a `ResourceLock` input (`_resourceLock`) but does not validate whether the `sessionKey` is already in use for the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "C01MissingSessionKeyUniquenessValidationInResourcelockD"
solodit_id: "61403"
solodit_slug: |
    c-01-missing-session-key-uniqueness-validation-in-resourcelock-due-to-key-data-overwrite-shieldify-none-etherspot-credibleaccountmodule-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(enableSessionKey|ResourceLock|sessionKey|sessionData).*"
primary_var_regex: ".*(enableSessionKey|lockedTokens|sessionData).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
