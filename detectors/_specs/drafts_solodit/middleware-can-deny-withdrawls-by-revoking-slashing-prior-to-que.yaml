skeleton: "name_match_missing_require"
name: "middleware-can-deny-withdrawls-by-revoking-slashing-prior-to-que"
class_name: "MiddlewareCanDenyWithdrawlsByRevokingSlashingPriorToQue"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #36022 (SigmaPrime/EigenLayer)"
help: "Middleware can Deny Withdrawls by Revoking Slashing Prior to Queueing Withdrawal"
wiki_title: "Middleware can Deny Withdrawls by Revoking Slashing Prior to Queueing Withdrawal"
wiki_description: |
    ## Description
    
    The `slasher.sol` contract manages middleware rights and capabilities for a given operator, whilst the `StrategyManager.sol` relies on this contract to determine if withdrawals are allowed. If a given slasher (middleware) revokes their own rights to slash for an operator using `Slash
wiki_exploit_scenario: |
    Per Solodit #36022: ## Description
    
    The `slasher.sol` contract manages middleware rights and capabilities for a given operator, whilst the `StrategyManager.sol` relies on this contract to determine if withdrawals are all
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MiddlewareCanDenyWithdrawlsByRevokingSlashingPriorToQue"
solodit_id: "36022"
solodit_slug: |
    middleware-can-deny-withdrawls-by-revoking-slashing-prior-to-queueing-withdrawal-sigmaprime-none-eigenlayer-pdf
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal onlynotfrozen;"
fn_name_regex: ".*(onlyNotFrozen).*"
write_var_regex: ".*(onlyNotFrozen).*"
guard_var_regex: ".*(onlyNotFrozen).*"
vuln_fn_name: "onlyNotFrozen"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "onlynotfrozen = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
