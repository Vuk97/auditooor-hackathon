skeleton: "name_match_missing_call"
name: "m-01-function-istargetcontractallowed-always-returns-true"
class_name: "M01FunctionIstargetcontractallowedAlwaysReturnsTrue"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42136 (Pashov Audit Group/Interpol)"
help: "[M-01] Function `isTargetContractAllowed` always returns true"
wiki_title: "[M-01] Function `isTargetContractAllowed` always returns true"
wiki_description: |
    ## Severity
    
    **Impact:** High
    
    **Likelihood:** Low
    
    ## Description
    
    The function `isTargetContractAllowed` is flawed and will always return true due to how memory strings are stored in the EVM.
    
    ```solidity
        function isTargetContractAllowed(address _target) public view returns (bool allowed) {
wiki_exploit_scenario: |
    Per Solodit #42136: ## Severity
    
    **Impact:** High
    
    **Likelihood:** Low
    
    ## Description
    
    The function `isTargetContractAllowed` is flawed and will always return true due to how memory strings are stored in the EVM.
    
    ```so
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01FunctionIstargetcontractallowedAlwaysReturnsTrue"
solodit_id: "42136"
solodit_slug: "m-01-function-istargetcontractallowed-always-returns-true-pashov-audit-group-none-interpol-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal allowed;"
fn_name_regex: ".*(isTargetContractAllowed|allowed|isSelectorAllowedForTarget|HoneyLocker).*"
read_var_regex: ".*(allowed|isSelectorAllowedForTarget|isTargetContractAllowed).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isTargetContractAllowed"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return allowed > 0;"
