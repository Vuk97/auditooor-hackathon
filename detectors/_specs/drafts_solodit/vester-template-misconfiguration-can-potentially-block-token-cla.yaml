skeleton: "name_match_missing_require"
name: "vester-template-misconfiguration-can-potentially-block-token-cla"
class_name: "VesterTemplateMisconfigurationCanPotentiallyBlockTokenCla"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #64397 (Cyfrin/Wlf)"
help: "Vester template misconfiguration can potentially block token claims"
wiki_title: "Vester template misconfiguration can potentially block token claims"
wiki_description: |
    **Description:** The `WorldLibertyFinancialVester` contract can make user tokens temporarily inaccessible when template `capPerUser` values don't sum to the user's total allocation.
    
    Users transfer their full allocation to the vester during activation, but can only claim back the portion covered by
wiki_exploit_scenario: |
    Per Solodit #64397: **Description:** The `WorldLibertyFinancialVester` contract can make user tokens temporarily inaccessible when template `capPerUser` values don't sum to the user's total allocation.
    
    Users transfer th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "VesterTemplateMisconfigurationCanPotentiallyBlockTokenCla"
solodit_id: "64397"
solodit_slug: "vester-template-misconfiguration-can-potentially-block-token-claims-cyfrin-none-wlf-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal capperuser;"
fn_name_regex: ".*(WorldLibertyFinancialVester|capPerUser|remainder).*"
write_var_regex: ".*(capPerUser|remainder).*"
guard_var_regex: ".*(capPerUser|remainder).*"
vuln_fn_name: "WorldLibertyFinancialVester"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "capperuser = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
