skeleton: "name_match_missing_call"
name: "m-11-missing-scaling-factor-in-recordkeypurchase"
class_name: "M11MissingScalingFactorInRecordkeypurchase"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42359 (Code4rena/Unlock Protocol)"
help: "[M-11] Missing scaling factor in `recordKeyPurchase`?"
wiki_title: "[M-11] Missing scaling factor in `recordKeyPurchase`?"
wiki_description: |
    _Submitted by cmichel_
    
    The `Unlock.recordKeyPurchase` function computes the `maxTokens` as:
    
    ```solidity
    maxTokens = IMintableERC20(udt).balanceOf(address(this)) * valueInETH / (2 + 2 * valueInETH / grossNetworkProduct) / grossNetworkProduct;
    ```
    
    Note that `grossNetworkProduct` was already increas
wiki_exploit_scenario: |
    Per Solodit #42359: _Submitted by cmichel_
    
    The `Unlock.recordKeyPurchase` function computes the `maxTokens` as:
    
    ```solidity
    maxTokens = IMintableERC20(udt).balanceOf(address(this)) * valueInETH / (2 + 2 * valueInETH /
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M11MissingScalingFactorInRecordkeypurchase"
solodit_id: "42359"
solodit_slug: "m-11-missing-scaling-factor-in-recordkeypurchase-code4rena-unlock-protocol-unlock-protocol-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal grossnetworkproduct;"
fn_name_regex: ".*(maxTokens|grossNetworkProduct|valueInETH|recordKeyPurchase).*"
read_var_regex: ".*(grossNetworkProduct|maxTokens|valueInETH).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "maxTokens"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return grossnetworkproduct > 0;"
