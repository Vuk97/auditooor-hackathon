skeleton: "name_match_missing_require"
name: "c-06-borrow-should-decrease-totalassets-value"
class_name: "C06BorrowShouldDecreaseTotalassetsValue"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #53317 (Pashov Audit Group/Omo_2025-01-25)"
help: "[C-06] `borrow()` should decrease `totalAssets` value"
wiki_title: "[C-06] `borrow()` should decrease `totalAssets` value"
wiki_description: |
    ## Severity
    
    **Impact:** High
    
    **Likelihood:** High
    
    ## Description
    
    The `OmoVault.sol` contract has a `borrow()` used only by the agent, which transfers assets to the Agent.
    It has no tracking system for those debts (it depends on off-chain security).
    Also, it does not update the `_totalAssets` var
wiki_exploit_scenario: |
    Per Solodit #53317: ## Severity
    
    **Impact:** High
    
    **Likelihood:** High
    
    ## Description
    
    The `OmoVault.sol` contract has a `borrow()` used only by the agent, which transfers assets to the Agent.
    It has no tracking system
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "C06BorrowShouldDecreaseTotalassetsValue"
solodit_id: "53317"
solodit_slug: "c-06-borrow-should-decrease-totalassets-value-pashov-audit-group-none-omo_2025-01-25-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal borrow;"
fn_name_regex: ".*(borrow|totalAssets).*"
write_var_regex: ".*(borrow|totalAssets).*"
guard_var_regex: ".*(borrow|totalAssets).*"
vuln_fn_name: "borrow"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "borrow = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
