skeleton: "name_match_missing_call"
name: "m-04-auction-won-t-work-correctly-with-fee-on-transfer-rebasing-"
class_name: "M04AuctionWonTWorkCorrectlyWithFeeOnTransferRebasing"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20554 (Pashov Audit Group/Rolling Dutch Auction)"
help: "[M-04] Auction won't work correctly with fee-on-transfer & rebasing tokens"
wiki_title: "[M-04] Auction won't work correctly with fee-on-transfer & rebasing tokens"
wiki_description: |
    **Impact:**
    High, as it can lead to a loss of value
    
    **Likelihood:**
    Low, as such tokens are not so common
    
    **Description**
    
    The code in `createAuction` does the following:
    
    ```solidity
    IERC20(reserveToken).transferFrom(msg.sender, address(this), reserveAmount);
    ...
    ...
    state.reserves = reserveAmoun
wiki_exploit_scenario: |
    Per Solodit #20554: **Impact:**
    High, as it can lead to a loss of value
    
    **Likelihood:**
    Low, as such tokens are not so common
    
    **Description**
    
    The code in `createAuction` does the following:
    
    ```solidity
    IERC20(reserve
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04AuctionWonTWorkCorrectlyWithFeeOnTransferRebasing"
solodit_id: "20554"
solodit_slug: |
    m-04-auction-wont-work-correctly-with-fee-on-transfer-rebasing-tokens-pashov-none-rolling-dutch-auction-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: "Auction, Fee On Transfer"
state_decl: "uint256 internal createauction;"
fn_name_regex: ".*(createAuction|reserveToken|reserves).*"
read_var_regex: ".*(createAuction|reserveToken|reserves).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "createAuction"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return createauction > 0;"
