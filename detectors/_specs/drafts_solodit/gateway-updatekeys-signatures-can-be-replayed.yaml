skeleton: "name_match_missing_call"
name: "gateway-updatekeys-signatures-can-be-replayed"
class_name: "GatewayUpdatekeysSignaturesCanBeReplayed"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #51938 (Halborn/Gateway)"
help: "Gateway.updateKeys() signatures can be replayed"
wiki_title: "Gateway.updateKeys() signatures can be replayed"
wiki_description: |
    ##### Description
    
    In the `Gateway` contract, the `updateKeys()` function is used to register shard public keys that then can be used to sign and send GMP messages:
    
    ```
    function updateKeys(Signature calldata signature, UpdateKeysMessage memory message) public {
        bytes32 messageHash = message.eip
wiki_exploit_scenario: |
    Per Solodit #51938: ##### Description
    
    In the `Gateway` contract, the `updateKeys()` function is used to register shard public keys that then can be used to sign and send GMP messages:
    
    ```
    function updateKeys(Signature
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "GatewayUpdatekeysSignaturesCanBeReplayed"
solodit_id: "51938"
solodit_slug: "gatewayupdatekeys-signatures-can-be-replayed-halborn-analog-labs-gateway-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal updatekeys;"
fn_name_regex: ".*(Gateway|updateKeys).*"
read_var_regex: ".*(updateKeys).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "Gateway"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return updatekeys > 0;"
