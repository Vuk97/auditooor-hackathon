skeleton: "name_match_missing_call"
name: "m-24-staking-presign-could-use-some-basic-validations"
class_name: "M24StakingPresignCouldUseSomeBasicValidations"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #2899 (Code4rena/Yieldy)"
help: "[M-24] Staking `preSign` could use some basic validations"
wiki_title: "[M-24] Staking `preSign` could use some basic validations"
wiki_description: |
    _Submitted by Alex the Entreprenerd_
    
    The function `preSign` accepts any `orderUid`.<br>
    `function preSign(bytes calldata orderUid) external onlyOwner`
    
    Because of how Cowswap works, accepting any `orderUid` can be used as a rug-vector.
    
    This is because the orderData contains a `receiver` which in l
wiki_exploit_scenario: |
    Per Solodit #2899: _Submitted by Alex the Entreprenerd_
    
    The function `preSign` accepts any `orderUid`.<br>
    `function preSign(bytes calldata orderUid) external onlyOwner`
    
    Because of how Cowswap works, accepting any `or
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M24StakingPresignCouldUseSomeBasicValidations"
solodit_id: "2899"
solodit_slug: "m-24-staking-presign-could-use-some-basic-validations-code4rena-yieldy-yieldy-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal onlyowner;"
fn_name_regex: ".*(preSign|orderUid|receiver|onlyOwner).*"
read_var_regex: ".*(onlyOwner|orderUid|preSign).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "preSign"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return onlyowner > 0;"
