skeleton: "name_match_missing_call"
name: "h-07-vault-balance-mixes-normalized-and-standard-amounts"
class_name: "H07VaultBalanceMixesNormalizedAndStandardAmounts"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #768 (Code4rena/yAxis)"
help: "[H-07] Vault.balance() mixes normalized and standard amounts"
wiki_title: "[H-07] Vault.balance() mixes normalized and standard amounts"
wiki_description: |
    ## Handle
    
    cmichel
    
    
    ## Vulnerability details
    
    The `Vault.balance` function uses the `balanceOfThis` function which scales ("normalizes") all balances to 18 decimals.
    
    ```
    for (uint8 i; i < _tokens.length; i++) {
        address _token = _tokens[i];
        // everything is padded to 18 decimals
        _balanc
wiki_exploit_scenario: |
    Per Solodit #768: ## Handle
    
    cmichel
    
    
    ## Vulnerability details
    
    The `Vault.balance` function uses the `balanceOfThis` function which scales ("normalizes") all balances to 18 decimals.
    
    ```
    for (uint8 i; i < _tokens.le
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H07VaultBalanceMixesNormalizedAndStandardAmounts"
solodit_id: "768"
solodit_slug: "h-07-vaultbalance-mixes-normalized-and-standard-amounts-code4rena-yaxis-yaxis-contest-git"
solodit_quality: "5"
solodit_rarity: "4"
solodit_tags: "Wrong Math, Decimals"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(balanceOfThis|balance|deposit|tokens).*"
read_var_regex: ".*(balance|balanceOfThis|deposit).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "balanceOfThis"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
