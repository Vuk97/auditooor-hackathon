skeleton: "name_match_missing_require"
name: "only-the-governance-can-initialize-the-portal"
class_name: "OnlyTheGovernanceCanInitializeThePortal"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20755 (ConsenSys/Geodefi)"
help: "Only the GOVERNANCE can initialize the Portal"
wiki_title: "Only the GOVERNANCE can initialize the Portal"
wiki_description: |
    #### Description
    
    
    In the `Portal`’s `initialize` function, the `_GOVERNANCE` is passed as a parameter:
    
    
    **code/contracts/Portal/Portal.sol:L156-L196**
    
    
    
    ```
    function initialize(
     address \_GOVERNANCE,
     address \_gETH,
     address \_ORACLE\_POSITION,
     address \_DEFAULT\_gETH\_INTERFACE,
     address \_DE
wiki_exploit_scenario: |
    Per Solodit #20755: #### Description
    
    
    In the `Portal`’s `initialize` function, the `_GOVERNANCE` is passed as a parameter:
    
    
    **code/contracts/Portal/Portal.sol:L156-L196**
    
    
    
    ```
    function initialize(
     address \_GOVERNAN
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OnlyTheGovernanceCanInitializeThePortal"
solodit_id: "20755"
solodit_slug: "only-the-governance-can-initialize-the-portal-consensys-none-geodefi-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal initialize;"
fn_name_regex: ".*(Portal|initialize|updateStakingParams).*"
write_var_regex: ".*(initialize|updateStakingParams).*"
guard_var_regex: ".*(initialize|updateStakingParams).*"
vuln_fn_name: "Portal"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "initialize = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
