skeleton: "name_match_missing_call"
name: "minigovernance-fetchupgradeproposal-will-always-revert"
class_name: "MinigovernanceFetchupgradeproposalWillAlwaysRevert"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #20751 (ConsenSys/Geodefi)"
help: "MiniGovernance - fetchUpgradeProposal will always revert"
wiki_title: "MiniGovernance - fetchUpgradeProposal will always revert"
wiki_description: |
    #### Description
    
    
    In the function `fetchUpgradeProposal()`, `newProposal()` is called with a hard coded `duration` of 4 weeks. This means the function will always revert since `newProposal()` checks that the proposal duration is not more than the constant `MAX_PROPOSAL_DURATION` of 2 weeks. Effecti
wiki_exploit_scenario: |
    Per Solodit #20751: #### Description
    
    
    In the function `fetchUpgradeProposal()`, `newProposal()` is called with a hard coded `duration` of 4 weeks. This means the function will always revert since `newProposal()` checks
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MinigovernanceFetchupgradeproposalWillAlwaysRevert"
solodit_id: "20751"
solodit_slug: "minigovernance-fetchupgradeproposal-will-always-revert-consensys-none-geodefi-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal duration;"
fn_name_regex: ".*(fetchUpgradeProposal|newProposal|duration|MAX_PROPOSAL_DURATION).*"
read_var_regex: ".*(duration|fetchUpgradeProposal|newProposal).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "fetchUpgradeProposal"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return duration > 0;"
