skeleton: "paired_function_divergence"
name: "pair-1-duplicate-dividends"
class_name: "Pair1DuplicateDividends"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #26575 (Guardian Audits/Bridges)"
help: "PAIR-1 | Duplicate Dividends"
wiki_title: "PAIR-1 | Duplicate Dividends"
wiki_description: |
    **Description**
    
    In the `mint` function, the only precondition for adding an address to the `users` list is if the balance of the address is 0. Additionally, an address is not removed from the `users` list if it transfers it’s balance of the BridgesPair token.
    This way an address can continually `mi
wiki_exploit_scenario: |
    Per Solodit #26575: **Description**
    
    In the `mint` function, the only precondition for adding an address to the `users` list is if the balance of the address is 0. Additionally, an address is not removed from the `users`
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "Pair1DuplicateDividends"
solodit_id: "26575"
solodit_slug: "pair-1-duplicate-dividends-guardian-audits-none-bridges-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(distributeDividends|mint|users).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
