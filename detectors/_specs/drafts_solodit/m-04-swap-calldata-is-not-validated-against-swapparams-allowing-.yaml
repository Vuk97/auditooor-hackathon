skeleton: "name_match_missing_call"
name: "m-04-swap-calldata-is-not-validated-against-swapparams-allowing-"
class_name: "M04SwapCalldataIsNotValidatedAgainstSwapparamsAllowing"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #64018 (Shieldify/Prime Vaults)"
help: |
    [M-04] Swap Calldata Is Not Validated Against `SwapParams`, Allowing `tokenOut`/`minAmountOut` Mismatches
wiki_title: |
    [M-04] Swap Calldata Is Not Validated Against `SwapParams`, Allowing `tokenOut`/`minAmountOut` Mismatches
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    The `swap()` function allows the vault manager (via `onlyManager()`) to execute a swap by forwarding a raw calldata blob (`params.data`) to an external target (`params.to`). The calldata is expected to be KyberSwap-compatible (it is gated by `KX_SWAP_SELECT
wiki_exploit_scenario: |
    Per Solodit #64018: ## Severity
    
    Medium Risk
    
    ## Description
    
    The `swap()` function allows the vault manager (via `onlyManager()`) to execute a swap by forwarding a raw calldata blob (`params.data`) to an external targe
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04SwapCalldataIsNotValidatedAgainstSwapparamsAllowing"
solodit_id: "64018"
solodit_slug: |
    m-04-swap-calldata-is-not-validated-against-swapparams-allowing-tokenoutminamountout-mismatches-shieldify-none-prime-vaults-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal amountin;"
fn_name_regex: ".*(swap|onlyManager|KX_SWAP_SELECTOR|SwapParams).*"
read_var_regex: ".*(amountIn|minAmountOut|onlyManager).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "swap"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amountin > 0;"
