skeleton: "name_match_missing_require"
name: "each-well-is-responsible-for-ensuring-that-an-update-call-cannot"
class_name: "EachWellIsResponsibleForEnsuringThatAnUpdateCallCannot"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #18432 (Cyfrin/Beanstalk Wells)"
help: "Each Well is responsible for ensuring that an `update` call cannot be made with a reserve of 0"
wiki_title: "Each Well is responsible for ensuring that an `update` call cannot be made with a reserve of 0"
wiki_description: |
    **Description:** The current implementation of `GeoEmaAndCumSmaPump` assumes each well will call `update()` with non-zero reserves, as commented at the beginning of the file:
    
    ```solidity
    /**
     * @title GeoEmaAndCumSmaPump
     * @author Publius
     * @notice Stores a geometric EMA and cumulative geometric
wiki_exploit_scenario: |
    Per Solodit #18432: **Description:** The current implementation of `GeoEmaAndCumSmaPump` assumes each well will call `update()` with non-zero reserves, as commented at the beginning of the file:
    
    ```solidity
    /**
     * @titl
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "EachWellIsResponsibleForEnsuringThatAnUpdateCallCannot"
solodit_id: "18432"
solodit_slug: |
    each-well-is-responsible-for-ensuring-that-an-update-call-cannot-be-made-with-a-reserve-of-0-cyfrin-beanstalk-wells-markdown_
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal update;"
fn_name_regex: ".*(GeoEmaAndCumSmaPump|update|Well|ConstantProduct2).*"
write_var_regex: ".*(update).*"
guard_var_regex: ".*(update).*"
vuln_fn_name: "GeoEmaAndCumSmaPump"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "update = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
