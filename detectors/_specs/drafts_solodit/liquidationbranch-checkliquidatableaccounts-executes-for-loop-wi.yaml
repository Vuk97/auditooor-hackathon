skeleton: "name_match_missing_call"
name: "liquidationbranch-checkliquidatableaccounts-executes-for-loop-wi"
class_name: "LiquidationbranchCheckliquidatableaccountsExecutesForLoopWi"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #37994 (Codehawks/Zaros)"
help: |
    `LiquidationBranch::checkLiquidatableAccounts()` executes `for` loop with wrong values, causing array out of bounds to be recovered, the program will not work as expected
wiki_title: |
    `LiquidationBranch::checkLiquidatableAccounts()` executes `for` loop with wrong values, causing array out of bounds to be recovered, the program will not work a
wiki_description: |
    ## Summary
    
    1. `LiquidationBranch::checkLiquidatableAccounts()` executes `for` loop with wrong values, causing array out of bounds to be recovered, the program will not work as expected
    2. Even if the liquidation conditions are not met, the array will still store objects with a value of 0, which wil
wiki_exploit_scenario: |
    Per Solodit #37994: ## Summary
    
    1. `LiquidationBranch::checkLiquidatableAccounts()` executes `for` loop with wrong values, causing array out of bounds to be recovered, the program will not work as expected
    2. Even if the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LiquidationbranchCheckliquidatableaccountsExecutesForLoopWi"
solodit_id: "37994"
solodit_slug: |
    liquidationbranchcheckliquidatableaccounts-executes-for-loop-with-wrong-values-causing-array-out-of-bounds-to-be-recovered-the-program-will-not-work-as-expected-codehawks-zaros-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal for;"
fn_name_regex: ".*(for|tradingAccountId).*"
read_var_regex: ".*(for|tradingAccountId).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "for"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return for > 0;"
