skeleton: "paired_function_divergence"
name: "h-02-missing-fromtoken-totoken-check"
class_name: "H02MissingFromtokenTotokenCheck"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #42149 (Code4rena/Marginswap)"
help: "[H-02] Missing `fromToken != toToken` check"
wiki_title: "[H-02] Missing `fromToken != toToken` check"
wiki_description: |
    Attacker calls `MarginRouter.crossSwapExactTokensForTokens` with a fake pair and the same token[0] == token[1].
    `crossSwapExactTokensForTokens(1000 WETH, 0, [ATTACKER_CONTRACT], [WETH, WETH])`. When the amounts are computed by the `amounts = UniswapStyleLib.getAmountsOut(amountIn - fees, pairs, tok
wiki_exploit_scenario: |
    Per Solodit #42149: Attacker calls `MarginRouter.crossSwapExactTokensForTokens` with a fake pair and the same token[0] == token[1].
    `crossSwapExactTokensForTokens(1000 WETH, 0, [ATTACKER_CONTRACT], [WETH, WETH])`. When
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02MissingFromtokenTotokenCheck"
solodit_id: "42149"
solodit_slug: "h-02-missing-fromtoken-totoken-check-code4rena-marginswap-marginswap-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(adjustAmounts|coolingOffPeriod|crossSwapExactTokensForTokens).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
