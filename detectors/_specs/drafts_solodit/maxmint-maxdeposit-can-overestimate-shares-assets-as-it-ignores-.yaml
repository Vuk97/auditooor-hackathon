skeleton: "name_match_missing_call"
name: "maxmint-maxdeposit-can-overestimate-shares-assets-as-it-ignores-"
class_name: "MaxmintMaxdepositCanOverestimateSharesAssetsAsItIgnores"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #35949 (Spearbit/Euler Labs - EVK)"
help: "maxMint / maxDeposit can overestimate shares / assets as it ignores totalShares overflow"
wiki_title: "maxMint / maxDeposit can overestimate shares / assets as it ignores totalShares overflow"
wiki_description: |
    ## Severity: Medium Risk
    
    ## Context
    Vault.sol#L64-L66
    
    ## Description
    The `maxMint` function currently returns 
    ```solidity
    shares < MAX_SANE_AMOUNT ? shares : MAX_SANE_AMOUNT
    ```
    where shares are the max-deposit assets converted to shares. However, it needs to take the current `totalSupply` into a
wiki_exploit_scenario: |
    Per Solodit #35949: ## Severity: Medium Risk
    
    ## Context
    Vault.sol#L64-L66
    
    ## Description
    The `maxMint` function currently returns 
    ```solidity
    shares < MAX_SANE_AMOUNT ? shares : MAX_SANE_AMOUNT
    ```
    where shares are th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MaxmintMaxdepositCanOverestimateSharesAssetsAsItIgnores"
solodit_id: "35949"
solodit_slug: |
    maxmint-maxdeposit-can-overestimate-shares-assets-as-it-ignores-totalshares-overflow-spearbit-none-euler-labs-evk-pdf
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal maxdeposit;"
fn_name_regex: ".*(maxMint|totalSupply|maxDepositInternal|MAX_SANE_AMOUNT).*"
read_var_regex: ".*(maxDeposit|maxDepositInternal|maxMint).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "maxMint"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return maxdeposit > 0;"
