skeleton: "paired_function_divergence"
name: "pairfor-does-not-correctly-sort-tokens-when-overriding-for-sinkc"
class_name: "PairforDoesNotCorrectlySortTokensWhenOverridingForSinkc"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #21390 (Spearbit/Velodrome Finance)"
help: "pairFor does not correctly sort tokens when overriding for SinkConverter"
wiki_title: "pairFor does not correctly sort tokens when overriding for SinkConverter"
wiki_description: |
    ## Severity: Medium Risk
    
    ## Context
    Router.sol#L59-L90
    
    ## Description
    The router will always search for pairs by sorting `tokenA` and `tokenB`. Notably, for the velo and Velo2 pair, the Router will not perform the sorting.
    
    ```sol
    // Router.sol#L69-L73
    if (factory == defaultFactory) {
        if ((tok
wiki_exploit_scenario: |
    Per Solodit #21390: ## Severity: Medium Risk
    
    ## Context
    Router.sol#L59-L90
    
    ## Description
    The router will always search for pairs by sorting `tokenA` and `tokenB`. Notably, for the velo and Velo2 pair, the Router will
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PairforDoesNotCorrectlySortTokensWhenOverridingForSinkc"
solodit_id: "21390"
solodit_slug: |
    pairfor-does-not-correctly-sort-tokens-when-overriding-for-sinkconverter-spearbit-none-velodrome-finance-pdf
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(createPair|getPair|setSinkConverter).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
