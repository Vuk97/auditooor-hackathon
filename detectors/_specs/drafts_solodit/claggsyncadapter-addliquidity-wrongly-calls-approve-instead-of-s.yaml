skeleton: "name_match_missing_call"
name: "claggsyncadapter-addliquidity-wrongly-calls-approve-instead-of-s"
class_name: "ClaggsyncadapterAddliquidityWronglyCallsApproveInsteadOfS"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #46284 (Cantina/Clave)"
help: "ClaggSyncAdapter._addLiquidity() wrongly calls approve() instead of safeApprove()"
wiki_title: "ClaggSyncAdapter._addLiquidity() wrongly calls approve() instead of safeApprove() "
wiki_description: |
    ## ClaggSyncAdapter Issue Report
    
    ## Context
    `ClaggSyncAdapter.sol#L186-L196`
    
    ## Description
    In `ClaggSyncAdapter._addLiquidity()`, `approve()` is called instead of `safeApprove()` when depositing token as liquidity:
    
    ```solidity
    IERC20(token).approve(address(syncRouter), amount);
    liquidity = syncR
wiki_exploit_scenario: |
    Per Solodit #46284: ## ClaggSyncAdapter Issue Report
    
    ## Context
    `ClaggSyncAdapter.sol#L186-L196`
    
    ## Description
    In `ClaggSyncAdapter._addLiquidity()`, `approve()` is called instead of `safeApprove()` when depositing to
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ClaggsyncadapterAddliquidityWronglyCallsApproveInsteadOfS"
solodit_id: "46284"
solodit_slug: "claggsyncadapter_addliquidity-wrongly-calls-approve-instead-of-safeapprove-cantina-none-clave-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal approve;"
fn_name_regex: ".*(approve|safeApprove).*"
read_var_regex: ".*(approve|safeApprove).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "approve"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return approve > 0;"
