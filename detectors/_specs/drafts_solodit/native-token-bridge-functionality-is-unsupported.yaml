skeleton: "name_match_missing_call"
name: "native-token-bridge-functionality-is-unsupported"
class_name: "NativeTokenBridgeFunctionalityIsUnsupported"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #43720 (OpenZeppelin/Tesseract Protocol Audit)"
help: "Native Token Bridge Functionality is Unsupported"
wiki_title: "Native Token Bridge Functionality is Unsupported"
wiki_description: |
    The protocol currently supports bridging ERC\-20 tokens. In this process, the `BridgeToken` contract approves the `amount` to the `Cell` contract, which then transfers the tokens for further operations.
    
    
    However, if the token is an ERC\-20 token on the origin Avalanche L1 but a native token on the
wiki_exploit_scenario: |
    Per Solodit #43720: The protocol currently supports bridging ERC\-20 tokens. In this process, the `BridgeToken` contract approves the `amount` to the `Cell` contract, which then transfers the tokens for further operation
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NativeTokenBridgeFunctionalityIsUnsupported"
solodit_id: "43720"
solodit_slug: "native-token-bridge-functionality-is-unsupported-openzeppelin-none-tesseract-protocol-audit-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(BridgeToken|amount|Cell|TokenHome).*"
read_var_regex: ".*(amount|fallback|fallbackRecipient).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "BridgeToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amount > 0;"
