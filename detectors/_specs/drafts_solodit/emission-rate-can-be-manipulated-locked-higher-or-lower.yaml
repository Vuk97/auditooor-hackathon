skeleton: "highlevelcall_missing_sibling"
name: "emission-rate-can-be-manipulated-locked-higher-or-lower"
class_name: "EmissionRateCanBeManipulatedLockedHigherOrLower"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #57307 (Codehawks/Core Contracts)"
help: "Emission Rate can be Manipulated (Locked, Higher or Lower)"
wiki_title: "Emission Rate can be Manipulated (Locked, Higher or Lower)"
wiki_description: |
    ## Summary
    
    When `calculateNewEmissionRate()` is called, it **only** adjusts the emission rate if the `utilizationRate` is strictly greater than (`>`) or strictly less than (`<`) the `utilizationTarget`. If the `utilizationRate` equals (`==`) the target, **no change** is applied and the function ret
wiki_exploit_scenario: |
    Per Solodit #57307: ## Summary
    
    When `calculateNewEmissionRate()` is called, it **only** adjusts the emission rate if the `utilizationRate` is strictly greater than (`>`) or strictly less than (`<`) the `utilizationTarge
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "EmissionRateCanBeManipulatedLockedHigherOrLower"
solodit_id: "57307"
solodit_slug: |
    emission-rate-can-be-manipulated-locked-higher-or-lower-codehawks-regnum-aurum-acquisition-corp-core-contracts-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(calculateNewEmissionRate|utilizationRate|utilizationTarget|emissionRate).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
