skeleton: "name_match_missing_call"
name: "m-13-confirmbaseinterestallocator-change-baseinterestallocator-m"
class_name: "M13ConfirmbaseinterestallocatorChangeBaseinterestallocatorM"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #35232 (Code4rena/Gondi)"
help: |
    [M-13] `confirmBaseInterestAllocator()` change `BaseInterestAllocator` may pay large `getReallocationBonus`
wiki_title: |
    [M-13] `confirmBaseInterestAllocator()` change `BaseInterestAllocator` may pay large `getReallocationBonus`
wiki_description: |
    `owner` can submit `getPendingBaseInterestAllocator` first, and then anyone can enable it by `confirmBaseInterestAllocator()`.
    
    ```solidity
        function confirmBaseInterestAllocator(address _newBaseInterestAllocator) external {
            address cachedAllocator = getBaseInterestAllocator;
            if
wiki_exploit_scenario: |
    Per Solodit #35232: `owner` can submit `getPendingBaseInterestAllocator` first, and then anyone can enable it by `confirmBaseInterestAllocator()`.
    
    ```solidity
        function confirmBaseInterestAllocator(address _newBaseI
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M13ConfirmbaseinterestallocatorChangeBaseinterestallocatorM"
solodit_id: "35232"
solodit_slug: |
    m-13-confirmbaseinterestallocator-change-baseinterestallocator-may-pay-large-getreallocationbonus-code4rena-gondi-gondi-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal confirmbaseinterestallocator;"
fn_name_regex: ".*(owner|getPendingBaseInterestAllocator|confirmBaseInterestAllocator|BaseInterestAllocator).*"
read_var_regex: ".*(confirmBaseInterestAllocator|getBaseInterestAllocator|getPendingBaseInterestAllocator).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "owner"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return confirmbaseinterestallocator > 0;"
