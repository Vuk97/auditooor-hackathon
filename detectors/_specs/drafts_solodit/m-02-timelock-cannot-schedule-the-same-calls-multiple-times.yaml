skeleton: "name_match_missing_call"
name: "m-02-timelock-cannot-schedule-the-same-calls-multiple-times"
class_name: "M02TimelockCannotScheduleTheSameCallsMultipleTimes"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42271 (Code4rena/Yield)"
help: "[M-02] `TimeLock` cannot schedule the same calls multiple times"
wiki_title: "[M-02] `TimeLock` cannot schedule the same calls multiple times"
wiki_description: |
    _Submitted by cmichel_
    
    The `TimeLock.schedule` function reverts if the same `targets` and `data` fields are used as the `txHash` will be the same.
    This means one cannot schedule the same transactions multiple times.
    
    Imagine the delay is set to 30 days, but a contractor needs to be paid every 2 wee
wiki_exploit_scenario: |
    Per Solodit #42271: _Submitted by cmichel_
    
    The `TimeLock.schedule` function reverts if the same `targets` and `data` fields are used as the `txHash` will be the same.
    This means one cannot schedule the same transactions
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02TimelockCannotScheduleTheSameCallsMultipleTimes"
solodit_id: "42271"
solodit_slug: "m-02-timelock-cannot-schedule-the-same-calls-multiple-times-code4rena-yield-yield-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal data;"
fn_name_regex: ".*(targets|data|txHash|eta).*"
read_var_regex: ".*(data|eta|targets).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "targets"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return data > 0;"
