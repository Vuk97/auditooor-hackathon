skeleton: "name_match_missing_call"
name: "m-07-maintoken-set-mint-multisig-doesn-t-check-that-minting-mult"
class_name: "M07MaintokenSetMintMultisigDoesnTCheckThatMintingMult"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #974 (Code4rena/Boot Finance)"
help: "[M-07] MainToken.set_mint_multisig() doesn’t check that _minting_multisig doesn’t equal zero"
wiki_title: "[M-07] MainToken.set_mint_multisig() doesn’t check that _minting_multisig doesn’t equal zero"
wiki_description: |
    ## Handle
    
    pants
    
    
    ## Vulnerability details
    
    The function `MainToken.set_mint_multisig()` doesn't check that `_minting_multisig` doesn't equal zero before it sets it as the new `minting_multisig`.
    
    ## Impact
    This function can be invoked by mistake with the zero address as `_minting_multisig`, causin
wiki_exploit_scenario: |
    Per Solodit #974: ## Handle
    
    pants
    
    
    ## Vulnerability details
    
    The function `MainToken.set_mint_multisig()` doesn't check that `_minting_multisig` doesn't equal zero before it sets it as the new `minting_multisig`.
    
    ##
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M07MaintokenSetMintMultisigDoesnTCheckThatMintingMult"
solodit_id: "974"
solodit_slug: |
    m-07-maintokenset_mint_multisig-doesnt-check-that-_minting_multisig-doesnt-equal-zero-code4rena-boot-finance-boot-finance-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal minting_multisig;"
fn_name_regex: ".*(minting_multisig).*"
read_var_regex: ".*(minting_multisig).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "minting_multisig"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return minting_multisig > 0;"
