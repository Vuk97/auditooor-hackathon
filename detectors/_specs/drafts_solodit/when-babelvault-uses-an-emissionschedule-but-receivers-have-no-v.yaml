skeleton: "name_match_missing_call"
name: "when-babelvault-uses-an-emissionschedule-but-receivers-have-no-v"
class_name: "WhenBabelvaultUsesAnEmissionscheduleButReceiversHaveNoV"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #38432 (Cyfrin/Bima)"
help: |
    When `BabelVault` uses an `EmissionSchedule` but receivers have no voting weight, the vault's unallocated supply will decrease even though no tokens are being allocated
wiki_title: |
    When `BabelVault` uses an `EmissionSchedule` but receivers have no voting weight, the vault's unallocated supply will decrease even though no tokens are being a
wiki_description: |
    **Description:** When `BabelVault` uses an `EmissionSchedule` but receivers have no voting weight, the vault's unallocated supply will decrease even though no tokens are being allocated.
    
    **Impact:** Tokens are effectively lost since the vault's unallocated supply decreases but no tokens are actuall
wiki_exploit_scenario: |
    Per Solodit #38432: **Description:** When `BabelVault` uses an `EmissionSchedule` but receivers have no voting weight, the vault's unallocated supply will decrease even though no tokens are being allocated.
    
    **Impact:**
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "WhenBabelvaultUsesAnEmissionscheduleButReceiversHaveNoV"
solodit_id: "38432"
solodit_slug: |
    when-babelvault-uses-an-emissionschedule-but-receivers-have-no-voting-weight-the-vaults-unallocated-supply-will-decrease-even-though-no-tokens-are-being-allocated-cyfrin-none-bima-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(BabelVault|EmissionSchedule).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "BabelVault"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
