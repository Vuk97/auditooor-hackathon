skeleton: "name_match_missing_call"
name: "m-04-excessive-require-makes-the-transaction-fail-unexpectedly"
class_name: "M04ExcessiveRequireMakesTheTransactionFailUnexpectedly"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42365 (Code4rena/BadgerDAO)"
help: "[M-04] Excessive `require` makes the transaction fail unexpectedly"
wiki_title: "[M-04] Excessive `require` makes the transaction fail unexpectedly"
wiki_description: |
    _Submitted by WatchPug_
    
    The check for `RENCRV_VAULT.blockLock` is only needed when `if (_amounts[1] > 0 || _amounts[2] > 0)`.
    
    However, in the current implementation, the check is done at the very first, making transactions unrelated to `RENCRV_VAULT` fail unexpectedly if there is a prior transacti
wiki_exploit_scenario: |
    Per Solodit #42365: _Submitted by WatchPug_
    
    The check for `RENCRV_VAULT.blockLock` is only needed when `if (_amounts[1] > 0 || _amounts[2] > 0)`.
    
    However, in the current implementation, the check is done at the very fi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04ExcessiveRequireMakesTheTransactionFailUnexpectedly"
solodit_id: "42365"
solodit_slug: "m-04-excessive-require-makes-the-transaction-fail-unexpectedly-code4rena-badgerdao-badgerdao-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(RENCRV_VAULT|require).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "RENCRV_VAULT"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
