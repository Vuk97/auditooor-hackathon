skeleton: "name_match_missing_call"
name: "incomplete-validation-in-opnetvaultautodeploy-allows-invalid-bur"
class_name: "IncompleteValidationInOpnetvaultautodeployAllowsInvalidBur"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #64339 (Cyfrin/Symbiotic)"
help: |
    Incomplete validation in `OpNetVaultAutoDeploy` allows invalid burner configuration for registering operators with auto vault deployments
wiki_title: |
    Incomplete validation in `OpNetVaultAutoDeploy` allows invalid burner configuration for registering operators with auto vault deployments
wiki_description: |
    **Description:** `OpNetVaultAutoDeploy._validateConfig()` is missing burner address validation that could lead to deployment failures. The function fails to validate a configuration scenario where `isBurnerHook` is enabled with burner as `address(0)`.
    
    This validation gap allows administrators to se
wiki_exploit_scenario: |
    Per Solodit #64339: **Description:** `OpNetVaultAutoDeploy._validateConfig()` is missing burner address validation that could lead to deployment failures. The function fails to validate a configuration scenario where `is
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncompleteValidationInOpnetvaultautodeployAllowsInvalidBur"
solodit_id: "64339"
solodit_slug: |
    incomplete-validation-in-opnetvaultautodeploy-allows-invalid-burner-configuration-for-registering-operators-with-auto-vault-deployments-cyfrin-none-symbiotic-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal isburnerhook;"
fn_name_regex: ".*(isBurnerHook|BaseSlasher|OpNetVaultAutoDeploy).*"
read_var_regex: ".*(isBurnerHook).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isBurnerHook"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return isburnerhook > 0;"
