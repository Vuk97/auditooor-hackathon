skeleton: "name_match_missing_require"
name: "circumvention-of-checks-in-validateborrow"
class_name: "CircumventionOfChecksInValidateborrow"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #19248 (SigmaPrime/Aave)"
help: "Circumvention of Checks in validateBorrow()"
wiki_title: "Circumvention of Checks in validateBorrow()"
wiki_description: |
    ## Description
    
    If a user makes a borrow then repays it in the same transaction, the time difference will be zero as the block timestamp will not have changed between the calls to `borrow()` and `repay()`. Therefore, users will not pay fees on a loan repaid in the same transaction since the interest
wiki_exploit_scenario: |
    Per Solodit #19248: ## Description
    
    If a user makes a borrow then repays it in the same transaction, the time difference will be zero as the block timestamp will not have changed between the calls to `borrow()` and `repa
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CircumventionOfChecksInValidateborrow"
solodit_id: "19248"
solodit_slug: "circumvention-of-checks-in-validateborrow-sigmaprime-none-aave-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal borrow;"
fn_name_regex: ".*(borrow|repay|validateBorrow|lastBorrowTimestamp).*"
write_var_regex: ".*(borrow|contractA|contractB).*"
guard_var_regex: ".*(borrow|contractA|contractB).*"
vuln_fn_name: "borrow"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "borrow = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
