skeleton: "name_match_missing_require"
name: "h-15-concentratedliquiditypool-initialprice-should-be-checked-to"
class_name: "H15ConcentratedliquiditypoolInitialpriceShouldBeCheckedTo"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #25575 (Code4rena/Sushi)"
help: "[H-15] `ConcentratedLiquidityPool`: `initialPrice` should be checked to be within allowable range"
wiki_title: "[H-15] `ConcentratedLiquidityPool`: `initialPrice` should be checked to be within allowable range"
wiki_description: |
    _Submitted by hickuphh3_
    
    ##### Impact
    No check is performed for the initial price. This means that it can be set to be below the `MIN_SQRT_RATIO` or above `MAX_SQRT_RATIO` (Eg. zero value), which will prevent the usability of all other functions (minting, swapping, burning).
    
    For example, `Ticks.in
wiki_exploit_scenario: |
    Per Solodit #25575: _Submitted by hickuphh3_
    
    ##### Impact
    No check is performed for the initial price. This means that it can be set to be below the `MIN_SQRT_RATIO` or above `MAX_SQRT_RATIO` (Eg. zero value), which wil
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H15ConcentratedliquiditypoolInitialpriceShouldBeCheckedTo"
solodit_id: "25575"
solodit_slug: |
    h-15-concentratedliquiditypool-initialprice-should-be-checked-to-be-within-allowable-range-code4rena-sushi-sushi-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal initialprice;"
fn_name_regex: ".*(MIN_SQRT_RATIO|MAX_SQRT_RATIO|initialPrice|ConcentratedLiquidityPool).*"
write_var_regex: ".*(initialPrice).*"
guard_var_regex: ".*(initialPrice).*"
vuln_fn_name: "MIN_SQRT_RATIO"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "initialprice = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
