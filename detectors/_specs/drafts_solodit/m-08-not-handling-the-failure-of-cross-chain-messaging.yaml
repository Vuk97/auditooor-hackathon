skeleton: "highlevelcall_missing_sibling"
name: "m-08-not-handling-the-failure-of-cross-chain-messaging"
class_name: "M08NotHandlingTheFailureOfCrossChainMessaging"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #33503 (Code4rena/Renzo)"
help: "[M-08] Not handling the failure of cross chain messaging"
wiki_title: "[M-08] Not handling the failure of cross chain messaging"
wiki_description: |
    If the `xReceive` function fails to handle reverts properly, it could result in funds getting stuck. This can happen for example when:
    
    - Depositing into L2 when protocol on L1 is paused: because pausing is not defined on L2.
    - Depositing large amount on L2: because it is not enforced to deposit le
wiki_exploit_scenario: |
    Per Solodit #33503: If the `xReceive` function fails to handle reverts properly, it could result in funds getting stuck. This can happen for example when:
    
    - Depositing into L2 when protocol on L1 is paused: because pau
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08NotHandlingTheFailureOfCrossChainMessaging"
solodit_id: "33503"
solodit_slug: "m-08-not-handling-the-failure-of-cross-chain-messaging-code4rena-renzo-renzo-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(xReceive|ezETH|xRenzoBridge|maxDepositTVL).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
