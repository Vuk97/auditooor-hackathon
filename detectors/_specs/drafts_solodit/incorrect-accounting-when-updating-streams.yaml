skeleton: "name_match_missing_call"
name: "incorrect-accounting-when-updating-streams"
class_name: "IncorrectAccountingWhenUpdatingStreams"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #19589 (SigmaPrime/Sushi)"
help: "Incorrect Accounting When Updating Streams"
wiki_title: "Incorrect Accounting When Updating Streams"
wiki_description: |
    ## Description
    
    The function `updateStream()` allows the creator of the stream to modify the parameters. There are potential accounting errors when the `topUpAmount` or `endTime` is modified.
    
    When `topUpAmount` is non-zero, the stream will immediately attribute part of the `topUpAmount` as already
wiki_exploit_scenario: |
    Per Solodit #19589: ## Description
    
    The function `updateStream()` allows the creator of the stream to modify the parameters. There are potential accounting errors when the `topUpAmount` or `endTime` is modified.
    
    When `t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectAccountingWhenUpdatingStreams"
solodit_id: "19589"
solodit_slug: "incorrect-accounting-when-updating-streams-sigmaprime-none-sushi-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal depositedshares;"
fn_name_regex: ".*(updateStream|topUpAmount|endTime|depositedShares).*"
read_var_regex: ".*(depositedShares|endTime|extendTime).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "updateStream"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return depositedshares > 0;"
