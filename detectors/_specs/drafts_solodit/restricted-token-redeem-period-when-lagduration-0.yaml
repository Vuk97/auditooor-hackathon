skeleton: "name_match_missing_require"
name: "restricted-token-redeem-period-when-lagduration-0"
class_name: "RestrictedTokenRedeemPeriodWhenLagduration0"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #38520 (SigmaPrime/August)"
help: "Restricted Token Redeem Period When lagDuration == 0"
wiki_title: "Restricted Token Redeem Period When lagDuration == 0"
wiki_description: |
    ## Description
    
    If `lagDuration` == 0, users may have only a limited period, potentially just 1 hour in a day, to claim their token. When `lagDuration` == 0, the pool is not time-locked. Therefore, users are supposed to redeem their tokens instantly. However, this is not the case, as the redeem phas
wiki_exploit_scenario: |
    Per Solodit #38520: ## Description
    
    If `lagDuration` == 0, users may have only a limited period, potentially just 1 hour in a day, to claim their token. When `lagDuration` == 0, the pool is not time-locked. Therefore, us
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RestrictedTokenRedeemPeriodWhenLagduration0"
solodit_id: "38520"
solodit_slug: "restricted-token-redeem-period-when-lagduration-0-sigmaprime-none-august-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal day;"
fn_name_regex: ".*(lagDuration|liquidationHour|requestRedeem|year).*"
write_var_regex: ".*(day|lagDuration|liquidationHour).*"
guard_var_regex: ".*(day|lagDuration|liquidationHour).*"
vuln_fn_name: "lagDuration"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "day = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
