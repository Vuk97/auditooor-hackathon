skeleton: "name_match_missing_call"
name: "m-03-allowance-checks-not-correctly-implemented"
class_name: "M03AllowanceChecksNotCorrectlyImplemented"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #25548 (Code4rena/Notional)"
help: "[M-03] Allowance checks not correctly implemented"
wiki_title: "[M-03] Allowance checks not correctly implemented"
wiki_description: |
    _Submitted by cmichel_
    
    The `nTokenAction` implements two token approvals, the `nTokenWhitelist` which is always used first, and the `nTokenAllowance` which is checked second.
    If the `nTokenWhitelist` does _not_ have enough allowance for the transfer, the transaction fails, even in the case where `n
wiki_exploit_scenario: |
    Per Solodit #25548: _Submitted by cmichel_
    
    The `nTokenAction` implements two token approvals, the `nTokenWhitelist` which is always used first, and the `nTokenAllowance` which is checked second.
    If the `nTokenWhitelist`
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03AllowanceChecksNotCorrectlyImplemented"
solodit_id: "25548"
solodit_slug: "m-03-allowance-checks-not-correctly-implemented-code4rena-notional-notional-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal ntokenaction;"
fn_name_regex: ".*(nTokenAction|nTokenWhitelist|nTokenAllowance).*"
read_var_regex: ".*(nTokenAction|nTokenAllowance|nTokenWhitelist).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "nTokenAction"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return ntokenaction > 0;"
