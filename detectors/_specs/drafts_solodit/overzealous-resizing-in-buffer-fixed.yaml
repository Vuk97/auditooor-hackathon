skeleton: "name_match_missing_require"
name: "overzealous-resizing-in-buffer-fixed"
class_name: "OverzealousResizingInBufferFixed"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #14016 (ConsenSys/ENS Permanent Registrar)"
help: "Overzealous resizing in Buffer ✓ Fixed"
wiki_title: "Overzealous resizing in Buffer ✓ Fixed"
wiki_description: |
    #### Resolution
    
    
    
    Issue has been closed in [ensdomains/buffer#4](https://github.com/ensdomains/buffer/pull/4/)
    
    
    #### Description
    
    
    In the following code, the buffer is resized even when sufficient `capacity` is available to perform the write. The `buf.buf.length` term is unnecessary and leads to u
wiki_exploit_scenario: |
    Per Solodit #14016: #### Resolution
    
    
    
    Issue has been closed in [ensdomains/buffer#4](https://github.com/ensdomains/buffer/pull/4/)
    
    
    #### Description
    
    
    In the following code, the buffer is resized even when sufficient `
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OverzealousResizingInBufferFixed"
solodit_id: "14016"
solodit_slug: "overzealous-resizing-in-buffer-fixed-consensys-ens-permanent-registrar-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal append;"
fn_name_regex: ".*(capacity|append).*"
write_var_regex: ".*(append|capacity).*"
guard_var_regex: ".*(append|capacity).*"
vuln_fn_name: "capacity"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "append = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
