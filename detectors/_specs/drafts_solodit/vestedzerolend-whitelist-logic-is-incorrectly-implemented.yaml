skeleton: "name_match_missing_require"
name: "vestedzerolend-whitelist-logic-is-incorrectly-implemented"
class_name: "VestedzerolendWhitelistLogicIsIncorrectlyImplemented"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #40824 (Cantina/ZeroLend)"
help: "vestedzerolend whitelist logic is incorrectly implemented"
wiki_title: "vestedzerolend whitelist logic is incorrectly implemented "
wiki_description: |
    ## Review Summary
    
    ## Context
    (No context files were provided by the reviewer)
    
    ## Description
    The whitelist and blacklist logic is implemented inside the `_afterTokenTransfer` function that is called when tokens are minted, burned, or transferred from an account to another:
    
    ```solidity
    function _a
wiki_exploit_scenario: |
    Per Solodit #40824: ## Review Summary
    
    ## Context
    (No context files were provided by the reviewer)
    
    ## Description
    The whitelist and blacklist logic is implemented inside the `_afterTokenTransfer` function that is called
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "VestedzerolendWhitelistLogicIsIncorrectlyImplemented"
solodit_id: "40824"
solodit_slug: "vestedzerolend-whitelist-logic-is-incorrectly-implemented-cantina-none-zerolend-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal enableblacklist;"
fn_name_regex: ".*(enableBlacklist|enableWhitelist).*"
write_var_regex: ".*(enableBlacklist|enableWhitelist).*"
guard_var_regex: ".*(enableBlacklist|enableWhitelist).*"
vuln_fn_name: "enableBlacklist"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "enableblacklist = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
