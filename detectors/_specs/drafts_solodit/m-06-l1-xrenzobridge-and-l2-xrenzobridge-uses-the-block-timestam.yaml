skeleton: "name_match_missing_call"
name: "m-06-l1-xrenzobridge-and-l2-xrenzobridge-uses-the-block-timestam"
class_name: "M06L1XrenzobridgeAndL2XrenzobridgeUsesTheBlockTimestam"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #33501 (Code4rena/Renzo)"
help: |
    [M-06] `L1::xRenzoBridge` and `L2::xRenzoBridge` uses the `block.timestamp` as dependency, which can cause issues
wiki_title: |
    [M-06] `L1::xRenzoBridge` and `L2::xRenzoBridge` uses the `block.timestamp` as dependency, which can cause issues
wiki_description: |
    In `L1::xRenzoBridge` the `block.timestamp` from L1 is encoded and sent to L2. When the message is delivered from L1 to L2  with `xRenzoBridge::_updatePrice()`, the function checks the `block.timestamp` like this:
    
    ```solidity
        if (_timestamp > block.timestamp) {
                revert InvalidTimest
wiki_exploit_scenario: |
    Per Solodit #33501: In `L1::xRenzoBridge` the `block.timestamp` from L1 is encoded and sent to L2. When the message is delivered from L1 to L2  with `xRenzoBridge::_updatePrice()`, the function checks the `block.timesta
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M06L1XrenzobridgeAndL2XrenzobridgeUsesTheBlockTimestam"
solodit_id: "33501"
solodit_slug: |
    m-06-l1xrenzobridge-and-l2xrenzobridge-uses-the-blocktimestamp-as-dependency-which-can-cause-issues-code4rena-renzo-renzo-git
solodit_quality: "5"
solodit_rarity: "5"
solodit_tags: "Timing, block.number vs block.timestamp"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(xRenzoBridge).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "xRenzoBridge"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
