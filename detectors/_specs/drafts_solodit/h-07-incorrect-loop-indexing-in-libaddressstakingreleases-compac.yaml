skeleton: "name_match_missing_require"
name: "h-07-incorrect-loop-indexing-in-libaddressstakingreleases-compac"
class_name: "H07IncorrectLoopIndexingInLibaddressstakingreleasesCompac"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #65094 (Code4rena/Recall)"
help: |
    [H-07] Incorrect loop indexing in `LibAddressStakingReleases.compact()` leads to unprocessed pending releases
wiki_title: |
    [H-07] Incorrect loop indexing in `LibAddressStakingReleases.compact()` leads to unprocessed pending releases
wiki_description: |
    The root cause of this issue is the use of an absolute index (`self.startIdx`) in the loop condition of the `compact()` function in `LibAddressStakingReleases`, while `self.length` represents the number of pending releases.
    
    If there are more processed releases than pending releases, the following
wiki_exploit_scenario: |
    Per Solodit #65094: The root cause of this issue is the use of an absolute index (`self.startIdx`) in the loop condition of the `compact()` function in `LibAddressStakingReleases`, while `self.length` represents the nu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H07IncorrectLoopIndexingInLibaddressstakingreleasesCompac"
solodit_id: "65094"
solodit_slug: |
    h-07-incorrect-loop-indexing-in-libaddressstakingreleasescompact-leads-to-unprocessed-pending-releases-code4rena-recall-recall-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal compact;"
fn_name_regex: ".*(compact|LibAddressStakingReleases|startIdx).*"
write_var_regex: ".*(compact|startIdx).*"
guard_var_regex: ".*(compact|startIdx).*"
vuln_fn_name: "compact"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "compact = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
