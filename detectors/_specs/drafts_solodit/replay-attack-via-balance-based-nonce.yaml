skeleton: "name_match_missing_call"
name: "replay-attack-via-balance-based-nonce"
class_name: "ReplayAttackViaBalanceBasedNonce"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #63501 (MixBytes/Barter DAO)"
help: "Replay Attack via Balance-Based Nonce"
wiki_title: "Replay Attack via Balance-Based Nonce"
wiki_description: |
    ##### Description
    The protocol uses only the user's token balance as a nonce to prevent replay attacks:
    
    ```solidity
    uint256 currentMakerBalance = order.makerToken.balanceOf(order.maker);
    if (currentMakerBalance < order.nonceBalance) {
        revert ReplayAttackDetected(currentMakerBalance, order.nonce
wiki_exploit_scenario: |
    Per Solodit #63501: ##### Description
    The protocol uses only the user's token balance as a nonce to prevent replay attacks:
    
    ```solidity
    uint256 currentMakerBalance = order.makerToken.balanceOf(order.maker);
    if (currentM
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ReplayAttackViaBalanceBasedNonce"
solodit_id: "63501"
solodit_slug: "replay-attack-via-balance-based-nonce-mixbytes-none-barter-dao-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(currentMakerBalance|makerToken|balanceOf|nonceBalance).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "currentMakerBalance"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
