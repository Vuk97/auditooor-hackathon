skeleton: "name_match_missing_call"
name: "h-01-cross-chain-replay-in-borrowasset-swaptoborrow"
class_name: "H01CrossChainReplayInBorrowassetSwaptoborrow"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #48929 (Kann/Rwa)"
help: "[H-01] Cross-chain Replay in borrowAsset() & swapToBorrow()"
wiki_title: "[H-01] Cross-chain Replay in borrowAsset() & swapToBorrow()"
wiki_description: |
    **Description**
    
    Cross-chain Replay: If an User borrows assets on Chain A, they could reuse the same signature (generated from the same data) on Chain B, assuming the signature is valid for the same borrow hash structure on both chains.
    
    **Recommendations**
    
    ```solidity
    bytes32 _borrowHash = keccak2
wiki_exploit_scenario: |
    Per Solodit #48929: **Description**
    
    Cross-chain Replay: If an User borrows assets on Chain A, they could reuse the same signature (generated from the same data) on Chain B, assuming the signature is valid for the same b
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01CrossChainReplayInBorrowassetSwaptoborrow"
solodit_id: "48929"
solodit_slug: "h-01-cross-chain-replay-in-borrowasset-swaptoborrow-kann-audits-none-rwa-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(collateralAmounts|borrowsAmounts|nonceOf|chainId).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "collateralAmounts"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
