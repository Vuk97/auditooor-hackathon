skeleton: "name_match_missing_require"
name: "incorrect-logic-in-a-while-loop"
class_name: "IncorrectLogicInAWhileLoop"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #37598 (Zokyo/Realize)"
help: "Incorrect logic in a while loop"
wiki_title: "Incorrect logic in a while loop"
wiki_description: |
    **Severity**: High
    
    **Status**: Resolved
    
    **Description**
    
    In the `_removeBurnedBatches()` function, if `batch.amount` is less than `burnAmount`, it means that remaining is greater than `batch.amount` and the function goes to the “else” case.
    In the else case, `ownerBatches[from][i]` is updated with
wiki_exploit_scenario: |
    Per Solodit #37598: **Severity**: High
    
    **Status**: Resolved
    
    **Description**
    
    In the `_removeBurnedBatches()` function, if `batch.amount` is less than `burnAmount`, it means that remaining is greater than `batch.amount`
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectLogicInAWhileLoop"
solodit_id: "37598"
solodit_slug: "incorrect-logic-in-a-while-loop-zokyo-none-realize-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal burnamount;"
fn_name_regex: ".*(burnAmount).*"
write_var_regex: ".*(burnAmount).*"
guard_var_regex: ".*(burnAmount).*"
vuln_fn_name: "burnAmount"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "burnamount = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
