skeleton: "highlevelcall_missing_sibling"
name: "h-04-oracle-price-can-be-manipulated"
class_name: "H04OraclePriceCanBeManipulated"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #32056 (Code4rena/Abracadabra Money)"
help: "[H-04] Oracle price can be manipulated"
wiki_title: "[H-04] Oracle price can be manipulated"
wiki_description: |
    Oracle price can be manipulated.
    
    ### Proof of Concept
    
    MagicLpAggregator uses pool reserves to calculate the price of the pair token,
    
    ```solidity
        function _getReserves() internal view virtual returns (uint256, uint256) {
            (uint256 baseReserve, uint256 quoteReserve) = pair.getReserves()
wiki_exploit_scenario: |
    Per Solodit #32056: Oracle price can be manipulated.
    
    ### Proof of Concept
    
    MagicLpAggregator uses pool reserves to calculate the price of the pair token,
    
    ```solidity
        function _getReserves() internal view virtual r
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H04OraclePriceCanBeManipulated"
solodit_id: "32056"
solodit_slug: "h-04-oracle-price-can-be-manipulated-code4rena-abracadabra-money-abracadabra-money-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(baseReserve|quoteReserve|getReserves|latestAnswer).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
