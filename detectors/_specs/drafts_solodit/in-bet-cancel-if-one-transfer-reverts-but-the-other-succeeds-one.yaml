skeleton: "name_match_missing_call"
name: "in-bet-cancel-if-one-transfer-reverts-but-the-other-succeeds-one"
class_name: "InBetCancelIfOneTransferRevertsButTheOtherSucceedsOne"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #64638 (Cyfrin/Wannabet)"
help: |
    In `Bet::cancel` if one transfer reverts but the other succeeds, one users's tokens are permanently locked in the `Bet` contract
wiki_title: |
    In `Bet::cancel` if one transfer reverts but the other succeeds, one users's tokens are permanently locked in the `Bet` contract
wiki_description: |
    **Description:** `Bet::cancel` does this when refunding token transfers:
    ```solidity
    try IERC20(b.asset).transfer(b.maker, makerRefund) {} catch {}
    try IERC20(b.asset).transfer(b.taker, takerRefund) {} catch {}
    ```
    
    But if one of the `transfer` calls reverts but the other succeeds, the transaction s
wiki_exploit_scenario: |
    Per Solodit #64638: **Description:** `Bet::cancel` does this when refunding token transfers:
    ```solidity
    try IERC20(b.asset).transfer(b.maker, makerRefund) {} catch {}
    try IERC20(b.asset).transfer(b.taker, takerRefund) {
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InBetCancelIfOneTransferRevertsButTheOtherSucceedsOne"
solodit_id: "64638"
solodit_slug: |
    in-betcancel-if-one-transfer-reverts-but-the-other-succeeds-one-userss-tokens-are-permanently-locked-in-the-bet-contract-cyfrin-none-wannabet-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal catch;"
fn_name_regex: ".*(transfer|CANCELLED|Bet|catch).*"
read_var_regex: ".*(catch|maker|taker).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transfer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return catch > 0;"
