skeleton: "name_match_missing_call"
name: "m-10-initial-deploy-won-t-succeed-because-of-too-high-initialmin"
class_name: "M10InitialDeployWonTSucceedBecauseOfTooHighInitialmin"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #26846 (Code4rena/Moonwell)"
help: "[M-10] Initial deploy won't succeed because of too high `initialMintAmount` for USDC market"
wiki_title: "[M-10] Initial deploy won't succeed because of too high `initialMintAmount` for USDC market"
wiki_description: |
    At the time of deploy, deployer initializes token markets with initial amount to prevent exploit. At least USDC and WETH markets will be initialized during deploy.
    But `initialMintAmount` is hardcoded to `1e18` which is of for WETH (1,876 USD), but unrealistic for USDC (1,000,000,000,000 USD). Ther
wiki_exploit_scenario: |
    Per Solodit #26846: At the time of deploy, deployer initializes token markets with initial amount to prevent exploit. At least USDC and WETH markets will be initialized during deploy.
    But `initialMintAmount` is hardcode
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M10InitialDeployWonTSucceedBecauseOfTooHighInitialmin"
solodit_id: "26846"
solodit_slug: |
    m-10-initial-deploy-wont-succeed-because-of-too-high-initialmintamount-for-usdc-market-code4rena-moonwell-moonwell-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal initialmintamount;"
fn_name_regex: ".*(initialMintAmount).*"
read_var_regex: ".*(initialMintAmount).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "initialMintAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return initialmintamount > 0;"
