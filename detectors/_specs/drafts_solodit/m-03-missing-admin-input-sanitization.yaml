skeleton: "name_match_missing_require"
name: "m-03-missing-admin-input-sanitization"
class_name: "M03MissingAdminInputSanitization"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20345 (Pashov Audit Group/Azuro)"
help: "[M-03] Missing admin input sanitization"
wiki_title: "[M-03] Missing admin input sanitization"
wiki_description: |
    **Likelihood:**
    Low, because it requires a malicious/compromised admin or an error on admin side
    
    **Impact:**
    High, because important protocol functionality can be bricked
    
    **Description**
    
    It is not checked that the `claimTimeout` property in `LP.sol` both in its setter function and in `initialize`
wiki_exploit_scenario: |
    Per Solodit #20345: **Likelihood:**
    Low, because it requires a malicious/compromised admin or an error on admin side
    
    **Impact:**
    High, because important protocol functionality can be bricked
    
    **Description**
    
    It is not
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03MissingAdminInputSanitization"
solodit_id: "20345"
solodit_slug: "m-03-missing-admin-input-sanitization-pashov-none-azuro-markdown"
solodit_quality: "1"
solodit_rarity: "1"
solodit_tags: ""
state_decl: "uint256 internal checkfee;"
fn_name_regex: ".*(claimTimeout|initialize|withdrawTimeout|checkFee).*"
write_var_regex: ".*(checkFee|claimTimeout|initialize).*"
guard_var_regex: ".*(checkFee|claimTimeout|initialize).*"
vuln_fn_name: "claimTimeout"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "checkfee = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
