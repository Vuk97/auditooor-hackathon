skeleton: "name_match_missing_call"
name: "checks-effects-interactions-pattern-is-not-followed-in-the-claim"
class_name: "ChecksEffectsInteractionsPatternIsNotFollowedInTheClaim"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #50415 (Halborn/Eon Delegated Staking)"
help: "Checks-Effects-Interactions pattern is not followed in the claimReward() function"
wiki_title: "Checks-Effects-Interactions pattern is not followed in the claimReward() function"
wiki_description: |
    ##### Description
    
    The `DelegatedStaking` contract implements the `claimReward()` function used by delegators to claim their rewards:
    
    ```
    function claimReward(address payable owner) external {
        (uint256 totalToClaim, ClaimData[] memory claimDetails) = calcReward(owner);
        if(totalToClaim == 0)
wiki_exploit_scenario: |
    Per Solodit #50415: ##### Description
    
    The `DelegatedStaking` contract implements the `claimReward()` function used by delegators to claim their rewards:
    
    ```
    function claimReward(address payable owner) external {
        (u
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ChecksEffectsInteractionsPatternIsNotFollowedInTheClaim"
solodit_id: "50415"
solodit_slug: |
    checks-effects-interactions-pattern-is-not-followed-in-the-claimreward-function-halborn-horizen-labs-eon-delegated-staking-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal claimreward;"
fn_name_regex: ".*(DelegatedStaking|claimReward|lastClaimedEpochForAddress|transfer).*"
read_var_regex: ".*(claimReward|lastClaimedEpochForAddress|transfer).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "DelegatedStaking"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return claimreward > 0;"
