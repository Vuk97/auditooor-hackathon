skeleton: "name_match_missing_call"
name: "exchange-insufficient-input-validation-in-matchorders-pending"
class_name: "ExchangeInsufficientInputValidationInMatchordersPending"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #13718 (ConsenSys/MCDEX Mai Protocol V2)"
help: "Exchange - insufficient input validation in matchOrders  Pending"
wiki_title: "Exchange - insufficient input validation in matchOrders  Pending"
wiki_description: |
    #### Resolution
    
    
    
    This issue was addressed by following the recommendation to verify that `amounts.length > 0 && makerOrderParams.length == amounts.length`. However, the code does not abort if one of the `amounts` is zero which should never happen and therefore raise an exception due to it likely b
wiki_exploit_scenario: |
    Per Solodit #13718: #### Resolution
    
    
    
    This issue was addressed by following the recommendation to verify that `amounts.length > 0 && makerOrderParams.length == amounts.length`. However, the code does not abort if one of
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ExchangeInsufficientInputValidationInMatchordersPending"
solodit_id: "13718"
solodit_slug: |
    exchange-insufficient-input-validation-in-matchorders-pending-consensys-mcdex-mai-protocol-v2-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(amounts|matchOrders|makerOrderParams|tradingLotSize).*"
read_var_regex: ".*(amount|amounts|makerOrderParams).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "amounts"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amount > 0;"
