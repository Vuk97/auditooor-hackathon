skeleton: "name_match_missing_call"
name: "m-15-first-depositors-will-incurr-a-rebalancing-loss"
class_name: "M15FirstDepositorsWillIncurrARebalancingLoss"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20252 (Code4rena/Polynomial Protocol)"
help: "[M-15] First Depositors will incurr a rebalancing Loss"
wiki_title: "[M-15] First Depositors will incurr a rebalancing Loss"
wiki_description: |
    ```solidity
        function getTokenPrice() public view returns (uint256) {
            if (totalFunds == 0) {
                return 1e18;
            }
    
            uint256 totalSupply = getTotalSupply();
            if (positionData.positionId == 0) {
                return totalFunds.divWadDown(totalSupply);
            }
    
    `
wiki_exploit_scenario: |
    Per Solodit #20252: ```solidity
        function getTokenPrice() public view returns (uint256) {
            if (totalFunds == 0) {
                return 1e18;
            }
    
            uint256 totalSupply = getTotalSupply();
            if
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M15FirstDepositorsWillIncurrARebalancingLoss"
solodit_id: "20252"
solodit_slug: |
    m-15-first-depositors-will-incurr-a-rebalancing-loss-code4rena-polynomial-protocol-polynomial-protocol-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal gettokenprice;"
fn_name_regex: ".*(getTokenPrice|openPosition).*"
read_var_regex: ".*(getTokenPrice|openPosition).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "getTokenPrice"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return gettokenprice > 0;"
