skeleton: "highlevelcall_missing_sibling"
name: "m-01-panopticfactory-uses-spot-price-when-deploying-new-pools-re"
class_name: "M01PanopticfactoryUsesSpotPriceWhenDeployingNewPoolsRe"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #33677 (Code4rena/Panoptic)"
help: |
    [M-01] `PanopticFactory` uses spot price when deploying new pools, resulting in liquidity manipulation when minting
wiki_title: |
    [M-01] `PanopticFactory` uses spot price when deploying new pools, resulting in liquidity manipulation when minting
wiki_description: |
    When `deployNewPool` is called it uses the spot price of the pool, which can be manipulated through a flashloan and thus could return a highly inaccurate result.
    
    The price is used when deciding how much liquidity should be minted for each token, so this can result in an unbalanced pool.
    
    In other
wiki_exploit_scenario: |
    Per Solodit #33677: When `deployNewPool` is called it uses the spot price of the pool, which can be manipulated through a flashloan and thus could return a highly inaccurate result.
    
    The price is used when deciding how
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01PanopticfactoryUsesSpotPriceWhenDeployingNewPoolsRe"
solodit_id: "33677"
solodit_slug: |
    m-01-panopticfactory-uses-spot-price-when-deploying-new-pools-resulting-in-liquidity-manipulation-when-minting-code4rena-panoptic-panoptic-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(deployNewPool|fullRangeLiquidity|PanopticFactory).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
