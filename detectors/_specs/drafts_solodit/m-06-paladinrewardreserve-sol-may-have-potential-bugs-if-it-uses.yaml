skeleton: "name_match_missing_call"
name: "m-06-paladinrewardreserve-sol-may-have-potential-bugs-if-it-uses"
class_name: "M06PaladinrewardreserveSolMayHavePotentialBugsIfItUses"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #24720 (Code4rena/Paladin)"
help: "[M-06] `PaladinRewardReserve.sol` may have potential bugs if it uses new tokens as rewards"
wiki_title: "[M-06] `PaladinRewardReserve.sol` may have potential bugs if it uses new tokens as rewards"
wiki_description: |
    _Submitted by TerrierLover_
    
    `PaladinRewardReserve.sol` may have potential bugs if it uses new tokens as rewards.
    
    ### Proof of Concept
    
    Currently, `PaladinRewardReserve.sol` has following behaviors:
    
    *   `mapping(address => bool) public approvedSpenders` does not store the info regarding which toke
wiki_exploit_scenario: |
    Per Solodit #24720: _Submitted by TerrierLover_
    
    `PaladinRewardReserve.sol` may have potential bugs if it uses new tokens as rewards.
    
    ### Proof of Concept
    
    Currently, `PaladinRewardReserve.sol` has following behaviors:
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M06PaladinrewardreserveSolMayHavePotentialBugsIfItUses"
solodit_id: "24720"
solodit_slug: |
    m-06-paladinrewardreservesol-may-have-potential-bugs-if-it-uses-new-tokens-as-rewards-code4rena-paladin-paladin-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal approvedspenders;"
fn_name_regex: ".*(setNewSpender|updateSpenderAllowance|removeSpender|transferToken).*"
read_var_regex: ".*(approvedSpenders|mapping|removeSpender).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "setNewSpender"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return approvedspenders > 0;"
