skeleton: "name_match_missing_call"
name: "token-transfers-may-fail-locking-auctions"
class_name: "TokenTransfersMayFailLockingAuctions"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #19595 (SigmaPrime/Sushi)"
help: "Token Transfers May Fail Locking Auctions"
wiki_title: "Token Transfers May Fail Locking Auctions"
wiki_description: |
    ## Description
    
    It is possible for token transfers to fail for certain ERC20 tokens. For example, some ERC20 tokens prevent transfers to blacklisted accounts.
    
    If the `token.transfer(bid.bidder, bid.rewardAmount);` fails during `end()`, then the entire transaction will fail. The impact is that it wi
wiki_exploit_scenario: |
    Per Solodit #19595: ## Description
    
    It is possible for token transfers to fail for certain ERC20 tokens. For example, some ERC20 tokens prevent transfers to blacklisted accounts.
    
    If the `token.transfer(bid.bidder, bid.r
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TokenTransfersMayFailLockingAuctions"
solodit_id: "19595"
solodit_slug: "token-transfers-may-fail-locking-auctions-sigmaprime-none-sushi-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal clearbid;"
fn_name_regex: ".*(end|clearBid|start|placeBid).*"
read_var_regex: ".*(clearBid|deposit|end).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "end"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return clearbid > 0;"
