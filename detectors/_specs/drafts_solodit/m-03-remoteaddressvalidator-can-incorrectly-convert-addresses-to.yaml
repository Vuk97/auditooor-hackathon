skeleton: "name_match_missing_call"
name: "m-03-remoteaddressvalidator-can-incorrectly-convert-addresses-to"
class_name: "M03RemoteaddressvalidatorCanIncorrectlyConvertAddressesTo"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #28763 (Code4rena/Axelar Network)"
help: "[M-03] `RemoteAddressValidator` can incorrectly convert addresses to lowercase"
wiki_title: "[M-03] `RemoteAddressValidator` can incorrectly convert addresses to lowercase"
wiki_description: |
    The `validateSender` and `addTrustedAddress` functions of `RemoteAddressValidator` can incorrectly handle the passed address arguments, which will result in false negatives. E.g. a valid sender address may be invalidated.
    
    ### Proof of Concept
    
    The [RemoteAddressValidator.\_lowerCase](https://githu
wiki_exploit_scenario: |
    Per Solodit #28763: The `validateSender` and `addTrustedAddress` functions of `RemoteAddressValidator` can incorrectly handle the passed address arguments, which will result in false negatives. E.g. a valid sender addre
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03RemoteaddressvalidatorCanIncorrectlyConvertAddressesTo"
solodit_id: "28763"
solodit_slug: |
    m-03-remoteaddressvalidator-can-incorrectly-convert-addresses-to-lowercase-code4rena-axelar-network-axelar-network-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal addtrustedaddress;"
fn_name_regex: ".*(validateSender|addTrustedAddress|RemoteAddressValidator).*"
read_var_regex: ".*(addTrustedAddress|validateSender).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "validateSender"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return addtrustedaddress > 0;"
