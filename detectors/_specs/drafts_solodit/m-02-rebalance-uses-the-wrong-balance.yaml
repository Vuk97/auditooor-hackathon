skeleton: "name_match_missing_require"
name: "m-02-rebalance-uses-the-wrong-balance"
class_name: "M02RebalanceUsesTheWrongBalance"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #36373 (Pashov Audit Group/Radiant June)"
help: "[M-02] `rebalance` uses the wrong balance"
wiki_title: "[M-02] `rebalance` uses the wrong balance"
wiki_description: |
    **Severity**
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    **Description**
    
    If the `swapQuantity` parameter is `0`, the `rebalance` function will perform the swap first. After the swap, the balances of `token0` and `token1` in the contract will change. However, `_liquidityForAmounts` still uses the b
wiki_exploit_scenario: |
    Per Solodit #36373: **Severity**
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    **Description**
    
    If the `swapQuantity` parameter is `0`, the `rebalance` function will perform the swap first. After the swap, the balances of
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02RebalanceUsesTheWrongBalance"
solodit_id: "36373"
solodit_slug: "m-02-rebalance-uses-the-wrong-balance-pashov-audit-group-none-radiant-june-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal rebalance;"
fn_name_regex: ".*(swapQuantity|rebalance|token0|token1).*"
write_var_regex: ".*(rebalance|swapQuantity|token0).*"
guard_var_regex: ".*(rebalance|swapQuantity|token0).*"
vuln_fn_name: "swapQuantity"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "rebalance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
