skeleton: "name_match_missing_call"
name: "c-01-value-of-leaf-argument-when-calling-addreserve-is-hardcoded"
class_name: "C01ValueOfLeafArgumentWhenCallingAddreserveIsHardcoded"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #20438 (Pashov Audit Group/Azuro)"
help: "[C-01] Value of `leaf` argument when calling `addReserve` is hardcoded incorrectly"
wiki_title: "[C-01] Value of `leaf` argument when calling `addReserve` is hardcoded incorrectly"
wiki_description: |
    **Impact:**
    High, because liquidity won't be returned to the LiquidityTree
    
    **Likelihood:**
    High, because the incorrect value is hardcoded and can't be changed
    
    **Description**
    
    In `BetExpress::resolvePayout` we can see the following code:
    
    ```solidity
    uint128 reward = lp.addReserve(
        0,
        full
wiki_exploit_scenario: |
    Per Solodit #20438: **Impact:**
    High, because liquidity won't be returned to the LiquidityTree
    
    **Likelihood:**
    High, because the incorrect value is hardcoded and can't be changed
    
    **Description**
    
    In `BetExpress::resolv
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "C01ValueOfLeafArgumentWhenCallingAddreserveIsHardcoded"
solodit_id: "20438"
solodit_slug: |
    c-01-value-of-leaf-argument-when-calling-addreserve-is-hardcoded-incorrectly-pashov-none-azuro-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal condition;"
fn_name_regex: ".*(leaf|condition|resolvePayout|addReserve).*"
read_var_regex: ".*(condition|leaf|resolvePayout).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "leaf"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return condition > 0;"
