skeleton: "name_match_missing_call"
name: "invalid-reward-updating-mechanism"
class_name: "InvalidRewardUpdatingMechanism"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #50758 (Halborn/SCProtection)"
help: "INVALID REWARD UPDATING MECHANISM"
wiki_title: "INVALID REWARD UPDATING MECHANISM"
wiki_description: |
    ##### Description
    
    The `_updateReward` in case of multiple months passed is not properly implemented/handled:
    
    * `monthPassed` should also consider if `currentY` and `lastY` are different and add the difference to `monthPassed` multiplied by 12. Otherwise, the `for` loop will only iterate over a max
wiki_exploit_scenario: |
    Per Solodit #50758: ##### Description
    
    The `_updateReward` in case of multiple months passed is not properly implemented/handled:
    
    * `monthPassed` should also consider if `currentY` and `lastY` are different and add the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InvalidRewardUpdatingMechanism"
solodit_id: "50758"
solodit_slug: "invalid-reward-updating-mechanism-halborn-degis-scprotection-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal currentm;"
fn_name_regex: ".*(monthPassed|currentY|lastY|for).*"
read_var_regex: ".*(currentM|currentY|endTimestamp).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "monthPassed"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return currentm > 0;"
