skeleton: "paired_function_divergence"
name: "m-02-the-removepool-function-permanently-locks-user-balances-for"
class_name: "M02TheRemovepoolFunctionPermanentlyLocksUserBalancesFor"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #65081 (Shieldify/Abster Freefall)"
help: "[M-02] The `removePool()` Function Permanently Locks User Balances for that Token"
wiki_title: "[M-02] The `removePool()` Function Permanently Locks User Balances for that Token"
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    The `removePool()` deletes the pool entry but does not clear or migrate `userBalances`.
    
    After removal, `isTokenSupported()` returns false for that token, which blocks:
    
    - `withdraw()`
    - `deposit()`
    - `claim()`
    - `addLiquidity()` / `removeLiquidity()`
    - `cr
wiki_exploit_scenario: |
    Per Solodit #65081: ## Severity
    
    Medium Risk
    
    ## Description
    
    The `removePool()` deletes the pool entry but does not clear or migrate `userBalances`.
    
    After removal, `isTokenSupported()` returns false for that token, wh
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02TheRemovepoolFunctionPermanentlyLocksUserBalancesFor"
solodit_id: "65081"
solodit_slug: |
    m-02-the-removepool-function-permanently-locks-user-balances-for-that-token-shieldify-none-abster-freefall-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(addLiquidity|claim|createGame).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
