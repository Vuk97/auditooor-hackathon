skeleton: "highlevelcall_missing_sibling"
name: "h-03-manipulation-of-liveprice-to-receive-defaultincentive-in-2-"
class_name: "H03ManipulationOfLivepriceToReceiveDefaultincentiveIn2"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #16075 (Code4rena/Malt Protocol)"
help: "[H-03] Manipulation of `livePrice` to receive `defaultIncentive` in 2 consecutive blocks"
wiki_title: "[H-03] Manipulation of `livePrice` to receive `defaultIncentive` in 2 consecutive blocks"
wiki_description: |
    In StabilizerNode, the default behaviour when twap is below the lower peg threshold, all transfers to the amm pool are blocked. However when `usePrimedWindow = true`, it will only block transfers for `primedWindow = 10` blocks. After 10 blocks, the block automatically stops and allows free market t
wiki_exploit_scenario: |
    Per Solodit #16075: In StabilizerNode, the default behaviour when twap is below the lower peg threshold, all transfers to the amm pool are blocked. However when `usePrimedWindow = true`, it will only block transfers for
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03ManipulationOfLivepriceToReceiveDefaultincentiveIn2"
solodit_id: "16075"
solodit_slug: |
    h-03-manipulation-of-liveprice-to-receive-defaultincentive-in-2-consecutive-blocks-code4rena-malt-protocol-malt-protocol-versus-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(defaultIncentive|primedBlock|livePrice|stabilize).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
