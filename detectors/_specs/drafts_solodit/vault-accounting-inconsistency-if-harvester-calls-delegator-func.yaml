skeleton: "name_match_missing_call"
name: "vault-accounting-inconsistency-if-harvester-calls-delegator-func"
class_name: "VaultAccountingInconsistencyIfHarvesterCallsDelegatorFunc"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #59385 (Quantstamp/Affine Labs - UltraETH LRT)"
help: "Vault Accounting Inconsistency if Harvester Calls Delegator Functions Directly"
wiki_title: "Vault Accounting Inconsistency if Harvester Calls Delegator Functions Directly"
wiki_description: |
    **Update**
    We reviewed the fix and confirmed that the team made the following changes to resolve the issue:
    
    *   `delegate()` function now transfers tokens from `msg.sender` instead of the vault
    *   The `withdraw()` function is restricted to the vault
    
    ![Image 16: Alert icon](https://certificate.qua
wiki_exploit_scenario: |
    Per Solodit #59385: **Update**
    We reviewed the fix and confirmed that the team made the following changes to resolve the issue:
    
    *   `delegate()` function now transfers tokens from `msg.sender` instead of the vault
    *   T
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "VaultAccountingInconsistencyIfHarvesterCallsDelegatorFunc"
solodit_id: "59385"
solodit_slug: |
    vault-accounting-inconsistency-if-harvester-calls-delegator-functions-directly-quantstamp-affine-labs-ultraeth-lrt-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal delegate;"
fn_name_regex: ".*(delegate|withdraw|UltraLRT|AffineDelegator).*"
read_var_regex: ".*(delegate|delegatorAssets|delegatorMap).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "delegate"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return delegate > 0;"
