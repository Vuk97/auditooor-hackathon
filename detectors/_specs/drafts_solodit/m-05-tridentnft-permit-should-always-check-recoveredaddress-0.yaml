skeleton: "name_match_missing_require"
name: "m-05-tridentnft-permit-should-always-check-recoveredaddress-0"
class_name: "M05TridentnftPermitShouldAlwaysCheckRecoveredaddress0"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #25582 (Code4rena/Sushi)"
help: "[M-05] `TridentNFT.permit` should always check `recoveredAddress != 0`"
wiki_title: "[M-05] `TridentNFT.permit` should always check `recoveredAddress != 0`"
wiki_description: |
    _Submitted by cmichel, also found by pauliax_
    
    The `TridentNFT.permit` function ignores the `recoveredAddress != 0` check if `isApprovedForAll[owner][recoveredAddress]` is true.
    
    #### Impact
    If a user accidentally set the zero address as the operator, tokens can be stolen by anyone as a wrong signat
wiki_exploit_scenario: |
    Per Solodit #25582: _Submitted by cmichel, also found by pauliax_
    
    The `TridentNFT.permit` function ignores the `recoveredAddress != 0` check if `isApprovedForAll[owner][recoveredAddress]` is true.
    
    #### Impact
    If a user
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M05TridentnftPermitShouldAlwaysCheckRecoveredaddress0"
solodit_id: "25582"
solodit_slug: "m-05-tridentnftpermit-should-always-check-recoveredaddress-0-code4rena-sushi-sushi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal require;"
fn_name_regex: ".*(require).*"
write_var_regex: ".*(require).*"
guard_var_regex: ".*(require).*"
vuln_fn_name: "require"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "require = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
