skeleton: "name_match_missing_require"
name: "missing-public-inputs-range-check-fixed"
class_name: "MissingPublicInputsRangeCheckFixed"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #26827 (ConsenSys/Linea Plonk Verifier)"
help: "Missing Public Inputs Range Check ✓ Fixed"
wiki_title: "Missing Public Inputs Range Check ✓ Fixed"
wiki_description: |
    #### Description
    
    
    The public input is an array of `uint256` numbers, there is no check if each public input is less than SNARK scalar field modulus `r_mod`, as mentioned in the step 3 of the verifier’s algorithm in the [Plonk paper](https://eprint.iacr.org/2019/953.pdf). Since public inputs are inv
wiki_exploit_scenario: |
    Per Solodit #26827: #### Description
    
    
    The public input is an array of `uint256` numbers, there is no check if each public input is less than SNARK scalar field modulus `r_mod`, as mentioned in the step 3 of the verifier
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingPublicInputsRangeCheckFixed"
solodit_id: "26827"
solodit_slug: "missing-public-inputs-range-check-fixed-consensys-none-linea-plonk-verifier-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal r_mod;"
fn_name_regex: ".*(r_mod).*"
write_var_regex: ".*(r_mod).*"
guard_var_regex: ".*(r_mod).*"
vuln_fn_name: "r_mod"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "r_mod = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
