skeleton: "name_match_missing_require"
name: "valid-new-periods-are-incorrectly-rejected-due-to-incorrect-trac"
class_name: "ValidNewPeriodsAreIncorrectlyRejectedDueToIncorrectTrac"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #57341 (Codehawks/Core Contracts)"
help: "Valid new periods are incorrectly rejected due to incorrect tracking of `totalDuration`"
wiki_title: " Valid new periods are incorrectly rejected due to incorrect tracking of `totalDuration`"
wiki_description: |
    ## Summary
    
    
    
    `totalDuration` **must equal the sum of all elapsed durations between updates but this can be broken.**
    
    ## Vulnerability Details
    
    
    
    ```Solidity
    function createPeriod(
        Period storage self,
        uint256 startTime,
        uint256 duration,
        uint256 initialValue,
        uint256 weight
    ) i
wiki_exploit_scenario: |
    Per Solodit #57341: ## Summary
    
    
    
    `totalDuration` **must equal the sum of all elapsed durations between updates but this can be broken.**
    
    ## Vulnerability Details
    
    
    
    ```Solidity
    function createPeriod(
        Period storage
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ValidNewPeriodsAreIncorrectlyRejectedDueToIncorrectTrac"
solodit_id: "57341"
solodit_slug: |
    valid-new-periods-are-incorrectly-rejected-due-to-incorrect-tracking-of-totalduration-codehawks-regnum-aurum-acquisition-corp-core-contracts-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal createperiod;"
fn_name_regex: ".*(totalDuration|createPeriod|updateValue).*"
write_var_regex: ".*(createPeriod|totalDuration|updateValue).*"
guard_var_regex: ".*(createPeriod|totalDuration|updateValue).*"
vuln_fn_name: "totalDuration"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "createperiod = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
