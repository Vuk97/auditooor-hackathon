skeleton: "name_match_missing_require"
name: "dos-with-block-gas-limit"
class_name: "DosWithBlockGasLimit"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #50193 (Halborn/Exchange Contracts)"
help: "DOS WITH BLOCK GAS LIMIT"
wiki_title: "DOS WITH BLOCK GAS LIMIT"
wiki_description: |
    ##### Description
    
    In the contract `TreasuryVester` the `distribute()` function is used to distribute the tokens to recipients based on their allocation:
    
    `TreasuryVester.sol`
    
    #### TreasuryVester.sol
    
    ```
    function distribute() public {
        require(vestingEnabled, "TreasuryVester::distribute: vestin
wiki_exploit_scenario: |
    Per Solodit #50193: ##### Description
    
    In the contract `TreasuryVester` the `distribute()` function is used to distribute the tokens to recipients based on their allocation:
    
    `TreasuryVester.sol`
    
    #### TreasuryVester.sol
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DosWithBlockGasLimit"
solodit_id: "50193"
solodit_slug: "dos-with-block-gas-limit-halborn-pangolin-exchange-contracts-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal distribute;"
fn_name_regex: ".*(TreasuryVester|distribute).*"
write_var_regex: ".*(distribute).*"
guard_var_regex: ".*(distribute).*"
vuln_fn_name: "TreasuryVester"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "distribute = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
