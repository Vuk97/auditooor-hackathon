skeleton: "highlevelcall_missing_sibling"
name: "h-10-anyone-can-list-anchors-curate-tokens"
class_name: "H10AnyoneCanListAnchorsCurateTokens"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #3913 (Code4rena/Vader Protocol)"
help: "[H-10] Anyone can list anchors / curate tokens"
wiki_title: "[H-10] Anyone can list anchors / curate tokens"
wiki_description: |
    The `Router.listAnchor` function can be called by anyone and tokens can be added. The only check is that `require(iPOOLS(POOLS).isAnchor(token));` but this can easily be set by calling `Pools.addLiquidity(VADER, token, _)` once even without actually sending any tokens to the contract. This makes it
wiki_exploit_scenario: |
    Per Solodit #3913: The `Router.listAnchor` function can be called by anyone and tokens can be added. The only check is that `require(iPOOLS(POOLS).isAnchor(token));` but this can easily be set by calling `Pools.addLiqu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H10AnyoneCanListAnchorsCurateTokens"
solodit_id: "3913"
solodit_slug: "h-10-anyone-can-list-anchors-curate-tokens-code4rena-vader-protocol-vader-protocol-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(anchorLimit|replaceAnchor|addLiquidity|listAnchor).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
