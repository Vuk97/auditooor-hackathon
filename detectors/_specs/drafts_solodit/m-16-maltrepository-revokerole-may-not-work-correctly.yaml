skeleton: "name_match_missing_call"
name: "m-16-maltrepository-revokerole-may-not-work-correctly"
class_name: "M16MaltrepositoryRevokeroleMayNotWorkCorrectly"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #16094 (Code4rena/Malt Protocol)"
help: "[M-16] MaltRepository._revokeRole may not work correctly"
wiki_title: "[M-16] MaltRepository._revokeRole may not work correctly"
wiki_description: |
    MaltRepository inherits from AccessControl and adds validation of validRoles to the hasRole function, which means that even if super.hasRole(role, account) == true, if validRoles\[role] == false hasRole will return false, which will cause `\_revokeRole` to not work correctly.
    
    ```solidity
      functio
wiki_exploit_scenario: |
    Per Solodit #16094: MaltRepository inherits from AccessControl and adds validation of validRoles to the hasRole function, which means that even if super.hasRole(role, account) == true, if validRoles\[role] == false hasR
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M16MaltrepositoryRevokeroleMayNotWorkCorrectly"
solodit_id: "16094"
solodit_slug: |
    m-16-maltrepository_revokerole-may-not-work-correctly-code4rena-malt-protocol-malt-protocol-versus-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validRoles|hasRole|removeRole|onlyRole).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "validRoles"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
