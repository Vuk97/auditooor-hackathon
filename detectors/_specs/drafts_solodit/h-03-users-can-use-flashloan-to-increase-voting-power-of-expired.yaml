skeleton: "highlevelcall_missing_sibling"
name: "h-03-users-can-use-flashloan-to-increase-voting-power-of-expired"
class_name: "H03UsersCanUseFlashloanToIncreaseVotingPowerOfExpired"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #45551 (Shieldify/Guanciale Stake)"
help: |
    [H-03] Users Can Use `Flashloan` to Increase Voting Power of Expired Positions and Execute Proposal for Their Benefits
wiki_title: |
    [H-03] Users Can Use `Flashloan` to Increase Voting Power of Expired Positions and Execute Proposal for Their Benefits
wiki_description: |
    ## Severity
    
    High Risk
    
    ## Description
    
    If we assume the Medium-01 from the report is fixed in the `increaseAndStake()` function which allows users to add the amount to their stake without updating the lock duration then the below scenario might be executable:
    
    - Assume Alice's `lockUntil` reached t
wiki_exploit_scenario: |
    Per Solodit #45551: ## Severity
    
    High Risk
    
    ## Description
    
    If we assume the Medium-01 from the report is fixed in the `increaseAndStake()` function which allows users to add the amount to their stake without updating th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03UsersCanUseFlashloanToIncreaseVotingPowerOfExpired"
solodit_id: "45551"
solodit_slug: |
    h-03-users-can-use-flashloan-to-increase-voting-power-of-expired-positions-and-execute-proposal-for-their-benefits-shieldify-none-guanciale-stake-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: "Flash Loan, Vote"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(increaseAndStake|lockUntil|veGUAN|Flashloan).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
