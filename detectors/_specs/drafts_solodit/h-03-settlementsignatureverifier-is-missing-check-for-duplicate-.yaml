skeleton: "name_match_missing_call"
name: "h-03-settlementsignatureverifier-is-missing-check-for-duplicate-"
class_name: "H03SettlementsignatureverifierIsMissingCheckForDuplicate"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #49136 (Code4rena/Chakra)"
help: "[H-03] `SettlementSignatureVerifier` is missing check for duplicate validator signatures"
wiki_title: "[H-03] `SettlementSignatureVerifier` is missing check for duplicate validator signatures"
wiki_description: |
    ### Impact
    
    `SettlementSignatureVerifier::verifyECDSA` and `settlement::check_chakra_signatures` lack checks for duplicate validators and allow passing the threshold with a single valid signature.
    
    ### Proof of Concept
    
    From the `README`, we see that `validators` aren’t trusted roles, they simply h
wiki_exploit_scenario: |
    Per Solodit #49136: ### Impact
    
    `SettlementSignatureVerifier::verifyECDSA` and `settlement::check_chakra_signatures` lack checks for duplicate validators and allow passing the threshold with a single valid signature.
    
    #
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03SettlementsignatureverifierIsMissingCheckForDuplicate"
solodit_id: "49136"
solodit_slug: |
    h-03-settlementsignatureverifier-is-missing-check-for-duplicate-validator-signatures-code4rena-chakra-chakra-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: "Signature Malleability"
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(README|validators|MANAGER|signatures).*"
read_var_regex: ".*(amount|send_cross_chain_msg|signatures).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "README"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amount > 0;"
