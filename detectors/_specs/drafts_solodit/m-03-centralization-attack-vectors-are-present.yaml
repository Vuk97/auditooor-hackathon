skeleton: "name_match_missing_require"
name: "m-03-centralization-attack-vectors-are-present"
class_name: "M03CentralizationAttackVectorsArePresent"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20597 (Pashov Audit Group/Bloom)"
help: "[M-03] Centralization attack vectors are present"
wiki_title: "[M-03] Centralization attack vectors are present"
wiki_description: |
    **Impact:**
    High, as it can break the protocol for users
    
    **Likelihood:**
    Low, as it requires a malicious or a compromised owner
    
    **Description**
    
    The `owner` of `SwapFacility` can change the `pool` variable any time, meaning it can be set to `address(0)` for example, breaking the protocol's `swap`
wiki_exploit_scenario: |
    Per Solodit #20597: **Impact:**
    High, as it can break the protocol for users
    
    **Likelihood:**
    Low, as it requires a malicious or a compromised owner
    
    **Description**
    
    The `owner` of `SwapFacility` can change the `pool` v
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03CentralizationAttackVectorsArePresent"
solodit_id: "20597"
solodit_slug: "m-03-centralization-attack-vectors-are-present-pashov-none-bloom-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal owner;"
fn_name_regex: ".*(owner|SwapFacility|pool|swap).*"
write_var_regex: ".*(owner|pool|setPool).*"
guard_var_regex: ".*(owner|pool|setPool).*"
vuln_fn_name: "owner"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "owner = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
