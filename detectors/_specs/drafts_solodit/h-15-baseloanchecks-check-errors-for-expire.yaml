skeleton: "name_match_missing_call"
name: "h-15-baseloanchecks-check-errors-for-expire"
class_name: "H15BaseloanchecksCheckErrorsForExpire"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #35217 (Code4rena/Gondi)"
help: "[H-15] `_baseLoanChecks()` check errors for expire"
wiki_title: "[H-15] `_baseLoanChecks()` check errors for expire"
wiki_description: |
    `_baseLoanChecks()` is used to check whether Loan has expired:
    
    ```solidity
        function _baseLoanChecks(uint256 _loanId, Loan memory _loan) private view {
            if (_loan.hash() != _loans[_loanId]) {
                revert InvalidLoanError(_loanId);
            }
    @>      if (_loan.startTime + _loan.dura
wiki_exploit_scenario: |
    Per Solodit #35217: `_baseLoanChecks()` is used to check whether Loan has expired:
    
    ```solidity
        function _baseLoanChecks(uint256 _loanId, Loan memory _loan) private view {
            if (_loan.hash() != _loans[_loanId]
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H15BaseloanchecksCheckErrorsForExpire"
solodit_id: "35217"
solodit_slug: "h-15-_baseloanchecks-check-errors-for-expire-code4rena-gondi-gondi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal addnewtranche;"
fn_name_regex: ".*(liquidateLoan|loandId|LoanLiquidator|addNewTranche).*"
read_var_regex: ".*(addNewTranche|liquidateLoan|loanLiquidated).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "liquidateLoan"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return addnewtranche > 0;"
