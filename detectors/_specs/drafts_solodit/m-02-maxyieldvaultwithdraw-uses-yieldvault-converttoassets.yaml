skeleton: "name_match_missing_call"
name: "m-02-maxyieldvaultwithdraw-uses-yieldvault-converttoassets"
class_name: "M02MaxyieldvaultwithdrawUsesYieldvaultConverttoassets"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #30668 (Code4rena/PoolTogether)"
help: "[M-02] `_maxYieldVaultWithdraw()` uses `yieldVault.convertToAssets()`"
wiki_title: "[M-02] `_maxYieldVaultWithdraw()` uses `yieldVault.convertToAssets()`"
wiki_description: |
    ### Proof of Concept
    
    ```solidity
    function _maxYieldVaultWithdraw() internal view returns (uint256) {
        return yieldVault.convertToAssets(yieldVault.maxRedeem(address(this)));
    }
    ```
    
    The above code uses `yieldVault.convertToAssets()` which, per EIP-4626, is only approximate. Especially, it might
wiki_exploit_scenario: |
    Per Solodit #30668: ### Proof of Concept
    
    ```solidity
    function _maxYieldVaultWithdraw() internal view returns (uint256) {
        return yieldVault.convertToAssets(yieldVault.maxRedeem(address(this)));
    }
    ```
    
    The above code
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02MaxyieldvaultwithdrawUsesYieldvaultConverttoassets"
solodit_id: "30668"
solodit_slug: "m-02-_maxyieldvaultwithdraw-uses-yieldvaultconverttoassets-code4rena-pooltogether-pooltogether-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal liquidatablebalanceof;"
fn_name_regex: ".*(maxWithdraw|maxRedeem|liquidatableBalanceOf).*"
read_var_regex: ".*(liquidatableBalanceOf|maxRedeem|maxWithdraw).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "maxWithdraw"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return liquidatablebalanceof > 0;"
