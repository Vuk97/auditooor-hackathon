skeleton: "name_match_missing_call"
name: "h-01-positionmanager-s-moveliquidity-can-freeze-funds-by-removin"
class_name: "H01PositionmanagerSMoveliquidityCanFreezeFundsByRemovin"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #20069 (Code4rena/Ajna Protocol)"
help: |
    [H-01] PositionManager's `moveLiquidity` can freeze funds by removing destination index even when the move was partial
wiki_title: |
    [H-01] PositionManager's `moveLiquidity` can freeze funds by removing destination index even when the move was partial
wiki_description: |
    `positionIndex.remove(params_.fromIndex)`removes the PositionManager entry even when it is only partial removal as a result of `IPool(params_.pool).moveQuoteToken(...)` call.
    
    I.e. it is correct to do `fromPosition.lps -= vars.lpbAmountFrom`, but the resulting amount might not be zero, moveQuoteTok
wiki_exploit_scenario: |
    Per Solodit #20069: `positionIndex.remove(params_.fromIndex)`removes the PositionManager entry even when it is only partial removal as a result of `IPool(params_.pool).moveQuoteToken(...)` call.
    
    I.e. it is correct to d
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01PositionmanagerSMoveliquidityCanFreezeFundsByRemovin"
solodit_id: "20069"
solodit_slug: |
    h-01-positionmanagers-moveliquidity-can-freeze-funds-by-removing-destination-index-even-when-the-move-was-partial-code4rena-ajna-protocol-ajna-protocol-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(moveLiquidity).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "moveLiquidity"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
