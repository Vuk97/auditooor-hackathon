skeleton: "name_match_missing_call"
name: "m-03-two-different-transactions-can-result-in-the-same-txnhash-v"
class_name: "M03TwoDifferentTransactionsCanResultInTheSameTxnhashV"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #27015 (Code4rena/Ondo Finance)"
help: |
    [M-03] Two different transactions can result in the same `txnHash` value, thus breaking the approval process of transaction minting
wiki_title: |
    [M-03] Two different transactions can result in the same `txnHash` value, thus breaking the approval process of transaction minting
wiki_description: |
    The `DestinationBridge._execute` is an internal function that is executed when contract is called by Axelar Gateway. The `_execute` function stores the `Transaction` struct in the `txnHashToTransaction` mapping as shown below:
    
        bytes32 txnHash = keccak256(payload);
        txnHashToTransaction[txnH
wiki_exploit_scenario: |
    Per Solodit #27015: The `DestinationBridge._execute` is an internal function that is executed when contract is called by Axelar Gateway. The `_execute` function stores the `Transaction` struct in the `txnHashToTransacti
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03TwoDifferentTransactionsCanResultInTheSameTxnhashV"
solodit_id: "27015"
solodit_slug: |
    m-03-two-different-transactions-can-result-in-the-same-txnhash-value-thus-breaking-the-approval-process-of-transaction-minting-code4rena-ondo-finance-ondo-finance-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal amt;"
fn_name_regex: ".*(Transaction|txnHashToTransaction|txnHash|keccak256).*"
read_var_regex: ".*(amt|keccak256|nonce).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "Transaction"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amt > 0;"
