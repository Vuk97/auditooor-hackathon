skeleton: "name_match_missing_call"
name: "m-03-lack-of-input-validation-in-replacepool-allows-curated-pool"
class_name: "M03LackOfInputValidationInReplacepoolAllowsCuratedPool"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42163 (Code4rena/Vader Protocol)"
help: "[M-03] Lack of input validation in `replacePool()` allows curated pool limit bypass in `Router.sol`"
wiki_title: "[M-03] Lack of input validation in `replacePool()` allows curated pool limit bypass in `Router.sol`"
wiki_description: |
    There is no input validation in `replacePool()` function to check if `oldToken` exists and is curated. Using a non-existing `oldToken` (even 0 address) passes the check on L236 (because `Pools.getBaseAmount()` will return 0 for the non-existing token) and `newToken` will be made curated. This can b
wiki_exploit_scenario: |
    Per Solodit #42163: There is no input validation in `replacePool()` function to check if `oldToken` exists and is curated. Using a non-existing `oldToken` (even 0 address) passes the check on L236 (because `Pools.getBas
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03LackOfInputValidationInReplacepoolAllowsCuratedPool"
solodit_id: "42163"
solodit_slug: |
    m-03-lack-of-input-validation-in-replacepool-allows-curated-pool-limit-bypass-in-routersol-code4rena-vader-protocol-vader-protocol-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal curatedpoollimit;"
fn_name_regex: ".*(replacePool|oldToken|newToken|curatedPoolLimit).*"
read_var_regex: ".*(curatedPoolLimit|newToken|oldToken).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "replacePool"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return curatedpoollimit > 0;"
