skeleton: "name_match_missing_require"
name: "unsafe-contract-initialization"
class_name: "UnsafeContractInitialization"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #48249 (OtterSec/Ax Protocol)"
help: "Unsafe Contract Initialization"
wiki_title: "Unsafe Contract Initialization"
wiki_description: |
    ## Initialization Vulnerabilities in Smart Contracts
    
    The `initialize` functions in `WormholeBridge.sol`, `LayerZeroBridge.sol`, and `USX.sol` do not perform sufficient validation of the arguments provided. This could result in a denial of service attack if the contract initialization can be front-r
wiki_exploit_scenario: |
    Per Solodit #48249: ## Initialization Vulnerabilities in Smart Contracts
    
    The `initialize` functions in `WormholeBridge.sol`, `LayerZeroBridge.sol`, and `USX.sol` do not perform sufficient validation of the arguments pro
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnsafeContractInitialization"
solodit_id: "48249"
solodit_slug: "unsafe-contract-initialization-ottersec-none-ax-protocol-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal initialize;"
fn_name_regex: ".*(initialize).*"
write_var_regex: ".*(initialize).*"
guard_var_regex: ".*(initialize).*"
vuln_fn_name: "initialize"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "initialize = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
