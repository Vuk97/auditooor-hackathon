skeleton: "name_match_missing_call"
name: "m-09-auth-only-works-well-with-external-functions"
class_name: "M09AuthOnlyWorksWellWithExternalFunctions"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #4088 (Code4rena/Yield)"
help: "[M-09] auth only works well with external functions"
wiki_title: "[M-09] auth only works well with external functions"
wiki_description: |
    <!-- appended to end of section -->
    
    The auth modifier of `AccessControl.sol` doesn't work as you would expect.
    It checks if you are authorized for `msg.sig`. However, `msg.sig` is the signature of the first function you have called (not the current function). So, if you call function A, which calls
wiki_exploit_scenario: |
    Per Solodit #4088: <!-- appended to end of section -->
    
    The auth modifier of `AccessControl.sol` doesn't work as you would expect.
    It checks if you are authorized for `msg.sig`. However, `msg.sig` is the signature of th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M09AuthOnlyWorksWellWithExternalFunctions"
solodit_id: "4088"
solodit_slug: "m-09-auth-only-works-well-with-external-functions-code4rena-yield-yield-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal auth;"
fn_name_regex: ".*(public|auth|external).*"
read_var_regex: ".*(auth|external|public).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "public"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return auth > 0;"
