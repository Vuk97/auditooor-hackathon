skeleton: "paired_function_divergence"
name: "trst-m-1-removing-a-trade-path-in-router-will-cause-serious-data"
class_name: "TrstM1RemovingATradePathInRouterWillCauseSeriousData"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #19028 (Trust Security/Orbital Finance)"
help: "TRST-M-1 Removing a trade path in router will cause serious data corruption"
wiki_title: "TRST-M-1 Removing a trade path in router will cause serious data corruption"
wiki_description: |
    **Description:**
    The RouterInfo represents a single UniV3-compatible router which supports a list of token 
    paths. It uses the following data structures:
    ```solidity
             mapping(address => mapping(address => listInfo)) private allowedPairsMap;
                      pair[] private allowedPairsList;
wiki_exploit_scenario: |
    Per Solodit #19028: **Description:**
    The RouterInfo represents a single UniV3-compatible router which supports a list of token 
    paths. It uses the following data structures:
    ```solidity
             mapping(address => mappin
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TrstM1RemovingATradePathInRouterWillCauseSeriousData"
solodit_id: "19028"
solodit_slug: |
    trst-m-1-removing-a-trade-path-in-router-will-cause-serious-data-corruption-trust-security-none-orbital-finance-markdown_
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(balance|amount|total|supply|reserve).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
