skeleton: "name_match_missing_call"
name: "h-01-conviction-scoring-fails-to-initialize-and-bootstrap"
class_name: "H01ConvictionScoringFailsToInitializeAndBootstrap"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #4044 (Code4rena/FairSide)"
help: "[H-01] Conviction scoring fails to initialize and bootstrap"
wiki_title: "[H-01] Conviction scoring fails to initialize and bootstrap"
wiki_description: |
    Conviction scores for new addresses/users fail to initialize+bootstrap in `ERC20ConvictionScore`’s `_updateConvictionScore()` because a new user’s `numCheckpoints` will be zero and never gets initialized.
    
    This effectively means that FairSide conviction scoring fails to bootstrap at all, leading to
wiki_exploit_scenario: |
    Per Solodit #4044: Conviction scores for new addresses/users fail to initialize+bootstrap in `ERC20ConvictionScore`’s `_updateConvictionScore()` because a new user’s `numCheckpoints` will be zero and never gets initial
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01ConvictionScoringFailsToInitializeAndBootstrap"
solodit_id: "4044"
solodit_slug: "h-01-conviction-scoring-fails-to-initialize-and-bootstrap-code4rena-fairside-fairside-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal numcheckpoints;"
fn_name_regex: ".*(ERC20ConvictionScore|numCheckpoints).*"
read_var_regex: ".*(numCheckpoints).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "ERC20ConvictionScore"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return numcheckpoints > 0;"
