skeleton: "highlevelcall_missing_sibling"
name: "m-04-flashproof-is-not-flash-proof"
class_name: "M04FlashproofIsNotFlashProof"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42164 (Code4rena/Vader Protocol)"
help: "[M-04] `flashProof` is not flash-proof"
wiki_title: "[M-04] `flashProof` is not flash-proof"
wiki_description: |
    The `flashProof` modifier is supposed to prevent flash-loan attacks by disallowing performing several sensitive functions in the same block.
    
    However, it performs this check on `tx.origin` and not on an individual user address basis. This only prevents flash loan attacks from happening within a sin
wiki_exploit_scenario: |
    Per Solodit #42164: The `flashProof` modifier is supposed to prevent flash-loan attacks by disallowing performing several sensitive functions in the same block.
    
    However, it performs this check on `tx.origin` and not on
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04FlashproofIsNotFlashProof"
solodit_id: "42164"
solodit_slug: "m-04-flashproof-is-not-flash-proof-code4rena-vader-protocol-vader-protocol-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(flashProof).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
