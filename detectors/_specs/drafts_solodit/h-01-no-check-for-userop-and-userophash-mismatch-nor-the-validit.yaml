skeleton: "name_match_missing_require"
name: "h-01-no-check-for-userop-and-userophash-mismatch-nor-the-validit"
class_name: "H01NoCheckForUseropAndUserophashMismatchNorTheValidit"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #61411 (Shieldify/Etherspot Credibleaccountmodule)"
help: "[H-01] No Check for `userOp()` and `userOpHash()` Mismatch Nor the Validity of the Sender"
wiki_title: "[H-01] No Check for `userOp()` and `userOpHash()` Mismatch Nor the Validity of the Sender"
wiki_description: |
    ## Severity
    
    High Risk
    
    ## Description
    
    The `CredibleAccountModule` does not hash the `userOp` and uses the hash to know the wallet that authorized the action. It uses `userOpHash` to derive the address that signed for that hash, and uses the data `userOp` to modify it.
    
    File: [src/modules/validato
wiki_exploit_scenario: |
    Per Solodit #61411: ## Severity
    
    High Risk
    
    ## Description
    
    The `CredibleAccountModule` does not hash the `userOp` and uses the hash to know the wallet that authorized the action. It uses `userOpHash` to derive the addr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01NoCheckForUseropAndUserophashMismatchNorTheValidit"
solodit_id: "61411"
solodit_slug: |
    h-01-no-check-for-userop-and-userophash-mismatch-nor-the-validity-of-the-sender-shieldify-none-etherspot-credibleaccountmodule-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal sessionkeysigner;"
fn_name_regex: ".*(CredibleAccountModule|userOp|userOpHash|sessionKeySigner).*"
write_var_regex: ".*(sessionKeySigner|userOp|userOpHash).*"
guard_var_regex: ".*(sessionKeySigner|userOp|userOpHash).*"
vuln_fn_name: "CredibleAccountModule"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "sessionkeysigner = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
