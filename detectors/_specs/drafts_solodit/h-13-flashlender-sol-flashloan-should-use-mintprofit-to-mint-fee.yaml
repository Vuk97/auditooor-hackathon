skeleton: "highlevelcall_missing_sibling"
name: "h-13-flashlender-sol-flashloan-should-use-mintprofit-to-mint-fee"
class_name: "H13FlashlenderSolFlashloanShouldUseMintprofitToMintFee"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #49035 (Code4rena/LoopFi)"
help: |
    [H-13] `Flashlender.sol#flashLoan()` should use `mintProfit()` to mint fees, as the current implementation may lead to locked up WETH in PoolV3
wiki_title: |
    [H-13] `Flashlender.sol#flashLoan()` should use `mintProfit()` to mint fees, as the current implementation may lead to locked up WETH in PoolV3
wiki_description: |
    When using flashlender to create loans, there is small amount of fees. The fees are sent to PoolV3 and is spread out to the lpETH stakers.
    
    The issue is, the current implementation uses `pool.repayCreditAccount()` to add the profit, by setting the `profit == fees`. This is incorrect, and the correc
wiki_exploit_scenario: |
    Per Solodit #49035: When using flashlender to create loans, there is small amount of fees. The fees are sent to PoolV3 and is spread out to the lpETH stakers.
    
    The issue is, the current implementation uses `pool.repayCr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H13FlashlenderSolFlashloanShouldUseMintprofitToMintFee"
solodit_id: "49035"
solodit_slug: |
    h-13-flashlendersolflashloan-should-use-mintprofit-to-mint-fees-as-the-current-implementation-may-lead-to-locked-up-weth-in-poolv3-code4rena-loopfi-loopfi-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(expectedLiquidity|profit|mintProfit).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
