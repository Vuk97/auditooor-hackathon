skeleton: "highlevelcall_missing_sibling"
name: "m-08-priceaware-uses-prices-from-getamountsout"
class_name: "M08PriceawareUsesPricesFromGetamountsout"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #3873 (Code4rena/Marginswap)"
help: "[M-08] PriceAware uses prices from getAmountsOut"
wiki_title: "[M-08] PriceAware uses prices from getAmountsOut"
wiki_description: |
    `getPriceFromAMM` relies on values returned from getAmountsOut which can be manipulated (e.g. with the large capital or the help of flash loans). The impact is reduced with UPDATE_MIN_PEG_AMOUNT and UPDATE_MAX_PEG_AMOUNT, however, it is not entirely eliminated.
    
    Uniswap v2 recommends using their TW
wiki_exploit_scenario: |
    Per Solodit #3873: `getPriceFromAMM` relies on values returned from getAmountsOut which can be manipulated (e.g. with the large capital or the help of flash loans). The impact is reduced with UPDATE_MIN_PEG_AMOUNT and
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08PriceawareUsesPricesFromGetamountsout"
solodit_id: "3873"
solodit_slug: "m-08-priceaware-uses-prices-from-getamountsout-code4rena-marginswap-marginswap-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(getPriceFromAMM).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
