skeleton: "name_match_missing_call"
name: "tokens-that-were-locked-when-lockuptime-0-will-be-impossible-to-"
class_name: "TokensThatWereLockedWhenLockuptime0WillBeImpossibleTo"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #61193 (Cyfrin/Remora Pledge)"
help: |
    Tokens that were locked when `lockUpTime > 0` will be impossible to unlock if `lockUpTime` is set to zero
wiki_title: |
    Tokens that were locked when `lockUpTime > 0` will be impossible to unlock if `lockUpTime` is set to zero
wiki_description: |
    **Description:** `LockUpManager::_unlockTokens` returns if `lockUpTime == 0`:
    ```solidity
    function _unlockTokens(
        address holder,
        uint256 amount,
        bool disregardTime
    ) internal {
        LockUpStorage storage $ = _getLockUpStorage();
        uint32 lockUpTime = $._lockUpTime;
        // @audit return
wiki_exploit_scenario: |
    Per Solodit #61193: **Description:** `LockUpManager::_unlockTokens` returns if `lockUpTime == 0`:
    ```solidity
    function _unlockTokens(
        address holder,
        uint256 amount,
        bool disregardTime
    ) internal {
        LockUp
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TokensThatWereLockedWhenLockuptime0WillBeImpossibleTo"
solodit_id: "61193"
solodit_slug: |
    tokens-that-were-locked-when-lockuptime-0-will-be-impossible-to-unlock-if-lockuptime-is-set-to-zero-cyfrin-none-remora-pledge-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal for;"
fn_name_regex: ".*(lockUpTime|for).*"
read_var_regex: ".*(for|lockUpTime).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "lockUpTime"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return for > 0;"
