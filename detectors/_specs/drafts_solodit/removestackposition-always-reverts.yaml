skeleton: "name_match_missing_require"
name: "removestackposition-always-reverts"
class_name: "RemovestackpositionAlwaysReverts"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #7293 (Spearbit/Astaria)"
help: "_removeStackPosition() always reverts"
wiki_title: "_removeStackPosition() always reverts"
wiki_description: |
    ## Security Report
    
    ## Severity
    **High Risk**
    
    ## Context
    `LienToken.sol#L823-L828`
    
    ## Description
    The function `removeStackPosition()` always reverts since it calls the stack array for an index beyond its length:
    
    ```solidity
    for (i; i < length; ) {
    unchecked {
    newStack[i] = stack[i + 1];
    ++i;
    }
    }
wiki_exploit_scenario: |
    Per Solodit #7293: ## Security Report
    
    ## Severity
    **High Risk**
    
    ## Context
    `LienToken.sol#L823-L828`
    
    ## Description
    The function `removeStackPosition()` always reverts since it calls the stack array for an index beyo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RemovestackpositionAlwaysReverts"
solodit_id: "7293"
solodit_slug: "removestackposition-always-reverts-spearbit-astaria-pdf"
solodit_quality: "5"
solodit_rarity: "3"
solodit_tags: "Broken Loop"
state_decl: "uint256 internal indexposition;"
fn_name_regex: ".*(removeStackPosition|indexPosition|makePayment).*"
write_var_regex: ".*(indexPosition|makePayment|removeStackPosition).*"
guard_var_regex: ".*(indexPosition|makePayment|removeStackPosition).*"
vuln_fn_name: "removeStackPosition"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "indexposition = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
