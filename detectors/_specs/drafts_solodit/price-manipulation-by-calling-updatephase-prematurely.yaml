skeleton: "highlevelcall_missing_sibling"
name: "price-manipulation-by-calling-updatephase-prematurely"
class_name: "PriceManipulationByCallingUpdatephasePrematurely"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #29443 (MixBytes/Divergence Protocol)"
help: "Price manipulation by calling `updatePhase` prematurely"
wiki_title: "Price manipulation by calling `updatePhase` prematurely"
wiki_description: |
    ##### Description
    When `updatePhase` is called before the current phase has ended, it records an incorrect `endRoundId` value. Additionally, if `updatePhase` is not called after the phase has ended, the price calculation will be compromised in a manner similar to issue High.3.
    
    This issue is even le
wiki_exploit_scenario: |
    Per Solodit #29443: ##### Description
    When `updatePhase` is called before the current phase has ended, it records an incorrect `endRoundId` value. Additionally, if `updatePhase` is not called after the phase has ended, t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PriceManipulationByCallingUpdatephasePrematurely"
solodit_id: "29443"
solodit_slug: "price-manipulation-by-calling-updatephase-prematurely-mixbytes-none-divergence-protocol-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(updatePhase|endRoundId).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
