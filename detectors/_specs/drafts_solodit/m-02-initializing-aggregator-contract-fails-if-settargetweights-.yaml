skeleton: "name_match_missing_call"
name: "m-02-initializing-aggregator-contract-fails-if-settargetweights-"
class_name: "M02InitializingAggregatorContractFailsIfSettargetweights"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #43998 (Shieldify/Builda)"
help: |
    [M-02] Initializing `Aggregator` Contract Fails If `setTargetWeights()` Function Is Not Called Manually Beforehand
wiki_title: |
    [M-02] Initializing `Aggregator` Contract Fails If `setTargetWeights()` Function Is Not Called Manually Beforehand
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    The `initialize()` function inside the `Aggregator` is called following a specific mechanism to prevent the first deposit issue, this mechanism will deposit the amount of eth to the LRT protocol and divide it among the supported LRT protocols depending on th
wiki_exploit_scenario: |
    Per Solodit #43998: ## Severity
    
    Medium Risk
    
    ## Description
    
    The `initialize()` function inside the `Aggregator` is called following a specific mechanism to prevent the first deposit issue, this mechanism will deposit t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02InitializingAggregatorContractFailsIfSettargetweights"
solodit_id: "43998"
solodit_slug: |
    m-02-initializing-aggregator-contract-fails-if-settargetweights-function-is-not-called-manually-beforehand-shieldify-none-builda-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal initialize;"
fn_name_regex: ".*(initialize|Aggregator|targetWeight|setTargetWeights).*"
read_var_regex: ".*(initialize|setTargetWeights|targetWeight).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "initialize"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return initialize > 0;"
