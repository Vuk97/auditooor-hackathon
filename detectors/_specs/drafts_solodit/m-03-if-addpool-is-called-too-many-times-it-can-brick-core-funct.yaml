skeleton: "name_match_missing_require"
name: "m-03-if-addpool-is-called-too-many-times-it-can-brick-core-funct"
class_name: "M03IfAddpoolIsCalledTooManyTimesItCanBrickCoreFunct"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20386 (Pashov Audit Group/Gmd)"
help: "[M-03] If `addPool` is called too many times it can brick core functionality"
wiki_title: "[M-03] If `addPool` is called too many times it can brick core functionality"
wiki_description: |
    **Likelihood:**
    Low, because it requires a malicious admin or a big admin error
    
    **Impact:**
    High, because it bricks core protocol functionality
    
    **Description**
    
    The `addPool` method pushes an entry to the `poolInfo` array. Methods like `swapGLPout` and `recoverTreasuryTokensFromGLP` have internal
wiki_exploit_scenario: |
    Per Solodit #20386: **Likelihood:**
    Low, because it requires a malicious admin or a big admin error
    
    **Impact:**
    High, because it bricks core protocol functionality
    
    **Description**
    
    The `addPool` method pushes an entry
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03IfAddpoolIsCalledTooManyTimesItCanBrickCoreFunct"
solodit_id: "20386"
solodit_slug: "m-03-if-addpool-is-called-too-many-times-it-can-brick-core-functionality-pashov-none-gmd-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal addpool;"
fn_name_regex: ".*(addPool|poolInfo|swapGLPout|recoverTreasuryTokensFromGLP).*"
write_var_regex: ".*(addPool|poolInfo|recoverTreasuryTokensFromGLP).*"
guard_var_regex: ".*(addPool|poolInfo|recoverTreasuryTokensFromGLP).*"
vuln_fn_name: "addPool"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "addpool = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
