skeleton: "name_match_missing_call"
name: "m-01-the-requestquery-and-verify-functions-in-chainlinklightclie"
class_name: "M01TheRequestqueryAndVerifyFunctionsInChainlinklightclie"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #44045 (Shieldify/Futaba)"
help: |
    [M-01] The `requestQuery()` and `verify()` Functions in `ChainlinkLightClient.sol` Contract Lacks Proper Access Control
wiki_title: |
    [M-01] The `requestQuery()` and `verify()` Functions in `ChainlinkLightClient.sol` Contract Lacks Proper Access Control
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    According to the documentation and protocol's logic, `requestQuery()` and `verify()` functions are supposed to be callable only from the `Gateway.sol` contract. The lack of access controls for these extremely sensitive features could be a serious problem as
wiki_exploit_scenario: |
    Per Solodit #44045: ## Severity
    
    Medium Risk
    
    ## Description
    
    According to the documentation and protocol's logic, `requestQuery()` and `verify()` functions are supposed to be callable only from the `Gateway.sol` contrac
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01TheRequestqueryAndVerifyFunctionsInChainlinklightclie"
solodit_id: "44045"
solodit_slug: |
    m-01-the-requestquery-and-verify-functions-in-chainlinklightclientsol-contract-lacks-proper-access-control-shieldify-none-futaba-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal requestquery;"
fn_name_regex: ".*(requestQuery|verify).*"
read_var_regex: ".*(requestQuery|verify).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "requestQuery"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return requestquery > 0;"
