skeleton: "highlevelcall_missing_sibling"
name: "m-16-privatepool-flashloan-takes-fee-from-the-wrong-address"
class_name: "M16PrivatepoolFlashloanTakesFeeFromTheWrongAddress"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #43395 (Code4rena/Caviar)"
help: "[M-16] `PrivatePool.flashLoan()` takes fee from the wrong address"
wiki_title: "[M-16] `PrivatePool.flashLoan()` takes fee from the wrong address"
wiki_description: |
    Instead of taking the fee from the receiver of the flashloan callback, it pulls it from `msg.sender`.
    
    As specified in [EIP-3156](https://eips.ethereum.org/EIPS/eip-3156#lender-specification):
    
    > "After the callback, the flashLoan function MUST take the amount + fee token from the receiver, or reve
wiki_exploit_scenario: |
    Per Solodit #43395: Instead of taking the fee from the receiver of the flashloan callback, it pulls it from `msg.sender`.
    
    As specified in [EIP-3156](https://eips.ethereum.org/EIPS/eip-3156#lender-specification):
    
    > "Af
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M16PrivatepoolFlashloanTakesFeeFromTheWrongAddress"
solodit_id: "43395"
solodit_slug: "m-16-privatepoolflashloan-takes-fee-from-the-wrong-address-code4rena-caviar-caviar-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(baseToken|flashLoan|receiver).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
