skeleton: "paired_function_divergence"
name: "m-08-weightedindex-creation-could-always-fail-if-pool-is-already"
class_name: "M08WeightedindexCreationCouldAlwaysFailIfPoolIsAlready"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #46002 (Pashov Audit Group/Peapods_2024-11-16)"
help: "[M-08] `WeightedIndex` creation could always fail if pool is already created"
wiki_title: "[M-08] `WeightedIndex` creation could always fail if pool is already created"
wiki_description: |
    ## Severity
    
    **Impact:** HIgh
    
    **Likelihood:** Low
    
    ## Description
    
    When `WeightedIndex` is first created, it will attempt to create a V2 pool with the pod and index tokens.
    
    ```solidity
        constructor(
            string memory _name,
            string memory _symbol,
            Config memory _config,
wiki_exploit_scenario: |
    Per Solodit #46002: ## Severity
    
    **Impact:** HIgh
    
    **Likelihood:** Low
    
    ## Description
    
    When `WeightedIndex` is first created, it will attempt to create a V2 pool with the pod and index tokens.
    
    ```solidity
        construct
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08WeightedindexCreationCouldAlwaysFailIfPoolIsAlready"
solodit_id: "46002"
solodit_slug: |
    m-08-weightedindex-creation-could-always-fail-if-pool-is-already-created-pashov-audit-group-none-peapods_2024-11-16-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(createV2Pool).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
