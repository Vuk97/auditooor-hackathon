skeleton: "name_match_missing_call"
name: "m-01-bounty-manager-can-set-maxrewardpervote-to-1-after-votes-ar"
class_name: "M01BountyManagerCanSetMaxrewardpervoteTo1AfterVotesAr"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #18779 (ZachObront/Stakedao)"
help: "[M-01] Bounty manager can set `maxRewardPerVote` to 1 after votes are locked, stealing free votes"
wiki_title: "[M-01] Bounty manager can set `maxRewardPerVote` to 1 after votes are locked, stealing free votes"
wiki_description: |
    When the amount to pay for a `claim` is calculated, we use the minimum value of `bias * rewardPerVote` and `bias * maxRewardPerVote`.
    
    ```solidity
    // Compute the reward amount based on
    // Reward / Total Votes.
    amount = _bias.mulWad(rewardPerVote[_bountyId]);
    
    // Compute the reward amount based on
    //
wiki_exploit_scenario: |
    Per Solodit #18779: When the amount to pay for a `claim` is calculated, we use the minimum value of `bias * rewardPerVote` and `bias * maxRewardPerVote`.
    
    ```solidity
    // Compute the reward amount based on
    // Reward / Tot
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01BountyManagerCanSetMaxrewardpervoteTo1AfterVotesAr"
solodit_id: "18779"
solodit_slug: |
    m-01-bounty-manager-can-set-maxrewardpervote-to-1-after-votes-are-locked-stealing-free-votes-zachobront-none-stakedao-markdown_
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal claim;"
fn_name_regex: ".*(claim|maxRewardPerVote|increaseBountyDuration|closeBounty).*"
read_var_regex: ".*(claim|closeBounty|increaseBountyDuration).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "claim"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return claim > 0;"
