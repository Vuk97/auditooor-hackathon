skeleton: "name_match_missing_call"
name: "m-05-deposits-will-fail-when-pool-s-price-reaches-the-min-max-ti"
class_name: "M05DepositsWillFailWhenPoolSPriceReachesTheMinMaxTi"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #45536 (Pashov Audit Group/GammaSwap_2024-12-30)"
help: "[M-05] Deposits will fail when pool's price reaches the min/max tick"
wiki_title: "[M-05] Deposits will fail when pool's price reaches the min/max tick"
wiki_description: |
    ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    During the deposit, code calls `calculateTokenRatio()` to calculate the `ratio0` and `ratio1`. If the pool's prices reaches the upper or lower price then `ratio0` or `ratio1` would be 0:
    
    ```solidity
            } else if (price <=
wiki_exploit_scenario: |
    Per Solodit #45536: ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    During the deposit, code calls `calculateTokenRatio()` to calculate the `ratio0` and `ratio1`. If the pool's prices reaches the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M05DepositsWillFailWhenPoolSPriceReachesTheMinMaxTi"
solodit_id: "45536"
solodit_slug: |
    m-05-deposits-will-fail-when-pools-price-reaches-the-minmax-tick-pashov-audit-group-none-gammaswap_2024-12-30-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal calculatemaxdeposit;"
fn_name_regex: ".*(calculateTokenRatio|ratio0|ratio1|calculateMaxDeposit).*"
read_var_regex: ".*(calculateMaxDeposit|calculateTokenRatio|ratio0).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "calculateTokenRatio"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return calculatemaxdeposit > 0;"
