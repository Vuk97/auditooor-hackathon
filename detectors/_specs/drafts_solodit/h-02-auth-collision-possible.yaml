skeleton: "name_match_missing_call"
name: "h-02-auth-collision-possible"
class_name: "H02AuthCollisionPossible"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #4078 (Code4rena/Yield)"
help: "[H-02] auth collision possible"
wiki_title: "[H-02] auth collision possible"
wiki_description: |
    The auth mechanism of `AccessControl.sol` uses function selectors `(msg.sig)` as a `(unique)` role definition. Also the `_moduleCall` allows the code to be extended.
    
    Suppose an attacker wants to add the innocent-looking function "`left_branch_block(uint32)` "in a new module. Suppose this module is
wiki_exploit_scenario: |
    Per Solodit #4078: The auth mechanism of `AccessControl.sol` uses function selectors `(msg.sig)` as a `(unique)` role definition. Also the `_moduleCall` allows the code to be extended.
    
    Suppose an attacker wants to add
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02AuthCollisionPossible"
solodit_id: "4078"
solodit_slug: "h-02-auth-collision-possible-code4rena-yield-yield-contest-git"
solodit_quality: "5"
solodit_rarity: "5"
solodit_tags: ""
state_decl: "uint256 internal auth;"
fn_name_regex: ".*(left_branch_block|auth).*"
read_var_regex: ".*(auth|left_branch_block).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "left_branch_block"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return auth > 0;"
