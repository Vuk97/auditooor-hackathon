skeleton: "state_write_without_paired_write"
name: "h-11-missing-updatesupply-call-in-updateweights"
class_name: "H11MissingUpdatesupplyCallInUpdateweights"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #36351 (Pashov Audit Group/Lucidly)"
help: "[H-11] Missing `_updateSupply` Call in `updateWeights`"
wiki_title: "[H-11] Missing `_updateSupply` Call in `updateWeights`"
wiki_description: |
    **Severity**
    
    **Impact:** Medium
    
    **Likelihood:** High
    
    **Description**
    
    The `updateWeights` function updates the virtual balance product (`vb_prod`) but does not include a call to `_updateSupply`. This inconsistency can result in the use of a stale `supply`, affecting the accuracy of swaps and liqu
wiki_exploit_scenario: |
    Per Solodit #36351: **Severity**
    
    **Impact:** Medium
    
    **Likelihood:** High
    
    **Description**
    
    The `updateWeights` function updates the virtual balance product (`vb_prod`) but does not include a call to `_updateSupply`. Th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H11MissingUpdatesupplyCallInUpdateweights"
solodit_id: "36351"
solodit_slug: "h-11-missing-_updatesupply-call-in-updateweights-pashov-audit-group-none-lucidly-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(updateWeights|vb_prod|supply).*"
primary_var_regex: ".*(supply|updateWeights|vb_prod).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
