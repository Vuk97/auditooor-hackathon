skeleton: "name_match_missing_call"
name: "m-07-createerc20dao-will-always-revert-when-depositchainids-has-"
class_name: "M07Createerc20daoWillAlwaysRevertWhenDepositchainidsHas"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #44408 (Pashov Audit Group/apDAO_2024-10-03)"
help: "[M-07] `createERC20DAO()` will always revert when `_depositChainIds` has a length bigger than 1"
wiki_title: "[M-07] `createERC20DAO()` will always revert when `_depositChainIds` has a length bigger than 1"
wiki_description: |
    ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    Whenever we call `createERC20DAO()`, we have the following **if** check:
    
    ```solidity
    if (_depositChainIds.length > 1)
    ```
    
    If we end up in there, we will always revert. This is due to this line:
    
    ```solidity
    IEmitter(emitterAd
wiki_exploit_scenario: |
    Per Solodit #44408: ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    Whenever we call `createERC20DAO()`, we have the following **if** check:
    
    ```solidity
    if (_depositChainIds.length > 1)
    ```
    
    If
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M07Createerc20daoWillAlwaysRevertWhenDepositchainidsHas"
solodit_id: "44408"
solodit_slug: |
    m-07-createerc20dao-will-always-revert-when-_depositchainids-has-a-length-bigger-than-1-pashov-audit-group-none-apdao_2024-10-03-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal createccdao;"
fn_name_regex: ".*(createERC20DAO|createCCDao|Emitter|IEmitter).*"
read_var_regex: ".*(createCCDao|createERC20DAO).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "createERC20DAO"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return createccdao > 0;"
