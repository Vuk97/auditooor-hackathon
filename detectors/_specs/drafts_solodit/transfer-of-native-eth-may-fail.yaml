skeleton: "name_match_missing_call"
name: "transfer-of-native-eth-may-fail"
class_name: "TransferOfNativeEthMayFail"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #35727 (Zokyo/Zap)"
help: "Transfer of native ETH may fail."
wiki_title: "Transfer of native ETH may fail."
wiki_description: |
    **Severity**: Medium
    
    **Status**: Resolved
    
    **Description**
    
    The `retrieveNative()` function in the `RetrieveAsset.sol` contract uses the native `transfer()` function to transfer ETH. `transfer()` only provides 2300 gas for its operation. This means the following cases can cause the transfer to fail
wiki_exploit_scenario: |
    Per Solodit #35727: **Severity**: Medium
    
    **Status**: Resolved
    
    **Description**
    
    The `retrieveNative()` function in the `RetrieveAsset.sol` contract uses the native `transfer()` function to transfer ETH. `transfer()` onl
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TransferOfNativeEthMayFail"
solodit_id: "35727"
solodit_slug: "transfer-of-native-eth-may-fail-zokyo-none-zap-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal retrievenative;"
fn_name_regex: ".*(retrieveNative|transfer).*"
read_var_regex: ".*(retrieveNative|transfer).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "retrieveNative"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return retrievenative > 0;"
