skeleton: "highlevelcall_missing_sibling"
name: "h-14-mergetranches-refinancepartial-lack-of-nonreentrant"
class_name: "H14MergetranchesRefinancepartialLackOfNonreentrant"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #35216 (Code4rena/Gondi)"
help: "[H-14] `mergeTranches()`/`refinancePartial()` lack of `nonReentrant`"
wiki_title: "[H-14] `mergeTranches()`/`refinancePartial()` lack of `nonReentrant`"
wiki_description: |
    In `mergeTranches()`, the method's code implementation is as follows:
    
    ```solidity
     function mergeTranches(uint256 _loanId, Loan memory _loan, uint256 _minTranche, uint256 _maxTranche)
            external
            returns (uint256, Loan memory)
        {
            _baseLoanChecks(_loanId, _loan);
            uin
wiki_exploit_scenario: |
    Per Solodit #35216: In `mergeTranches()`, the method's code implementation is as follows:
    
    ```solidity
     function mergeTranches(uint256 _loanId, Loan memory _loan, uint256 _minTranche, uint256 _maxTranche)
            extern
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H14MergetranchesRefinancepartialLackOfNonreentrant"
solodit_id: "35216"
solodit_slug: "h-14-mergetranchesrefinancepartial-lack-of-nonreentrant-code4rena-gondi-gondi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(mergeTranches|refinanceFromLoanExecutionData|nonReentrant|repayLoan).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
