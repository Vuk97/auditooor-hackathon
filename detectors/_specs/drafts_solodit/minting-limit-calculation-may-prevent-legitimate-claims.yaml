skeleton: "name_match_missing_require"
name: "minting-limit-calculation-may-prevent-legitimate-claims"
class_name: "MintingLimitCalculationMayPreventLegitimateClaims"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #52222 (Halborn/Pepper)"
help: "Minting Limit Calculation May Prevent Legitimate Claims"
wiki_title: "Minting Limit Calculation May Prevent Legitimate Claims"
wiki_description: |
    ##### Description
    
    In the Pepper contract, the `mint` function implements a cap on minting that is intended to limit mints to 40% of the total supply.
    
    ```
    function mint(address to, uint256 amount) public {
        require(hasRole(MINTER_ROLE, msg.sender), "Must have minter role to mint");
        uint256 _
wiki_exploit_scenario: |
    Per Solodit #52222: ##### Description
    
    In the Pepper contract, the `mint` function implements a cap on minting that is intended to limit mints to 40% of the total supply.
    
    ```
    function mint(address to, uint256 amount) pu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MintingLimitCalculationMayPreventLegitimateClaims"
solodit_id: "52222"
solodit_slug: "minting-limit-calculation-may-prevent-legitimate-claims-halborn-pepper-pepper-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal mint;"
fn_name_regex: ".*(mint|MAX_SUPPLY).*"
write_var_regex: ".*(mint).*"
guard_var_regex: ".*(mint).*"
vuln_fn_name: "mint"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "mint = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
