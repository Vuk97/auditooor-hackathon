skeleton: "name_match_missing_require"
name: "trst-m-2-swaphelper-getcalldata-should-check-whitelistedrouters-"
class_name: "TrstM2SwaphelperGetcalldataShouldCheckWhitelistedrouters"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #19054 (Trust Security/Stella)"
help: "TRST-M-2 SwapHelper.getCalldata should check whitelistedRouters[_router]"
wiki_title: "TRST-M-2 SwapHelper.getCalldata should check whitelistedRouters[_router]"
wiki_description: |
    **Description:**
    `SwapHelper.getCalldata()` returns data for swap based on the input, and uses 
    **whitelistedRouters** to limit the **_router** param. The issue here is that when 
    `setWhitelistedRouters()` sets the **_routers** state to **false**, it does not reset the data in 
    **routerTypes** and *
wiki_exploit_scenario: |
    Per Solodit #19054: **Description:**
    `SwapHelper.getCalldata()` returns data for swap based on the input, and uses 
    **whitelistedRouters** to limit the **_router** param. The issue here is that when 
    `setWhitelistedRoute
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TrstM2SwaphelperGetcalldataShouldCheckWhitelistedrouters"
solodit_id: "19054"
solodit_slug: |
    trst-m-2-swaphelpergetcalldata-should-check-whitelistedrouters_router-trust-security-none-stella-markdown_
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal getcalldata;"
fn_name_regex: ".*(setWhitelistedRouters|getCalldata).*"
write_var_regex: ".*(getCalldata|setWhitelistedRouters).*"
guard_var_regex: ".*(getCalldata|setWhitelistedRouters).*"
vuln_fn_name: "setWhitelistedRouters"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "getcalldata = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
