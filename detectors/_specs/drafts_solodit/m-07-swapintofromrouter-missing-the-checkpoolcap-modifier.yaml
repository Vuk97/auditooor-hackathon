skeleton: "name_match_missing_require"
name: "m-07-swapintofromrouter-missing-the-checkpoolcap-modifier"
class_name: "M07SwapintofromrouterMissingTheCheckpoolcapModifier"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #36537 (Pashov Audit Group/Nabla)"
help: "[M-07] `swapIntoFromRouter` missing the `checkPoolCap` modifier"
wiki_title: "[M-07] `swapIntoFromRouter` missing the `checkPoolCap` modifier"
wiki_description: |
    **Severity**
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    **Description**
    
    When depositing into the pools, the pool cap is enforced. This is done through the `checkPoolCap` modifier. The modifier tracks the pool's asset's balance, with the amount being deposited, and compares them against the pool c
wiki_exploit_scenario: |
    Per Solodit #36537: **Severity**
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    **Description**
    
    When depositing into the pools, the pool cap is enforced. This is done through the `checkPoolCap` modifier. The modifier trac
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M07SwapintofromrouterMissingTheCheckpoolcapModifier"
solodit_id: "36537"
solodit_slug: "m-07-swapintofromrouter-missing-the-checkpoolcap-modifier-pashov-audit-group-none-nabla-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal checkpoolcap;"
fn_name_regex: ".*(checkPoolCap|deposit|swapIntoFromRouter).*"
write_var_regex: ".*(checkPoolCap|deposit|swapIntoFromRouter).*"
guard_var_regex: ".*(checkPoolCap|deposit|swapIntoFromRouter).*"
vuln_fn_name: "checkPoolCap"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "checkpoolcap = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
