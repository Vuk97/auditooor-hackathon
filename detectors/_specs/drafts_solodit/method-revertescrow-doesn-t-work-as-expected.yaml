skeleton: "name_match_missing_call"
name: "method-revertescrow-doesn-t-work-as-expected"
class_name: "MethodRevertescrowDoesnTWorkAsExpected"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #35677 (Zokyo/Legion Otc)"
help: "Method `revertEscrow()` doesn’t work as expected"
wiki_title: "Method `revertEscrow()` doesn’t work as expected"
wiki_description: |
    **Severity**: High
    
    **Status**: Resolved
    
    
    **Description**
    
    Method `revertEscrow()` has the following logic:
    ```solidity
    function revertEscrow(string calldata refId) external onlyOwner {
       Escrow storage escrow = escrows[refId];
    
    
       require(escrow.seller != address(0) || escrow.buyer != address(0)
wiki_exploit_scenario: |
    Per Solodit #35677: **Severity**: High
    
    **Status**: Resolved
    
    
    **Description**
    
    Method `revertEscrow()` has the following logic:
    ```solidity
    function revertEscrow(string calldata refId) external onlyOwner {
       Escrow sto
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MethodRevertescrowDoesnTWorkAsExpected"
solodit_id: "35677"
solodit_slug: "method-revertescrow-doesnt-work-as-expected-zokyo-none-legion-otc-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal revertescrow;"
fn_name_regex: ".*(revertEscrow).*"
read_var_regex: ".*(revertEscrow).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "revertEscrow"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return revertescrow > 0;"
