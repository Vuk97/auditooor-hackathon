skeleton: "name_match_missing_call"
name: "m-06-computeavailable-the-calculations-are-wrong"
class_name: "M06ComputeavailableTheCalculationsAreWrong"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #25966 (Code4rena/PoolTogether)"
help: "[M-06] `_computeAvailable()` the calculations are wrong"
wiki_title: "[M-06] `_computeAvailable()` the calculations are wrong"
wiki_description: |
    `_computeAvailable()` incorrect calculations that result in a return value greater than the current balance, causing methods such as `liquidate` to fail.
    
    ### Proof of Concept
    
    `VaultBooster._computeAvailable()` used to count the number of `tokens` currently available.<br>
    There are two conditions:
wiki_exploit_scenario: |
    Per Solodit #25966: `_computeAvailable()` incorrect calculations that result in a return value greater than the current balance, causing methods such as `liquidate` to fail.
    
    ### Proof of Concept
    
    `VaultBooster._compute
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M06ComputeavailableTheCalculationsAreWrong"
solodit_id: "25966"
solodit_slug: "m-06-_computeavailable-the-calculations-are-wrong-code4rena-pooltogether-pooltogether-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal accrue;"
fn_name_regex: ".*(liquidate|tokens|accrue|deltaAmount).*"
read_var_regex: ".*(accrue|deltaAmount|liquidate).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "liquidate"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return accrue > 0;"
