skeleton: "name_match_missing_call"
name: "m-9-dosed-liquidations-as-prizevault-liquidatablebalanceof-does-"
class_name: "M9DosedLiquidationsAsPrizevaultLiquidatablebalanceofDoes"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #34730 (Sherlock/PoolTogether: The Prize Layer for DeFi)"
help: |
    M-9: DoSed liquidations as `PrizeVault::liquidatableBalanceOf()` does not take into account the `mintLimit` when the token out is the asset
wiki_title: |
    M-9: DoSed liquidations as `PrizeVault::liquidatableBalanceOf()` does not take into account the `mintLimit` when the token out is the asset
wiki_description: |
    Source: https://github.com/sherlock-audit/2024-05-pooltogether-judging/issues/88 
    
    ## Found by 
    0x73696d616f
    ## Summary
    
    `PrizeVault::liquidatableBalanceOf()` is called in `TpdaLiquidationPair::_availableBalance()` to get the maximum amount to liquidate, which will be incorrect when `_tokenOut` is t
wiki_exploit_scenario: |
    Per Solodit #34730: Source: https://github.com/sherlock-audit/2024-05-pooltogether-judging/issues/88 
    
    ## Found by 
    0x73696d616f
    ## Summary
    
    `PrizeVault::liquidatableBalanceOf()` is called in `TpdaLiquidationPair::_avail
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M9DosedLiquidationsAsPrizevaultLiquidatablebalanceofDoes"
solodit_id: "34730"
solodit_slug: |
    m-9-dosed-liquidations-as-prizevaultliquidatablebalanceof-does-not-take-into-account-the-mintlimit-when-the-token-out-is-the-asset-sherlock-pooltogether-the-prize-layer-for-defi-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal asset;"
fn_name_regex: ".*(asset|PrizeVault|mintLimit).*"
read_var_regex: ".*(asset).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "asset"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return asset > 0;"
