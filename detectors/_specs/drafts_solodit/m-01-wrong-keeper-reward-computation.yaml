skeleton: "name_match_missing_call"
name: "m-01-wrong-keeper-reward-computation"
class_name: "M01WrongKeeperRewardComputation"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #25589 (Code4rena/Tracer)"
help: "[M-01] Wrong keeper reward computation"
wiki_title: "[M-01] Wrong keeper reward computation"
wiki_description: |
    _Submitted by cmichel_.
    
    The `PoolKeeper.keeperReward` computation mixes WADs and Quads which leads to issues.
    
    1.  Note that `keeperTip` returns values where `1` = `1%`, and `100 = 100%`, the same way `BASE_TIP = 5 = 5%`. Thus `_tipPercent = ABDKMathQuad.fromUInt(keeperTip)` is a Quad value of this
wiki_exploit_scenario: |
    Per Solodit #25589: _Submitted by cmichel_.
    
    The `PoolKeeper.keeperReward` computation mixes WADs and Quads which leads to issues.
    
    1.  Note that `keeperTip` returns values where `1` = `1%`, and `100 = 100%`, the same wa
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01WrongKeeperRewardComputation"
solodit_id: "25589"
solodit_slug: "m-01-wrong-keeper-reward-computation-code4rena-tracer-tracer-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal byte16;"
fn_name_regex: ".*(keeperTip|wadRewardValue|fixedPoint|byte16).*"
read_var_regex: ".*(byte16|fixedPoint|keeperTip).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "keeperTip"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return byte16 > 0;"
