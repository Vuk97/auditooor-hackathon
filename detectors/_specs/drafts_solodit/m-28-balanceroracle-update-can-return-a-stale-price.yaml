skeleton: "state_write_without_paired_write"
name: "m-28-balanceroracle-update-can-return-a-stale-price"
class_name: "M28BalanceroracleUpdateCanReturnAStalePrice"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #49065 (Code4rena/LoopFi)"
help: "[M-28] `BalancerOracle::update()` can return a stale price"
wiki_title: "[M-28] `BalancerOracle::update()` can return a stale price"
wiki_description: |
    Whenever `block.timestamp - lastUpdate > updateWaitWindow` and needs to update the price, it will return a stale price because it will fetch the price from the `lastUpdate` not the `currentUpdate`.
    
    ### Proof of Concept
    
    In the `update()` function these are the lines we'll find:
    
    ```
         //@audit
wiki_exploit_scenario: |
    Per Solodit #49065: Whenever `block.timestamp - lastUpdate > updateWaitWindow` and needs to update the price, it will return a stale price because it will fetch the price from the `lastUpdate` not the `currentUpdate`.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M28BalanceroracleUpdateCanReturnAStalePrice"
solodit_id: "49065"
solodit_slug: "m-28-balanceroracleupdate-can-return-a-stale-price-code4rena-loopfi-loopfi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(lastUpdate|currentUpdate|update|currentPrice).*"
primary_var_regex: ".*(currentPrice|currentUpdate|lastUpdate).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
