skeleton: "state_write_without_paired_write"
name: "m-08-users-can-omit-to-update-the-price-feed-to-use-stale-prices"
class_name: "M08UsersCanOmitToUpdateThePriceFeedToUseStalePrices"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #36538 (Pashov Audit Group/Nabla)"
help: "[M-08] Users can omit to update the price feed to use stale prices"
wiki_title: "[M-08] Users can omit to update the price feed to use stale prices"
wiki_description: |
    **Severity**
    
    **Impact:** High
    
    **Likelihood:** Low
    
    **Description**
    
    > [!NOTE]  
    > All functions that internally call `_updatePriceFeeds()` are vulnerable to this attack.
    
    When invoking any function in the Nabla protocol, users must provide an update fee and `_priceUpdateData()` to update the asset
wiki_exploit_scenario: |
    Per Solodit #36538: **Severity**
    
    **Impact:** High
    
    **Likelihood:** Low
    
    **Description**
    
    > [!NOTE]  
    > All functions that internally call `_updatePriceFeeds()` are vulnerable to this attack.
    
    When invoking any function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08UsersCanOmitToUpdateThePriceFeedToUseStalePrices"
solodit_id: "36538"
solodit_slug: |
    m-08-users-can-omit-to-update-the-price-feed-to-use-stale-prices-pashov-audit-group-none-nabla-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(redeemCrossSwapPoolShares|priceMaxAge).*"
primary_var_regex: ".*(priceMaxAge|redeemCrossSwapPoolShares).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
