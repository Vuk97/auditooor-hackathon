skeleton: "name_match_missing_call"
name: "fee-on-transfer-token-handling-issue-in-treasury-deposit-leads-t"
class_name: "FeeOnTransferTokenHandlingIssueInTreasuryDepositLeadsT"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #57238 (Codehawks/Core Contracts)"
help: "Fee-on-transfer token handling issue in `Treasury::deposit` leads to permanent fund loss"
wiki_title: "Fee-on-transfer token handling issue in `Treasury::deposit` leads to permanent fund loss"
wiki_description: |
    ## Summary
    
    The `Treasury::deposit` and `Treasury::withdraw` functions in the `Treasury` contract do not properly account for fee-on-transfer tokens, such as `RAAC` Token. This leads to incorrect balance tracking, causing users to deposit more than what is actually received by the contract. As a res
wiki_exploit_scenario: |
    Per Solodit #57238: ## Summary
    
    The `Treasury::deposit` and `Treasury::withdraw` functions in the `Treasury` contract do not properly account for fee-on-transfer tokens, such as `RAAC` Token. This leads to incorrect bala
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FeeOnTransferTokenHandlingIssueInTreasuryDepositLeadsT"
solodit_id: "57238"
solodit_slug: |
    fee-on-transfer-token-handling-issue-in-treasurydeposit-leads-to-permanent-fund-loss-codehawks-regnum-aurum-acquisition-corp-core-contracts-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(Treasury|RAAC).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "Treasury"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
