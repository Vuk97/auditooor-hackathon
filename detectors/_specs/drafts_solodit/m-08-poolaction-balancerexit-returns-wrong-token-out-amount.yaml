skeleton: "highlevelcall_missing_sibling"
name: "m-08-poolaction-balancerexit-returns-wrong-token-out-amount"
class_name: "M08PoolactionBalancerexitReturnsWrongTokenOutAmount"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #49045 (Code4rena/LoopFi)"
help: "[M-08] `PoolAction::_balancerExit` returns wrong token out amount"
wiki_title: "[M-08] `PoolAction::_balancerExit` returns wrong token out amount"
wiki_description: |
    Users use `PoolAction::exit` to exit a Balancer pool position, it calls `_balancerExit` to do the job. It is expected to exit the pool and return the amount out of the token; however, it returns the whole recipient's balance, without considering the case where the recipient is holding an amount of
wiki_exploit_scenario: |
    Per Solodit #49045: Users use `PoolAction::exit` to exit a Balancer pool position, it calls `_balancerExit` to do the job. It is expected to exit the pool and return the amount out of the token; however, it returns the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08PoolactionBalancerexitReturnsWrongTokenOutAmount"
solodit_id: "49045"
solodit_slug: "m-08-poolaction_balancerexit-returns-wrong-token-out-amount-code4rena-loopfi-loopfi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(tokenOut|withdrawnCollateral|residualAmount).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
