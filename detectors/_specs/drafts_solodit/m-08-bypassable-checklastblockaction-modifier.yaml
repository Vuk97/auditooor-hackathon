skeleton: "highlevelcall_missing_sibling"
name: "m-08-bypassable-checklastblockaction-modifier"
class_name: "M08BypassableChecklastblockactionModifier"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #36379 (Pashov Audit Group/Radiant June)"
help: "[M-08] Bypassable `checkLastBlockAction` Modifier"
wiki_title: "[M-08] Bypassable `checkLastBlockAction` Modifier"
wiki_description: |
    **Severity**
    
    **Impact:** Low
    
    **Likelihood:** High
    
    **Description**
    
    The `checkLastBlockAction` modifier is intended to prevent users from depositing and withdrawing in the same block, likely as a measure against flash loan attacks. However, this check can be easily bypassed. A user can deposit and
wiki_exploit_scenario: |
    Per Solodit #36379: **Severity**
    
    **Impact:** Low
    
    **Likelihood:** High
    
    **Description**
    
    The `checkLastBlockAction` modifier is intended to prevent users from depositing and withdrawing in the same block, likely as a me
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08BypassableChecklastblockactionModifier"
solodit_id: "36379"
solodit_slug: "m-08-bypassable-checklastblockaction-modifier-pashov-audit-group-none-radiant-june-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(checkLastBlockAction).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
