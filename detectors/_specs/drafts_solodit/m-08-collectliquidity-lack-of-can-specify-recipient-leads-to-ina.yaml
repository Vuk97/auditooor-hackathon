skeleton: "name_match_missing_call"
name: "m-08-collectliquidity-lack-of-can-specify-recipient-leads-to-ina"
class_name: "M08CollectliquidityLackOfCanSpecifyRecipientLeadsToIna"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #29705 (Code4rena/Particle Protocol)"
help: |
    [M-08] collectLiquidity() Lack of can specify recipient leads to inability to retrieve token1 after entering the blacklist of token0
wiki_title: |
    [M-08] collectLiquidity() Lack of can specify recipient leads to inability to retrieve token1 after entering the blacklist of token0
wiki_description: |
    `LP` has only one way to retrieve `token`, first `decreaseLiquidity()`, then retrieve through the `collectLiquidity()` method.
    
    `collectLiquidity()` only has one parameter, `tokenId`.
    
    ```solidity
        function collectLiquidity(
            uint256 tokenId
        ) external override nonReentrant returns (u
wiki_exploit_scenario: |
    Per Solodit #29705: `LP` has only one way to retrieve `token`, first `decreaseLiquidity()`, then retrieve through the `collectLiquidity()` method.
    
    `collectLiquidity()` only has one parameter, `tokenId`.
    
    ```solidity
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08CollectliquidityLackOfCanSpecifyRecipientLeadsToIna"
solodit_id: "29705"
solodit_slug: |
    m-08-collectliquidity-lack-of-can-specify-recipient-leads-to-inability-to-retrieve-token1-after-entering-the-blacklist-of-token0-code4rena-particle-protocol-particle-protocol-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal collectliquidity;"
fn_name_regex: ".*(token|decreaseLiquidity|collectLiquidity|tokenId).*"
read_var_regex: ".*(collectLiquidity|decreaseLiquidity|recipient).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "token"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return collectliquidity > 0;"
