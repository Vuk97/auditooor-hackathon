skeleton: "highlevelcall_missing_sibling"
name: "m-09-nft-flashloans-can-bypass-sale-constraints"
class_name: "M09NftFlashloansCanBypassSaleConstraints"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #976 (Code4rena/Boot Finance)"
help: "[M-09] NFT flashloans can bypass sale constraints"
wiki_title: "[M-09] NFT flashloans can bypass sale constraints"
wiki_description: |
    ## Handle
    
    pauliax
    
    
    ## Vulnerability details
    
    ## Impact
    Public sale has a constraint that for the first 4 weeks only NFT holders can access the sale:
      if (currentEra < firstPublicEra) {
        require(nft.balanceOf(msg.sender) > 0, "You need NFT to participate in the sale.");
      }
    
    However, this check
wiki_exploit_scenario: |
    Per Solodit #976: ## Handle
    
    pauliax
    
    
    ## Vulnerability details
    
    ## Impact
    Public sale has a constraint that for the first 4 weeks only NFT holders can access the sale:
      if (currentEra < firstPublicEra) {
        require(
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M09NftFlashloansCanBypassSaleConstraints"
solodit_id: "976"
solodit_slug: "m-09-nft-flashloans-can-bypass-sale-constraints-code4rena-boot-finance-boot-finance-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(currentEra|firstPublicEra|balanceOf|burnEtherForMember).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
