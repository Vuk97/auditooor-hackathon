skeleton: "name_match_missing_require"
name: "m-04-the-postop-function-in-gastank-is-not-handling-gas-penalty-"
class_name: "M04ThePostopFunctionInGastankIsNotHandlingGasPenalty"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #62854 (Shieldify/Etherspot Gastankpaymastermodule Extended)"
help: "[M-04] The `_postOp()` Function in `GasTank` Is Not Handling Gas Penalty in Calculations"
wiki_title: "[M-04] The `_postOp()` Function in `GasTank` Is Not Handling Gas Penalty in Calculations"
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    After executing the user Operation, call `_postExecution()` on the `EntryPoint`. the EntryPoint calls `Paymaster::postOp()` for handling gas paying.
    
    `EntryPoint::_postExecution()` is fired with `actualGas` parameter, which is the amount of gas used in all
wiki_exploit_scenario: |
    Per Solodit #62854: ## Severity
    
    Medium Risk
    
    ## Description
    
    After executing the user Operation, call `_postExecution()` on the `EntryPoint`. the EntryPoint calls `Paymaster::postOp()` for handling gas paying.
    
    `EntryP
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04ThePostopFunctionInGastankIsNotHandlingGasPenalty"
solodit_id: "62854"
solodit_slug: |
    m-04-the-_postop-function-in-gastank-is-not-handling-gas-penalty-in-calculations-shieldify-none-etherspot-gastankpaymastermodule-extended-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal actualgas;"
fn_name_regex: ".*(EntryPoint|actualGas|postOp|call).*"
write_var_regex: ".*(actualGas|call|postOp).*"
guard_var_regex: ".*(actualGas|call|postOp).*"
vuln_fn_name: "EntryPoint"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "actualgas = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
