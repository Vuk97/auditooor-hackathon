skeleton: "name_match_missing_call"
name: "m-04-call-to-updatepoolrate-is-missing"
class_name: "M04CallToUpdatepoolrateIsMissing"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20387 (Pashov Audit Group/Gmd)"
help: "[M-04] Call to `updatePoolRate` is missing"
wiki_title: "[M-04] Call to `updatePoolRate` is missing"
wiki_description: |
    **Likelihood:**
    Medium, because it happens only for a paused and then resumed pool
    
    **Impact:**
    Medium, because it can hardly lead to big losses
    
    **Description**
    
    Every time the `totalStaked` amount of a pool is updated, the `updatePoolRate` method is called to update the `EarnRateSec`. This is not
wiki_exploit_scenario: |
    Per Solodit #20387: **Likelihood:**
    Medium, because it happens only for a paused and then resumed pool
    
    **Impact:**
    Medium, because it can hardly lead to big losses
    
    **Description**
    
    Every time the `totalStaked` amount o
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04CallToUpdatepoolrateIsMissing"
solodit_id: "20387"
solodit_slug: "m-04-call-to-updatepoolrate-is-missing-pashov-none-gmd-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal pausereward;"
fn_name_regex: ".*(totalStaked|updatePoolRate|EarnRateSec|pauseReward).*"
read_var_regex: ".*(pauseReward|totalStaked|updatePool).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "totalStaked"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return pausereward > 0;"
