skeleton: "name_match_missing_call"
name: "remoratoken-transfers-are-bricked-when-from-is-not-whitelisted-h"
class_name: "RemoratokenTransfersAreBrickedWhenFromIsNotWhitelistedH"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #61192 (Cyfrin/Remora Pledge)"
help: |
    `RemoraToken` transfers are bricked when `from` is not whitelisted, has sufficient tokens to transfer but no tokens locked
wiki_title: |
    `RemoraToken` transfers are bricked when `from` is not whitelisted, has sufficient tokens to transfer but no tokens locked
wiki_description: |
    **Description:** `RemoraToken::adminTransferFrom`, `transfer` and `transferFrom` always check if `from` is on the whitelist and if not, call `_unlockTokens`:
    ```solidity
        bool fromWL = whitelist[from];
        bool toWL = whitelist[to];
    
        if (!fromWL) _unlockTokens(from, value, false);
        if (!to
wiki_exploit_scenario: |
    Per Solodit #61192: **Description:** `RemoraToken::adminTransferFrom`, `transfer` and `transferFrom` always check if `from` is on the whitelist and if not, call `_unlockTokens`:
    ```solidity
        bool fromWL = whitelist[fr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RemoratokenTransfersAreBrickedWhenFromIsNotWhitelistedH"
solodit_id: "61192"
solodit_slug: |
    remoratoken-transfers-are-bricked-when-from-is-not-whitelisted-has-sufficient-tokens-to-transfer-but-no-tokens-locked-cyfrin-none-remora-pledge-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal from;"
fn_name_regex: ".*(transfer|transferFrom|from|RemoraToken).*"
read_var_regex: ".*(from|transfer|transferFrom).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transfer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return from > 0;"
