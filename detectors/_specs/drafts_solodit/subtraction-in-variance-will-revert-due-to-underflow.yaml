skeleton: "name_match_missing_call"
name: "subtraction-in-variance-will-revert-due-to-underflow"
class_name: "SubtractionInVarianceWillRevertDueToUnderflow"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #49819 (Codehawks/Dria)"
help: "Subtraction in `variance()` will revert due to underflow"
wiki_title: "Subtraction in `variance()` will revert due to underflow"
wiki_description: |
    ## Summary
    
    The function `variance()` in Statistics.sol subtracts the average from each number in the array but the type is uint, because of that the function will revert unless all numbers are the same
    
    ## Vulnerability Details
    
    The function `variance()` does a subtraction by iterating on every num
wiki_exploit_scenario: |
    Per Solodit #49819: ## Summary
    
    The function `variance()` in Statistics.sol subtracts the average from each number in the array but the type is uint, because of that the function will revert unless all numbers are the sa
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SubtractionInVarianceWillRevertDueToUnderflow"
solodit_id: "49819"
solodit_slug: "subtraction-in-variance-will-revert-due-to-underflow-codehawks-swan-dria-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal finalizevalidation;"
fn_name_regex: ".*(variance|finalizeValidation|validate).*"
read_var_regex: ".*(finalizeValidation|validate|variance).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "variance"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return finalizevalidation > 0;"
