skeleton: "name_match_missing_call"
name: "wrappednativetoken-can-only-work-in-nativetoken-mode"
class_name: "WrappednativetokenCanOnlyWorkInNativetokenMode"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #38069 (Codehawks/Tadle)"
help: "`WrappedNativeToken` Can Only Work in `NativeToken` Mode"
wiki_title: "`WrappedNativeToken` Can Only Work in `NativeToken` Mode"
wiki_description: |
    ## Summary
    
    In the `TokenManager::tillIn` function, if `_tokenAddress` is equal to `wrappedNativeToken`, the function directly assumes that a native token is being used and checks `msg.value` for sufficient funds. This approach limits the functionality of `wrappedNativeToken` when it is used as an E
wiki_exploit_scenario: |
    Per Solodit #38069: ## Summary
    
    In the `TokenManager::tillIn` function, if `_tokenAddress` is equal to `wrappedNativeToken`, the function directly assumes that a native token is being used and checks `msg.value` for suff
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "WrappednativetokenCanOnlyWorkInNativetokenMode"
solodit_id: "38069"
solodit_slug: "wrappednativetoken-can-only-work-in-nativetoken-mode-codehawks-tadle-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal wrappednativetoken;"
fn_name_regex: ".*(wrappedNativeToken|WETH|WrappedNativeToken|NativeToken).*"
read_var_regex: ".*(wrappedNativeToken).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "wrappedNativeToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return wrappednativetoken > 0;"
