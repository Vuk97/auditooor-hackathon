skeleton: "name_match_missing_call"
name: "m-30-vault-creator-can-t-change-quitperiod"
class_name: "M30VaultCreatorCanTChangeQuitperiod"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #22027 (Code4rena/Popcorn)"
help: "[M-30] Vault creator can't change quitPeriod"
wiki_title: "[M-30] Vault creator can't change quitPeriod"
wiki_description: |
    The vault's `quitPeriod` can be updated through the `setQuitPeriod()` function. Only the owner of the contract (AdminProxy through VaultController) can call it. But, the VaultController contract doesn't implement a function to call `setQuitPeriod()`. Thus, the function is actually not usable.
    
    This
wiki_exploit_scenario: |
    Per Solodit #22027: The vault's `quitPeriod` can be updated through the `setQuitPeriod()` function. Only the owner of the contract (AdminProxy through VaultController) can call it. But, the VaultController contract does
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M30VaultCreatorCanTChangeQuitperiod"
solodit_id: "22027"
solodit_slug: "m-30-vault-creator-cant-change-quitperiod-code4rena-popcorn-popcorn-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal onlyowner;"
fn_name_regex: ".*(quitPeriod|setQuitPeriod|onlyOwner).*"
read_var_regex: ".*(onlyOwner|quitPeriod|setQuitPeriod).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "quitPeriod"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return onlyowner > 0;"
