skeleton: "name_match_missing_call"
name: "deployprotocol-s-sol-incorrectly-allows-transfers-to-stakemanage"
class_name: "DeployprotocolSSolIncorrectlyAllowsTransfersToStakemanage"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #65336 (Cyfrin/Statusl)"
help: "`DeployProtocol.s.sol` incorrectly allows transfers to `StakeManager.sol`"
wiki_title: "`DeployProtocol.s.sol` incorrectly allows transfers to `StakeManager.sol`"
wiki_description: |
    **Description:** `DeployProtocol.s.sol` is used to automatically deploy and configure protocol. Contracts `StakeManager.sol` and `SimpleKarmaDistributor.sol` should be allowed to transfer Karma token. However it provides `stakeManager` both times:
    ```solidity
            // whitelist reward distributors
wiki_exploit_scenario: |
    Per Solodit #65336: **Description:** `DeployProtocol.s.sol` is used to automatically deploy and configure protocol. Contracts `StakeManager.sol` and `SimpleKarmaDistributor.sol` should be allowed to transfer Karma token.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DeployprotocolSSolIncorrectlyAllowsTransfersToStakemanage"
solodit_id: "65336"
solodit_slug: "deployprotocolssol-incorrectly-allows-transfers-to-stakemanagersol-cyfrin-none-statusl-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal stakemanager;"
fn_name_regex: ".*(stakeManager).*"
read_var_regex: ".*(stakeManager).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "stakeManager"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return stakemanager > 0;"
