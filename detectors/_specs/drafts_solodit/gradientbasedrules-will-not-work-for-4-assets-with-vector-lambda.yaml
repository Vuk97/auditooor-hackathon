skeleton: "name_match_missing_require"
name: "gradientbasedrules-will-not-work-for-4-assets-with-vector-lambda"
class_name: "GradientbasedrulesWillNotWorkFor4AssetsWithVectorLambda"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #49694 (Codehawks/QuantAMM)"
help: "GradientBasedRules will not work for >=4 assets with vector lambdas"
wiki_title: "GradientBasedRules will not work for >=4 assets with vector lambdas"
wiki_description: |
    ## Summary
    
    UpdateRules based on `GradientBasedRule`will revert/produce incorrect result when  if `numAssets >= `4 and `lambda.length > 1`. It's due to a simple mistake in updating `intermediateGradientStates`.
    
    ## Vulnerability Details
    
    ### Root Cause
    
    We have the following in [`QuantammGradientBas
wiki_exploit_scenario: |
    Per Solodit #49694: ## Summary
    
    UpdateRules based on `GradientBasedRule`will revert/produce incorrect result when  if `numAssets >= `4 and `lambda.length > 1`. It's due to a simple mistake in updating `intermediateGradie
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "GradientbasedrulesWillNotWorkFor4AssetsWithVectorLambda"
solodit_id: "49694"
solodit_slug: "gradientbasedrules-will-not-work-for-4-assets-with-vector-lambdas-codehawks-quantamm-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal intermediategradientstates;"
fn_name_regex: ".*(GradientBasedRule|intermediateGradientStates|IntermediateGradientStates).*"
write_var_regex: ".*(intermediateGradientStates).*"
guard_var_regex: ".*(intermediateGradientStates).*"
vuln_fn_name: "GradientBasedRule"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "intermediategradientstates = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
