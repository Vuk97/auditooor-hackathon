skeleton: "name_match_missing_call"
name: "silent-truncation-in-permit2-transfers"
class_name: "SilentTruncationInPermit2Transfers"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #63502 (MixBytes/Barter DAO)"
help: "Silent Truncation in Permit2 Transfers"
wiki_title: "Silent Truncation in Permit2 Transfers"
wiki_description: |
    ##### Description
    The use of `uint160(amount)` in Permit2 transfers may silently truncate large amounts:
    
    ```solidity
    if (usePermit2) {
        permit2.transferFrom(
            order.maker, 
            order.taker, 
            uint160(amount), 
            address(order.makerToken)
        );
    } else {
        order.makerTok
wiki_exploit_scenario: |
    Per Solodit #63502: ##### Description
    The use of `uint160(amount)` in Permit2 transfers may silently truncate large amounts:
    
    ```solidity
    if (usePermit2) {
        permit2.transferFrom(
            order.maker, 
            order.ta
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SilentTruncationInPermit2Transfers"
solodit_id: "63502"
solodit_slug: "silent-truncation-in-permit2-transfers-mixbytes-none-barter-dao-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal uint160;"
fn_name_regex: ".*(uint160).*"
read_var_regex: ".*(uint160).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "uint160"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return uint160 > 0;"
