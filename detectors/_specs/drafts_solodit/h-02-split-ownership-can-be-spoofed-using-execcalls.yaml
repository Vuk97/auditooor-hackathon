skeleton: "name_match_missing_call"
name: "h-02-split-ownership-can-be-spoofed-using-execcalls"
class_name: "H02SplitOwnershipCanBeSpoofedUsingExeccalls"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #31751 (ZachObront/Splits)"
help: "[H-02] Split ownership can be spoofed using `execCalls()`"
wiki_title: "[H-02] Split ownership can be spoofed using `execCalls()`"
wiki_description: |
    Because the `onlyOwner` modifier allows calls from `address(this)`, we are able to call `transferOwnership()` from the `execCalls()` function.
    
    Notably, once an `execCalls()` transaction has been started and the modifier has been passed, it can continue to call any `onlyOwner` functions (including `
wiki_exploit_scenario: |
    Per Solodit #31751: Because the `onlyOwner` modifier allows calls from `address(this)`, we are able to call `transferOwnership()` from the `execCalls()` function.
    
    Notably, once an `execCalls()` transaction has been star
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02SplitOwnershipCanBeSpoofedUsingExeccalls"
solodit_id: "31751"
solodit_slug: "h-02-split-ownership-can-be-spoofed-using-execcalls-zachobront-none-splits-v2-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal execcalls;"
fn_name_regex: ".*(onlyOwner|transferOwnership|execCalls|registerSplit).*"
read_var_regex: ".*(execCalls|onlyOwner|registerSplit).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "onlyOwner"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return execcalls > 0;"
