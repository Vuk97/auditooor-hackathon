skeleton: "name_match_missing_require"
name: "m-02-entry-point-contract-cannot-interact-with-omoagent-sol-func"
class_name: "M02EntryPointContractCannotInteractWithOmoagentSolFunc"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #53332 (Pashov Audit Group/Omo_2025-01-25)"
help: "[M-02] Entry point contract cannot interact with `OmoAgent.sol` functions"
wiki_title: "[M-02] Entry point contract cannot interact with `OmoAgent.sol` functions"
wiki_description: |
    ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    Most of the functions in `OmoAgent.sol` contract will be triggered by the entry point contract e.g.`borrowAsset()`, `repayAsset()`, `topOffToAgent()` and more.
    But they have an `onlyAgent` modifier
    
    ```solidity
        modifier onl
wiki_exploit_scenario: |
    Per Solodit #53332: ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    Most of the functions in `OmoAgent.sol` contract will be triggered by the entry point contract e.g.`borrowAsset()`, `repayAsset
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02EntryPointContractCannotInteractWithOmoagentSolFunc"
solodit_id: "53332"
solodit_slug: |
    m-02-entry-point-contract-cannot-interact-with-omoagentsol-functions-pashov-audit-group-none-omo_2025-01-25-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal borrowasset;"
fn_name_regex: ".*(borrowAsset|repayAsset|topOffToAgent|onlyAgent).*"
write_var_regex: ".*(borrowAsset|onlyAgent|repayAsset).*"
guard_var_regex: ".*(borrowAsset|onlyAgent|repayAsset).*"
vuln_fn_name: "borrowAsset"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "borrowasset = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
