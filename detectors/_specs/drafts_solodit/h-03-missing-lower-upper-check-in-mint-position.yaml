skeleton: "name_match_missing_require"
name: "h-03-missing-lower-upper-check-in-mint-position"
class_name: "H03MissingLowerUpperCheckInMintPosition"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #41514 (Code4rena/Superposition)"
help: "[H-03] Missing `lower<upper` check in `mint_position`"
wiki_title: "[H-03] Missing `lower<upper` check in `mint_position`"
wiki_description: |
    The current implementation does not perform a check for `lower < upper` during `mint_position`, while many other functions assume `lower < upper`. This discrepancy allows malicious actors to exploit inconsistencies in handling undefined behavior in other parts of the code for their benefit.
    
    1. **C
wiki_exploit_scenario: |
    Per Solodit #41514: The current implementation does not perform a check for `lower < upper` during `mint_position`, while many other functions assume `lower < upper`. This discrepancy allows malicious actors to exploit
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03MissingLowerUpperCheckInMintPosition"
solodit_id: "41514"
solodit_slug: "h-03-missing-lowerupper-check-in-mint_position-code4rena-superposition-superposition-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal mint_position;"
fn_name_regex: ".*(mint_position).*"
write_var_regex: ".*(mint_position).*"
guard_var_regex: ".*(mint_position).*"
vuln_fn_name: "mint_position"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "mint_position = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
