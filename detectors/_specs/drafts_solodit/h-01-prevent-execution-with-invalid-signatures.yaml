skeleton: "name_match_missing_require"
name: "h-01-prevent-execution-with-invalid-signatures"
class_name: "H01PreventExecutionWithInvalidSignatures"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #25585 (Code4rena/Ambire)"
help: "[H-01] Prevent execution with invalid signatures"
wiki_title: "[H-01] Prevent execution with invalid signatures"
wiki_description: |
    _Submitted by gpersoon_
    
    #### Impact
    Suppose one of the supplied `addrs\[i]` to the constructor of `Identity.sol` happens to be 0 ( by accident).
    
    In that case: `privileges\[0] = 1`
    
    Now suppose you call `execute()` with an invalid signature, then `recoverAddrImpl` will return a value of 0 and thus
wiki_exploit_scenario: |
    Per Solodit #25585: _Submitted by gpersoon_
    
    #### Impact
    Suppose one of the supplied `addrs\[i]` to the constructor of `Identity.sol` happens to be 0 ( by accident).
    
    In that case: `privileges\[0] = 1`
    
    Now suppose you c
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01PreventExecutionWithInvalidSignatures"
solodit_id: "25585"
solodit_slug: "h-01-prevent-execution-with-invalid-signatures-code4rena-ambire-ambire-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal erecover;"
fn_name_regex: ".*(execute|recoverAddrImpl|erecover).*"
write_var_regex: ".*(erecover|execute|recoverAddrImpl).*"
guard_var_regex: ".*(erecover|execute|recoverAddrImpl).*"
vuln_fn_name: "execute"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "erecover = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
