skeleton: "highlevelcall_missing_sibling"
name: "m-01-short-lookback-period-in-createtokenmanager-function"
class_name: "M01ShortLookbackPeriodInCreatetokenmanagerFunction"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #44073 (Shieldify/Gooserun V1)"
help: "[M-01] Short Lookback Period in `createTokenManager()` Function"
wiki_title: "[M-01] Short Lookback Period in `createTokenManager()` Function"
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    The `createTokenManager` function specifies a **30-seconds lookback period** for calculating the Time-Weighted Average Price (TWAP) when creating a new pool through the `createPermissioned` function.
    
    ```solidity
    IMaverickV2Pool pool = factory.createPermissi
wiki_exploit_scenario: |
    Per Solodit #44073: ## Severity
    
    Medium Risk
    
    ## Description
    
    The `createTokenManager` function specifies a **30-seconds lookback period** for calculating the Time-Weighted Average Price (TWAP) when creating a new pool t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01ShortLookbackPeriodInCreatetokenmanagerFunction"
solodit_id: "44073"
solodit_slug: "m-01-short-lookback-period-in-createtokenmanager-function-shieldify-none-gooserun-v1-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(createTokenManager|createPermissioned).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
