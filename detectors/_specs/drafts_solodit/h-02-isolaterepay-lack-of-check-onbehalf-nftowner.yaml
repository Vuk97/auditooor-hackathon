skeleton: "name_match_missing_require"
name: "h-02-isolaterepay-lack-of-check-onbehalf-nftowner"
class_name: "H02IsolaterepayLackOfCheckOnbehalfNftowner"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #36876 (Code4rena/BendDAO)"
help: "[H-02] `isolateRepay()` lack of check `onBehalf == nftOwner`"
wiki_title: "[H-02] `isolateRepay()` lack of check `onBehalf == nftOwner`"
wiki_description: |
    When calling `isolateRepay()` we can specify `nftTokenIds` and `onBehalf`.
    The current implementation just restricts `onBehalf!=address(0)` and does not restrict `onBehalf == nftOwner`.
    
    ```solidity
      function validateIsolateRepayBasic(
        InputTypes.ExecuteIsolateRepayParams memory inputParams,
wiki_exploit_scenario: |
    Per Solodit #36876: When calling `isolateRepay()` we can specify `nftTokenIds` and `onBehalf`.
    The current implementation just restricts `onBehalf!=address(0)` and does not restrict `onBehalf == nftOwner`.
    
    ```solidity
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02IsolaterepayLackOfCheckOnbehalfNftowner"
solodit_id: "36876"
solodit_slug: "h-02-isolaterepay-lack-of-check-onbehalf-nftowner-code4rena-benddao-benddao-git"
solodit_quality: "4"
solodit_rarity: "3"
solodit_tags: ""
state_decl: "uint256 internal isolaterepay;"
fn_name_regex: ".*(isolateRepay|nftTokenIds|onBehalf|underflow).*"
write_var_regex: ".*(isolateRepay|nftTokenIds|onBehalf).*"
guard_var_regex: ".*(isolateRepay|nftTokenIds|onBehalf).*"
vuln_fn_name: "isolateRepay"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "isolaterepay = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
