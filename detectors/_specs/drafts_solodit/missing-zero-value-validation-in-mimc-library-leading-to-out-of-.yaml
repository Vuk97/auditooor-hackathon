skeleton: "name_match_missing_require"
name: "missing-zero-value-validation-in-mimc-library-leading-to-out-of-"
class_name: "MissingZeroValueValidationInMimcLibraryLeadingToOutOf"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #34977 (ConsenSys/Linea ENS)"
help: "Missing Zero Value Validation in MIMC Library Leading to Out-of-Gas Error ✓ Fixed"
wiki_title: "Missing Zero Value Validation in MIMC Library Leading to Out-of-Gas Error ✓ Fixed"
wiki_description: |
    #### Description
    
    
    In the `hash` function of the `Mimc` library, there is no validation of the length of the `_msg` variable. The `_msg` is divided into `chunks` in the `assembly` block, and after that, there is an iteration over the `chunks`:
    
    
    
    ```
            assembly {
                let chunks := div(
wiki_exploit_scenario: |
    Per Solodit #34977: #### Description
    
    
    In the `hash` function of the `Mimc` library, there is no validation of the length of the `_msg` variable. The `_msg` is divided into `chunks` in the `assembly` block, and after tha
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingZeroValueValidationInMimcLibraryLeadingToOutOf"
solodit_id: "34977"
solodit_slug: |
    missing-zero-value-validation-in-mimc-library-leading-to-out-of-gas-error-fixed-consensys-none-linea-ens-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal assembly;"
fn_name_regex: ".*(hash|Mimc|chunks|assembly).*"
write_var_regex: ".*(assembly|chunks|for).*"
guard_var_regex: ".*(assembly|chunks|for).*"
vuln_fn_name: "hash"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "assembly = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
