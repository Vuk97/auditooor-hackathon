skeleton: "name_match_missing_call"
name: "precision-loss-in-cashasset-withdraw"
class_name: "PrecisionLossInCashassetWithdraw"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #53726 (SigmaPrime/Derive)"
help: "Precision Loss in CashAsset.withdraw()"
wiki_title: "Precision Loss in CashAsset.withdraw()"
wiki_description: |
    ## Description
    
    `CashAsset.withdraw()` appears to have some precision loss, which can lead to reverts when using stable assets with native decimals larger than 18 digits. The `withdraw()` function converts the requested `stableAmount` by calling `to18DecimalsRoundUp()` on it, which rounds up the res
wiki_exploit_scenario: |
    Per Solodit #53726: ## Description
    
    `CashAsset.withdraw()` appears to have some precision loss, which can lead to reverts when using stable assets with native decimals larger than 18 digits. The `withdraw()` function con
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PrecisionLossInCashassetWithdraw"
solodit_id: "53726"
solodit_slug: "precision-loss-in-cashassetwithdraw-sigmaprime-none-derive-pdf"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal from18decimals;"
fn_name_regex: ".*(withdraw|stableAmount|to18DecimalsRoundUp|from18Decimals).*"
read_var_regex: ".*(from18Decimals|stableAmount|to18DecimalsRoundUp).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "withdraw"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return from18decimals > 0;"
