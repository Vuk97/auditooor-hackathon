skeleton: "name_match_missing_call"
name: "m-05-stabilizernode-stabilize-should-update-lasttracking-as-well"
class_name: "M05StabilizernodeStabilizeShouldUpdateLasttrackingAsWell"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #16083 (Code4rena/Malt Protocol)"
help: |
    [M-05] `StabilizerNode.stabilize()` should update `lastTracking` as well to avoid an unnecessary incentive.
wiki_title: |
    [M-05] `StabilizerNode.stabilize()` should update `lastTracking` as well to avoid an unnecessary incentive.
wiki_description: |
    `StabilizerNode.stabilize()` should update `lastTracking` as well to avoid an unnecessary incentive.
    
    Current logic pays unnecessary incentives to track the pool.
    
    ### Proof of Concept
    
    `trackPool()` pays an incentive per `trackingBackoff` in order to ensure pool consistency.
    
    ```solidity
    File: 202
wiki_exploit_scenario: |
    Per Solodit #16083: `StabilizerNode.stabilize()` should update `lastTracking` as well to avoid an unnecessary incentive.
    
    Current logic pays unnecessary incentives to track the pool.
    
    ### Proof of Concept
    
    `trackPool()`
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M05StabilizernodeStabilizeShouldUpdateLasttrackingAsWell"
solodit_id: "16083"
solodit_slug: |
    m-05-stabilizernodestabilize-should-update-lasttracking-as-well-to-avoid-an-unnecessary-incentive-code4rena-malt-protocol-malt-protocol-versus-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal lasttracking;"
fn_name_regex: ".*(lastTracking|trackPool|trackingBackoff|stabilize).*"
read_var_regex: ".*(lastTracking|stabilize|trackPool).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "lastTracking"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return lasttracking > 0;"
