skeleton: "name_match_missing_call"
name: "c-02-contract-fails-to-handle-address-0-as-a-native-asset-for-it"
class_name: "C02ContractFailsToHandleAddress0AsANativeAssetForIt"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #20510 (Pashov Audit Group/Parcel Payroll)"
help: "[C-02] Contract fails to handle `address(0)` as a native asset for it's zero balance left invariant"
wiki_title: "[C-02] Contract fails to handle `address(0)` as a native asset for it's zero balance left invariant"
wiki_description: |
    **Impact:**
    High, as all transactions that use native assets will revert
    
    **Likelihood:**
    High, as it is well expected that native assets will be used as a `paymentToken` often
    
    **Description**
    
    The `executePayroll` method has the following code at the end of it:
    
    ```solidity
    // Check if the contrac
wiki_exploit_scenario: |
    Per Solodit #20510: **Impact:**
    High, as all transactions that use native assets will revert
    
    **Likelihood:**
    High, as it is well expected that native assets will be used as a `paymentToken` often
    
    **Description**
    
    The `
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "C02ContractFailsToHandleAddress0AsANativeAssetForIt"
solodit_id: "20510"
solodit_slug: |
    c-02-contract-fails-to-handle-address0-as-a-native-asset-for-its-zero-balance-left-invariant-pashov-none-parcel-payroll-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal executepayroll;"
fn_name_regex: ".*(paymentToken|executePayroll|paymentTokens).*"
read_var_regex: ".*(executePayroll|paymentToken|paymentTokens).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "paymentToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return executepayroll > 0;"
