skeleton: "state_write_without_paired_write"
name: "reporting-deadlock-if-quorum-not-reached"
class_name: "ReportingDeadlockIfQuorumNotReached"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #50753 (Halborn/SCProtection)"
help: "REPORTING DEADLOCK IF QUORUM NOT REACHED"
wiki_title: "REPORTING DEADLOCK IF QUORUM NOT REACHED"
wiki_description: |
    ##### Description
    
    The `settle` function under `IncidentReport` does not reset the `reported` state when the quorum is not reached those causing the contract to not accept new reports for the same `poolId` ever again.
    
    Code Location
    -------------
    
    #### src/voting/incidentReport/IncidentReport.sol
    
    `
wiki_exploit_scenario: |
    Per Solodit #50753: ##### Description
    
    The `settle` function under `IncidentReport` does not reset the `reported` state when the quorum is not reached those causing the contract to not accept new reports for the same `po
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ReportingDeadlockIfQuorumNotReached"
solodit_id: "50753"
solodit_slug: "reporting-deadlock-if-quorum-not-reached-halborn-degis-scprotection-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(settle|IncidentReport|reported|poolId).*"
primary_var_regex: ".*(poolId|reported|settle).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
