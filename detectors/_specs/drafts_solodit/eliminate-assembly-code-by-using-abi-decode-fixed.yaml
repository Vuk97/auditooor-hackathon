skeleton: "name_match_missing_call"
name: "eliminate-assembly-code-by-using-abi-decode-fixed"
class_name: "EliminateAssemblyCodeByUsingAbiDecodeFixed"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #13667 (ConsenSys/Amp)"
help: "Eliminate assembly code by using ABI decode ✓ Fixed"
wiki_title: "Eliminate assembly code by using ABI decode ✓ Fixed"
wiki_description: |
    #### Resolution
    
    
    
    All assembly code was replaced with proper use of `abi.decode()`.
    
    
    #### Description
    
    
    There are several locations where assembly code is used to access and decode byte arrays (including uses inside loops).
    Even though assembly code was used for gas optimization, it reduces the re
wiki_exploit_scenario: |
    Per Solodit #13667: #### Resolution
    
    
    
    All assembly code was replaced with proper use of `abi.decode()`.
    
    
    #### Description
    
    
    There are several locations where assembly code is used to access and decode byte arrays (incl
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "EliminateAssemblyCodeByUsingAbiDecodeFixed"
solodit_id: "13667"
solodit_slug: "eliminate-assembly-code-by-using-abi-decode-fixed-consensys-amp-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(toPartition).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "toPartition"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
