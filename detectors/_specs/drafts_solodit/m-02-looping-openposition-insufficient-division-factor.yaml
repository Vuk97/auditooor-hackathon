skeleton: "highlevelcall_missing_sibling"
name: "m-02-looping-openposition-insufficient-division-factor"
class_name: "M02LoopingOpenpositionInsufficientDivisionFactor"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #64822 (Pashov Audit Group/Hyperlend_2025-01-11)"
help: "[M-02] `Looping.openPosition()` insufficient division factor"
wiki_title: "[M-02] `Looping.openPosition()` insufficient division factor"
wiki_description: |
    ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    In `Looping.openPosition()` function, when users provide the expected flashloan premium payment in the `yieldAsset`, it is swapped into `debtAsset`, where the `minAmountOut` expected from the swap is calculated as follows:
    
    ``
wiki_exploit_scenario: |
    Per Solodit #64822: ## Severity
    
    **Impact:** Medium
    
    **Likelihood:** Medium
    
    ## Description
    
    In `Looping.openPosition()` function, when users provide the expected flashloan premium payment in the `yieldAsset`, it is swa
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02LoopingOpenpositionInsufficientDivisionFactor"
solodit_id: "64822"
solodit_slug: |
    m-02-loopingopenposition-insufficient-division-factor-pashov-audit-group-none-hyperlend_2025-01-11-markdown_
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(yieldAsset|debtAsset|minAmountOut).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
