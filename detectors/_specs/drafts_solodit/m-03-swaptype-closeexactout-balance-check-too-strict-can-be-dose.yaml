skeleton: "name_match_missing_call"
name: "m-03-swaptype-closeexactout-balance-check-too-strict-can-be-dose"
class_name: "M03SwaptypeCloseexactoutBalanceCheckTooStrictCanBeDose"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #30262 (Code4rena/Init Capital)"
help: "[M-03] SwapType.CloseExactOut balance check too strict can be DOSed"
wiki_title: "[M-03] SwapType.CloseExactOut balance check too strict can be DOSed"
wiki_description: |
    SwapType.CloseExactOut balance check too strict can be DOSed.
    
    ### Proof of Concept
    
    In CoreCallback function, we have the logic below:
    
    ```solidity
    uint amtOut = IERC20(swapInfo.tokenOut).balanceOf(address(this));
    if (swapInfo.swapType == SwapType.OpenExactIn) {
    	// transfer to coll pool to mint
wiki_exploit_scenario: |
    Per Solodit #30262: SwapType.CloseExactOut balance check too strict can be DOSed.
    
    ### Proof of Concept
    
    In CoreCallback function, we have the logic below:
    
    ```solidity
    uint amtOut = IERC20(swapInfo.tokenOut).balanceOf(
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03SwaptypeCloseexactoutBalanceCheckTooStrictCanBeDose"
solodit_id: "30262"
solodit_slug: |
    m-03-swaptypecloseexactout-balance-check-too-strict-can-be-dosed-code4rena-init-capital-init-capital-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(amtOut|swapInfo|tokenOut|balanceOf).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "amtOut"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
