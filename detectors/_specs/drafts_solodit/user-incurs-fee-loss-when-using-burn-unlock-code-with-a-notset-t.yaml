skeleton: "name_match_missing_call"
name: "user-incurs-fee-loss-when-using-burn-unlock-code-with-a-notset-t"
class_name: "UserIncursFeeLossWhenUsingBurnUnlockCodeWithANotsetT"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #41151 (MixBytes/EYWA)"
help: "User incurs fee loss when using `BURN_UNLOCK_CODE` with a `NotSet` token status"
wiki_title: "User incurs fee loss when using `BURN_UNLOCK_CODE` with a `NotSet` token status"
wiki_description: |
    ##### Description
    
    A user's funds may get stuck when using `BURN_UNLOCK_CODE` for a `NotSet` token.
    
    For example, consider the following scenario:
    1. A user locks `1000e18` LP tokens on chain A and mints corresponding synthLp tokens on chain B.
    2. As time progresses, the `WhitelistV2` contract's own
wiki_exploit_scenario: |
    Per Solodit #41151: ##### Description
    
    A user's funds may get stuck when using `BURN_UNLOCK_CODE` for a `NotSet` token.
    
    For example, consider the following scenario:
    1. A user locks `1000e18` LP tokens on chain A and mi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UserIncursFeeLossWhenUsingBurnUnlockCodeWithANotsetT"
solodit_id: "41151"
solodit_slug: |
    user-incurs-fee-loss-when-using-burn_unlock_code-with-a-notset-token-status-mixbytes-none-eywa-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(BURN_UNLOCK_CODE|NotSet|WhitelistV2|InOut).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "BURN_UNLOCK_CODE"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
