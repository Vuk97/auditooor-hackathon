skeleton: "name_match_missing_call"
name: "m-04-verifyingcontract-set-incorrectly-for-eip712-domain-separat"
class_name: "M04VerifyingcontractSetIncorrectlyForEip712DomainSeparat"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #18713 (ZachObront/Hook)"
help: "[M-04] verifyingContract set incorrectly for EIP712 Domain Separator"
wiki_title: "[M-04] verifyingContract set incorrectly for EIP712 Domain Separator"
wiki_description: |
    When the EIP712 Domain Separator is calculated, we input the `hookAddress` and perform the following function:
    
    ```solidity
    function setAddressForEipDomain(address hookAddress) internal {
    // Compute `EIP712_DOMAIN_SEPARATOR`
    {
    uint256 chainId;
    assembly {
    chainId := chainid()
    }
    EIP712_DOMAIN_SEPARATO
wiki_exploit_scenario: |
    Per Solodit #18713: When the EIP712 Domain Separator is calculated, we input the `hookAddress` and perform the following function:
    
    ```solidity
    function setAddressForEipDomain(address hookAddress) internal {
    // Compute `
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04VerifyingcontractSetIncorrectlyForEip712DomainSeparat"
solodit_id: "18713"
solodit_slug: "m-04-verifyingcontract-set-incorrectly-for-eip712-domain-separator-zachobront-none-hook-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal hookaddress;"
fn_name_regex: ".*(hookAddress|EIP712_DOMAIN_SEPARATOR|setProtocol|immutable).*"
read_var_regex: ".*(hookAddress|immutable|setProtocol).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "hookAddress"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return hookaddress > 0;"
