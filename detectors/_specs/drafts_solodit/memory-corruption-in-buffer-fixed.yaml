skeleton: "name_match_missing_require"
name: "memory-corruption-in-buffer-fixed"
class_name: "MemoryCorruptionInBufferFixed"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #14010 (ConsenSys/ENS Permanent Registrar)"
help: "Memory corruption in Buffer ✓ Fixed"
wiki_title: "Memory corruption in Buffer ✓ Fixed"
wiki_description: |
    #### Resolution
    
    
    
    Issue has been closed in [ensdomains/buffer#3](https://github.com/ensdomains/buffer/pull/3/)
    
    
    #### Description
    
    
    Although out of scope for this audit, the audit team noticed a memory corruption issue in the `Buffer` library. The `init` function is as follows:
    
    
    **contracts/Buffer
wiki_exploit_scenario: |
    Per Solodit #14010: #### Resolution
    
    
    
    Issue has been closed in [ensdomains/buffer#3](https://github.com/ensdomains/buffer/pull/3/)
    
    
    #### Description
    
    
    Although out of scope for this audit, the audit team noticed a memo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MemoryCorruptionInBufferFixed"
solodit_id: "14010"
solodit_slug: "memory-corruption-in-buffer-fixed-consensys-ens-permanent-registrar-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal capacity;"
fn_name_regex: ".*(Buffer|init|capacity).*"
write_var_regex: ".*(capacity|init).*"
guard_var_regex: ".*(capacity|init).*"
vuln_fn_name: "Buffer"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "capacity = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
