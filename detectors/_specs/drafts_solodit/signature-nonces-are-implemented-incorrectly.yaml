skeleton: "name_match_missing_require"
name: "signature-nonces-are-implemented-incorrectly"
class_name: "SignatureNoncesAreImplementedIncorrectly"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #50346 (Halborn/Mini Miners)"
help: "SIGNATURE NONCES ARE IMPLEMENTED INCORRECTLY"
wiki_title: "SIGNATURE NONCES ARE IMPLEMENTED INCORRECTLY"
wiki_description: |
    ##### Description
    
    In the `MinerGame` contract a nonce state variable is used to prevent signature replay attacks:
    
    #### MinerGame.sol
    
    ```
    uint256 public nonce;
    
    ```
    
    #### MinerGame.sol
    
    ```
    function importNft(uint256 _nftId, uint8 _v, bytes32 _r, bytes32 _s) external {
      require(_nftId > 0, "Miner
wiki_exploit_scenario: |
    Per Solodit #50346: ##### Description
    
    In the `MinerGame` contract a nonce state variable is used to prevent signature replay attacks:
    
    #### MinerGame.sol
    
    ```
    uint256 public nonce;
    
    ```
    
    #### MinerGame.sol
    
    ```
    function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SignatureNoncesAreImplementedIncorrectly"
solodit_id: "50346"
solodit_slug: "signature-nonces-are-implemented-incorrectly-halborn-seascape-mini-miners-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(MinerGame).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "MinerGame"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
