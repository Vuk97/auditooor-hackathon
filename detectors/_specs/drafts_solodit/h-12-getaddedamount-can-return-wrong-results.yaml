skeleton: "paired_function_divergence"
name: "h-12-getaddedamount-can-return-wrong-results"
class_name: "H12GetaddedamountCanReturnWrongResults"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #42157 (Code4rena/Vader Protocol)"
help: "[H-12] `getAddedAmount` can return wrong results"
wiki_title: "[H-12] `getAddedAmount` can return wrong results"
wiki_description: |
    The `getAddedAmount` function only works correctly when called with `(VADER/USDV, pool)` or `(pool, pool)`.
    However, when called with (`token, pool)` where `token` is neither `VADER/USDV/pool`, it returns the wrong results:
    
    1. It gets the `token` balance
    2. And subtracts it from the stored `mapTok
wiki_exploit_scenario: |
    Per Solodit #42157: The `getAddedAmount` function only works correctly when called with `(VADER/USDV, pool)` or `(pool, pool)`.
    However, when called with (`token, pool)` where `token` is neither `VADER/USDV/pool`, it re
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H12GetaddedamountCanReturnWrongResults"
solodit_id: "42157"
solodit_slug: "h-12-getaddedamount-can-return-wrong-results-code4rena-vader-protocol-vader-protocol-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(else|getAddedAmount|swap).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
