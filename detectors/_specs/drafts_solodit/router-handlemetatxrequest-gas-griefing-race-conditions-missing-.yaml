skeleton: "name_match_missing_require"
name: "router-handlemetatxrequest-gas-griefing-race-conditions-missing-"
class_name: "RouterHandlemetatxrequestGasGriefingRaceConditionsMissing"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #13323 (ConsenSys/Connext NXTP — Noncustodial Xchain Transfer Protocol)"
help: |
    Router - handleMetaTxRequest - gas griefing / race conditions / missing validations / free meta transactions
wiki_title: |
    Router - handleMetaTxRequest - gas griefing / race conditions / missing validations / free meta transactions
wiki_description: |
    #### Description
    
    
    There’s a comment in `handleMetaTxRequest` that asks whether data needs to be validated before interacting with the contract and the answer is yes, always, or else this opens up a gas griefing vector on the router side.
    
    
    For example, someone might flood broadcast masses of metaTx
wiki_exploit_scenario: |
    Per Solodit #13323: #### Description
    
    
    There’s a comment in `handleMetaTxRequest` that asks whether data needs to be validated before interacting with the contract and the answer is yes, always, or else this opens up a g
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RouterHandlemetatxrequestGasGriefingRaceConditionsMissing"
solodit_id: "13323"
solodit_slug: |
    router-handlemetatxrequest-gas-griefing-race-conditions-missing-validations-free-meta-transactions-consensys-connext-nxtp-noncustodial-xchain-transfer-protocol-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal handlemetatxrequest;"
fn_name_regex: ".*(handleMetaTxRequest).*"
write_var_regex: ".*(handleMetaTxRequest).*"
guard_var_regex: ".*(handleMetaTxRequest).*"
vuln_fn_name: "handleMetaTxRequest"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "handlemetatxrequest = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
