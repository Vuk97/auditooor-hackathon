skeleton: "name_match_missing_call"
name: "h-03-predictable-token-deployment-address-enables-liquidity-pool"
class_name: "H03PredictableTokenDeploymentAddressEnablesLiquidityPool"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #57870 (Shieldify/Daoslive)"
help: |
    [H-03] Predictable Token Deployment Address Enables Liquidity Pool Exploit in `finalize()` in `DaosLive`
wiki_title: |
    [H-03] Predictable Token Deployment Address Enables Liquidity Pool Exploit in `finalize()` in `DaosLive`
wiki_description: |
    ## Severity
    
    High Risk
    
    ## Description
    
    An attacker can exploit the deterministic nature of contract deployment using the `CREATE` opcode to precompute the address of the `DaosToken` deployed within the `DaosLive::finalize()` function. This predictability enables a front-running attack that severely
wiki_exploit_scenario: |
    Per Solodit #57870: ## Severity
    
    High Risk
    
    ## Description
    
    An attacker can exploit the deterministic nature of contract deployment using the `CREATE` opcode to precompute the address of the `DaosToken` deployed within t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03PredictableTokenDeploymentAddressEnablesLiquidityPool"
solodit_id: "57870"
solodit_slug: |
    h-03-predictable-token-deployment-address-enables-liquidity-pool-exploit-in-finalize-in-daoslive-shieldify-none-daoslive-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal finalize;"
fn_name_regex: ".*(CREATE|DaosToken|finalize|DaosLive).*"
read_var_regex: ".*(finalize).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "CREATE"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return finalize > 0;"
