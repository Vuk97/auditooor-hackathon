skeleton: "highlevelcall_missing_sibling"
name: "h-15-attacker-can-specify-any-receiver-in-usd0-flashloan-to-drai"
class_name: "H15AttackerCanSpecifyAnyReceiverInUsd0FlashloanToDrai"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #27505 (Code4rena/Tapioca DAO)"
help: "[H-15] Attacker can specify any `receiver` in `USD0.flashLoan()` to drain `receiver` balance"
wiki_title: "[H-15] Attacker can specify any `receiver` in `USD0.flashLoan()` to drain `receiver` balance"
wiki_description: |
    The flash loan feature in USD0's `flashLoan()` function allows the caller to specify the `receiver` address. USD0 is then minted to this address and burnt from this address plus a fee after the callback. Since there is a fee in each flash loan, an attacker can abuse this to drain the balance of the
wiki_exploit_scenario: |
    Per Solodit #27505: The flash loan feature in USD0's `flashLoan()` function allows the caller to specify the `receiver` address. USD0 is then minted to this address and burnt from this address plus a fee after the callb
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H15AttackerCanSpecifyAnyReceiverInUsd0FlashloanToDrai"
solodit_id: "27505"
solodit_slug: |
    h-15-attacker-can-specify-any-receiver-in-usd0flashloan-to-drain-receiver-balance-code4rena-tapioca-dao-tapioca-dao-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(flashLoan|receiver).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
