skeleton: "name_match_missing_require"
name: "buffer-over-read-in-erc1410-getdestinationpartition-fixed"
class_name: "BufferOverReadInErc1410GetdestinationpartitionFixed"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #13687 (ConsenSys/Codefi ERC1400 Assessment)"
help: "Buffer over-read in ERC1410._getDestinationPartition ✓ Fixed"
wiki_title: "Buffer over-read in ERC1410._getDestinationPartition ✓ Fixed"
wiki_description: |
    #### Resolution
    
    
    
    This is fixed in [ConsenSys/ERC1400#16](https://github.com/ConsenSys/ERC1400/pull/16).
    
    
    #### Description
    
    
    There’s no check that `data` is at least 64 bytes long, so the following code can read past the end of `data`:
    
    
    **code/contracts/token/ERC1410/ERC1410.sol:L348-L361**
    
    
    
    ``
wiki_exploit_scenario: |
    Per Solodit #13687: #### Resolution
    
    
    
    This is fixed in [ConsenSys/ERC1400#16](https://github.com/ConsenSys/ERC1400/pull/16).
    
    
    #### Description
    
    
    There’s no check that `data` is at least 64 bytes long, so the following
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BufferOverReadInErc1410GetdestinationpartitionFixed"
solodit_id: "13687"
solodit_slug: |
    buffer-over-read-in-erc1410_getdestinationpartition-fixed-consensys-codefi-erc1400-assessment-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal data;"
fn_name_regex: ".*(data|operatorData).*"
write_var_regex: ".*(data|operatorData).*"
guard_var_regex: ".*(data|operatorData).*"
vuln_fn_name: "data"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "data = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
