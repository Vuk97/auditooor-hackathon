skeleton: "highlevelcall_missing_sibling"
name: "m-03-the-gateway-query-function-does-not-return-excess-fees-for-"
class_name: "M03TheGatewayQueryFunctionDoesNotReturnExcessFeesFor"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #44047 (Shieldify/Futaba)"
help: "[M-03] The `Gateway.query()` Function Does Not Return Excess Fees for Executing `Query` Request"
wiki_title: "[M-03] The `Gateway.query()` Function Does Not Return Excess Fees for Executing `Query` Request"
wiki_description: |
    ## Severity
    
    Medium Risk
    
    ## Description
    
    In the `query()` function, the contract `Gateway.sol` requires the users to pay a `estimateFee()` for executing a query.
    
    However, it does not handle the situation where the user pays more `msg.value` than the required `estimateFee()`. Currently, if `msg.val
wiki_exploit_scenario: |
    Per Solodit #44047: ## Severity
    
    Medium Risk
    
    ## Description
    
    In the `query()` function, the contract `Gateway.sol` requires the users to pay a `estimateFee()` for executing a query.
    
    However, it does not handle the situ
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03TheGatewayQueryFunctionDoesNotReturnExcessFeesFor"
solodit_id: "44047"
solodit_slug: |
    m-03-the-gatewayquery-function-does-not-return-excess-fees-for-executing-query-request-shieldify-none-futaba-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(query|estimateFee|Query).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
