skeleton: "state_write_without_paired_write"
name: "m-10-owner-in-vaultproxy-sol-is-address-0"
class_name: "M10OwnerInVaultproxySolIsAddress0"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20183 (Code4rena/Stader Labs)"
help: "[M-10] Owner in `VaultProxy.sol ` is `address(0)`"
wiki_title: "[M-10] Owner in `VaultProxy.sol ` is `address(0)`"
wiki_description: |
    The owner variable in the `VaultProxy.sol` is going to be zero because of a bug in `staderConfig`.
    The `intialize` function in `staderConfig` grants the admin role to an admin passed in the native `_grantRole()`, but does not update the mapping that `getAdmin` reads from.
    
    ```solidity
        function
wiki_exploit_scenario: |
    Per Solodit #20183: The owner variable in the `VaultProxy.sol` is going to be zero because of a bug in `staderConfig`.
    The `intialize` function in `staderConfig` grants the admin role to an admin passed in the native `_
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M10OwnerInVaultproxySolIsAddress0"
solodit_id: "20183"
solodit_slug: "m-10-owner-in-vaultproxysol-is-address0-code4rena-stader-labs-stader-labs-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(staderConfig|intialize|getAdmin|updateAdmin).*"
primary_var_regex: ".*(getAdmin|intialize|staderConfig).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
