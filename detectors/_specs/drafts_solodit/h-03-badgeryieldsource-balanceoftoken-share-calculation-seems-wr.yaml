skeleton: "name_match_missing_call"
name: "h-03-badgeryieldsource-balanceoftoken-share-calculation-seems-wr"
class_name: "H03BadgeryieldsourceBalanceoftokenShareCalculationSeemsWr"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #25473 (Code4rena/PoolTogether)"
help: "[H-03] `BadgerYieldSource` `balanceOfToken` share calculation seems wrong"
wiki_title: "[H-03] `BadgerYieldSource` `balanceOfToken` share calculation seems wrong"
wiki_description: |
    _Submitted by cmichel_
    
    When suppling to the `BadgerYieldSource`, some `amount` of `badger` is deposited to `badgerSett` and one receives `badgerSett` share tokens in return which are stored in the `balances` mapping of the user. So far this is correct.
    
    The `balanceOfToken` function should then ret
wiki_exploit_scenario: |
    Per Solodit #25473: _Submitted by cmichel_
    
    When suppling to the `BadgerYieldSource`, some `amount` of `badger` is deposited to `badgerSett` and one receives `badgerSett` share tokens in return which are stored in the `b
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03BadgeryieldsourceBalanceoftokenShareCalculationSeemsWr"
solodit_id: "25473"
solodit_slug: |
    h-03-badgeryieldsource-balanceoftoken-share-calculation-seems-wrong-code4rena-pooltogether-pooltogether-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(BadgerYieldSource|amount|badger|badgerSett).*"
read_var_regex: ".*(amount|badger|badgerSett).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "BadgerYieldSource"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amount > 0;"
