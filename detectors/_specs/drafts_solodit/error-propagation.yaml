skeleton: "name_match_missing_call"
name: "error-propagation"
class_name: "ErrorPropagation"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #11829 (OpenZeppelin/Compound Audit)"
help: "Error Propagation"
wiki_title: "Error Propagation"
wiki_description: |
    The codebase uses an error propagation mechanism that works as follows:
    
    
    * A function that is supposed to return value `X`, instead returns a tuple `(NO_ERROR, X)`
    * A function that would normally error out instead returns a tuple `(ERROR_NAME, 0)`
    * Note that in both cases, the first return value
wiki_exploit_scenario: |
    Per Solodit #11829: The codebase uses an error propagation mechanism that works as follows:
    
    
    * A function that is supposed to return value `X`, instead returns a tuple `(NO_ERROR, X)`
    * A function that would normally er
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ErrorPropagation"
solodit_id: "11829"
solodit_slug: "error-propagation-openzeppelin-compound-audit-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal enum;"
fn_name_regex: ".*(enum|Error).*"
read_var_regex: ".*(enum).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "enum"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return enum > 0;"
