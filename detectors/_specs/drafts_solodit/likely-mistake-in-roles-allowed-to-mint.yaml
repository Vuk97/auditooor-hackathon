skeleton: "name_match_missing_call"
name: "likely-mistake-in-roles-allowed-to-mint"
class_name: "LikelyMistakeInRolesAllowedToMint"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #28062 (MixBytes/Fantium)"
help: "Likely mistake in roles allowed to mint()"
wiki_title: "Likely mistake in roles allowed to mint()"
wiki_description: |
    ##### Description
    At FantiumMinterV1 function mint() has the following lines:
    
    ```
    // sender must be KYCed or Admin or Manager
            if (
                !hasRole(PLATFORM_MANAGER_ROLE, msg.sender) ||
                !hasRole(DEFAULT_ADMIN_ROLE, msg.sender)
            ) {
                require(isAddressKYCed(m
wiki_exploit_scenario: |
    Per Solodit #28062: ##### Description
    At FantiumMinterV1 function mint() has the following lines:
    
    ```
    // sender must be KYCed or Admin or Manager
            if (
                !hasRole(PLATFORM_MANAGER_ROLE, msg.sender) ||
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LikelyMistakeInRolesAllowedToMint"
solodit_id: "28062"
solodit_slug: "likely-mistake-in-roles-allowed-to-mint-mixbytes-none-fantium-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(hasRole|isAddressKYCed).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "hasRole"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
