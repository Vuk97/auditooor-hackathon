skeleton: "name_match_missing_call"
name: "m-02-the-voterupgradeablev2-createv3gauge-function-incorrectly-u"
class_name: "M02TheVoterupgradeablev2Createv3gaugeFunctionIncorrectlyU"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #41462 (Code4rena/Fenix Finance)"
help: |
    [M-02] The `VoterUpgradeableV2.createV3Gauge` function incorrectly uses `v2GaugeFactory` instead of `v3GaugeFactory`
wiki_title: |
    [M-02] The `VoterUpgradeableV2.createV3Gauge` function incorrectly uses `v2GaugeFactory` instead of `v3GaugeFactory`
wiki_description: |
    The gauges for the V3 pool are managed incorrectly by `v2GaugeFactory` rather than `v3GaugeFactory`.
    
    ### Proof of Concept
    
    In the `VoterUpgradeableV2.createV3Gauge` function, `v2GaugeFactory` is used instead of the appropriate `v3GaugeFactory`.
    
    ```solidity
    File: contracts\core\VoterUpgradeableV2.
wiki_exploit_scenario: |
    Per Solodit #41462: The gauges for the V3 pool are managed incorrectly by `v2GaugeFactory` rather than `v3GaugeFactory`.
    
    ### Proof of Concept
    
    In the `VoterUpgradeableV2.createV3Gauge` function, `v2GaugeFactory` is use
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02TheVoterupgradeablev2Createv3gaugeFunctionIncorrectlyU"
solodit_id: "41462"
solodit_slug: |
    m-02-the-voterupgradeablev2createv3gauge-function-incorrectly-uses-v2gaugefactory-instead-of-v3gaugefactory-code4rena-fenix-finance-fenix-finance-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal creategauge;"
fn_name_regex: ".*(v2GaugeFactory|v3GaugeFactory|GaugeFactoryUpgradeable|defaultBlastGovernor).*"
read_var_regex: ".*(createGauge|defaultBlastGovernor|merklGaugeMiddleman).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "v2GaugeFactory"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return creategauge > 0;"
