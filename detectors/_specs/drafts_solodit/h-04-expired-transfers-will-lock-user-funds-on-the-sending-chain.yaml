skeleton: "name_match_missing_call"
name: "h-04-expired-transfers-will-lock-user-funds-on-the-sending-chain"
class_name: "H04ExpiredTransfersWillLockUserFundsOnTheSendingChain"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #25505 (Code4rena/Connext)"
help: "[H-04] Expired transfers will lock user funds on the sending chain"
wiki_title: "[H-04] Expired transfers will lock user funds on the sending chain"
wiki_description: |
    _Submitted by 0xRajeev_
    
    The cancelling relayer is being paid in `receivingAssetId` on the `sendingChain` instead of in `sendingAssetID`. If the user relies on a relayer to cancel transactions, and that `receivingAssetId` asset does not exist on the sending chain (assuming only `sendingAssetID` on t
wiki_exploit_scenario: |
    Per Solodit #25505: _Submitted by 0xRajeev_
    
    The cancelling relayer is being paid in `receivingAssetId` on the `sendingChain` instead of in `sendingAssetID`. If the user relies on a relayer to cancel transactions, and th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H04ExpiredTransfersWillLockUserFundsOnTheSendingChain"
solodit_id: "25505"
solodit_slug: "h-04-expired-transfers-will-lock-user-funds-on-the-sending-chain-code4rena-connext-connext-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal receivingassetid;"
fn_name_regex: ".*(receivingAssetId|sendingChain|sendingAssetID|sendingAssetId).*"
read_var_regex: ".*(receivingAssetId|sendingAssetID|sendingAssetId).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "receivingAssetId"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return receivingassetid > 0;"
