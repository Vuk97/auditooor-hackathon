skeleton: "name_match_missing_call"
name: "m-03-votes-can-be-arbitrarily-extended-by-manager-until-they-mee"
class_name: "M03VotesCanBeArbitrarilyExtendedByManagerUntilTheyMee"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #18703 (ZachObront/Optimismgovernor.Md)"
help: "[M-03] Votes can be arbitrarily extended by Manager until they meet quorum"
wiki_title: "[M-03] Votes can be arbitrarily extended by Manager until they meet quorum"
wiki_description: |
    In `OptimismGovernorV5.sol`, new proposals are created with a `snapshot` (start time) and `deadline` (end time):
    
    ```solidity
    uint64 snapshot = block.number.toUint64() + votingDelay().toUint64();
    uint64 deadline = snapshot + votingPeriod().toUint64();
    
    proposal.voteStart.setDeadline(snapshot);
    propo
wiki_exploit_scenario: |
    Per Solodit #18703: In `OptimismGovernorV5.sol`, new proposals are created with a `snapshot` (start time) and `deadline` (end time):
    
    ```solidity
    uint64 snapshot = block.number.toUint64() + votingDelay().toUint64();
    uint
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03VotesCanBeArbitrarilyExtendedByManagerUntilTheyMee"
solodit_id: "18703"
solodit_slug: |
    m-03-votes-can-be-arbitrarily-extended-by-manager-until-they-meet-quorum-zachobront-none-optimismgovernormd-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal deadline;"
fn_name_regex: ".*(snapshot|deadline|votingModule|quorum).*"
read_var_regex: ".*(deadline|proposalDeadline|quorum).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "snapshot"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return deadline > 0;"
