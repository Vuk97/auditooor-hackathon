skeleton: "highlevelcall_missing_sibling"
name: "m-12-admin-configuration-isallowedforcollateral-mode-pool-can-be"
class_name: "M12AdminConfigurationIsallowedforcollateralModePoolCanBe"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #29603 (Code4rena/INIT Capital)"
help: |
    [M-12] Admin configuration isAllowedForCollateral(mode, pool) can be bypassed by donating asset to the pool directly and then trigger sync cash via flashloan
wiki_title: |
    [M-12] Admin configuration isAllowedForCollateral(mode, pool) can be bypassed by donating asset to the pool directly and then trigger sync cash via flashloan
wiki_description: |
    In the current implementation, after user deposits funds into lending pool and mint lending pool shares, user can call collateralize function to add collateral:
    
    ```solidity
        function collateralize(uint _posId, address _pool) public virtual onlyAuthorized(_posId) nonReentrant {
            IConfig _
wiki_exploit_scenario: |
    Per Solodit #29603: In the current implementation, after user deposits funds into lending pool and mint lending pool shares, user can call collateralize function to add collateral:
    
    ```solidity
        function collateraliz
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M12AdminConfigurationIsallowedforcollateralModePoolCanBe"
solodit_id: "29603"
solodit_slug: |
    m-12-admin-configuration-isallowedforcollateralmode-pool-can-be-bypassed-by-donating-asset-to-the-pool-directly-and-then-trigger-sync-cash-via-flashloan-code4rena-init-capital-init-capital-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(onlyAuthorized|nonReentrant|getModeStatus|canCollateralize).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
