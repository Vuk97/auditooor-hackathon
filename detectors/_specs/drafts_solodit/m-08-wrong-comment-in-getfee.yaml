skeleton: "name_match_missing_require"
name: "m-08-wrong-comment-in-getfee"
class_name: "M08WrongCommentInGetfee"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42399 (Code4rena/Yeti Finance)"
help: "[M-08] Wrong comment in `getFee`"
wiki_title: "[M-08] Wrong comment in `getFee`"
wiki_description: |
    _Submitted by cmichel_
    
    The `ThreePieceWiseLinearPriceCurve.getFee` comment states that the total + the input must be less than the cap:
    
    > If dollarCap == 0, then it is not capped. Otherwise, **then the total + the total input** must be less than the cap.
    
    The code only checks if the input is less
wiki_exploit_scenario: |
    Per Solodit #42399: _Submitted by cmichel_
    
    The `ThreePieceWiseLinearPriceCurve.getFee` comment states that the total + the input must be less than the cap:
    
    > If dollarCap == 0, then it is not capped. Otherwise, **then
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M08WrongCommentInGetfee"
solodit_id: "42399"
solodit_slug: "m-08-wrong-comment-in-getfee-code4rena-yeti-finance-yeti-finance-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getFee).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getFee"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
