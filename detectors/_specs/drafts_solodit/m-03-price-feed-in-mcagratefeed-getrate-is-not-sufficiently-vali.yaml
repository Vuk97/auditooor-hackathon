skeleton: "state_write_without_paired_write"
name: "m-03-price-feed-in-mcagratefeed-getrate-is-not-sufficiently-vali"
class_name: "M03PriceFeedInMcagratefeedGetrateIsNotSufficientlyVali"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #19900 (Code4rena/KUMA Protocol)"
help: "[M-03] Price feed in `MCAGRateFeed#getRate` is not sufficiently validated and can return stale price"
wiki_title: "[M-03] Price feed in `MCAGRateFeed#getRate` is not sufficiently validated and can return stale price"
wiki_description: |
    `MCAGRateFeed#getRate` may return stale data.
    
    ### Proof of Concept
    
            (, int256 answer,,,) = oracle.latestRoundData();
    
    getRate only uses answer but never checks the freshness of the data, which can lead to stale bond pricing data. Stale pricing data can lead to bonds being bought and sold o
wiki_exploit_scenario: |
    Per Solodit #19900: `MCAGRateFeed#getRate` may return stale data.
    
    ### Proof of Concept
    
            (, int256 answer,,,) = oracle.latestRoundData();
    
    getRate only uses answer but never checks the freshness of the data, whi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03PriceFeedInMcagratefeedGetrateIsNotSufficientlyVali"
solodit_id: "19900"
solodit_slug: |
    m-03-price-feed-in-mcagratefeedgetrate-is-not-sufficiently-validated-and-can-return-stale-price-code4rena-kuma-protocol-kuma-protocol-versus-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(updatedAt).*"
primary_var_regex: ".*(updatedAt).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
