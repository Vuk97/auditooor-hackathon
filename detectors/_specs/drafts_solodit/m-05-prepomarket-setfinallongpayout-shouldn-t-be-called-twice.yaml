skeleton: "name_match_missing_call"
name: "m-05-prepomarket-setfinallongpayout-shouldn-t-be-called-twice"
class_name: "M05PrepomarketSetfinallongpayoutShouldnTBeCalledTwice"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #6067 (Code4rena/prePO)"
help: "[M-05] `PrePOMarket.setFinalLongPayout()` shouldn't be called twice."
wiki_title: "[M-05] `PrePOMarket.setFinalLongPayout()` shouldn't be called twice."
wiki_description: |
    If `finalLongPayout` is changed twice by admin fault, the market would be insolvent as it should pay more collateral than it has.
    
    ### Proof of Concept
    
    If `finalLongPayout` is less than `MAX_PAYOUT`, it means the market is ended and `longToken Price = finalLongPayout, shortToken Price = MAX_PAYOUT
wiki_exploit_scenario: |
    Per Solodit #6067: If `finalLongPayout` is changed twice by admin fault, the market would be insolvent as it should pay more collateral than it has.
    
    ### Proof of Concept
    
    If `finalLongPayout` is less than `MAX_PAYOUT`
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M05PrepomarketSetfinallongpayoutShouldnTBeCalledTwice"
solodit_id: "6067"
solodit_slug: "m-05-prepomarketsetfinallongpayout-shouldnt-be-called-twice-code4rena-prepo-prepo-contest-git"
solodit_quality: "5"
solodit_rarity: "4"
solodit_tags: "Business Logic"
state_decl: "uint256 internal collateral;"
fn_name_regex: ".*(finalLongPayout|MAX_PAYOUT|mint|setFinalLongPayout).*"
read_var_regex: ".*(collateral|collaterals|finalLongPayout).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "finalLongPayout"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return collateral > 0;"
