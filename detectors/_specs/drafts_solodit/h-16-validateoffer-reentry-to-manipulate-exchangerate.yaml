skeleton: "name_match_missing_call"
name: "h-16-validateoffer-reentry-to-manipulate-exchangerate"
class_name: "H16ValidateofferReentryToManipulateExchangerate"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #35218 (Code4rena/Gondi)"
help: "[H-16] `validateOffer()` reentry to manipulate `exchangeRate`"
wiki_title: "[H-16] `validateOffer()` reentry to manipulate `exchangeRate`"
wiki_description: |
    The current mechanism of `validateOffer()` is to first book `_outstandingValues` to increase, but `assets.balanceOf(address(this))` doesn't decrease immediately.
    
    ```solidity
        function validateOffer(bytes calldata _offer, uint256 _protocolFee) external override onlyAcceptedCallers {
    ..
wiki_exploit_scenario: |
    Per Solodit #35218: The current mechanism of `validateOffer()` is to first book `_outstandingValues` to increase, but `assets.balanceOf(address(this))` doesn't decrease immediately.
    
    ```solidity
        function validateOff
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H16ValidateofferReentryToManipulateExchangerate"
solodit_id: "35218"
solodit_slug: "h-16-validateoffer-reentry-to-manipulate-exchangerate-code4rena-gondi-gondi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal exchangerate;"
fn_name_regex: ".*(validateOffer|totalAssets|totalSupply|exchangeRate).*"
read_var_regex: ".*(exchangeRate|totalAssets|totalSupply).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "validateOffer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return exchangerate > 0;"
