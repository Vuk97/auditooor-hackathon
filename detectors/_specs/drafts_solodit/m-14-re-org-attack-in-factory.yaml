skeleton: "name_match_missing_call"
name: "m-14-re-org-attack-in-factory"
class_name: "M14ReOrgAttackInFactory"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20035 (Code4rena/Frankencoin)"
help: "[M-14] Re-org attack in factory"
wiki_title: "[M-14] Re-org attack in factory"
wiki_description: |
    The `createClone` function deploys a clone contract using the create, where the address derivation depends only on the `PositionFactory` nonce.
    
    Re-orgs can happen in all EVM chains. In ethereum, where currently Frankencoin is deployed, it is not "super common" but it still happens, being the last
wiki_exploit_scenario: |
    Per Solodit #20035: The `createClone` function deploys a clone contract using the create, where the address derivation depends only on the `PositionFactory` nonce.
    
    Re-orgs can happen in all EVM chains. In ethereum, whe
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M14ReOrgAttackInFactory"
solodit_id: "20035"
solodit_slug: "m-14-re-org-attack-in-factory-code4rena-frankencoin-frankencoin-git"
solodit_quality: "3"
solodit_rarity: "4"
solodit_tags: "Chain Reorganization Attack"
state_decl: "uint256 internal cloneposition;"
fn_name_regex: ".*(createClone|PositionFactory|clonePosition).*"
read_var_regex: ".*(clonePosition|createClone).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "createClone"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return cloneposition > 0;"
