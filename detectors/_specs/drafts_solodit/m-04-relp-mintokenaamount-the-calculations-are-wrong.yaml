skeleton: "paired_function_divergence"
name: "m-04-relp-mintokenaamount-the-calculations-are-wrong"
class_name: "M04RelpMintokenaamountTheCalculationsAreWrong"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #29466 (Code4rena/Dopex)"
help: "[M-04] reLP() mintokenAAmount the calculations are wrong"
wiki_title: "[M-04] reLP() mintokenAAmount the calculations are wrong"
wiki_description: |
    in `reLP()` , Calculating ` mintokenAAmount  ` using the wrong formula can invalidate the slippage protection
    
    ### Proof of Concept
    
    `ReLPContract.reLP()`The implementation is as follows:
    
    ```solidity
      function reLP(uint256 _amount) external onlyRole(RDPXV2CORE_ROLE) {
    ...
    
    @>  mintokenAAmount =
    @
wiki_exploit_scenario: |
    Per Solodit #29466: in `reLP()` , Calculating ` mintokenAAmount  ` using the wrong formula can invalidate the slippage protection
    
    ### Proof of Concept
    
    `ReLPContract.reLP()`The implementation is as follows:
    
    ```solidit
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04RelpMintokenaamountTheCalculationsAreWrong"
solodit_id: "29466"
solodit_slug: "m-04-relp-mintokenaamount-the-calculations-are-wrong-code4rena-dopex-dopex-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: "Wrong Math"
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(amountB|mintokenAAmount|reLP).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
