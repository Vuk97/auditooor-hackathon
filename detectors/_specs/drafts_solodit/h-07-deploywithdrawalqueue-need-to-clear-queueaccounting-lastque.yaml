skeleton: "name_match_missing_require"
name: "h-07-deploywithdrawalqueue-need-to-clear-queueaccounting-lastque"
class_name: "H07DeploywithdrawalqueueNeedToClearQueueaccountingLastque"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #35209 (Code4rena/Gondi)"
help: "[H-07] `deployWithdrawalQueue()` need to clear `_queueAccounting[lastQueueIndex]`"
wiki_title: "[H-07] `deployWithdrawalQueue()` need to clear `_queueAccounting[lastQueueIndex]`"
wiki_description: |
    In `deployWithdrawalQueue()`, only clears `_queueOutstandingValues[lastQueueIndex]` and `_outstandingValues`, but doesn't clear `_queueAccounting[lastQueueIndex]`.
    
    ```solidity
        function deployWithdrawalQueue() external nonReentrant {
    ...
    
            /// @dev We move outstanding values from the poo
wiki_exploit_scenario: |
    Per Solodit #35209: In `deployWithdrawalQueue()`, only clears `_queueOutstandingValues[lastQueueIndex]` and `_outstandingValues`, but doesn't clear `_queueAccounting[lastQueueIndex]`.
    
    ```solidity
        function deployWit
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H07DeploywithdrawalqueueNeedToClearQueueaccountingLastque"
solodit_id: "35209"
solodit_slug: "h-07-deploywithdrawalqueue-need-to-clear-_queueaccountinglastqueueindex-code4rena-gondi-gondi-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal deploywithdrawalqueue;"
fn_name_regex: ".*(deployWithdrawalQueue|queueClaimAll).*"
write_var_regex: ".*(deployWithdrawalQueue|queueClaimAll).*"
guard_var_regex: ".*(deployWithdrawalQueue|queueClaimAll).*"
vuln_fn_name: "deployWithdrawalQueue"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "deploywithdrawalqueue = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
