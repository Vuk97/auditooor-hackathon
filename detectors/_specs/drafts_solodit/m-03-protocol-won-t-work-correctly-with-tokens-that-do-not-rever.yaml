skeleton: "name_match_missing_call"
name: "m-03-protocol-won-t-work-correctly-with-tokens-that-do-not-rever"
class_name: "M03ProtocolWonTWorkCorrectlyWithTokensThatDoNotRever"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20553 (Pashov Audit Group/Rolling Dutch Auction)"
help: "[M-03] Protocol won't work correctly with tokens that do not revert on failed `transfer`"
wiki_title: "[M-03] Protocol won't work correctly with tokens that do not revert on failed `transfer`"
wiki_description: |
    **Impact:**
    High, as it can lead to a loss of value
    
    **Likelihood:**
    Low, as such tokens are not so common
    
    **Description**
    
    Some tokens do not revert on failure in `transfer` or `transferFrom` but instead return `false` (example is [ZRX](https://etherscan.io/address/0xe41d2489571d322189246dafa5ebde
wiki_exploit_scenario: |
    Per Solodit #20553: **Impact:**
    High, as it can lead to a loss of value
    
    **Likelihood:**
    Low, as such tokens are not so common
    
    **Description**
    
    Some tokens do not revert on failure in `transfer` or `transferFrom` but in
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03ProtocolWonTWorkCorrectlyWithTokensThatDoNotRever"
solodit_id: "20553"
solodit_slug: |
    m-03-protocol-wont-work-correctly-with-tokens-that-do-not-revert-on-failed-transfer-pashov-none-rolling-dutch-auction-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal safe;"
fn_name_regex: ".*(transfer|transferFrom|SafeERC20|safe).*"
read_var_regex: ".*(safe|transfer|transferFrom).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transfer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return safe > 0;"
