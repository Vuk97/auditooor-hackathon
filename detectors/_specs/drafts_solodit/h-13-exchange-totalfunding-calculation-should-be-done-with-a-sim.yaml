skeleton: "name_match_missing_call"
name: "h-13-exchange-totalfunding-calculation-should-be-done-with-a-sim"
class_name: "H13ExchangeTotalfundingCalculationShouldBeDoneWithASim"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #20236 (Code4rena/Polynomial Protocol)"
help: |
    [H-13] Exchange: `totalFunding` calculation should be done with a simple multiplication operation instead of a `wadMul` operation
wiki_title: |
    [H-13] Exchange: `totalFunding` calculation should be done with a simple multiplication operation instead of a `wadMul` operation
wiki_description: |
    `wad` operations are meant to be done with int/uint which represent numbers with 18 decimals.
    
    While the funding rate follows this representation, a simple time difference does not.
    
    In `Exchange.getMarkPrice` as well as in `Exchange._updateFundingRate` a `wadMul` operation is done to multiply the
wiki_exploit_scenario: |
    Per Solodit #20236: `wad` operations are meant to be done with int/uint which represent numbers with 18 decimals.
    
    While the funding rate follows this representation, a simple time difference does not.
    
    In `Exchange.get
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H13ExchangeTotalfundingCalculationShouldBeDoneWithASim"
solodit_id: "20236"
solodit_slug: |
    h-13-exchange-totalfunding-calculation-should-be-done-with-a-simple-multiplication-operation-instead-of-a-wadmul-operation-code4rena-polynomial-protocol-polynomial-protocol-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal normalizationfactor;"
fn_name_regex: ".*(wad|wadMul|normalizationFactor|totalFunding).*"
read_var_regex: ".*(normalizationFactor|totalFunding|wad).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "wad"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return normalizationfactor > 0;"
