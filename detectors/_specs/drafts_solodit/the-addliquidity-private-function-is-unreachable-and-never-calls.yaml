skeleton: "paired_function_divergence"
name: "the-addliquidity-private-function-is-unreachable-and-never-calls"
class_name: "TheAddliquidityPrivateFunctionIsUnreachableAndNeverCalls"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #51659 (Halborn/Qoda DAO)"
help: "The `addLiquidity` private function is unreachable and never calls `uniswapV2Router.addLiquidity`"
wiki_title: "The `addLiquidity` private function is unreachable and never calls `uniswapV2Router.addLiquidity`"
wiki_description: |
    ##### Description
    
    The overriding `_update` function in the `QodaToken` contract performs a call to the `swapBack` private function when certain conditions are met:
    
    **- Function** `_update` **- src/QodaToken.sol [Lines: 296-305]**
    
    ```
            if (
                canSwap && swapEnabled && !swapping &&
wiki_exploit_scenario: |
    Per Solodit #51659: ##### Description
    
    The overriding `_update` function in the `QodaToken` contract performs a call to the `swapBack` private function when certain conditions are met:
    
    **- Function** `_update` **- src/Q
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheAddliquidityPrivateFunctionIsUnreachableAndNeverCalls"
solodit_id: "51659"
solodit_slug: |
    the-addliquidity-private-function-is-unreachable-and-never-calls-uniswapv2routeraddliquidity-halborn-steadily-consulting-inc-qoda-dao-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(addLiquidity|liquidityTokens|swapBack).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
