skeleton: "name_match_missing_require"
name: "m-02-use-safetransfer-safetransferfrom-consistently-instead-of-t"
class_name: "M02UseSafetransferSafetransferfromConsistentlyInsteadOfT"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42184 (Code4rena/NFTX)"
help: "[M-02] Use `safeTransfer`/`safeTransferFrom` consistently instead of `transfer`/`transferFrom`"
wiki_title: "[M-02] Use `safeTransfer`/`safeTransferFrom` consistently instead of `transfer`/`transferFrom`"
wiki_description: |
    It is good to add a `require()` statement that checks the return value of token transfers, or to use something like OpenZeppelin’s `safeTransfer`/`safeTransferFrom` unless one is sure the given token reverts in case of a failure. Failure to do so will cause silent failures of transfers and affect t
wiki_exploit_scenario: |
    Per Solodit #42184: It is good to add a `require()` statement that checks the return value of token transfers, or to use something like OpenZeppelin’s `safeTransfer`/`safeTransferFrom` unless one is sure the given token
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M02UseSafetransferSafetransferfromConsistentlyInsteadOfT"
solodit_id: "42184"
solodit_slug: |
    m-02-use-safetransfersafetransferfrom-consistently-instead-of-transfertransferfrom-code4rena-nftx-nftx-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal require;"
fn_name_regex: ".*(require|safeTransfer|safeTransferFrom|FeeDistributor).*"
write_var_regex: ".*(require|safeTransfer|safeTransferFrom).*"
guard_var_regex: ".*(require|safeTransfer|safeTransferFrom).*"
vuln_fn_name: "require"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "require = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
