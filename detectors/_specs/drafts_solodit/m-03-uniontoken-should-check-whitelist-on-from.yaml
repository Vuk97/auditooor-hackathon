skeleton: "name_match_missing_call"
name: "m-03-uniontoken-should-check-whitelist-on-from"
class_name: "M03UniontokenShouldCheckWhitelistOnFrom"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #25596 (Code4rena/Union Finance)"
help: "[M-03] `UnionToken` should check whitelist on `from`?"
wiki_title: "[M-03] `UnionToken` should check whitelist on `from`?"
wiki_description: |
    _Submitted by cmichel_
    
    The `UnionToken` can check for a whitelist on each transfer in `_beforeTokenTransfer`:
    
    ```solidity
    if (whitelistEnabled) {
        require(isWhitelisted(msg.sender) || to == address(0), "Whitelistable: address not whitelisted");
    }
    ```
    
    This whitelist is checked on `msg.sender` n
wiki_exploit_scenario: |
    Per Solodit #25596: _Submitted by cmichel_
    
    The `UnionToken` can check for a whitelist on each transfer in `_beforeTokenTransfer`:
    
    ```solidity
    if (whitelistEnabled) {
        require(isWhitelisted(msg.sender) || to == addre
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M03UniontokenShouldCheckWhitelistOnFrom"
solodit_id: "25596"
solodit_slug: "m-03-uniontoken-should-check-whitelist-on-from-code4rena-union-finance-union-finance-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal from;"
fn_name_regex: ".*(UnionToken|from).*"
read_var_regex: ".*(from).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "UnionToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return from > 0;"
