skeleton: "name_match_missing_call"
name: "m-04-liquiditypool-getdelta-and-liquiditypool-calculatemargin-fu"
class_name: "M04LiquiditypoolGetdeltaAndLiquiditypoolCalculatemarginFu"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #20241 (Code4rena/Polynomial Protocol)"
help: |
    [M-04] `LiquidityPool._getDelta` and `LiquidityPool._calculateMargin` functions should execute `require(!isInvalid && spotPrice > 0)` instead of `require(!isInvalid || spotPrice >
wiki_title: |
    [M-04] `LiquidityPool._getDelta` and `LiquidityPool._calculateMargin` functions should execute `require(!isInvalid && spotPrice > 0)` instead of `require(!isInv
wiki_description: |
    The following `KangarooVault.transferPerpMargin` and `KangarooVault._openPosition` functions execute `require(!isInvalid && baseAssetPrice != 0)`, where `isInvalid` and `baseAssetPrice` are returned from calling `LIQUIDITY_POOL.baseAssetPrice()`. Because `LIQUIDITY_POOL.baseAssetPrice()` returns th
wiki_exploit_scenario: |
    Per Solodit #20241: The following `KangarooVault.transferPerpMargin` and `KangarooVault._openPosition` functions execute `require(!isInvalid && baseAssetPrice != 0)`, where `isInvalid` and `baseAssetPrice` are returned
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M04LiquiditypoolGetdeltaAndLiquiditypoolCalculatemarginFu"
solodit_id: "20241"
solodit_slug: |
    m-04-liquiditypool_getdelta-and-liquiditypool_calculatemargin-functions-should-execute-requireisinvalid-spotprice-0-instead-of-requireisinvalid-spotprice-0-code4rena-polynomial-protocol-polynomial-protocol-contest-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal baseassetprice;"
fn_name_regex: ".*(require|isInvalid|baseAssetPrice|spotPrice).*"
read_var_regex: ".*(baseAssetPrice|isInvalid|require).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "require"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return baseassetprice > 0;"
