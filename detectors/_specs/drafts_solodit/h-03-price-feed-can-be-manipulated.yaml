skeleton: "highlevelcall_missing_sibling"
name: "h-03-price-feed-can-be-manipulated"
class_name: "H03PriceFeedCanBeManipulated"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #3858 (Code4rena/Marginswap)"
help: "[H-03] Price feed can be manipulated"
wiki_title: "[H-03] Price feed can be manipulated"
wiki_description: |
    Anyone can trigger an update to the price feed by calling `PriceAware.getCurrentPriceInPeg(token, inAmount, forceCurBlock=true)`.
    If the update window has passed, the price will be computed by simulating a Uniswap-like trade with the amounts.
    This simulation uses the reserves of the Uniswap pairs w
wiki_exploit_scenario: |
    Per Solodit #3858: Anyone can trigger an update to the price feed by calling `PriceAware.getCurrentPriceInPeg(token, inAmount, forceCurBlock=true)`.
    If the update window has passed, the price will be computed by simula
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H03PriceFeedCanBeManipulated"
solodit_id: "3858"
solodit_slug: "h-03-price-feed-can-be-manipulated-code4rena-marginswap-marginswap-contest-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(getCurrentPriceInPeg|inAmount|forceCurBlock).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
