skeleton: "name_match_missing_call"
name: "redeemdepositsandinternalbalances-should-mow-before-adding-migra"
class_name: "RedeemdepositsandinternalbalancesShouldMowBeforeAddingMigra"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #36283 (Codehawks/Beanstalk: The Finale)"
help: "`redeemDepositsAndInternalBalances` should mow before adding migrated deposits"
wiki_title: "`redeemDepositsAndInternalBalances` should mow before adding migrated deposits"
wiki_description: |
    ## Summary
    `redeemDepositsAndInternalBalances` should mow before adding migrated deposits
    
    ## Vulnerability Details
    The `redeemDepositsAndInternalBalances` will add `stalk`, `roots` and `bdv` to a user's profile, but will not set their necessary variables (`lastSop`, `lastUpdated` and `lastRain`) as
wiki_exploit_scenario: |
    Per Solodit #36283: ## Summary
    `redeemDepositsAndInternalBalances` should mow before adding migrated deposits
    
    ## Vulnerability Details
    The `redeemDepositsAndInternalBalances` will add `stalk`, `roots` and `bdv` to a use
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RedeemdepositsandinternalbalancesShouldMowBeforeAddingMigra"
solodit_id: "36283"
solodit_slug: |
    redeemdepositsandinternalbalances-should-mow-before-adding-migrated-deposits-codehawks-beanstalk-the-finale-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal bdv;"
fn_name_regex: ".*(redeemDepositsAndInternalBalances|stalk|roots|bdv).*"
read_var_regex: ".*(bdv|lastRain|lastSop).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "redeemDepositsAndInternalBalances"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return bdv > 0;"
