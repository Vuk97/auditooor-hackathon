skeleton: "name_match_missing_require"
name: "h-02-order-s-creator-can-update-tokenout-to-arbitrary-token"
class_name: "H02OrderSCreatorCanUpdateTokenoutToArbitraryToken"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #30258 (Code4rena/Init Capital)"
help: "[H-02] Order's creator can update `tokenOut` to arbitrary token"
wiki_title: "[H-02] Order's creator can update `tokenOut` to arbitrary token"
wiki_description: |
    When an order is created, it checks whether the `order.tokenOut` is equal to `marginPos.baseAsset` or `_tokenOut` is equal to `marginPos.quoteAsset`. However, when `tokenOut` is updated via `updateOrder`, it can be changed to an arbitrary token. This has the potential to be exploited by changing `t
wiki_exploit_scenario: |
    Per Solodit #30258: When an order is created, it checks whether the `order.tokenOut` is equal to `marginPos.baseAsset` or `_tokenOut` is equal to `marginPos.quoteAsset`. However, when `tokenOut` is updated via `updateOr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H02OrderSCreatorCanUpdateTokenoutToArbitraryToken"
solodit_id: "30258"
solodit_slug: "h-02-orders-creator-can-update-tokenout-to-arbitrary-token-code4rena-init-capital-init-capital-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: "Front-Running"
state_decl: "uint256 internal fillorder;"
fn_name_regex: ".*(tokenOut|updateOrder|fillOrder).*"
write_var_regex: ".*(fillOrder|tokenOut|updateOrder).*"
guard_var_regex: ".*(fillOrder|tokenOut|updateOrder).*"
vuln_fn_name: "tokenOut"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "fillorder = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
