skeleton: "paired_function_divergence"
name: "m-19-unclear-twaporacle-consult-algorithm"
class_name: "M19UnclearTwaporacleConsultAlgorithm"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #42354 (Code4rena/Vader Protocol)"
help: "[M-19] Unclear `TwapOracle.consult` algorithm"
wiki_title: "[M-19] Unclear `TwapOracle.consult` algorithm"
wiki_description: |
    _Submitted by cmichel_
    
    The `TWAPOracle.consult` function is unclear to the auditor.
    It seems to iterate through all registered pairs that share the `token` parameter (USDV or VADER) and then sums up the foreign token pair per `token` price.
    And divides this sum (`sumNative`) by the summed-up USD pr
wiki_exploit_scenario: |
    Per Solodit #42354: _Submitted by cmichel_
    
    The `TWAPOracle.consult` function is unclear to the auditor.
    It seems to iterate through all registered pairs that share the `token` parameter (USDV or VADER) and then sums up
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M19UnclearTwaporacleConsultAlgorithm"
solodit_id: "42354"
solodit_slug: "m-19-unclear-twaporacleconsult-algorithm-code4rena-vader-protocol-vader-protocol-git"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(sumNative|sumUSD|token).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
