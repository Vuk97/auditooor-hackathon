skeleton: "name_match_missing_call"
name: "m01-callbacks-to-wrong-address"
class_name: "M01CallbacksToWrongAddress"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #10708 (OpenZeppelin/UMA Audit – L2 Bridges)"
help: "[M01] Callbacks to wrong address"
wiki_title: "[M01] Callbacks to wrong address"
wiki_description: |
    The `SkinnyOptimisticOracle` invokes callback functions on the price requester, if they exist, so the requester can respond to significant state changes. However, the callback is incorrectly invoked on the price proposer instead of the price requester in [the `proposePriceFor` function](https://gith
wiki_exploit_scenario: |
    Per Solodit #10708: The `SkinnyOptimisticOracle` invokes callback functions on the price requester, if they exist, so the requester can respond to significant state changes. However, the callback is incorrectly invoked o
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01CallbacksToWrongAddress"
solodit_id: "10708"
solodit_slug: "m01-callbacks-to-wrong-address-openzeppelin-uma-audit-l2-bridges-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal priceproposed;"
fn_name_regex: ".*(SkinnyOptimisticOracle|proposePriceFor|priceProposed).*"
read_var_regex: ".*(priceProposed|proposePriceFor).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "SkinnyOptimisticOracle"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return priceproposed > 0;"
