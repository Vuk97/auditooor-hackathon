skeleton: "name_match_missing_require"
name: "potentially-insufficient-validation-for-operator-transfers-fixed"
class_name: "PotentiallyInsufficientValidationForOperatorTransfersFixed"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #13670 (ConsenSys/Amp)"
help: "Potentially insufficient validation for operator transfers ✓ Fixed"
wiki_title: "Potentially insufficient validation for operator transfers ✓ Fixed"
wiki_description: |
    #### Resolution
    
    
    
    removing `operatorTransferByPartition` and simplifying the interfaces to only `tranferByPartition`
    
    
    
    > 
    > This removes the existing tranferByPartition, converting operatorTransferByPartition to it.
    > The reason for this is to make the client interface simpler, where there is one
wiki_exploit_scenario: |
    Per Solodit #13670: #### Resolution
    
    
    
    removing `operatorTransferByPartition` and simplifying the interfaces to only `tranferByPartition`
    
    
    
    > 
    > This removes the existing tranferByPartition, converting operatorTransferB
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PotentiallyInsufficientValidationForOperatorTransfersFixed"
solodit_id: "13670"
solodit_slug: "potentially-insufficient-validation-for-operator-transfers-fixed-consensys-amp-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal and;"
fn_name_regex: ".*(operatorTransferByPartition|tranferByPartition|and).*"
write_var_regex: ".*(and|operatorTransferByPartition|tranferByPartition).*"
guard_var_regex: ".*(and|operatorTransferByPartition|tranferByPartition).*"
vuln_fn_name: "operatorTransferByPartition"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "and = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
