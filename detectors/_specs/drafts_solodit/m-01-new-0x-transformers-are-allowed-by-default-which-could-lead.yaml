skeleton: "highlevelcall_missing_sibling"
name: "m-01-new-0x-transformers-are-allowed-by-default-which-could-lead"
class_name: "M01New0xTransformersAreAllowedByDefaultWhichCouldLead"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #31760 (ZachObront/Sentiment 0X Aura)"
help: "[M-01] New 0x transformers are allowed by default, which could lead to unexpected problems"
wiki_title: "[M-01] New 0x transformers are allowed by default, which could lead to unexpected problems"
wiki_description: |
    When the controller checks `canCall()` for calls to the 0x contract, it begins by checking that `sig == 0x415565b0`.
    
    If this check fails (ie if any other function is called), the call is not allowed. However, if it passes, all possible calls are allowed. There is no further validation.
    
    The `transf
wiki_exploit_scenario: |
    Per Solodit #31760: When the controller checks `canCall()` for calls to the 0x contract, it begins by checking that `sig == 0x415565b0`.
    
    If this check fails (ie if any other function is called), the call is not allowed.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M01New0xTransformersAreAllowedByDefaultWhichCouldLead"
solodit_id: "31760"
solodit_slug: |
    m-01-new-0x-transformers-are-allowed-by-default-which-could-lead-to-unexpected-problems-zachobront-none-sentiment-0x-aura-markdown
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(canCall|transformERC20|exec).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
