skeleton: "state_write_without_paired_write"
name: "h-01-wrong-fee-calculation-after-totalsupply-was-0"
class_name: "H01WrongFeeCalculationAfterTotalsupplyWas0"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #19837 (Code4rena/Kuiper)"
help: "[H-01] Wrong fee calculation after `totalSupply` was 0"
wiki_title: "[H-01] Wrong fee calculation after `totalSupply` was 0"
wiki_description: |
    _Submitted by kenzo_
    
    `handleFees` does not update `lastFee` if `startSupply == 0`.
    This means that wrongly, extra fee tokens would be minted once the basket is resupplied and `handleFees` is called again.
    
    ### Impact
    
    Loss of user funds.
    The extra minting of fee tokens comes on the expense of the r
wiki_exploit_scenario: |
    Per Solodit #19837: _Submitted by kenzo_
    
    `handleFees` does not update `lastFee` if `startSupply == 0`.
    This means that wrongly, extra fee tokens would be minted once the basket is resupplied and `handleFees` is called a
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "H01WrongFeeCalculationAfterTotalsupplyWas0"
solodit_id: "19837"
solodit_slug: "h-01-wrong-fee-calculation-after-totalsupply-was-0-code4rena-kuiper-kuiper-contest-git_"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(handleFees|lastFee|totalSupply).*"
primary_var_regex: ".*(handleFees|lastFee).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
