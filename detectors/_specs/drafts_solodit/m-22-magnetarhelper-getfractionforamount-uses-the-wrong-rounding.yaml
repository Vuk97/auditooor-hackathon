skeleton: "name_match_missing_call"
name: "m-22-magnetarhelper-getfractionforamount-uses-the-wrong-rounding"
class_name: "M22MagnetarhelperGetfractionforamountUsesTheWrongRounding"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #32345 (Code4rena/Tapioca DAO)"
help: "[M-22] `MagnetarHelper.getFractionForAmount` uses the wrong rounding and will yield the wrong result"
wiki_title: "[M-22] `MagnetarHelper.getFractionForAmount` uses the wrong rounding and will yield the wrong result"
wiki_description: |
    `MagnetarHelper.getFractionForAmount` is used to determine the `fraction` (shares) that will be received when an `amount` is deposited. The code needs to compute the `totalShares` and then determine what the fraction is going to be.
    
    The logic is used in `Singularity._removeAsset` and since it's ti
wiki_exploit_scenario: |
    Per Solodit #32345: `MagnetarHelper.getFractionForAmount` is used to determine the `fraction` (shares) that will be received when an `amount` is deposited. The code needs to compute the `totalShares` and then determine
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M22MagnetarhelperGetfractionforamountUsesTheWrongRounding"
solodit_id: "32345"
solodit_slug: |
    m-22-magnetarhelpergetfractionforamount-uses-the-wrong-rounding-and-will-yield-the-wrong-result-code4rena-tapioca-dao-tapioca-dao-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(fraction|amount|totalShares|down).*"
read_var_regex: ".*(amount|down|fraction).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "fraction"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amount > 0;"
