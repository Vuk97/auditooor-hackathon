skeleton: "name_match_missing_call"
name: "incorrect-proposalmanager-clock-behaviour"
class_name: "IncorrectProposalmanagerClockBehaviour"
wave: "15"
severity: "HIGH"
confidence: "MEDIUM"
source: "Solodit #44336 (MixBytes/EYWA)"
help: "Incorrect `ProposalManager.CLOCK()` behaviour"
wiki_title: "Incorrect `ProposalManager.CLOCK()` behaviour"
wiki_description: |
    ##### Description
    The `ProposalManager` contract interacts with `EscrowManager` as a **token**, but the `EscrowManager` contract does not implement the `CLOCK()` and `CLOCK_MODE()` methods.
    
    When attempting to create a proposal using `ProposalManager.propose()`, the `clock()` method is invoked. The
wiki_exploit_scenario: |
    Per Solodit #44336: ##### Description
    The `ProposalManager` contract interacts with `EscrowManager` as a **token**, but the `EscrowManager` contract does not implement the `CLOCK()` and `CLOCK_MODE()` methods.
    
    When atte
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectProposalmanagerClockBehaviour"
solodit_id: "44336"
solodit_slug: "incorrect-proposalmanagerclock-behaviour-mixbytes-none-eywa-markdown"
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal clock;"
fn_name_regex: ".*(ProposalManager|EscrowManager|CLOCK|CLOCK_MODE).*"
read_var_regex: ".*(clock).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "ProposalManager"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return clock > 0;"
