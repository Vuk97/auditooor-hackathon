skeleton: "name_match_missing_require"
name: "m-14-encodeddata-argument-of-hashstruct-is-not-calculated-perfec"
class_name: "M14EncodeddataArgumentOfHashstructIsNotCalculatedPerfec"
wave: "15"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "Solodit #30105 (Code4rena/Collective)"
help: |
    [M-14] `encodedData` argument of `hashStruct` is not calculated perfectly for EIP712 singed messages in `CultureIndex.sol`
wiki_title: |
    [M-14] `encodedData` argument of `hashStruct` is not calculated perfectly for EIP712 singed messages in `CultureIndex.sol`
wiki_description: |
    Encoding of `encodedData` is not done correctly for the verification of EIP712 signed messages.
    
    ### Impact
    
    `CultureIndex::_verifyVoteSignature()` is called by other functions like `CultureIndex::voteForManyWithSig()` and `CultureIndex::batchVoteForManyWithSig()` for the verification of the signed
wiki_exploit_scenario: |
    Per Solodit #30105: Encoding of `encodedData` is not done correctly for the verification of EIP712 signed messages.
    
    ### Impact
    
    `CultureIndex::_verifyVoteSignature()` is called by other functions like `CultureIndex::vo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "M14EncodeddataArgumentOfHashstructIsNotCalculatedPerfec"
solodit_id: "30105"
solodit_slug: |
    m-14-encodeddata-argument-of-hashstruct-is-not-calculated-perfectly-for-eip712-singed-messages-in-cultureindexsol-code4rena-collective-collective-git
solodit_quality: "0"
solodit_rarity: "0"
solodit_tags: ""
state_decl: "uint256 internal encodeddata;"
fn_name_regex: ".*(encodedData|hashStruct|typeHash).*"
write_var_regex: ".*(encodedData|hashStruct|typeHash).*"
guard_var_regex: ".*(encodedData|hashStruct|typeHash).*"
vuln_fn_name: "encodedData"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "encodeddata = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
