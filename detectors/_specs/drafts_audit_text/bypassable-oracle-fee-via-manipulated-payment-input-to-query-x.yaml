skeleton: "name_match_missing_call"
name: "bypassable-oracle-fee-via-manipulated-payment-input-to-query-x"
class_name: "BypassableOracleFeeViaManipulatedPaymentInputToQueryX"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit eBridge AElf Bridge - Zellic Audit Report"
help: |
    In the ReportContract, the QueryOracle function is designed to handle oracle queries and collect oracle fees. However, it has been identified that the oracle fee can be skipped ent
wiki_title: "Bypassable oracle fee via manipulated payment input to QueryOracle"
wiki_description: |
    In the ReportContract, the QueryOracle function is designed to handle oracle queries and collect oracle fees. However, it has been identified that the oracle fee can be skipped entirely by passing a negative value for input.Payment that equals the fee amount. This manipulation allows an attacker to
wiki_exploit_scenario: |
    Per audit finding: In the ReportContract, the QueryOracle function is designed to handle oracle queries and collect oracle fees. However, it has been identified that the oracle fee can be skipped entirely by passing a n
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BypassableOracleFeeViaManipulatedPaymentInputToQueryX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(totalPayment).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "totalPayment"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
