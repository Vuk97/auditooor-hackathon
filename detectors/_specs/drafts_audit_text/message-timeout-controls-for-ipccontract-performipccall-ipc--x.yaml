skeleton: "name_match_missing_require"
name: "message-timeout-controls-for-ipccontract-performipccall-ipc--x"
class_name: "MessageTimeoutControlsForIpccontractPerformipccallIpcX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2025-02-recall"
help: |
    [L-11] Message timeout controls for @IpcContract.performIpcCall() IPC implementation lacks a timeout mechanism for cross-chain messages to handle cases where target subnet becomes
wiki_title: |
    Message timeout controls for @IpcContract.performIpcCall() IPC implementation lacks a timeout mechanism for cross-chain messages to handle cases where target su
wiki_description: |
    [L-11] Message timeout controls for @IpcContract.performIpcCall() IPC implementation lacks a timeout mechanism for cross-chain messages to handle cases where target subnet becomes unresponsive or experiences significant delays. Impact Adding timeouts prevents cross-chain transactions from remaining
wiki_exploit_scenario: |
    Per audit finding: [L-11] Message timeout controls for @IpcContract.performIpcCall() IPC implementation lacks a timeout mechanism for cross-chain messages to handle cases where target subnet becomes unresponsive or expe
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MessageTimeoutControlsForIpccontractPerformipccallIpcX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(performIpcCall|inflightMsgs|targetActor).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "performIpcCall"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
