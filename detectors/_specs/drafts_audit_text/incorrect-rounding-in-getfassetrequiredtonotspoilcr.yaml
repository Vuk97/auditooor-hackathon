skeleton: "name_match_missing_call"
name: "incorrect-rounding-in-getfassetrequiredtonotspoilcr"
class_name: "IncorrectRoundingInGetfassetrequiredtonotspoilcr"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Flare FAssets - Zellic Audit Report"
help: |
    In the else branch of _getFAssetRequiredToNotSpoilCR, the function currently rounds down when calculating the FAsset amount required to maintain the pool's collateralization ratio.
wiki_title: "Incorrect rounding in _getFAssetRequiredToNotSpoilCR"
wiki_description: |
    In the else branch of _getFAssetRequiredToNotSpoilCR, the function currently rounds down when calculating the FAsset amount required to maintain the pool's collateralization ratio. However, to preserve the pool's CR, it should be rounded up to ensure at least the minimum required amount is burned. f
wiki_exploit_scenario: |
    Per audit finding: In the else branch of _getFAssetRequiredToNotSpoilCR, the function currently rounds down when calculating the FAsset amount required to maintain the pool's collateralization ratio. However, to preserv
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectRoundingInGetfassetrequiredtonotspoilcr"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(poolNatBalance|natShare|backedFAssets).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "poolNatBalance"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
