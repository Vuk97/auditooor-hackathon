skeleton: "highlevelcall_missing_sibling"
name: "fee-transfers-occur-without-owner-consent-and-can-be-front-run"
class_name: "FeeTransfersOccurWithoutOwnerConsentAndCanBeFrontRun"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Berachain Airdrop - Zellic Audit Report"
help: |
    The createBatchStream and createStream functions are used to create streams for the owner of NFTs. In case the NFTs are not owned by the caller of the functions, some fees are tran
wiki_title: "Fee transfers occur without owner consent and can be front-run"
wiki_description: |
    The createBatchStream and createStream functions are used to create streams for the owner of NFTs. In case the NFTs are not owned by the caller of the functions, some fees are transferred to the address that originated the transaction: function createStream(uint256 tokenId) external nonReentrant whe
wiki_exploit_scenario: |
    Per audit finding: The createBatchStream and createStream functions are used to create streams for the owner of NFTs. In case the NFTs are not owned by the caller of the functions, some fees are transferred to the addre
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FeeTransfersOccurWithoutOwnerConsentAndCanBeFrontRun"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(createBatchStream|createStream|tokenId).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
