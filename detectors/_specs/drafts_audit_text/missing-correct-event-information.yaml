skeleton: "name_match_missing_call"
name: "missing-correct-event-information"
class_name: "MissingCorrectEventInformation"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Revest Resonate Pt. 1 - Zellic Audit Report"
help: |
    The event triggered in dequeue(...))) does not contain the dequeued order information but rather the order information of the remaining head. if(isProvider) { delete providerQueue[
wiki_title: "Missing correct event information"
wiki_description: |
    The event triggered in dequeue(...))) does not contain the dequeued order information but rather the order information of the remaining head. if(isProvider) { delete providerQueue[poolId][qm.providerHead++]; emit DequeueProvider(poolId, msg.sender, providerQueue[poolId][qm. providerHead]); } else {
wiki_exploit_scenario: |
    Per audit finding: The event triggered in dequeue(...))) does not contain the dequeued order information but rather the order information of the remaining head. if(isProvider) { delete providerQueue[poolId][qm.providerH
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingCorrectEventInformation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(isProvider|providerQueue|poolId).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isProvider"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
