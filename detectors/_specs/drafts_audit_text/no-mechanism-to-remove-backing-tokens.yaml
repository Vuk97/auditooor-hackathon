skeleton: "name_match_missing_call"
name: "no-mechanism-to-remove-backing-tokens"
class_name: "NoMechanismToRemoveBackingTokens"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Blackhaven (Core Contracts) - Zellic Audit Report"
help: |
    Once a backing token is added via addBackingToken, there is no way to remove it from the backingTokens array or update its oracle: function addBackingToken(address _token, address
wiki_title: "No mechanism to remove backing tokens"
wiki_description: |
    Once a backing token is added via addBackingToken, there is no way to remove it from the backingTokens array or update its oracle: function addBackingToken(address _token, address _oracle) external onlyOwner { require(!backingTokenDetailsForAddress[_token].isBackingToken, "Already Backing Token"); b
wiki_exploit_scenario: |
    Per audit finding: Once a backing token is added via addBackingToken, there is no way to remove it from the backingTokens array or update its oracle: function addBackingToken(address _token, address _oracle) external on
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NoMechanismToRemoveBackingTokens"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(addBackingToken|backingTokens|onlyOwner).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "addBackingToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
