skeleton: "name_match_missing_require"
name: "non-queued-transactions-can-be-canceled-30"
class_name: "NonQueuedTransactionsCanBeCanceled30"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit eBridge Ethereum Bridge - Zellic Audit Report"
help: |
    The MerkleTree is used to store cross-chain receipts of reports by a privileged actor (i.e., an oracle), through the usage of the transmit function. function transmit( bytes32 swap
wiki_title: "Non-queued transactions can be canceled                              30"
wiki_description: |
    The MerkleTree is used to store cross-chain receipts of reports by a privileged actor (i.e., an oracle), through the usage of the transmit function. function transmit( bytes32 swapHashId, bytes calldata _report, bytes32[] calldata _rs, // observer signatures->r bytes32[] calldata _ss, //observer sig
wiki_exploit_scenario: |
    Per audit finding: The MerkleTree is used to store cross-chain receipts of reports by a privileged actor (i.e., an oracle), through the usage of the transmit function. function transmit( bytes32 swapHashId, bytes callda
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NonQueuedTransactionsCanBeCanceled30"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(swapHashId|swapInfo|swapInfos).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "swapHashId"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
