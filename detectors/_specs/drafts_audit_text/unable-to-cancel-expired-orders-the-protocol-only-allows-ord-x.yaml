skeleton: "name_match_missing_call"
name: "unable-to-cancel-expired-orders-the-protocol-only-allows-ord-x"
class_name: "UnableToCancelExpiredOrdersTheProtocolOnlyAllowsOrdX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2025-07-gte-spot-clob-and-router"
help: |
    [L-04] Unable to cancel expired orders The protocol only allows order cancellations by owners, causing expired orders to permanently occupy slots until manually removed. This might
wiki_title: |
    Unable to cancel expired orders The protocol only allows order cancellations by owners, causing expired orders to permanently occupy slots until manually remove
wiki_description: |
    [L-04] Unable to cancel expired orders The protocol only allows order cancellations by owners, causing expired orders to permanently occupy slots until manually removed. This might block new orders when the book is full and force the system to retain worthless dust positions indefinitely. Finding de
wiki_exploit_scenario: |
    Per audit finding: [L-04] Unable to cancel expired orders The protocol only allows order cancellations by owners, causing expired orders to permanently occupy slots until manually removed. This might block new orders wh
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnableToCancelExpiredOrdersTheProtocolOnlyAllowsOrdX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(orderId|toOrderId|isNull).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "orderId"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
