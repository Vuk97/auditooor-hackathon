skeleton: "name_match_missing_call"
name: "error-handling-in-depositwithpermit"
class_name: "ErrorHandlingInDepositwithpermit"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Hyperliquid November - Zellic Audit Report"
help: |
    ling in depositWithPermit The function depositWithPermit allows a user to deposit USDC into the contract with a per- mit signature, bypassing the need for a separate approval trans
wiki_title: "Error handling in depositWithPermit"
wiki_description: |
    ling in depositWithPermit The function depositWithPermit allows a user to deposit USDC into the contract with a per- mit signature, bypassing the need for a separate approval transaction. The function’s current implementation includes a try-catch block surrounding the permit call but not the subsequ
wiki_exploit_scenario: |
    Per audit finding: ling in depositWithPermit The function depositWithPermit allows a user to deposit USDC into the contract with a per- mit signature, bypassing the need for a separate approval transaction. The function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ErrorHandlingInDepositwithpermit"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(depositWithPermit|safeTransferFrom).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "depositWithPermit"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
