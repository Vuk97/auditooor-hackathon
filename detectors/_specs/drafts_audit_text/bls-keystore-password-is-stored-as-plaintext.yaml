skeleton: "name_match_missing_call"
name: "bls-keystore-password-is-stored-as-plaintext"
class_name: "BlsKeystorePasswordIsStoredAsPlaintext"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Babylon Genesis Chain - Zellic Audit Report"
help: |
    The ERC-2335 BLS keystore implementation in Babylon stores the keystore password in plaintext in a file on the machine running the validator node. // Save saves the bls12381 key to
wiki_title: "BLS keystore password is stored as plaintext"
wiki_description: |
    The ERC-2335 BLS keystore implementation in Babylon stores the keystore password in plaintext in a file on the machine running the validator node. // Save saves the bls12381 key to the file. // The file stores an erc2335 structure containing the encrypted bls private key. func (k *BlsKey) Save(passw
wiki_exploit_scenario: |
    Per audit finding: The ERC-2335 BLS keystore implementation in Babylon stores the keystore password in plaintext in a file on the machine running the validator node. // Save saves the bls12381 key to the file. // The fi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BlsKeystorePasswordIsStoredAsPlaintext"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(filePath|jsonBytes|passwordPath).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "filePath"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
