skeleton: "name_match_missing_call"
name: "redundant-check-on-prevsynccommitteehash"
class_name: "RedundantCheckOnPrevsynccommitteehash"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit SP1 Helios - Zellic Audit Report"
help: |
    In the update function, both currentSyncCommitteeHash and po.prevSyncCommitteeHash are derived from syncCommittees[getSyncCommitteePeriod(head)]. Therefore, the check [D] is redund
wiki_title: "Redundant check on prevSyncCommitteeHash"
wiki_description: |
    In the update function, both currentSyncCommitteeHash and po.prevSyncCommitteeHash are derived from syncCommittees[getSyncCommitteePeriod(head)]. Therefore, the check [D] is redundant in the current implementation. ProofOutputs memory po = ProofOutputs({ prevHeader: headers[head], prevHead: head, pr
wiki_exploit_scenario: |
    Per audit finding: In the update function, both currentSyncCommitteeHash and po.prevSyncCommitteeHash are derived from syncCommittees[getSyncCommitteePeriod(head)]. Therefore, the check [D] is redundant in the current i
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RedundantCheckOnPrevsynccommitteehash"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(currentSyncCommitteeHash|prevSyncCommitteeHash|syncCommittees).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "currentSyncCommitteeHash"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
