skeleton: "name_match_missing_require"
name: "too-harsh-competitive-requirement-when-tree-is-full-the-clob-x"
class_name: "TooHarshCompetitiveRequirementWhenTreeIsFullTheClobX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2025-07-gte-spot-clob-and-router"
help: |
    [L-03] Too harsh competitive requirement when tree is full The CLOB contract rejects new orders at the worst existing price (when newOrder.price == maxAskPrice ) even though they w
wiki_title: |
    Too harsh competitive requirement when tree is full The CLOB contract rejects new orders at the worst existing price (when newOrder.price == maxAskPrice ) even 
wiki_description: |
    [L-03] Too harsh competitive requirement when tree is full The CLOB contract rejects new orders at the worst existing price (when newOrder.price == maxAskPrice ) even though they wouldn’t increase tree size. Finding description and impact CLOB will not accept the incoming order if it’s least competi
wiki_exploit_scenario: |
    Per audit finding: [L-03] Too harsh competitive requirement when tree is full The CLOB contract rejects new orders at the worst existing price (when newOrder.price == maxAskPrice ) even though they wouldn’t increase tre
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TooHarshCompetitiveRequirementWhenTreeIsFullTheClobX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(newOrder|maxAskPrice|askTree).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "newOrder"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
