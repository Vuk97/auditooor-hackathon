skeleton: "name_match_missing_require"
name: "feed-price-validation-can-be-bypassed-14"
class_name: "FeedPriceValidationCanBeBypassed14"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Benqi Oracle - Zellic Audit Report"
help: |
    BenqiDualOracle implements a dual oracle system with Edge as primary and Chainlink as backup. It has three different modes: /// @notice Enum representing different oracle modes enu
wiki_title: "Feed price validation can be bypassed                                         14"
wiki_description: |
    BenqiDualOracle implements a dual oracle system with Edge as primary and Chainlink as backup. It has three different modes: /// @notice Enum representing different oracle modes enum OracleMode { EDGE_ONLY, // Use only Edge oracle CL_ONLY, // Use only Chainlink oracle DUAL // Use both oracles (defaul
wiki_exploit_scenario: |
    Per audit finding: BenqiDualOracle implements a dual oracle system with Edge as primary and Chainlink as backup. It has three different modes: /// @notice Enum representing different oracle modes enum OracleMode { EDGE_
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FeedPriceValidationCanBeBypassed14"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getUnderlyingPrice|oracleMode|edgePriceResult).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getUnderlyingPrice"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
