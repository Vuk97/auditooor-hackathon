skeleton: "name_match_missing_call"
name: "asset-removal-is-broken"
class_name: "AssetRemovalIsBroken"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Maia DAO Ulysses Protocol May 2023 - Zellic Audit Report"
help: |
    The function removeAsset(address asset) removes an asset from the global assets[] and weights[] arrays and updates some globals. function removeAsset(address asset) external nonRee
wiki_title: "Asset removal is broken"
wiki_description: |
    The function removeAsset(address asset) removes an asset from the global assets[] and weights[] arrays and updates some globals. function removeAsset(address asset) external nonReentrant onlyOwner { /) No need to check if index is 0, it will underflow and revert if it is 0 uint256 assetIndex = asset
wiki_exploit_scenario: |
    Per audit finding: The function removeAsset(address asset) removes an asset from the global assets[] and weights[] arrays and updates some globals. function removeAsset(address asset) external nonReentrant onlyOwner { /
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AssetRemovalIsBroken"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(removeAsset|nonReentrant|onlyOwner).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "removeAsset"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
