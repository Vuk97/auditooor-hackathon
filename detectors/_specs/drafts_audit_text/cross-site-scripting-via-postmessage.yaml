skeleton: "name_match_missing_call"
name: "cross-site-scripting-via-postmessage"
class_name: "CrossSiteScriptingViaPostmessage"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The witness SDK receives incoming window messages and insecurely passes them to JSONPath without validation. This allows an attacker to execute arbitrary JavaScript code. This issu
wiki_title: "Cross-site scripting via postMessage"
wiki_description: |
    The witness SDK receives incoming window messages and insecurely passes them to JSONPath without validation. This allows an attacker to execute arbitrary JavaScript code. This issue occurs in witness-sdk/src/providers/http-provider/utils.ts where JSONPath evaluates any JavaScript code wrapped in ():
wiki_exploit_scenario: |
    Per audit finding: The witness SDK receives incoming window messages and insecurely passes them to JSONPath without validation. This allows an attacker to execute arbitrary JavaScript code. This issue occurs in witness-
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CrossSiteScriptingViaPostmessage"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(extractJSONValueIndex|jsonPath|contentWindow).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "extractJSONValueIndex"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
