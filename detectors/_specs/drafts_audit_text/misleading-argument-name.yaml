skeleton: "name_match_missing_require"
name: "misleading-argument-name"
class_name: "MisleadingArgumentName"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "hexens audit Algebra_public_11_08"
help: |
    The ﬁrst argument of the verifyCallback() function is called "factory", but Algebra uses poolDeployer instead of the factory contract. function verifyCallback( address factory, add
wiki_title: "MISLEADING ARGUMENT NAME"
wiki_description: |
    The ﬁrst argument of the verifyCallback() function is called "factory", but Algebra uses poolDeployer instead of the factory contract. function verifyCallback( address factory, address tokenA, address tokenB ) internal view returns (IAlgebraPool pool) { return verifyCallback(factory, PoolAddress.get
wiki_exploit_scenario: |
    Per audit finding: The ﬁrst argument of the verifyCallback() function is called "factory", but Algebra uses poolDeployer instead of the factory contract. function verifyCallback( address factory, address tokenA, address
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MisleadingArgumentName"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(verifyCallback|poolDeployer|getPoolKey).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "verifyCallback"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
