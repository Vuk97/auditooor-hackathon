skeleton: "name_match_missing_require"
name: "incomplete-chain-comparison"
class_name: "IncompleteChainComparison"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit SoSoValue - Zellic Audit Report"
help: |
    During the check of a token set by the function checkTokenset, the chain string from the Token structure is compared with the chain from the swap: function checkTokenset(Token[] me
wiki_title: "Incomplete chain comparison"
wiki_description: |
    During the check of a token set by the function checkTokenset, the chain string from the Token structure is compared with the chain from the swap: function checkTokenset(Token[] memory tokenset, string[] memory addressList) internal view { require(tokenset.length == addressList.length, "tokenset len
wiki_exploit_scenario: |
    Per audit finding: During the check of a token set by the function checkTokenset, the chain string from the Token structure is compared with the chain from the swap: function checkTokenset(Token[] memory tokenset, strin
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncompleteChainComparison"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(checkTokenset|addressList|tokenAddress).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "checkTokenset"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
