skeleton: "name_match_missing_require"
name: "incorrect-negative-checks"
class_name: "IncorrectNegativeChecks"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Babylon Genesis Chain - Zellic Audit Report"
help: |
    In btc-staking-ts, there are multiple functions that claim to validate whether values are negative but do so incorrectly. For example, in the StakingScripts class: // check that ma
wiki_title: "Incorrect negative checks"
wiki_description: |
    In btc-staking-ts, there are multiple functions that claim to validate whether values are negative but do so incorrectly. For example, in the StakingScripts class: // check that maximum value for staking time is not greater than uint16 and above 0 if (this.stakingTimeLock == 0 || this.stakingTimeLoc
wiki_exploit_scenario: |
    Per audit finding: In btc-staking-ts, there are multiple functions that claim to validate whether values are negative but do so incorrectly. For example, in the StakingScripts class: // check that maximum value for stak
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectNegativeChecks"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(stakingTimeLock|unbondingTimeLock|btcActivationHeight).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "stakingTimeLock"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
