skeleton: "paired_function_divergence"
name: "wrong-input-length-to-static-call-20"
class_name: "WrongInputLengthToStaticCall20"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Session Token - Zellic Audit Report"
help: |
    Upon examining the code where the _aggregatePubkey is updated through the addBLSPublicKey function, we have identified a potentially significant scenario that requires attention. A
wiki_title: "Wrong input length to static call                                       20"
wiki_description: |
    Upon examining the code where the _aggregatePubkey is updated through the addBLSPublicKey function, we have identified a potentially significant scenario that requires attention. Assuming that the DEX swap pair that includes SENT tokens has a large liquidity in the future, there exists a potential v
wiki_exploit_scenario: |
    Per audit finding: Upon examining the code where the _aggregatePubkey is updated through the addBLSPublicKey function, we have identified a potentially significant scenario that requires attention. Assuming that the DEX
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "WrongInputLengthToStaticCall20"
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(balance|amount|total|supply|reserve).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
