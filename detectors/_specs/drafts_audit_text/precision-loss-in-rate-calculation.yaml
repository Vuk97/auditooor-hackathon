skeleton: "name_match_missing_call"
name: "precision-loss-in-rate-calculation"
class_name: "PrecisionLossInRateCalculation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Adrastia Protocol - Zellic Audit Report"
help: |
    The function computeRateInternal calculates the rate for the specified token by summing its base rate and the weighted rates of its components. The weighted rates are divided by 10
wiki_title: "Precision loss in rate calculation"
wiki_description: |
    The function computeRateInternal calculates the rate for the specified token by summing its base rate and the weighted rates of its components. The weighted rates are divided by 10,000 (100%) first to convert them into percentages and then added to the componentRate variable, which is then added to
wiki_exploit_scenario: |
    Per audit finding: The function computeRateInternal calculates the rate for the specified token by summing its base rate and the weighted rates of its components. The weighted rates are divided by 10,000 (100%) first to
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PrecisionLossInRateCalculation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(computeRateInternal|componentRate|componentWeights).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "computeRateInternal"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
