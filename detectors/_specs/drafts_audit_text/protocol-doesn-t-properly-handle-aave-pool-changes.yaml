skeleton: "name_match_missing_require"
name: "protocol-doesn-t-properly-handle-aave-pool-changes"
class_name: "ProtocolDoesnTProperlyHandleAavePoolChanges"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-08-morpheus"
help: |
    ZanyBonzy , also found by anchabadze Finding description and impact setAavePool is used to set new aave pool address, presumably to handle pool changes, upgrades, or migration situ
wiki_title: "Protocol doesn’t properly handle Aave Pool changes"
wiki_description: |
    ZanyBonzy , also found by anchabadze Finding description and impact setAavePool is used to set new aave pool address, presumably to handle pool changes, upgrades, or migration situations. function setAavePool ( address value_ ) public onlyOwner { require ( value_ != address ( 0 ), "DR: invalid Aave
wiki_exploit_scenario: |
    Per audit finding: ZanyBonzy , also found by anchabadze Finding description and impact setAavePool is used to set new aave pool address, presumably to handle pool changes, upgrades, or migration situations. function set
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ProtocolDoesnTProperlyHandleAavePoolChanges"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setAavePool|onlyOwner|aavePool).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "setAavePool"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
