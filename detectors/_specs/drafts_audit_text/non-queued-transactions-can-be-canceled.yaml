skeleton: "name_match_missing_require"
name: "non-queued-transactions-can-be-canceled"
class_name: "NonQueuedTransactionsCanBeCanceled"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit eBridge Ethereum Bridge - Zellic Audit Report"
help: |
    In the Timelock contract, the cancelTransaction function is used to cancel a queued transaction: function cancelTransaction( address target, uint value, string memory signature, by
wiki_title: "Non-queued transactions can be canceled"
wiki_description: |
    In the Timelock contract, the cancelTransaction function is used to cancel a queued transaction: function cancelTransaction( address target, uint value, string memory signature, bytes memory data, uint eta ) public { require(target != address(0),"Invalid input."); require( msg.sender == admin, "Time
wiki_exploit_scenario: |
    Per audit finding: In the Timelock contract, the cancelTransaction function is used to cancel a queued transaction: function cancelTransaction( address target, uint value, string memory signature, bytes memory data, uin
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NonQueuedTransactionsCanBeCanceled"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(cancelTransaction|txHash|queuedTransactions).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "cancelTransaction"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
