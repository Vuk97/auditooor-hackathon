skeleton: "name_match_missing_require"
name: "potentially-obtaining-a-stale-price"
class_name: "PotentiallyObtainingAStalePrice"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Plaza Core Protocol - Zellic Audit Report"
help: |
    The comment of the function getOraclePrice states, Reverts if the price data is older than 1 day. The implementation checks whether the price is stale by comparing the sum of the u
wiki_title: "Potentially obtaining a stale price"
wiki_description: |
    The comment of the function getOraclePrice states, Reverts if the price data is older than 1 day. The implementation checks whether the price is stale by comparing the sum of the updatedTimestamp and the heartbeat with the current timestamp. /** * @dev Retrieves the latest price from the oracle * @r
wiki_exploit_scenario: |
    Per audit finding: The comment of the function getOraclePrice states, Reverts if the price data is older than 1 day. The implementation checks whether the price is stale by comparing the sum of the updatedTimestamp and
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PotentiallyObtainingAStalePrice"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getOraclePrice|updatedTimestamp|oracleFeeds).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getOraclePrice"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
