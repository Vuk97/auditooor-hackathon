skeleton: "name_match_missing_require"
name: "allowance-given-to-incorrect-address-20"
class_name: "AllowanceGivenToIncorrectAddress20"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Bracket Fi Escrow - Zellic Audit Report"
help: |
    The function withdraw is responsible for the withdrawals of tokens from the escrow contract. The function first checks if there is enough balance in the usersBalance for the caller
wiki_title: "Allowance given to incorrect address                20"
wiki_description: |
    The function withdraw is responsible for the withdrawals of tokens from the escrow contract. The function first checks if there is enough balance in the usersBalance for the caller, then unwraps the token if unwrap is true, and finally decreases the amount from the usersBalance mapping and to- talSt
wiki_exploit_scenario: |
    Per audit finding: The function withdraw is responsible for the withdrawals of tokens from the escrow contract. The function first checks if there is enough balance in the usersBalance for the caller, then unwraps the t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AllowanceGivenToIncorrectAddress20"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(usersBalance|talStaked|onlyNotBroke).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "usersBalance"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
