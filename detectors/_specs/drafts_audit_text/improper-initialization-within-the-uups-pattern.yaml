skeleton: "name_match_missing_call"
name: "improper-initialization-within-the-uups-pattern"
class_name: "ImproperInitializationWithinTheUupsPattern"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Pyth Lazer EVM - Zellic Audit Report"
help: |
    In the UUPS pattern, the upgrade functionality of the proxy is typically found within the implementation contract. // PythLazer.sol::initialize() function initialize(address _topAu
wiki_title: "Improper initialization within the UUPS pattern"
wiki_description: |
    In the UUPS pattern, the upgrade functionality of the proxy is typically found within the implementation contract. // PythLazer.sol::initialize() function initialize(address _topAuthority) public initializer { __Ownable_init(_topAuthority); __UUPSUpgradeable_init(); verification_fee = 1 wei; } Prior
wiki_exploit_scenario: |
    Per audit finding: In the UUPS pattern, the upgrade functionality of the proxy is typically found within the implementation contract. // PythLazer.sol::initialize() function initialize(address _topAuthority) public init
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ImproperInitializationWithinTheUupsPattern"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(upgradeToAndCall|newImplementation|onlyProxy).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "upgradeToAndCall"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
