skeleton: "name_match_missing_call"
name: "unbounded-withdrawal-loop-in-vault-could-theoretically-block-x"
class_name: "UnboundedWithdrawalLoopInVaultCouldTheoreticallyBlockX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Avon - Zellic Audit Report"
help: |
    The withdraw and redeem functions in the contract Vault call the function _performWithdraw, which contains an unbounded loop through the withdrawal queue: uint256 toWithdraw = asse
wiki_title: "Unbounded withdrawal loop in Vault could theoretically block withdrawals"
wiki_description: |
    The withdraw and redeem functions in the contract Vault call the function _performWithdraw, which contains an unbounded loop through the withdrawal queue: uint256 toWithdraw = assets; uint256 len = _queue.withdrawQueue.length; while (toWithdraw > 0 && len > 0) If the withdrawal queue contains many p
wiki_exploit_scenario: |
    Per audit finding: The withdraw and redeem functions in the contract Vault call the function _performWithdraw, which contains an unbounded loop through the withdrawal queue: uint256 toWithdraw = assets; uint256 len = _q
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnboundedWithdrawalLoopInVaultCouldTheoreticallyBlockX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(toWithdraw|withdrawQueue).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "toWithdraw"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
