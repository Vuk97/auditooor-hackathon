skeleton: "name_match_missing_call"
name: "incorrect-withdrawnfeeearnings-after-finalization"
class_name: "IncorrectWithdrawnfeeearningsAfterFinalization"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Lido Fixed Income - Zellic Audit Report"
help: |
    While the vault is in progress, the helper function calculateVariableFeeEarningsShare is used to calculate the current amount owed to the sender as a variable-side depositor: funct
wiki_title: "Incorrect withdrawnFeeEarnings after finalization"
wiki_description: |
    While the vault is in progress, the helper function calculateVariableFeeEarningsShare is used to calculate the current amount owed to the sender as a variable-side depositor: function calculateVariableFeeEarningsShare() internal returns (uint256) { (uint256 currentState, uint256 feeEarningsShare) =
wiki_exploit_scenario: |
    Per audit finding: While the vault is in progress, the helper function calculateVariableFeeEarningsShare is used to calculate the current amount owed to the sender as a variable-side depositor: function calculateVariabl
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectWithdrawnfeeearningsAfterFinalization"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(currentState|feeEarningsShare|calculateVariableWithdrawState).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "currentState"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
