skeleton: "name_match_missing_require"
name: "centralization-risk-locked-user-funds"
class_name: "CentralizationRiskLockedUserFunds"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit H20 vlPSDN - Zellic Audit Report"
help: |
    The implementation of some protocol mechanics gives the owner the ability to lock user funds in the contract. 1. If deposit time is enforced, withdrawals require that the user has
wiki_title: "Centralization risk: locked user funds"
wiki_description: |
    The implementation of some protocol mechanics gives the owner the ability to lock user funds in the contract. 1. If deposit time is enforced, withdrawals require that the user has no more remaining epochs to wait. /** * @notice Implements internal withdraw logic * @dev The withdraw is always done in
wiki_exploit_scenario: |
    Per audit finding: The implementation of some protocol mechanics gives the owner the ability to lock user funds in the contract. 1. If deposit time is enforced, withdrawals require that the user has no more remaining ep
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CentralizationRiskLockedUserFunds"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(lockEpochs|enforceTime|lockToken).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "lockEpochs"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
