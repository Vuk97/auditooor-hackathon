skeleton: "name_match_missing_call"
name: "user-ve-balance-vebalance"
class_name: "UserVeBalanceVebalance"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LayerZero VeDistributor - Zellic Audit Report"
help: |
    er VE balance (veBalance) This value is used in two places: in getUsdcClaimable and in _getAndCacheVeBalance. The former does not cache the value and will use whatever value is ret
wiki_title: "User VE balance (veBalance)"
wiki_description: |
    er VE balance (veBalance) This value is used in two places: in getUsdcClaimable and in _getAndCacheVeBalance. The former does not cache the value and will use whatever value is returned from the VE contract. The latter will use the cached value where the key is the msg.sender. This value is never up
wiki_exploit_scenario: |
    Per audit finding: er VE balance (veBalance) This value is used in two places: in getUsdcClaimable and in _getAndCacheVeBalance. The former does not cache the value and will use whatever value is returned from the VE co
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UserVeBalanceVebalance"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(veBalance|getUsdcClaimable|balanceOfAt).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "veBalance"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
