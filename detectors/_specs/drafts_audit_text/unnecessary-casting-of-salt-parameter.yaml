skeleton: "name_match_missing_call"
name: "unnecessary-casting-of-salt-parameter"
class_name: "UnnecessaryCastingOfSaltParameter"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit LightWallet - Zellic Audit Report"
help: |
    The type of the parameter salt in the createAccount and getAddress functions is uint 256, but the functions both cast it to bytes32 in all uses of the parameter.
wiki_title: "Unnecessary casting of salt parameter"
wiki_description: |
    The type of the parameter salt in the createAccount and getAddress functions is uint 256, but the functions both cast it to bytes32 in all uses of the parameter.
wiki_exploit_scenario: |
    Per audit finding: The type of the parameter salt in the createAccount and getAddress functions is uint 256, but the functions both cast it to bytes32 in all uses of the parameter.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnnecessaryCastingOfSaltParameter"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(createAccount|getAddress).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "createAccount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
