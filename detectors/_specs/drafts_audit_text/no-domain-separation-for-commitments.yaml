skeleton: "name_match_missing_call"
name: "no-domain-separation-for-commitments"
class_name: "NoDomainSeparationForCommitments"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Brevis - Zellic Audit Report"
help: |
    The circuits constructed using the sdk use receipt, storage, and transaction data but do not verify their correctness. Instead, they commit to these data points by hashing, and the
wiki_title: "No domain separation for commitments"
wiki_description: |
    The circuits constructed using the sdk use receipt, storage, and transaction data but do not verify their correctness. Instead, they commit to these data points by hashing, and they ultimately expose a Merkle root hash of these commitments as a public input to the circuit. Correctness of the data po
wiki_exploit_scenario: |
    Per audit finding: The circuits constructed using the sdk use receipt, storage, and transaction data but do not verify their correctness. Instead, they commit to these data points by hashing, and they ultimately expose
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NoDomainSeparationForCommitments"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(commitInput|hashOrZero|inputCommits).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "commitInput"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
