skeleton: "name_match_missing_call"
name: "inconsistent-decimal-handling-across-codebase"
class_name: "InconsistentDecimalHandlingAcrossCodebase"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Blackhaven (Core Contracts) - Zellic Audit Report"
help: |
    The codebase inconsistently handles RBT decimals. Some code hardcodes 1e18, // Bond.sol require(payout >= 10 ** IERC20Metadata(address(RBT)).decimals() / 100, "Bond too small"); wh
wiki_title: "Inconsistent decimal handling across codebase"
wiki_description: |
    The codebase inconsistently handles RBT decimals. Some code hardcodes 1e18, // Bond.sol require(payout >= 10 ** IERC20Metadata(address(RBT)).decimals() / 100, "Bond too small"); while other code reads decimals dynamically: // Bond.sol function valueOfToken(address _token, uint256 _amount) public vie
wiki_exploit_scenario: |
    Per audit finding: The codebase inconsistently handles RBT decimals. Some code hardcodes 1e18, // Bond.sol require(payout >= 10 ** IERC20Metadata(address(RBT)).decimals() / 100, "Bond too small"); while other code reads
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InconsistentDecimalHandlingAcrossCodebase"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(valueOfToken).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "valueOfToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
