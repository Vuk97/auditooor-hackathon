skeleton: "name_match_missing_call"
name: "using-deprecated-chainlink-function"
class_name: "UsingDeprecatedChainlinkFunction"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Molend Protocol - Zellic Audit Report"
help: |
    In the UiPoolDataProvider contract, The function getReservesData() is used to return the list of ag- gregated reserves data. This uses latestAnswer() to get the Chainlink oracle pr
wiki_title: "Using deprecated Chainlink function"
wiki_description: |
    In the UiPoolDataProvider contract, The function getReservesData() is used to return the list of ag- gregated reserves data. This uses latestAnswer() to get the Chainlink oracle price. function getReservesData(ILendingPoolAddressesProvider provider) public view override returns (AggregatedReserveDat
wiki_exploit_scenario: |
    Per audit finding: In the UiPoolDataProvider contract, The function getReservesData() is used to return the list of ag- gregated reserves data. This uses latestAnswer() to get the Chainlink oracle price. function getRes
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UsingDeprecatedChainlinkFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getReservesData|latestAnswer|baseCurrencyInfo).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "getReservesData"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
