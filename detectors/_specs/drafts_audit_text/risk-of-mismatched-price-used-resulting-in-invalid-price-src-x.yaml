skeleton: "name_match_missing_require"
name: "risk-of-mismatched-price-used-resulting-in-invalid-price-src-x"
class_name: "RiskOfMismatchedPriceUsedResultingInInvalidPriceSrcX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-12-benddao-invitational"
help: |
    [L-01] Risk of mismatched price used resulting in invalid price src/oracles/EETHPriceAdapter.sol Instances(4) Vulnerabilities getAnswer will take a roundId input for BASE_AGGREGATO
wiki_title: |
    Risk of mismatched price used resulting in invalid price src/oracles/EETHPriceAdapter.sol Instances(4) Vulnerabilities getAnswer will take a roundId input for B
wiki_description: |
    [L-01] Risk of mismatched price used resulting in invalid price src/oracles/EETHPriceAdapter.sol Instances(4) Vulnerabilities getAnswer will take a roundId input for BASE_AGGREGATOR to get a round-specific price. However, it only takes the latest price for WEETH_AGGREGATOR . Since both prices will b
wiki_exploit_scenario: |
    Per audit finding: [L-01] Risk of mismatched price used resulting in invalid price src/oracles/EETHPriceAdapter.sol Instances(4) Vulnerabilities getAnswer will take a roundId input for BASE_AGGREGATOR to get a round-spe
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RiskOfMismatchedPriceUsedResultingInInvalidPriceSrcX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getAnswer|roundId|convertWEETHPrice).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getAnswer"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
