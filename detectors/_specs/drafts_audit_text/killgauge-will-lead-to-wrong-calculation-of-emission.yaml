skeleton: "name_match_missing_call"
name: "killgauge-will-lead-to-wrong-calculation-of-emission"
class_name: "KillgaugeWillLeadToWrongCalculationOfEmission"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "code4arena audit 2024-09-fenix-finance"
help: |
    Ch_301 , also found by KupiaSec Description The VoterUpgradeableV2.sol contract has killGauge() that disables the gauge to prevent it from further rewards distribution, only the ad
wiki_title: "killGauge() will lead to wrong calculation of emission"
wiki_description: |
    Ch_301 , also found by KupiaSec Description The VoterUpgradeableV2.sol contract has killGauge() that disables the gauge to prevent it from further rewards distribution, only the address with GOVERNANCE_ROLE role can call it. the killGauge() only updates three state variables. File: VoterUpgradeableV
wiki_exploit_scenario: |
    Per audit finding: Ch_301 , also found by KupiaSec Description The VoterUpgradeableV2.sol contract has killGauge() that disables the gauge to prevent it from further rewards distribution, only the address with GOVERNANC
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "KillgaugeWillLeadToWrongCalculationOfEmission"
state_decl: "uint256 internal distribute;"
fn_name_regex: ".*(killGauge|distribute).*"
read_var_regex: ".*(distribute|killGauge).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "killGauge"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return distribute > 0;"
