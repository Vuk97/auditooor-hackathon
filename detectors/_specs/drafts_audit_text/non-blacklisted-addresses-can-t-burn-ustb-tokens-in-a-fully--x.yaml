skeleton: "name_match_missing_call"
name: "non-blacklisted-addresses-can-t-burn-ustb-tokens-in-a-fully--x"
class_name: "NonBlacklistedAddressesCanTBurnUstbTokensInAFullyX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-11-ethena-labs"
help: |
    [L-09] Non-blacklisted addresses can’t burn UStb tokens in a FULLY_ENABLED transfer state if address(0) is blaklisted From the Main invariants : Main invariants Only non-blackliste
wiki_title: |
    Non-blacklisted addresses can’t burn UStb tokens in a FULLY_ENABLED transfer state if address(0) is blaklisted From the Main invariants : Main invariants Only n
wiki_description: |
    [L-09] Non-blacklisted addresses can’t burn UStb tokens in a FULLY_ENABLED transfer state if address(0) is blaklisted From the Main invariants : Main invariants Only non-blacklisted addresses can send/receive/burn UStb tokens in a FULLY_ENABLED transfer state. However, if address(0) is blacklisted,
wiki_exploit_scenario: |
    Per audit finding: [L-09] Non-blacklisted addresses can’t burn UStb tokens in a FULLY_ENABLED transfer state if address(0) is blaklisted From the Main invariants : Main invariants Only non-blacklisted addresses can send
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NonBlacklistedAddressesCanTBurnUstbTokensInAFullyX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(hasRole).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "hasRole"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
