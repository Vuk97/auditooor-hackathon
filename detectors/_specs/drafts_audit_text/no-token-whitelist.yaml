skeleton: "name_match_missing_require"
name: "no-token-whitelist"
class_name: "NoTokenWhitelist"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Mina Token Bridge EVM - Zellic Audit Report"
help: |
    The bridge is intended to have some whitelisted tokens, but the Mina contract has no whitelist for the tokens. @method async lock(amount: UInt64, address: Field, tokenAddr: PublicK
wiki_title: "No token whitelist"
wiki_description: |
    The bridge is intended to have some whitelisted tokens, but the Mina contract has no whitelist for the tokens. @method async lock(amount: UInt64, address: Field, tokenAddr: PublicKey) { // Check if the amount is within the allowed range const minAmount = this.minAmount.getAndRequireEquals(); const m
wiki_exploit_scenario: |
    Per audit finding: The bridge is intended to have some whitelisted tokens, but the Mina contract has no whitelist for the tokens. @method async lock(amount: UInt64, address: Field, tokenAddr: PublicKey) { // Check if th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NoTokenWhitelist"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokenAddr|minAmount|getAndRequireEquals).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "tokenAddr"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
