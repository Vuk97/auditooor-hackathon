skeleton: "name_match_missing_call"
name: "documentation-inconsistencies-14"
class_name: "DocumentationInconsistencies14"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Filecoin Services Payments - Zellic Audit Report"
help: |
    The mapping payerRails is defined as mapping(address => mapping(address => uint256[])) private payerRails;, which maps an address to another address<->array mapping. During creatio
wiki_title: "Documentation inconsistencies                                  14"
wiki_description: |
    The mapping payerRails is defined as mapping(address => mapping(address => uint256[])) private payerRails;, which maps an address to another address<->array mapping. During creation of rails, the railId of the new rail is pushed onto this array. payeeRails[token][to].push(railId); payerRails[token][
wiki_exploit_scenario: |
    Per audit finding: The mapping payerRails is defined as mapping(address => mapping(address => uint256[])) private payerRails;, which maps an address to another address<->array mapping. During creation of rails, the rail
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DocumentationInconsistencies14"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(payerRails|railId|payeeRails).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "payerRails"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
