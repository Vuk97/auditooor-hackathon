skeleton: "name_match_missing_require"
name: "staking-manager-may-become-locked"
class_name: "StakingManagerMayBecomeLocked"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Rainmaker - Zellic Audit Report"
help: |
    The underlying vaults contain functionality that allows Definitive to pause contracts and the vault admin to unpause them. In BaseAccessControl, /** * @dev Inherited from CoreStopG
wiki_title: "Staking manager may become locked"
wiki_description: |
    The underlying vaults contain functionality that allows Definitive to pause contracts and the vault admin to unpause them. In BaseAccessControl, /** * @dev Inherited from CoreStopGuardian *) function enableStopGuardian() public override onlyAdmins { return _enableStopGuardian(); } /** * @dev Inherit
wiki_exploit_scenario: |
    Per audit finding: The underlying vaults contain functionality that allows Definitive to pause contracts and the vault admin to unpause them. In BaseAccessControl, /** * @dev Inherited from CoreStopGuardian *) function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "StakingManagerMayBecomeLocked"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(enableStopGuardian|onlyAdmins|disableStopGuardian).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "enableStopGuardian"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
