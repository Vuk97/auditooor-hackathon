skeleton: "name_match_missing_call"
name: "incorrect-state-update-on-settlement-failure"
class_name: "IncorrectStateUpdateOnSettlementFailure"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Aori 0.3.1 Upgrade - Zellic Audit Report"
help: |
    The _settleOrder function settles a single order and updates balances. When balance updates fail, it skips settling the current order by returning early. function _settleOrder(byte
wiki_title: "Incorrect state update on settlement failure"
wiki_description: |
    The _settleOrder function settles a single order and updates balances. When balance updates fail, it skips settling the current order by returning early. function _settleOrder(bytes32 orderId, address filler) internal { // [...] bool successLock = balances[order.offerer][order.inputToken].decreaseLo
wiki_exploit_scenario: |
    Per audit finding: The _settleOrder function settles a single order and updates balances. When balance updates fail, it skips settling the current order by returning early. function _settleOrder(bytes32 orderId, address
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectStateUpdateOnSettlementFailure"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(orderId|successLock|inputToken).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "orderId"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
