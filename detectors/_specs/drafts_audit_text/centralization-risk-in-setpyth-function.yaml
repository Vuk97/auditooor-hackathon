skeleton: "name_match_missing_call"
name: "centralization-risk-in-setpyth-function"
class_name: "CentralizationRiskInSetpythFunction"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Yei Finance Oracle - Zellic Audit Report"
help: |
    The following function can only be executed by the owner address. In other words, if the private key of the owner address is leaked for some reason, such as an accident or a system
wiki_title: "Centralization risk in setPyth function"
wiki_description: |
    The following function can only be executed by the owner address. In other words, if the private key of the owner address is leaked for some reason, such as an accident or a system compromise, it means that all price data returned by the Oracle can be manipulated. function setPyth(address newPythAdd
wiki_exploit_scenario: |
    Per audit finding: The following function can only be executed by the owner address. In other words, if the private key of the owner address is leaked for some reason, such as an accident or a system compromise, it mean
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CentralizationRiskInSetpythFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setPyth|newPythAddress|newPythID).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "setPyth"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
