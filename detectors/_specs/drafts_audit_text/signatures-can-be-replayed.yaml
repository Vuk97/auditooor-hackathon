skeleton: "name_match_missing_require"
name: "signatures-can-be-replayed"
class_name: "SignaturesCanBeReplayed"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Mina Token Bridge EVM - Zellic Audit Report"
help: |
    A minter can submit up to three signatures, and if the signatures are validated, they can receive a certain amount of tokens. However, there is no logic to check whether the signat
wiki_title: "Signatures can be replayed"
wiki_description: |
    A minter can submit up to three signatures, and if the signatures are validated, they can receive a certain amount of tokens. However, there is no logic to check whether the signatures have already been submitted. this.validateSig(msg, sig1, validator1, useSig1); this.validateSig(msg, sig2, validato
wiki_exploit_scenario: |
    Per audit finding: A minter can submit up to three signatures, and if the signatures are validated, they can receive a certain amount of tokens. However, there is no logic to check whether the signatures have already be
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SignaturesCanBeReplayed"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validateSig|useSig1|useSig2).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validateSig"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
