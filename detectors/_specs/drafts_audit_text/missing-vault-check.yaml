skeleton: "name_match_missing_require"
name: "missing-vault-check"
class_name: "MissingVaultCheck"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Smart Vault - Zellic Audit Report"
help: |
    The SmartVaultManager contract has functions to manage each vault. So each function checks if the vault address is valid. However, there are some functions that do not check if the
wiki_title: "Missing vault check"
wiki_description: |
    The SmartVaultManager contract has functions to manage each vault. So each function checks if the vault address is valid. However, there are some functions that do not check if the vault address is valid. • pauseSmartVault • resumeSmartVault • updateVaultConfig • updateStakingVault • setStrategy • s
wiki_exploit_scenario: |
    Per audit finding: The SmartVaultManager contract has functions to manage each vault. So each function checks if the vault address is valid. However, there are some functions that do not check if the vault address is va
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingVaultCheck"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(pauseSmartVault|resumeSmartVault|updateVaultConfig).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "pauseSmartVault"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
