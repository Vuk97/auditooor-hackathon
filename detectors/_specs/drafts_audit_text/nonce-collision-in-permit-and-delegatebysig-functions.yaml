skeleton: "name_match_missing_require"
name: "nonce-collision-in-permit-and-delegatebysig-functions"
class_name: "NonceCollisionInPermitAndDelegatebysigFunctions"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Takara Lend Contracts - Zellic Audit Report"
help: |
    In the Rate token implementation, both permit() and delegateBySig() share the same nonce for signature verification: function permit(address owner, address spender, uint256 rawAmou
wiki_title: "Nonce collision in permit and delegateBySig functions"
wiki_description: |
    In the Rate token implementation, both permit() and delegateBySig() share the same nonce for signature verification: function permit(address owner, address spender, uint256 rawAmount, uint256 deadline, uint8 v, bytes32 r, bytes32 s) external { // ... bytes32 domainSeparator = keccak256(abi.encode(DO
wiki_exploit_scenario: |
    Per audit finding: In the Rate token implementation, both permit() and delegateBySig() share the same nonce for signature verification: function permit(address owner, address spender, uint256 rawAmount, uint256 deadline
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NonceCollisionInPermitAndDelegatebysigFunctions"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(delegateBySig|rawAmount|domainSeparator).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "delegateBySig"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
