skeleton: "name_match_missing_require"
name: "incorrect-size-check-when-encoding-unicode"
class_name: "IncorrectSizeCheckWhenEncodingUnicode"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Cosmos SDK Sign Mode Textual - Zellic Audit Report"
help: |
    The following code is part of the tx_display_translation function and converts a se- quence of Unicode codepoints into a string of escaped UTF-8 characters: *dst+) = '\)'; ASSERT_P
wiki_title: "Incorrect size check when encoding Unicode"
wiki_description: |
    The following code is part of the tx_display_translation function and converts a se- quence of Unicode codepoints into a string of escaped UTF-8 characters: *dst+) = '\)'; ASSERT_PTR_BOUNDS(count, dstLen); uint8_t bytes_to_print = 8; int32_t swapped = ZX_SWAP(tmp_codepoint); if (tmp_codepoint > 0xFF
wiki_exploit_scenario: |
    Per audit finding: The following code is part of the tx_display_translation function and converts a se- quence of Unicode codepoints into a string of escaped UTF-8 characters: *dst+) = '\)'; ASSERT_PTR_BOUNDS(count, dst
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectSizeCheckWhenEncodingUnicode"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(dstLen).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "dstLen"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
