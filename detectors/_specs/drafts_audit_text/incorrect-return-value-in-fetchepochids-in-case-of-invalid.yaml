skeleton: "name_match_missing_require"
name: "incorrect-return-value-in-fetchepochids-in-case-of-invalid"
class_name: "IncorrectReturnValueInFetchepochidsInCaseOfInvalid"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Y2K Finance - Zellic Audit Report"
help: |
    The function fetchEpochIds is used to get the list of epochIds, validVaults, and vault Type for the vaults that are active. The function loops through the vault’s array, calls the
wiki_title: "Incorrect return value in fetchEpochIds in case of invalid"
wiki_description: |
    The function fetchEpochIds is used to get the list of epochIds, validVaults, and vault Type for the vaults that are active. The function loops through the vault’s array, calls the function epochValid for each array, and returns the epochId, vaultType, and a boolean valid that describes if the vault
wiki_exploit_scenario: |
    Per audit finding: The function fetchEpochIds is used to get the list of epochIds, validVaults, and vault Type for the vaults that are active. The function loops through the vault’s array, calls the function epochValid
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectReturnValueInFetchepochidsInCaseOfInvalid"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(fetchEpochIds|epochIds|validVaults).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "fetchEpochIds"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
