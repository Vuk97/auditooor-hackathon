skeleton: "name_match_missing_require"
name: "add-length-validation-for-calldata-in-validatesessionusero"
class_name: "AddLengthValidationForCalldataInValidatesessionusero"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Biconomy Batched Session Router Module - Zellic Audit Report"
help: |
    In the validateSessionUserOp function, the length check for op.callData is incomplete, and there may be callData with illegal length. function validateSessionUserOp( UserOperation
wiki_title: "Add Length Validation for callData in validateSessionUserO"
wiki_description: |
    In the validateSessionUserOp function, the length check for op.callData is incomplete, and there may be callData with illegal length. function validateSessionUserOp( UserOperation calldata _op, bytes32 _userOpHash, bytes calldata _sessionKeyData, bytes calldata _sessionKeySignature ) external pure o
wiki_exploit_scenario: |
    Per audit finding: In the validateSessionUserOp function, the length check for op.callData is incomplete, and there may be callData with illegal length. function validateSessionUserOp( UserOperation calldata _op, bytes3
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AddLengthValidationForCalldataInValidatesessionusero"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validateSessionUserOp|callData|userOp).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validateSessionUserOp"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
