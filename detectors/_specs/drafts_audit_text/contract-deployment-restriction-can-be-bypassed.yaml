skeleton: "highlevelcall_missing_sibling"
name: "contract-deployment-restriction-can-be-bypassed"
class_name: "ContractDeploymentRestrictionCanBeBypassed"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-02-initia-cosmos"
help: |
    berndartmueller , also found by givn Finding description and impact The Create and Create2 message handlers check the sender is allowed to deploy a contract by calling assertAllowe
wiki_title: "Contract deployment restriction can be bypassed"
wiki_description: |
    berndartmueller , also found by givn Finding description and impact The Create and Create2 message handlers check the sender is allowed to deploy a contract by calling assertAllowedPublishers(..) here and here . The deployers can either be restricted to specific addresses ( params.AllowedPublishers
wiki_exploit_scenario: |
    Per audit finding: berndartmueller , also found by givn Finding description and impact The Create and Create2 message handlers check the sender is allowed to deploy a contract by calling assertAllowedPublishers(..) here
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ContractDeploymentRestrictionCanBeBypassed"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(assertAllowedPublishers).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
