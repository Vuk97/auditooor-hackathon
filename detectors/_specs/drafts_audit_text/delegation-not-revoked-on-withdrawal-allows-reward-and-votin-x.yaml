skeleton: "name_match_missing_require"
name: "delegation-not-revoked-on-withdrawal-allows-reward-and-votin-x"
class_name: "DelegationNotRevokedOnWithdrawalAllowsRewardAndVotinX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-04-virtuals-protocol"
help: |
    yaractf , also found by Ishenxx and nnamdi0482 Finding description and impact Stakers delegate their voting power using the stake() function, which internally calls _delegate(recei
wiki_title: "Delegation not revoked on withdrawal allows reward and voting power inflation post-maturity"
wiki_description: |
    yaractf , also found by Ishenxx and nnamdi0482 Finding description and impact Stakers delegate their voting power using the stake() function, which internally calls _delegate(receiver, delegatee) and records the delegatee via checkpointing at the current clock() (block number). Upon maturity ( matur
wiki_exploit_scenario: |
    Per audit finding: yaractf , also found by Ishenxx and nnamdi0482 Finding description and impact Stakers delegate their voting power using the stake() function, which internally calls _delegate(receiver, delegatee) and
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DelegationNotRevokedOnWithdrawalAllowsRewardAndVotinX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(matureAt|canStake|totalSupply).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "matureAt"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
