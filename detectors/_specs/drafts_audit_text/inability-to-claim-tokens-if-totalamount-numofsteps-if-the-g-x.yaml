skeleton: "name_match_missing_require"
name: "inability-to-claim-tokens-if-totalamount-numofsteps-if-the-g-x"
class_name: "InabilityToClaimTokensIfTotalamountNumofstepsIfTheGX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-12-secondswap"
help: |
    [L-03] Inability to claim tokens if totalAmount < numOfSteps If the grantor transfer an amount that make totalAmount < numOfSteps then the grantor will have a releaseRate of 0 lead
wiki_title: |
    Inability to claim tokens if totalAmount < numOfSteps If the grantor transfer an amount that make totalAmount < numOfSteps then the grantor will have a releaseR
wiki_description: |
    [L-03] Inability to claim tokens if totalAmount < numOfSteps If the grantor transfer an amount that make totalAmount < numOfSteps then the grantor will have a releaseRate of 0 leading to inability to claim his tokens once available for claim. Proof of Concept function transferVesting ( address _gran
wiki_exploit_scenario: |
    Per audit finding: [L-03] Inability to claim tokens if totalAmount < numOfSteps If the grantor transfer an amount that make totalAmount < numOfSteps then the grantor will have a releaseRate of 0 leading to inability to
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InabilityToClaimTokensIfTotalamountNumofstepsIfTheGX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(totalAmount|numOfSteps|releaseRate).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "totalAmount"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
