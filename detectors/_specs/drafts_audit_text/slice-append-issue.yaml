skeleton: "name_match_missing_call"
name: "slice-append-issue"
class_name: "SliceAppendIssue"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Fairyring - Zellic Audit Report"
help: |
    The BeginBlock function in module.go contains an inappropriate usage of make and append when initializing a slice. In the following snippet, the indexes slice is preallocated with
wiki_title: "Slice append issue"
wiki_description: |
    The BeginBlock function in module.go contains an inappropriate usage of make and append when initializing a slice. In the following snippet, the indexes slice is preallocated with a specific size, but elements are added using append instead of directly filling each position. This results in default
wiki_exploit_scenario: |
    Per audit finding: The BeginBlock function in module.go contains an inappropriate usage of make and append when initializing a slice. In the following snippet, the indexes slice is preallocated with a specific size, but
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SliceAppendIssue"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(encryptedTxs).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "encryptedTxs"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
