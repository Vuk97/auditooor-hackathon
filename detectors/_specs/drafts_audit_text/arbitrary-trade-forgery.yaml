skeleton: "name_match_missing_require"
name: "arbitrary-trade-forgery"
class_name: "ArbitraryTradeForgery"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Bebop - Zellic Audit Report"
help: |
    A malicious user can forge an arbitrary trade including any trader. function validateMakerSignature( address maker_address, bytes32 hash, Signature memory signature ) public view o
wiki_title: "Arbitrary trade forgery"
wiki_description: |
    A malicious user can forge an arbitrary trade including any trader. function validateMakerSignature( address maker_address, bytes32 hash, Signature memory signature ) public view override{ ...)) } else if (signature.signatureType =) SignatureType.EIP1271) { require(IERC1271(signature.walletAddress).
wiki_exploit_scenario: |
    Per audit finding: A malicious user can forge an arbitrary trade including any trader. function validateMakerSignature( address maker_address, bytes32 hash, Signature memory signature ) public view override{ ...)) } els
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArbitraryTradeForgery"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validateMakerSignature|signatureType|walletAddress).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validateMakerSignature"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
