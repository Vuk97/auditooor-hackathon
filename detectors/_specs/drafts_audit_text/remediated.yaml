skeleton: "name_match_missing_call"
name: "remediated"
class_name: "Remediated"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Gasp Node and Monorepo - Zellic Audit Report"
help: |
    After the updater invokes update_l1_from_l2 to update the Merkle tree root for a given range, any- one can call close_withdrawal or close_cancel to finalize processing of a pending
wiki_title: "Remediated"
wiki_description: |
    After the updater invokes update_l1_from_l2 to update the Merkle tree root for a given range, any- one can call close_withdrawal or close_cancel to finalize processing of a pending request, per- fecting the withdrawal or cancellation according to the corresponding entry in the Merkle tree. Merkle tr
wiki_exploit_scenario: |
    Per audit finding: After the updater invokes update_l1_from_l2 to update the Merkle tree root for a given range, any- one can call close_withdrawal or close_cancel to finalize processing of a pending request, per- fecti
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "Remediated"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(requestId|tokenAddress).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "requestId"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
