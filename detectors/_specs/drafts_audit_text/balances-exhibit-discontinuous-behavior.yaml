skeleton: "paired_function_divergence"
name: "balances-exhibit-discontinuous-behavior"
class_name: "BalancesExhibitDiscontinuousBehavior"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Beefy UniswapV3 - Zellic Audit Report"
help: |
    The strategy contract’s balances() function exhibits discontinuous behavior (that is, the function value “jumps”) stemming from two different sources. The first is in the balances(
wiki_title: "Balances exhibit discontinuous behavior"
wiki_description: |
    The strategy contract’s balances() function exhibits discontinuous behavior (that is, the function value “jumps”) stemming from two different sources. The first is in the balances() function itself: function balances() public view returns (uint256 token0Bal, uint256 token1Bal) { (uint256 thisBal0, u
wiki_exploit_scenario: |
    Per audit finding: The strategy contract’s balances() function exhibits discontinuous behavior (that is, the function value “jumps”) stemming from two different sources. The first is in the balances() function itself: f
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BalancesExhibitDiscontinuousBehavior"
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(balance|amount|total|supply|reserve).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
