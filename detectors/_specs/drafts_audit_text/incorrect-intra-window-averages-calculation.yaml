skeleton: "name_match_missing_require"
name: "incorrect-intra-window-averages-calculation"
class_name: "IncorrectIntraWindowAveragesCalculation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "hexens audit Algebra_public_11_08"
help: |
    The case of the oldest timepoint being within the WINDOW is managed with an assumption that such situation is only possible at the ﬁrst time window. As the timepoint array is uint1
wiki_title: "INCORRECT INTRA-WINDOW AVERAGES CALCULATION"
wiki_description: |
    The case of the oldest timepoint being within the WINDOW is managed with an assumption that such situation is only possible at the ﬁrst time window. As the timepoint array is uint16.max size (65536) and the default WINDOW is 86400 seconds, and also considering that only one timepoint can be written
wiki_exploit_scenario: |
    Per audit finding: The case of the oldest timepoint being within the WINDOW is managed with an assumption that such situation is only possible at the ﬁrst time window. As the timepoint array is uint16.max size (65536) a
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectIntraWindowAveragesCalculation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(volatilityCumulative|startOfWindow|getSingleTimepoint).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "volatilityCumulative"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
