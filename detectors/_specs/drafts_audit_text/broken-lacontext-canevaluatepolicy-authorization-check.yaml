skeleton: "name_match_missing_require"
name: "broken-lacontext-canevaluatepolicy-authorization-check"
class_name: "BrokenLacontextCanevaluatepolicyAuthorizationCheck"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Family Wallet - Zellic Audit Report"
help: |
    Family Wallet uses a check to LAContext.canEvaluatePolicy(..) to determine if a user presence check is needed for a given action. The check relies on canEvaluatePolicy(..) returnin
wiki_title: "Broken LAContext canEvaluatePolicy authorization check"
wiki_description: |
    Family Wallet uses a check to LAContext.canEvaluatePolicy(..) to determine if a user presence check is needed for a given action. The check relies on canEvaluatePolicy(..) returning true if a device-authentication mode is configured. This check is used in multiple places in the codebase, namely the
wiki_exploit_scenario: |
    Per audit finding: Family Wallet uses a check to LAContext.canEvaluatePolicy(..) to determine if a user presence check is needed for a given action. The check relies on canEvaluatePolicy(..) returning true if a device-a
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BrokenLacontextCanevaluatepolicyAuthorizationCheck"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(canEvaluatePolicy|edStore).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "canEvaluatePolicy"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
