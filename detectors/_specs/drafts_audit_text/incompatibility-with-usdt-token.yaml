skeleton: "name_match_missing_call"
name: "incompatibility-with-usdt-token"
class_name: "IncompatibilityWithUsdtToken"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Y2K Finance - Zellic Audit Report"
help: |
    While depositing ERC-20 tokens to the vault, the contract first approves the token to the vault using safeApprove from the solmate library and then calls deposit on the earthquake
wiki_title: "Incompatibility with USDT token"
wiki_description: |
    While depositing ERC-20 tokens to the vault, the contract first approves the token to the vault using safeApprove from the solmate library and then calls deposit on the earthquake vault in a try-catch. The code is as follows: function _depositToVault( uint256 id, uint256 amount, address inputToken,
wiki_exploit_scenario: |
    Per audit finding: While depositing ERC-20 tokens to the vault, the contract first approves the token to the vault using safeApprove from the solmate library and then calls deposit on the earthquake vault in a try-catch
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncompatibilityWithUsdtToken"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(safeApprove|inputToken|vaultAddress).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "safeApprove"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
