skeleton: "name_match_missing_call"
name: "the-routeprocessor3-should-not-hold-nontransient-tokens"
class_name: "TheRouteprocessor3ShouldNotHoldNontransientTokens"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit SushiSwap RouteProcessor3 - Zellic Audit Report"
help: |
    There are numerous ways in which tokens can be stolen if they are held by the RoutePro- cessor3. For example, one way is by directly asking the contract to wrap or unwrap Ether and
wiki_title: "The RouteProcessor3 should not hold nontransient tokens"
wiki_description: |
    There are numerous ways in which tokens can be stolen if they are held by the RoutePro- cessor3. For example, one way is by directly asking the contract to wrap or unwrap Ether and transfer it to the user. function wrapNative(uint256 stream, address from, address tokenIn, uint256 amountIn) private {
wiki_exploit_scenario: |
    Per audit finding: There are numerous ways in which tokens can be stolen if they are held by the RoutePro- cessor3. For example, one way is by directly asking the contract to wrap or unwrap Ether and transfer it to the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheRouteprocessor3ShouldNotHoldNontransientTokens"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(wrapNative|tokenIn|amountIn).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "wrapNative"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
