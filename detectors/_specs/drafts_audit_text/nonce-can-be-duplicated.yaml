skeleton: "name_match_missing_call"
name: "nonce-can-be-duplicated"
class_name: "NonceCanBeDuplicated"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Apyx Stablecoin - Zellic Audit Report"
help: |
    The MinterV0 contract uses an EIP-712 signature scheme that includes a full uint48 nonce to prevent replay attacks. However, when scheduling the mint operation via the AccessManage
wiki_title: "Nonce can be duplicated"
wiki_description: |
    The MinterV0 contract uses an EIP-712 signature scheme that includes a full uint48 nonce to prevent replay attacks. However, when scheduling the mint operation via the AccessManager, the _encodeOrderData function appends only a truncated version of the nonce (nonce % 256) to the calldata: function _
wiki_exploit_scenario: |
    Per audit finding: The MinterV0 contract uses an EIP-712 signature scheme that includes a full uint48 nonce to prevent replay attacks. However, when scheduling the mint operation via the AccessManager, the _encodeOrderD
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NonceCanBeDuplicated"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(encodePacked|encodeCall|apxUSD).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "encodePacked"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
