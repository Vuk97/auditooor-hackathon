skeleton: "name_match_missing_require"
name: "temporary-high-vote-power-when-claiming-in-native-token-36"
class_name: "TemporaryHighVotePowerWhenClaimingInNativeToken36"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Mitosis (May) - Zellic Audit Report"
help: |
    The _requestUnstake function in the ValidatorStaking contract does not properly validate whether the unstaking amount is less than or equal to the user's staked amount. This oversi
wiki_title: "Temporary high vote power when claiming in native token                       36"
wiki_description: |
    The _requestUnstake function in the ValidatorStaking contract does not properly validate whether the unstaking amount is less than or equal to the user's staked amount. This oversight could potentially allow users to unstake more tokens than they have actually staked. The _assertUnstakeAmountConditi
wiki_exploit_scenario: |
    Per audit finding: The _requestUnstake function in the ValidatorStaking contract does not properly validate whether the unstaking amount is less than or equal to the user's staked amount. This oversight could potentiall
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TemporaryHighVotePowerWhenClaimingInNativeToken36"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(valAddr|currentStaked|minUnstaking).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "valAddr"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
