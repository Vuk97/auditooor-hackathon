skeleton: "name_match_missing_call"
name: "improper-status-tracking"
class_name: "ImproperStatusTracking"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Gasp Node and Monorepo - Zellic Audit Report"
help: |
    The functions respondToOpTask, respondToRdTask, and forceRespondToOpTask contain multiple occurrences of a basic coding mistake. The code uses the comparison operator == instead of
wiki_title: "Improper status tracking"
wiki_description: |
    The functions respondToOpTask, respondToRdTask, and forceRespondToOpTask contain multiple occurrences of a basic coding mistake. The code uses the comparison operator == instead of the assignment operator = when updating the idToTaskStatus mapping. The task statuses are there- fore not being updated
wiki_exploit_scenario: |
    Per audit finding: The functions respondToOpTask, respondToRdTask, and forceRespondToOpTask contain multiple occurrences of a basic coding mistake. The code uses the comparison operator == instead of the assignment oper
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ImproperStatusTracking"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(respondToOpTask|respondToRdTask|forceRespondToOpTask).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "respondToOpTask"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
