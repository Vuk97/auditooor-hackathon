skeleton: "name_match_missing_require"
name: "the-checkcap-function-is-missing-a-check"
class_name: "TheCheckcapFunctionIsMissingACheck"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Plaza - Zellic Audit Report"
help: |
    The depositCap state variable in the PreDeposit contract is used to represent the maximum deposit amount of the contract. In the PreDeposit::_checkCap function, there is no check t
wiki_title: "The _checkCap function is missing a check"
wiki_description: |
    The depositCap state variable in the PreDeposit contract is used to represent the maximum deposit amount of the contract. In the PreDeposit::_checkCap function, there is no check to ensure that totalUserDepositValue > 0. function _checkCap(address[] memory tokens, uint256[] memory amounts) private v
wiki_exploit_scenario: |
    Per audit finding: The depositCap state variable in the PreDeposit contract is used to represent the maximum deposit amount of the contract. In the PreDeposit::_checkCap function, there is no check to ensure that totalU
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheCheckcapFunctionIsMissingACheck"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(depositCap|totalUserDepositValue|balancerOracleAdapter).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "depositCap"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
