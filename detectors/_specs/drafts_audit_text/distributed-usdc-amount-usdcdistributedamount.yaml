skeleton: "name_match_missing_call"
name: "distributed-usdc-amount-usdcdistributedamount"
class_name: "DistributedUsdcAmountUsdcdistributedamount"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LayerZero VeDistributor - Zellic Audit Report"
help: |
    ount (usdcDistributedAmount) This value is incremented when distribute is called by the owner — and that function may be called at any time after the contract is deployed. It is ne
wiki_title: "Distributed USDC amount (usdcDistributedAmount)"
wiki_description: |
    ount (usdcDistributedAmount) This value is incremented when distribute is called by the owner — and that function may be called at any time after the contract is deployed. It is never decremented. It is used for calculating the amount of USDC the user may claim: function _calculateUsdcClaimable(addr
wiki_exploit_scenario: |
    Per audit finding: ount (usdcDistributedAmount) This value is incremented when distribute is called by the owner — and that function may be called at any time after the contract is deployed. It is never decremented. It
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DistributedUsdcAmountUsdcdistributedamount"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(usdcDistributedAmount|usdcClaimable|usdcClaimed).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "usdcDistributedAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
