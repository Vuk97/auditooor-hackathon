skeleton: "name_match_missing_call"
name: "token-transfer-permanent-loss"
class_name: "TokenTransferPermanentLoss"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit d3-doma - Zellic Audit Report"
help: |
    Domain tokens can be permanently lost due to interactions between token transfer and the bridge mechanisms. The specific scenario occurs through the following sequence: 1. Alice ow
wiki_title: "Token-transfer permanent loss"
wiki_description: |
    Domain tokens can be permanently lost due to interactions between token transfer and the bridge mechanisms. The specific scenario occurs through the following sequence: 1. Alice owns a non-EOI ownership token for alice.xyz on chain A. 2. Alice transfers the token to Bob, triggering the _beforeTokenT
wiki_exploit_scenario: |
    Per audit finding: Domain tokens can be permanently lost due to interactions between token transfer and the bridge mechanisms. The specific scenario occurs through the following sequence: 1. Alice owns a non-EOI ownersh
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TokenTransferPermanentLoss"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(claimedBy|tokenTransfer|isClaimedByDomaProxy).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "claimedBy"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
