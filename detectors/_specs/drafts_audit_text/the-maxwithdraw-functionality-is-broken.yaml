skeleton: "name_match_missing_call"
name: "the-maxwithdraw-functionality-is-broken"
class_name: "TheMaxwithdrawFunctionalityIsBroken"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Voyage - Zellic Audit Report"
help: |
    Depositors will be unable to use the intended maxWithdraw functionality in withdra w(...))): uint256 userBalance = vToken.maxWithdraw(msg.sender); uint256 amountToWithdraw = _amoun
wiki_title: "The maxWithdraw functionality is broken"
wiki_description: |
    Depositors will be unable to use the intended maxWithdraw functionality in withdra w(...))): uint256 userBalance = vToken.maxWithdraw(msg.sender); uint256 amountToWithdraw = _amount; if (_amount == type(uint256).max) { amountToWithdraw = userBalance; } BorrowState storage borrowState = LibAppStorage
wiki_exploit_scenario: |
    Per audit finding: Depositors will be unable to use the intended maxWithdraw functionality in withdra w(...))): uint256 userBalance = vToken.maxWithdraw(msg.sender); uint256 amountToWithdraw = _amount; if (_amount == ty
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheMaxwithdrawFunctionalityIsBroken"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(maxWithdraw|userBalance|vToken).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "maxWithdraw"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
