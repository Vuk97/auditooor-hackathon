skeleton: "name_match_missing_call"
name: "null-terminated-strings-enable-trickery-attacks"
class_name: "NullTerminatedStringsEnableTrickeryAttacks"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Cosmos SDK Sign Mode Textual - Zellic Audit Report"
help: |
    The tx_display_translation function copies the src buffer to dst, incrementing the pointer p each iteration, formatting bytes as desired. However, since the src buffer can contain
wiki_title: "Null-terminated strings enable trickery attacks"
wiki_description: |
    The tx_display_translation function copies the src buffer to dst, incrementing the pointer p each iteration, formatting bytes as desired. However, since the src buffer can contain a null byte, the while loop may be stopped early: parser_error_t tx_display_translation(char *dst, uint16_t dstLen, char
wiki_exploit_scenario: |
    Per audit finding: The tx_display_translation function copies the src buffer to dst, incrementing the pointer p each iteration, formatting bytes as desired. However, since the src buffer can contain a null byte, the whi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NullTerminatedStringsEnableTrickeryAttacks"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(dstLen|srcLen).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "dstLen"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
