skeleton: "name_match_missing_require"
name: "insufficient-validation-of-parameters"
class_name: "InsufficientValidationOfParameters"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit 1inch Farming Audit Report"
help: |
    The function setDistributor verifies that the new distributor is not the old distributor, but it does not verify that the new distributor is nonzero. function setDistributor(addres
wiki_title: "Insufficient validation of parameters"
wiki_description: |
    The function setDistributor verifies that the new distributor is not the old distributor, but it does not verify that the new distributor is nonzero. function setDistributor(address distributor_) external onlyOwner { address oldDistributor = distributor; require(distributor_ != oldDistributor, ”FP:
wiki_exploit_scenario: |
    Per audit finding: The function setDistributor verifies that the new distributor is not the old distributor, but it does not verify that the new distributor is nonzero. function setDistributor(address distributor_) exte
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InsufficientValidationOfParameters"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setDistributor|onlyOwner|oldDistributor).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "setDistributor"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
