skeleton: "name_match_missing_require"
name: "failed-approval-check-in-calculateandclaiminterest"
class_name: "FailedApprovalCheckInCalculateandclaiminterest"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Revest Resonate Pt. 1 - Zellic Audit Report"
help: |
    The allowance check for token transfer approval always fails in calculateAndClaimIn terest(...))): ) public override onlyMaster returns (uint interest, uint sharesRedeemed) { IERC4
wiki_title: "Failed approval check in calculateAndClaimInterest"
wiki_description: |
    The allowance check for token transfer approval always fails in calculateAndClaimIn terest(...))): ) public override onlyMaster returns (uint interest, uint sharesRedeemed) { IERC4626 vault = IERC4626(vaultAdapter); if(IERC20(vaultToken).allowance(address(this), vaultAdapter) < interest) { IERC20(va
wiki_exploit_scenario: |
    Per audit finding: The allowance check for token transfer approval always fails in calculateAndClaimIn terest(...))): ) public override onlyMaster returns (uint interest, uint sharesRedeemed) { IERC4626 vault = IERC4626
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FailedApprovalCheckInCalculateandclaiminterest"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(calculateAndClaimIn|onlyMaster|sharesRedeemed).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "calculateAndClaimIn"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
