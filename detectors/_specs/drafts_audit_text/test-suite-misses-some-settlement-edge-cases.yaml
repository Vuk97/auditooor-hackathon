skeleton: "name_match_missing_require"
name: "test-suite-misses-some-settlement-edge-cases"
class_name: "TestSuiteMissesSomeSettlementEdgeCases"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit RFQ Protocol - Zellic Audit Report"
help: |
    The project includes a solid baseline test suite (10 passing tests) that covers valid settlement flows and several revert paths. However, it still misses some edge-case scenarios i
wiki_title: "Test suite misses some settlement edge cases"
wiki_description: |
    The project includes a solid baseline test suite (10 passing tests) that covers valid settlement flows and several revert paths. However, it still misses some edge-case scenarios identified during this audit. In particular, as of the time of writing, the tests do not explicitly exercise • relayer-si
wiki_exploit_scenario: |
    Per audit finding: The project includes a solid baseline test suite (10 passing tests) that covers valid settlement flows and several revert paths. However, it still misses some edge-case scenarios identified during thi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TestSuiteMissesSomeSettlementEdgeCases"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(feePips|setFeeTreasury).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "feePips"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
