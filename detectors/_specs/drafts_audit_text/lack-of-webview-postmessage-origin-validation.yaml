skeleton: "name_match_missing_call"
name: "lack-of-webview-postmessage-origin-validation"
class_name: "LackOfWebviewPostmessageOriginValidation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The Reclaim app uses a WebView to render the callback and authentication pages. The WebView components listen to message events sent by injected JavaScript running on the embedded
wiki_title: "Lack of WebView postMessage origin validation"
wiki_description: |
    The Reclaim app uses a WebView to render the callback and authentication pages. The WebView components listen to message events sent by injected JavaScript running on the embedded web- pages. However, Reclaim does not validate the origin of these messages against an expected value, allowing a malici
wiki_exploit_scenario: |
    Per audit finding: The Reclaim app uses a WebView to render the callback and authentication pages. The WebView components listen to message events sent by injected JavaScript running on the embedded web- pages. However,
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfWebviewPostmessageOriginValidation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(webviewRpc|useWitnessWebview|onMessage).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "webviewRpc"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
