skeleton: "name_match_missing_call"
name: "transfer-block-bypass"
class_name: "TransferBlockBypass"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit d3-doma - Zellic Audit Report"
help: |
    When blockAllTransfers is set to true in NameToken, the intention is for all transfers to be blocked. However, even with blockAllTransfers being set, users can still use the bridgi
wiki_title: "Transfer block bypass"
wiki_description: |
    When blockAllTransfers is set to true in NameToken, the intention is for all transfers to be blocked. However, even with blockAllTransfers being set, users can still use the bridging functionality to transfer tokens to other chains to arbitrary addresses. This allows circumvention of transfer restri
wiki_exploit_scenario: |
    Per audit finding: When blockAllTransfers is set to true in NameToken, the intention is for all transfers to be blocked. However, even with blockAllTransfers being set, users can still use the bridging functionality to
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TransferBlockBypass"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(blockAllTransfers).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "blockAllTransfers"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
