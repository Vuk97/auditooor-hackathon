skeleton: "name_match_missing_call"
name: "lack-of-extractedparametervalues-validation-in-callback"
class_name: "LackOfExtractedparametervaluesValidationInCallback"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The Reclaim documentation recommends websites use the values in extractedParameterValues. However, the SDK does not validate this data and only validates the claim claimData.parame
wiki_title: "Lack of extractedParameterValues validation in callback"
wiki_description: |
    The Reclaim documentation recommends websites use the values in extractedParameterValues. However, the SDK does not validate this data and only validates the claim claimData.parameters. A malicious user can modify the extractedParameterValues of a valid, signed claim to contain any value they like.
wiki_exploit_scenario: |
    Per audit finding: The Reclaim documentation recommends websites use the values in extractedParameterValues. However, the SDK does not validate this data and only validates the claim claimData.parameters. A malicious us
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfExtractedparametervaluesValidationInCallback"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(extractedParameterValues|claimData|verifySignedProof).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "extractedParameterValues"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
