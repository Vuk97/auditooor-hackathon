skeleton: "name_match_missing_call"
name: "overflow-in-readbytes"
class_name: "OverflowInReadbytes"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit SushiSwap RouteProcessor3 - Zellic Audit Report"
help: |
    The InputStream library can be used to treat a bytes variable as a read-only stream, managing a cursor that is incremented automatically as the stream is consumed. The readBytes fu
wiki_title: "Overflow in readBytes"
wiki_description: |
    The InputStream library can be used to treat a bytes variable as a read-only stream, managing a cursor that is incremented automatically as the stream is consumed. The readBytes function can be used to read a sequence of bytes from the stream. The sequence is encoded as a length, followed by the con
wiki_exploit_scenario: |
    Per audit finding: The InputStream library can be used to treat a bytes variable as a read-only stream, managing a cursor that is incremented automatically as the stream is consumed. The readBytes function can be used t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OverflowInReadbytes"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(readBytes).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "readBytes"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
