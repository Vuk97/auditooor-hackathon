skeleton: "name_match_missing_call"
name: "extra-calls-into-timelock-eoa-will-fail"
class_name: "ExtraCallsIntoTimelockEoaWillFail"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Cultured - Zellic Audit Report"
help: |
    Plume Network intends to replace the Timelock contract with a governance-owned EOA. In order to implement this change, these calls into the timelock were removed in PositionUtils:
wiki_title: "Extra calls into timelock EOA will fail"
wiki_description: |
    Plume Network intends to replace the Timelock contract with a governance-owned EOA. In order to implement this change, these calls into the timelock were removed in PositionUtils: ITimelock(timelock).enableLeverage(_vault); // ITimelock(timelock).enableLeverage(_vault); IRouter(_router).pluginIncrea
wiki_exploit_scenario: |
    Per audit finding: Plume Network intends to replace the Timelock contract with a governance-owned EOA. In order to implement this change, these calls into the timelock were removed in PositionUtils: ITimelock(timelock).
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ExtraCallsIntoTimelockEoaWillFail"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(enableLeverage|pluginIncreasePosition|disableLeverage).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "enableLeverage"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
