skeleton: "name_match_missing_call"
name: "race-condition-allows-admin-to-drain-the-vault"
class_name: "RaceConditionAllowsAdminToDrainTheVault"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Solera - Zellic Audit Report"
help: |
    There is an opportunity for a race condition during the redemption step. After calling requestRedeem but before redeem is executed, an admin changing the withdraw fee can cause mor
wiki_title: "Race condition allows admin to drain the vault"
wiki_description: |
    There is an opportunity for a race condition during the redemption step. After calling requestRedeem but before redeem is executed, an admin changing the withdraw fee can cause more assets than a user paid for to be transferred out of the vault. The root cause is that the fee percentage is calculate
wiki_exploit_scenario: |
    Per audit finding: There is an opportunity for a race condition during the redemption step. After calling requestRedeem but before redeem is executed, an admin changing the withdraw fee can cause more assets than a user
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RaceConditionAllowsAdminToDrainTheVault"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(requestRedeem).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "requestRedeem"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
