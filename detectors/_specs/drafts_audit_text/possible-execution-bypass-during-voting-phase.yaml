skeleton: "highlevelcall_missing_sibling"
name: "possible-execution-bypass-during-voting-phase"
class_name: "PossibleExecutionBypassDuringVotingPhase"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "hexens audit API3_public"
help: |
    The function _canExecute on lines 352, 107 and 88 systematically gates the only way to execute an EVMScript, _unsafeExecuteVote, on lines 327 and 334. In turn, on line 347, _unsafe
wiki_title: "POSSIBLE EXECUTION BYPASS DURING VOTING PHASE"
wiki_description: |
    The function _canExecute on lines 352, 107 and 88 systematically gates the only way to execute an EVMScript, _unsafeExecuteVote, on lines 327 and 334. In turn, on line 347, _unsafeExecuteVote calls runScript with the passed vote's EVMScript as argument, and without any additional input, nor any blac
wiki_exploit_scenario: |
    Per audit finding: The function _canExecute on lines 352, 107 and 88 systematically gates the only way to execute an EVMScript, _unsafeExecuteVote, on lines 327 and 334. In turn, on line 347, _unsafeExecuteVote calls ru
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PossibleExecutionBypassDuringVotingPhase"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(supportRequiredPct).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
