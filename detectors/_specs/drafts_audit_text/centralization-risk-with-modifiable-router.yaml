skeleton: "name_match_missing_call"
name: "centralization-risk-with-modifiable-router"
class_name: "CentralizationRiskWithModifiableRouter"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit LayerZero DVN - Zellic Audit Report"
help: |
    The setRouter function can be used by the admin to modify the CCIP router. By setting the router to a malicious contract, it is possible to send arbitrary messages to the DVN to de
wiki_title: "Centralization risk with modifiable router"
wiki_description: |
    The setRouter function can be used by the admin to modify the CCIP router. By setting the router to a malicious contract, it is possible to send arbitrary messages to the DVN to deliver.
wiki_exploit_scenario: |
    Per audit finding: The setRouter function can be used by the admin to modify the CCIP router. By setting the router to a malicious contract, it is possible to send arbitrary messages to the DVN to deliver.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CentralizationRiskWithModifiableRouter"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setRouter).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "setRouter"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
