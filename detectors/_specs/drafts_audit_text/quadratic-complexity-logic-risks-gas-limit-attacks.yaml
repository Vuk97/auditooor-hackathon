skeleton: "name_match_missing_call"
name: "quadratic-complexity-logic-risks-gas-limit-attacks"
class_name: "QuadraticComplexityLogicRisksGasLimitAttacks"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Lido Fixed Income - Zellic Audit Report"
help: |
    In the LidoVault, the variable-length array fixedOngoingWithdrawalUsers is used to store a list of fixed-side users that requested a withdrawal while the vault has not ended. Durin
wiki_title: "Quadratic-complexity logic risks gas-limit attacks"
wiki_description: |
    In the LidoVault, the variable-length array fixedOngoingWithdrawalUsers is used to store a list of fixed-side users that requested a withdrawal while the vault has not ended. During the vault-finalization process, the internal function claimOngoingFixedWithdrawals() is called to finish all of the on
wiki_exploit_scenario: |
    Per audit finding: In the LidoVault, the variable-length array fixedOngoingWithdrawalUsers is used to store a list of fixed-side users that requested a withdrawal while the vault has not ended. During the vault-finaliza
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "QuadraticComplexityLogicRisksGasLimitAttacks"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(fixedOngoingWithdrawalUsers|claimOngoingFixedWithdrawals|arrayLength).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "fixedOngoingWithdrawalUsers"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
