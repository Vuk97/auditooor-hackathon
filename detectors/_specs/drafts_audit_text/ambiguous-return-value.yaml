skeleton: "name_match_missing_call"
name: "ambiguous-return-value"
class_name: "AmbiguousReturnValue"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Socket Data Layer - Zellic Audit Report"
help: |
    Within BaseCapacitor, the getLastFilledPacket() is written as follows: /** * @dev Returns the count of the latest packet that finished filling. * @dev Returns 0 in case no packets
wiki_title: "Ambiguous return value"
wiki_description: |
    Within BaseCapacitor, the getLastFilledPacket() is written as follows: /** * @dev Returns the count of the latest packet that finished filling. * @dev Returns 0 in case no packets are filled. * @return lastFilledPacket count of the latest packet. *) function getLastFilledPacket() external view retur
wiki_exploit_scenario: |
    Per audit finding: Within BaseCapacitor, the getLastFilledPacket() is written as follows: /** * @dev Returns the count of the latest packet that finished filling. * @dev Returns 0 in case no packets are filled. * @retur
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AmbiguousReturnValue"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getLastFilledPacket|lastFilledPacket).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "getLastFilledPacket"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
