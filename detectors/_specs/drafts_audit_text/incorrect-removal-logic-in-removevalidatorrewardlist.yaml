skeleton: "name_match_missing_require"
name: "incorrect-removal-logic-in-removevalidatorrewardlist"
class_name: "IncorrectRemovalLogicInRemovevalidatorrewardlist"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Spectral Modelers - Zellic Audit Report"
help: |
    The removeValidatorRewardList function is designed to remove a validator from the valida- torAddresses array. However, the current implementation has a flaw. If the validator to be
wiki_title: "Incorrect removal logic in removeValidatorRewardList"
wiki_description: |
    The removeValidatorRewardList function is designed to remove a validator from the valida- torAddresses array. However, the current implementation has a flaw. If the validator to be re- moved is not the last one in the array, the function incorrectly removes the last validator instead of the intended
wiki_exploit_scenario: |
    Per audit finding: The removeValidatorRewardList function is designed to remove a validator from the valida- torAddresses array. However, the current implementation has a flaw. If the validator to be re- moved is not th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectRemovalLogicInRemovevalidatorrewardlist"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(removeValidatorRewardList|torAddresses|validatorAddresses).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "removeValidatorRewardList"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
