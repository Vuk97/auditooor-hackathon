skeleton: "name_match_missing_call"
name: "protocol-fee-calculation-is-reversed"
class_name: "ProtocolFeeCalculationIsReversed"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Maia DAO February 2023 - Zellic Audit Report"
help: |
    The unstakeAndWithdraw function first unstakes the NFT and then calculates the pend- ing rewards and splits it between the user and the protocol based on the current pro tocolFee (
wiki_title: "Protocol fee calculation is reversed"
wiki_description: |
    The unstakeAndWithdraw function first unstakes the NFT and then calculates the pend- ing rewards and splits it between the user and the protocol based on the current pro tocolFee (the default being 20%). function unstakeAndWithdraw(uint256 tokenId) external { address user = tokenIdToUser[tokenId]; i
wiki_exploit_scenario: |
    Per audit finding: The unstakeAndWithdraw function first unstakes the NFT and then calculates the pend- ing rewards and splits it between the user and the protocol based on the current pro tocolFee (the default being 20
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ProtocolFeeCalculationIsReversed"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(unstakeAndWithdraw|tocolFee|tokenId).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "unstakeAndWithdraw"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
