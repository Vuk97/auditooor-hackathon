skeleton: "name_match_missing_require"
name: "erc777-re-entrancy-attack"
class_name: "Erc777ReEntrancyAttack"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "hexens audit Hexens_Polygon_zkEVM_PUBLIC_27.02.23"
help: |
    In the function bridgeAsset() any user can bridge his ERC20 tokens specifying the destination network and the destination address, in case it is not a wrapped token it will call tr
wiki_title: "ERC777 RE-ENTRANCY ATTACK"
wiki_description: |
    In the function bridgeAsset() any user can bridge his ERC20 tokens specifying the destination network and the destination address, in case it is not a wrapped token it will call transferFrom (SafeERC20.safeTransferFrom) and update the balance of the contract and eventually create a deposit leaf in t
wiki_exploit_scenario: |
    Per audit finding: In the function bridgeAsset() any user can bridge his ERC20 tokens specifying the destination network and the destination address, in case it is not a wrapped token it will call transferFrom (SafeERC2
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "Erc777ReEntrancyAttack"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(bridgeAsset|transferFrom|safeTransferFrom).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "bridgeAsset"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
