skeleton: "paired_function_divergence"
name: "executionmanager-should-assert-function-requirements"
class_name: "ExecutionmanagerShouldAssertFunctionRequirements"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Socket Data Layer - Zellic Audit Report"
help: |
    The payAndCheckFees function uses the maxPacketLength_ argument to reduce the transmission fees to split the fee between messages in the packet: function payAndCheckFees( uint256 m
wiki_title: "ExecutionManager should assert function requirements"
wiki_description: |
    The payAndCheckFees function uses the maxPacketLength_ argument to reduce the transmission fees to split the fee between messages in the packet: function payAndCheckFees( uint256 minMsgGasLimit_, uint256 payloadSize_, bytes32 executionParams_, bytes32, uint32 siblingChainSlug_, uint128 switchboardFe
wiki_exploit_scenario: |
    Per audit finding: The payAndCheckFees function uses the maxPacketLength_ argument to reduce the transmission fees to split the fee between messages in the packet: function payAndCheckFees( uint256 minMsgGasLimit_, uint
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ExecutionmanagerShouldAssertFunctionRequirements"
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(maxPacketLength_).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
