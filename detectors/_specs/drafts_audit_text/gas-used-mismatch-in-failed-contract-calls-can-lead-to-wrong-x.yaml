skeleton: "name_match_missing_require"
name: "gas-used-mismatch-in-failed-contract-calls-can-lead-to-wrong-x"
class_name: "GasUsedMismatchInFailedContractCallsCanLeadToWrongX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-11-nibiru"
help: |
    0x41 In call_contract.go , when a contract call fails, the code only consumes gas for the failed transaction but does not account for previously accumulated block gas usage. This c
wiki_title: "Gas used mismatch in failed contract calls can lead to wrong gas deductions"
wiki_description: |
    0x41 In call_contract.go , when a contract call fails, the code only consumes gas for the failed transaction but does not account for previously accumulated block gas usage. This creates a mismatch between the actual gas used in the block and what gets consumed in the gas meter. The issue occurs in
wiki_exploit_scenario: |
    Per audit finding: 0x41 In call_contract.go , when a contract call fails, the code only consumes gas for the failed transaction but does not account for previously accumulated block gas usage. This creates a mismatch be
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "GasUsedMismatchInFailedContractCallsCanLeadToWrongX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(evmResp|blockGasUsed|gasLimit).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "evmResp"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
