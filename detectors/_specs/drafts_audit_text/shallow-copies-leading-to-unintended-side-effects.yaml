skeleton: "name_match_missing_require"
name: "shallow-copies-leading-to-unintended-side-effects"
class_name: "ShallowCopiesLeadingToUnintendedSideEffects"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Brevis - Zellic Audit Report"
help: |
    Some functions provided by the sdk take in pointer-like types as arguments (possibly occurring in a nested manner, as fields of a struct) and return these pointer-like types again
wiki_title: "Shallow copies leading to unintended side effects"
wiki_description: |
    Some functions provided by the sdk take in pointer-like types as arguments (possibly occurring in a nested manner, as fields of a struct) and return these pointer-like types again (usually as part of a struct). In most such instances, a deep copy is performed, with the data that is being pointed at
wiki_exploit_scenario: |
    Per audit finding: Some functions provided by the sdk take in pointer-like types as arguments (possibly occurring in a nested manner, as fields of a struct) and return these pointer-like types again (usually as part of
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ShallowCopiesLeadingToUnintendedSideEffects"
state_decl: "uint256 internal toggles;"
fn_name_regex: ".*(toggles).*"
write_var_regex: ".*(toggles).*"
guard_var_regex: ".*(toggles).*"
vuln_fn_name: "toggles"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "toggles = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
