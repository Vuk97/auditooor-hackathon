skeleton: "name_match_missing_call"
name: "refunds-sent-to-tx-origin"
class_name: "RefundsSentToTxOrigin"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit SushiXSwap V2 - Zellic Audit Report"
help: |
    The Axelar, CCTP, and Stargate adapters use tx.origin as the address that receives gas refunds in their implementation of adapterBridge. This might not be the desired recipient of
wiki_title: "Refunds sent to tx.origin"
wiki_description: |
    The Axelar, CCTP, and Stargate adapters use tx.origin as the address that receives gas refunds in their implementation of adapterBridge. This might not be the desired recipient of the refund. For example, consider the case of an EOA (user) invoking a contract that in turn calls SushiXSwap to bridge
wiki_exploit_scenario: |
    Per audit finding: The Axelar, CCTP, and Stargate adapters use tx.origin as the address that receives gas refunds in their implementation of adapterBridge. This might not be the desired recipient of the refund. For exam
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RefundsSentToTxOrigin"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(adapterBridge).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "adapterBridge"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
