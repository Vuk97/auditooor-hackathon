skeleton: "name_match_missing_call"
name: "unnecessary-approvals-of-zero-tokens-14"
class_name: "UnnecessaryApprovalsOfZeroTokens14"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Solera - Zellic Audit Report (March)"
help: |
    The code deposits the asset using the following function: currentAmount = IAggregateToken(nRWA).deposit(borrowAmount, address(this), address(this)); function deposit(uint256 assets
wiki_title: "Unnecessary approvals of zero tokens                            14"
wiki_description: |
    The code deposits the asset using the following function: currentAmount = IAggregateToken(nRWA).deposit(borrowAmount, address(this), address(this)); function deposit(uint256 assets, address receiver, address controller) public override returns (uint256 shares) { if (receiver != msg.sender) { revert
wiki_exploit_scenario: |
    Per audit finding: The code deposits the asset using the following function: currentAmount = IAggregateToken(nRWA).deposit(borrowAmount, address(this), address(this)); function deposit(uint256 assets, address receiver,
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnnecessaryApprovalsOfZeroTokens14"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(currentAmount|nRWA|borrowAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "currentAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
