skeleton: "name_match_missing_call"
name: "loop-optimisation"
class_name: "LoopOptimisation"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "hexens audit Hexens_Polygon_zkEVM_PUBLIC_27.02.23"
help: |
    In the function updateBatchFee the function calculates number of blocks that were veriﬁed above and below the veriﬁcation time target. The fee is updated based corresponding to the
wiki_title: "LOOP OPTIMISATION"
wiki_description: |
    In the function updateBatchFee the function calculates number of blocks that were veriﬁed above and below the veriﬁcation time target. The fee is updated based corresponding to their ratio. The loop that checks for targets that are above the target does a backward loop from the newly veriﬁed batch u
wiki_exploit_scenario: |
    Per audit finding: In the function updateBatchFee the function calculates number of blocks that were veriﬁed above and below the veriﬁcation time target. The fee is updated based corresponding to their ratio. The loop t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LoopOptimisation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(updateBatchFee|sequencedTimestamp|currentBatch).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "updateBatchFee"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
