skeleton: "name_match_missing_require"
name: "output-note-footers-can-be-reused-within-the-same-action"
class_name: "OutputNoteFootersCanBeReusedWithinTheSameAction"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Singularity - Zellic Audit Report"
help: |
    This issue is similar to Finding 3.1. ↗, but it is for note footers instead of for nullifiers. In actions where multiple note footers are involved for outputs, checking whether the
wiki_title: "Output-note footers can be reused within the same action"
wiki_description: |
    This issue is similar to Finding 3.1. ↗, but it is for note footers instead of for nullifiers. In actions where multiple note footers are involved for outputs, checking whether these note footers have already been used is done first, and only later are both marked used. This allows for the same note
wiki_exploit_scenario: |
    Per audit finding: This issue is similar to Finding 3.1. ↗, but it is for note footers instead of for nullifiers. In actions where multiple note footers are involved for outputs, checking whether these note footers have
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OutputNoteFootersCanBeReusedWithinTheSameAction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(uniswapRemoveLiquidity|dataToken1|dataToken2).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "uniswapRemoveLiquidity"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
