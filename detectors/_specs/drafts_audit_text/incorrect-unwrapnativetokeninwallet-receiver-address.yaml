skeleton: "name_match_missing_require"
name: "incorrect-unwrapnativetokeninwallet-receiver-address"
class_name: "IncorrectUnwrapnativetokeninwalletReceiverAddress"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-benddao"
help: |
    SpicyMeatball , also found by oakcobalt Impact If the user specifies native tokens as output assets, the protocol will mistakenly try to unwrap wrapped tokens from the msg.sender i
wiki_title: "Incorrect unwrapNativeTokenInWallet receiver address"
wiki_description: |
    SpicyMeatball , also found by oakcobalt Impact If the user specifies native tokens as output assets, the protocol will mistakenly try to unwrap wrapped tokens from the msg.sender instead of the receiver address that was specified in the function call. Proof of Concept This issue occurs in the follow
wiki_exploit_scenario: |
    Per audit finding: SpicyMeatball , also found by oakcobalt Impact If the user specifies native tokens as output assets, the protocol will mistakenly try to unwrap wrapped tokens from the msg.sender instead of the receiv
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectUnwrapnativetokeninwalletReceiverAddress"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(erc20TransferOutLiquidity|unwrapNativeTokenInWallet|wrappedNativeToken).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "erc20TransferOutLiquidity"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
