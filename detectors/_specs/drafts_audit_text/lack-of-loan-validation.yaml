skeleton: "name_match_missing_require"
name: "lack-of-loan-validation"
class_name: "LackOfLoanValidation"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit GammaSwap - Zellic Audit Report"
help: |
    There is a lack of validation on the _loan object. 1. The ExternalLongStrategy._rebalanceExternally function calls _getLoan in or- der to retrieve the _loan associated with the spe
wiki_title: "Lack of _loan validation"
wiki_description: |
    There is a lack of validation on the _loan object. 1. The ExternalLongStrategy._rebalanceExternally function calls _getLoan in or- der to retrieve the _loan associated with the specified tokenId and verify that the msg.sender is the creator of the loan. function _getLoan(uint256 tokenId) internal vi
wiki_exploit_scenario: |
    Per audit finding: There is a lack of validation on the _loan object. 1. The ExternalLongStrategy._rebalanceExternally function calls _getLoan in or- der to retrieve the _loan associated with the specified tokenId and v
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfLoanValidation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokenId|ernalLiquidationStrategy|lpTokens).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "tokenId"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
