skeleton: "name_match_missing_require"
name: "missing-valid-vault-address-check-in-processdepositqueue"
class_name: "MissingValidVaultAddressCheckInProcessdepositqueue"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Cega - Zellic Audit Report"
help: |
    Investors are able to deposit assets into an FCNVault through the FCNProduct contract’s addToDepositQueue() function. This function pulls funds from the investor’s wallet and adds
wiki_title: "Missing valid vault address check in processDepositQueue"
wiki_description: |
    Investors are able to deposit assets into an FCNVault through the FCNProduct contract’s addToDepositQueue() function. This function pulls funds from the investor’s wallet and adds Deposit objects into a global depositQueue array within the FCNProduct contract. Subsequently, a trader admin is able to
wiki_exploit_scenario: |
    Per audit finding: Investors are able to deposit assets into an FCNVault through the FCNProduct contract’s addToDepositQueue() function. This function pulls funds from the investor’s wallet and adds Deposit objects into
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingValidVaultAddressCheckInProcessdepositqueue"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(addToDepositQueue|depositQueue|processDepositQueue).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "addToDepositQueue"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
