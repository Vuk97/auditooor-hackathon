skeleton: "name_match_missing_require"
name: "users-can-dos-the-token-deposits-for-any-future-bridges"
class_name: "UsersCanDosTheTokenDepositsForAnyFutureBridges"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-01-initia-rollup-modules"
help: |
    0xAlix2 Users can create a bridge on L1 that points to L2 chains, which allows them to transfer funds between the 2 chains. When creating a bridge an incremental ID is generated fo
wiki_title: "Users can DOS the token deposits for any future bridges"
wiki_description: |
    0xAlix2 Users can create a bridge on L1 that points to L2 chains, which allows them to transfer funds between the 2 chains. When creating a bridge an incremental ID is generated for that bridge, and saved. To deposit coins to an L2, users have to call InitiateTokenDeposit on the L1. . This generates
wiki_exploit_scenario: |
    Per audit finding: 0xAlix2 Users can create a bridge on L1 that points to L2 chains, which allows them to transfer funds between the 2 chains. When creating a bridge an incremental ID is generated for that bridge, and s
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UsersCanDosTheTokenDepositsForAnyFutureBridges"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(l1Sequence|bridgeId|finalizedL1Sequence).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "l1Sequence"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
