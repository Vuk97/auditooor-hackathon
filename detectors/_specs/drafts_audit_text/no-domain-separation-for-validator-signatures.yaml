skeleton: "name_match_missing_require"
name: "no-domain-separation-for-validator-signatures"
class_name: "NoDomainSeparationForValidatorSignatures"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Mina Token Bridge EVM - Zellic Audit Report"
help: |
    In both directions of the bridge, validator signatures are needed to unlock funds. On the Mina side, the message signed by the validator consists of the receiver address (two field
wiki_title: "No domain separation for validator signatures"
wiki_description: |
    In both directions of the bridge, validator signatures are needed to unlock funds. On the Mina side, the message signed by the validator consists of the receiver address (two field elements), the amount (one field element), and the token address (two field elements): @method async unlock( amount: UI
wiki_exploit_scenario: |
    Per audit finding: In both directions of the bridge, validator signatures are needed to unlock funds. On the Mina side, the message signed by the validator consists of the receiver address (two field elements), the amou
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NoDomainSeparationForValidatorSignatures"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokenAddr|useSig1|useSig2).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "tokenAddr"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
