skeleton: "name_match_missing_require"
name: "insufficient-test-coverage"
class_name: "InsufficientTestCoverage"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Apyx Stablecoin - Zellic Audit Report"
help: |
    During the audit, we identified that the project's test suite lacks comprehensive coverage for several critical components and complex state transitions. While some happy-path scen
wiki_title: "Insufficient test coverage"
wiki_description: |
    During the audit, we identified that the project's test suite lacks comprehensive coverage for several critical components and complex state transitions. While some happy-path scenarios are tested, there is a distinct lack of negative testing and integration scenarios that mimic real-world usage, pa
wiki_exploit_scenario: |
    Per audit finding: During the audit, we identified that the project's test suite lacks comprehensive coverage for several critical components and complex state transitions. While some happy-path scenarios are tested, th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InsufficientTestCoverage"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(redeemRequests|requestRedeem|balanceOf).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "redeemRequests"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
