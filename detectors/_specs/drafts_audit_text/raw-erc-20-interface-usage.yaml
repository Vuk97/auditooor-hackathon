skeleton: "name_match_missing_call"
name: "raw-erc-20-interface-usage"
class_name: "RawErc20InterfaceUsage"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Gasp Node and Monorepo - Zellic Audit Report"
help: |
    The deposit_erc20 function directly uses the ERC-20 transferFrom interface to transfer tokens. The call is wrapped in a require statement. While this is technically correct, some t
wiki_title: "Raw ERC-20 interface usage"
wiki_description: |
    The deposit_erc20 function directly uses the ERC-20 transferFrom interface to transfer tokens. The call is wrapped in a require statement. While this is technically correct, some tokens do not im- plement the ERC-20 standard correctly, including not returning a boolean value from transferFrom. Notab
wiki_exploit_scenario: |
    Per audit finding: The deposit_erc20 function directly uses the ERC-20 transferFrom interface to transfer tokens. The call is wrapped in a require statement. While this is technically correct, some tokens do not im- ple
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RawErc20InterfaceUsage"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(transferFrom).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transferFrom"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
