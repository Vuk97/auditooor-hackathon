skeleton: "highlevelcall_missing_sibling"
name: "arbitraging-against-socket-data-layer"
class_name: "ArbitragingAgainstSocketDataLayer"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Socket Data Layer - Zellic Audit Report"
help: |
    A feature of Socket Data Layer is the ability to send msg.value with messages cross- chain. This essentially acts as a swap from the source chain’s native coin to that of the desti
wiki_title: "Arbitraging against Socket Data Layer"
wiki_description: |
    A feature of Socket Data Layer is the ability to send msg.value with messages cross- chain. This essentially acts as a swap from the source chain’s native coin to that of the destination chain. The price of the source chain’s native coin in terms of the destination chain’s native coin is determined
wiki_exploit_scenario: |
    Per audit finding: A feature of Socket Data Layer is the ability to send msg.value with messages cross- chain. This essentially acts as a swap from the source chain’s native coin to that of the destination chain. The pr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArbitragingAgainstSocketDataLayer"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(msgValue|paramType|executionFees).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
