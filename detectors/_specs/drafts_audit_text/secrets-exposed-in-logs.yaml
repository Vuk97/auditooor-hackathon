skeleton: "name_match_missing_call"
name: "secrets-exposed-in-logs"
class_name: "SecretsExposedInLogs"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Jovay Relayer - Zellic Audit Report"
help: |
    The PrefixedDynamicConfig class defines initCacheFromStorage, which logs config values upon initialization. Configs contain sensitive values including API keys, which will then be
wiki_title: "Secrets exposed in logs"
wiki_description: |
    The PrefixedDynamicConfig class defines initCacheFromStorage, which logs config values upon initialization. Configs contain sensitive values including API keys, which will then be exposed in logs. private void initCacheFromStorage() { var configs = systemConfigRepository.getPrefixedSystemConfig(pref
wiki_exploit_scenario: |
    Per audit finding: The PrefixedDynamicConfig class defines initCacheFromStorage, which logs config values upon initialization. Configs contain sensitive values including API keys, which will then be exposed in logs. pri
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SecretsExposedInLogs"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(initCacheFromStorage|systemConfigRepository|getPrefixedSystemConfig).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "initCacheFromStorage"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
