skeleton: "name_match_missing_call"
name: "escrow-counterparty-misassignment"
class_name: "EscrowCounterpartyMisassignment"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit cyberRaise - Zellic Audit Report"
help: |
    The signDealAndPay and signAndFinalizeDeal functions in DealManager pass msg.sender to updateEscrow, causing the escrow to store the transaction caller as the counterparty instead
wiki_title: "Escrow counterparty misassignment"
wiki_description: |
    The signDealAndPay and signAndFinalizeDeal functions in DealManager pass msg.sender to updateEscrow, causing the escrow to store the transaction caller as the counterparty instead of the authenticated signer. function signDealAndPay( address signer, bytes32 agreementId, bytes memory signature, strin
wiki_exploit_scenario: |
    Per audit finding: The signDealAndPay and signAndFinalizeDeal functions in DealManager pass msg.sender to updateEscrow, causing the escrow to store the transaction caller as the counterparty instead of the authenticated
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "EscrowCounterpartyMisassignment"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(signDealAndPay|signAndFinalizeDeal|updateEscrow).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "signDealAndPay"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
