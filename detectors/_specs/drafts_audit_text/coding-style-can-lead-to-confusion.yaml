skeleton: "name_match_missing_call"
name: "coding-style-can-lead-to-confusion"
class_name: "CodingStyleCanLeadToConfusion"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Odos - Zellic Audit Report"
help: |
    to confusion The code makes use of short if clauses without explicit scoping, which can lead to some confusion during development and auditing. One example is the following snip- p
wiki_title: "Coding style can lead to confusion"
wiki_description: |
    to confusion The code makes use of short if clauses without explicit scoping, which can lead to some confusion during development and auditing. One example is the following snip- pet from the function _swapMulti(). Zellic 9 Odos referralInfo memory thisReferralInfo; if (referralCode > REFERRAL_WITH_
wiki_exploit_scenario: |
    Per audit finding: to confusion The code makes use of short if clauses without explicit scoping, which can lead to some confusion during development and auditing. One example is the following snip- pet from the function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CodingStyleCanLeadToConfusion"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(referralInfo|thisReferralInfo|referralCode).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "referralInfo"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
