skeleton: "name_match_missing_call"
name: "bundle-nonce-is-set-incorrectly"
class_name: "BundleNonceIsSetIncorrectly"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "hexens audit RociFi SMA_public"
help: |
    In the function mintToken the mapping _bundleNonce is set to true for the NFCS token ID, even if there was no bundle attached (e.g. only primary address is being passed). A similar
wiki_title: "BUNDLE NONCE IS SET INCORRECTLY"
wiki_description: |
    In the function mintToken the mapping _bundleNonce is set to true for the NFCS token ID, even if there was no bundle attached (e.g. only primary address is being passed). A similar ﬂaw is present in the function addAddressToBundle. A check is missing to ensure that if _bundleNonce is false then it s
wiki_exploit_scenario: |
    Per audit finding: In the function mintToken the mapping _bundleNonce is set to true for the NFCS token ID, even if there was no bundle attached (e.g. only primary address is being passed). A similar ﬂaw is present in t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BundleNonceIsSetIncorrectly"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(mintToken|addAddressToBundle|ifNotPaused).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "mintToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
