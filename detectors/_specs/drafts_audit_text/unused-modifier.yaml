skeleton: "name_match_missing_call"
name: "unused-modifier"
class_name: "UnusedModifier"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "hexens audit Azuro_public"
help: |
    The onlyMaintainer modiﬁer is deﬁned but never used in the LP contract. The function it calls (checkMaintainer) is also called externally from CoreBase child contracts, and is cruc
wiki_title: "UNUSED MODIFIER"
wiki_description: |
    The onlyMaintainer modiﬁer is deﬁned but never used in the LP contract. The function it calls (checkMaintainer) is also called externally from CoreBase child contracts, and is crucial to the correct operation of the protocol, but this modiﬁer itself is dead code and might be confusing to developers
wiki_exploit_scenario: |
    Per audit finding: The onlyMaintainer modiﬁer is deﬁned but never used in the LP contract. The function it calls (checkMaintainer) is also called externally from CoreBase child contracts, and is crucial to the correct o
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnusedModifier"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(onlyMaintainer|checkMaintainer).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "onlyMaintainer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
