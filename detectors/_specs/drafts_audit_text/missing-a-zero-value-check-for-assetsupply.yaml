skeleton: "name_match_missing_require"
name: "missing-a-zero-value-check-for-assetsupply"
class_name: "MissingAZeroValueCheckForAssetsupply"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Plaza Core Protocol - Zellic Audit Report"
help: |
    When the collateralLevel is less than or equal to COLLATERAL_THRESHOLD and the token type is LEVERAGE, the function getCreateAmount does not check whether the levSupply is zero. fu
wiki_title: "Missing a zero-value check for assetSupply"
wiki_description: |
    When the collateralLevel is less than or equal to COLLATERAL_THRESHOLD and the token type is LEVERAGE, the function getCreateAmount does not check whether the levSupply is zero. function getCreateAmount( // [...] ) public pure returns(uint256) { if (bondSupply == 0) { revert ZeroDebtSupply(); } uint
wiki_exploit_scenario: |
    Per audit finding: When the collateralLevel is less than or equal to COLLATERAL_THRESHOLD and the token type is LEVERAGE, the function getCreateAmount does not check whether the levSupply is zero. function getCreateAmou
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingAZeroValueCheckForAssetsupply"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(collateralLevel|getCreateAmount|levSupply).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "collateralLevel"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
