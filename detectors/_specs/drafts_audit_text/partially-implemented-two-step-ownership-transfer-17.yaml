skeleton: "name_match_missing_require"
name: "partially-implemented-two-step-ownership-transfer-17"
class_name: "PartiallyImplementedTwoStepOwnershipTransfer17"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Anzen Finance - Zellic Audit Report"
help: |
    The redeemBackSPCT function in the USDz contract redeems USDz tokens to the corresponding SPCT tokens, taking a redemption fee: function redeemBackSPCT(uint256 _amount) external wh
wiki_title: "Partially implemented two-step ownership transfer                        17"
wiki_description: |
    The redeemBackSPCT function in the USDz contract redeems USDz tokens to the corresponding SPCT tokens, taking a redemption fee: function redeemBackSPCT(uint256 _amount) external whenNotPaused checkCollateralRate { require(_amount > 0, "REDEEM_AMOUNT_IS_ZERO"); require(!_blacklist[msg.sender], "SENDE
wiki_exploit_scenario: |
    Per audit finding: The redeemBackSPCT function in the USDz contract redeems USDz tokens to the corresponding SPCT tokens, taking a redemption fee: function redeemBackSPCT(uint256 _amount) external whenNotPaused checkCol
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PartiallyImplementedTwoStepOwnershipTransfer17"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(redeemBackSPCT|whenNotPaused|checkCollateralRate).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "redeemBackSPCT"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
