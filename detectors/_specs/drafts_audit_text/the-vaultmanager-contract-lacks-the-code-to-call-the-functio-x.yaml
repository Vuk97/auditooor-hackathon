skeleton: "name_match_missing_require"
name: "the-vaultmanager-contract-lacks-the-code-to-call-the-functio-x"
class_name: "TheVaultmanagerContractLacksTheCodeToCallTheFunctioX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Concrete - Zellic Audit Report"
help: |
    The function emergencyRemoveStrategy can only be invoked by the owner of the ConcreteMultiStrategyVault contract. However, the owner — the VaultManager contract — does not implemen
wiki_title: "The VaultManager contract lacks the code to call the function emergencyRe-"
wiki_description: |
    The function emergencyRemoveStrategy can only be invoked by the owner of the ConcreteMultiStrategyVault contract. However, the owner — the VaultManager contract — does not implement code to call this function. function emergencyRemoveStrategy(uint256 index_, bool forceEject_) external onlyOwner
wiki_exploit_scenario: |
    Per audit finding: The function emergencyRemoveStrategy can only be invoked by the owner of the ConcreteMultiStrategyVault contract. However, the owner — the VaultManager contract — does not implement code to call this
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheVaultmanagerContractLacksTheCodeToCallTheFunctioX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(emergencyRemoveStrategy|onlyOwner|emergencyRe).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "emergencyRemoveStrategy"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
