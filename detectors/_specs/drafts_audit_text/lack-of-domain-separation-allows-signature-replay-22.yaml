skeleton: "name_match_missing_call"
name: "lack-of-domain-separation-allows-signature-replay-22"
class_name: "LackOfDomainSeparationAllowsSignatureReplay22"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit SoSoValue - Zellic Audit Report"
help: |
    The function containTokenset works by looping over a set of tokens (the parameter b) to ensure a larger token set (the parameter a) contains all the tokens in b and contains more t
wiki_title: "Lack of domain separation allows signature replay                                 22"
wiki_description: |
    The function containTokenset works by looping over a set of tokens (the parameter b) to ensure a larger token set (the parameter a) contains all the tokens in b and contains more than the amount in b: function containTokenset(Token[] memory a, Token[] memory b) internal pure returns (bool) { uint k;
wiki_exploit_scenario: |
    Per audit finding: The function containTokenset works by looping over a set of tokens (the parameter b) to ensure a larger token set (the parameter a) contains all the tokens in b and contains more than the amount in b:
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfDomainSeparationAllowsSignatureReplay22"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(containTokenset|isSameToken).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "containTokenset"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
