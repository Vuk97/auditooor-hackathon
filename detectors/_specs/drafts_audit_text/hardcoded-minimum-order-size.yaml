skeleton: "name_match_missing_call"
name: "hardcoded-minimum-order-size"
class_name: "HardcodedMinimumOrderSize"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Tristero - Zellic Audit Report"
help: |
    There is a minimum taker or maker order size of 1e4: require(_quantity > 1e4, ”!minOrder”); Also, note that users are allowed to use arbitrary tokens.
wiki_title: "Hardcoded minimum order size"
wiki_description: |
    There is a minimum taker or maker order size of 1e4: require(_quantity > 1e4, ”!minOrder”); Also, note that users are allowed to use arbitrary tokens.
wiki_exploit_scenario: |
    Per audit finding: There is a minimum taker or maker order size of 1e4: require(_quantity > 1e4, ”!minOrder”); Also, note that users are allowed to use arbitrary tokens.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "HardcodedMinimumOrderSize"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(minOrder).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "minOrder"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
