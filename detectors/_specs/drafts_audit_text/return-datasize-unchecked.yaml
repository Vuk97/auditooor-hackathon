skeleton: "name_match_missing_require"
name: "return-datasize-unchecked"
class_name: "ReturnDatasizeUnchecked"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Biconomy Security Policy Plugin - Zellic Audit Report"
help: |
    3.1. Return datasize unchecked In several snippets of YUL, external functions are called and data is loaded from the return data, but there are no checks on the length of the retur
wiki_title: "Return datasize unchecked"
wiki_description: |
    3.1. Return datasize unchecked In several snippets of YUL, external functions are called and data is loaded from the return data, but there are no checks on the length of the return data. As a consequence, irrelevant data is loaded into variables / returned. This mostly occurs when returning the mod
wiki_exploit_scenario: |
    Per audit finding: 3.1. Return datasize unchecked In several snippets of YUL, external functions are called and data is loaded from the return data, but there are no checks on the length of the return data. As a consequ
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ReturnDatasizeUnchecked"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(checkSetupAndEnableModule|setupContract|setupData).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "checkSetupAndEnableModule"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
