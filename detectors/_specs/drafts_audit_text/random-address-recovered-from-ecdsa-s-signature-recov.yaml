skeleton: "name_match_missing_call"
name: "random-address-recovered-from-ecdsa-s-signature-recov"
class_name: "RandomAddressRecoveredFromEcdsaSSignatureRecov"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Socket Data Layer - Zellic Audit Report"
help: |
    In OpenExecutionManager, the NatSpec for isExecutor states the following: /** * @notice This function allows all executors * @notice The executor recovered here can be a random add
wiki_title: "Random address recovered from ECDSA’s signature recov-"
wiki_description: |
    In OpenExecutionManager, the NatSpec for isExecutor states the following: /** * @notice This function allows all executors * @notice The executor recovered here can be a random address hence should not be used for fee accounting * @param packedMessage Packed message to be executed * @param sig Signa
wiki_exploit_scenario: |
    Per audit finding: In OpenExecutionManager, the NatSpec for isExecutor states the following: /** * @notice This function allows all executors * @notice The executor recovered here can be a random address hence should no
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RandomAddressRecoveredFromEcdsaSSignatureRecov"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(isExecutor|packedMessage|isValidExecutor).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isExecutor"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
