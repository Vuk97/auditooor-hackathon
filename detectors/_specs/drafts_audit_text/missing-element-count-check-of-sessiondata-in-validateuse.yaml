skeleton: "name_match_missing_require"
name: "missing-element-count-check-of-sessiondata-in-validateuse"
class_name: "MissingElementCountCheckOfSessiondataInValidateuse"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Biconomy Batched Session Router Module - Zellic Audit Report"
help: |
    The function validateUserOp decodes an array named sessionData and iterates over it to perform various validations and computations. However, there is no explicit check in the code
wiki_title: "Missing element count check of sessionData in validateUse"
wiki_description: |
    The function validateUserOp decodes an array named sessionData and iterates over it to perform various validations and computations. However, there is no explicit check in the code to ensure that the sessionData array contains at least one element. uint256 length = sessionData.length; ( address sess
wiki_exploit_scenario: |
    Per audit finding: The function validateUserOp decodes an array named sessionData and iterates over it to perform various validations and computations. However, there is no explicit check in the code to ensure that the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingElementCountCheckOfSessiondataInValidateuse"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validateUserOp|sessionData|sessionKeyManager).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validateUserOp"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
