skeleton: "name_match_missing_call"
name: "arbitrary-external-call"
class_name: "ArbitraryExternalCall"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "hexens audit 1inch_FusionMode_Hexens.1"
help: |
    In the function ﬁllOrderInteraction that is being called as a callback within OrderMixin (limit-order-protocol) the parameter interactiveData is being parsed as arrays of targets a
wiki_title: "ARBITRARY EXTERNAL CALL"
wiki_description: |
    In the function ﬁllOrderInteraction that is being called as a callback within OrderMixin (limit-order-protocol) the parameter interactiveData is being parsed as arrays of targets and calldata with the purpose of making arbitrary external calls. There were no impactful vectors found for the reviewed
wiki_exploit_scenario: |
    Per audit finding: In the function ﬁllOrderInteraction that is being called as a callback within OrderMixin (limit-order-protocol) the parameter interactiveData is being parsed as arrays of targets and calldata with the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArbitraryExternalCall"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(interactiveData).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "interactiveData"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
