skeleton: "name_match_missing_call"
name: "incorrect-calculation-in-terminate-function"
class_name: "IncorrectCalculationInTerminateFunction"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Metavest - Zellic Audit Report"
help: |
    In the function terminate in the VestingAllocation contract, the tokensToRecover is used to calcu- late the remaining tokens, excluding the tokens vested and withdrawn. However, th
wiki_title: "Incorrect calculation in terminate function"
wiki_description: |
    In the function terminate in the VestingAllocation contract, the tokensToRecover is used to calcu- late the remaining tokens, excluding the tokens vested and withdrawn. However, the getVestedTokenAmount() function returns the total vested tokens, so adding to- kensWithdrawn is unnecessary. It allows
wiki_exploit_scenario: |
    Per audit finding: In the function terminate in the VestingAllocation contract, the tokensToRecover is used to calcu- late the remaining tokens, excluding the tokens vested and withdrawn. However, the getVestedTokenAmou
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectCalculationInTerminateFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokensToRecover|getVestedTokenAmount|kensWithdrawn).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "tokensToRecover"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
