skeleton: "name_match_missing_call"
name: "hardcoded-expiry-and-protocolfee"
class_name: "HardcodedExpiryAndProtocolfee"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Vendor Finance - Zellic Audit Report"
help: |
    The LendingPool.setPoolRates() is a wrapper function for FeesManager.setPoolRates( ) that sends expiry and protocolFee to the FeesManager contract by passing the global poolSetting
wiki_title: "Hardcoded expiry and protocolFee"
wiki_description: |
    The LendingPool.setPoolRates() is a wrapper function for FeesManager.setPoolRates( ) that sends expiry and protocolFee to the FeesManager contract by passing the global poolSettings struct. This makes the parameters uncontrollable by the pool owner, but the values are not verified in any meaningful
wiki_exploit_scenario: |
    Per audit finding: The LendingPool.setPoolRates() is a wrapper function for FeesManager.setPoolRates( ) that sends expiry and protocolFee to the FeesManager contract by passing the global poolSettings struct. This makes
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "HardcodedExpiryAndProtocolfee"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setPoolRates|protocolFee|poolSettings).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "setPoolRates"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
