skeleton: "name_match_missing_require"
name: "missing-constraint-in-pil-leading-to-execution-flow-hijack"
class_name: "MissingConstraintInPilLeadingToExecutionFlowHijack"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "hexens audit Hexens_Polygon_zkEVM_PUBLIC_27.02.23"
help: |
    The combination of the free input checking in zkEVM ROM and a missing constraint in main.pil leads to execution hijack with a possibility to jump to an arbitrary address in ROM. On
wiki_title: "MISSING CONSTRAINT IN PIL LEADING TO EXECUTION FLOW HIJACK"
wiki_description: |
    The combination of the free input checking in zkEVM ROM and a missing constraint in main.pil leads to execution hijack with a possibility to jump to an arbitrary address in ROM. One of the impacts is the arbitrary increase of balance for any caller. In the ﬁle utils.zkasm some of the procedures use
wiki_exploit_scenario: |
    Per audit finding: The combination of the free input checking in zkEVM ROM and a missing constraint in main.pil leads to execution hijack with a possibility to jump to an arbitrary address in ROM. One of the impacts is
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingConstraintInPilLeadingToExecutionFlowHijack"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(zkEVM|computeSendGasCall|failAssert).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "zkEVM"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
