skeleton: "name_match_missing_call"
name: "preminting-can-stake-from-premint-supply"
class_name: "PremintingCanStakeFromPremintSupply"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Asymmetry Finanace safETH - Zellic Audit Report"
help: |
    When the owner calls preMint(), the function ends up calling this.stake(), the same function that external users can call. function stake( uint256 _minOut ) external payable nonRee
wiki_title: "Preminting can stake from premint supply"
wiki_description: |
    When the owner calls preMint(), the function ends up calling this.stake(), the same function that external users can call. function stake( uint256 _minOut ) external payable nonReentrant returns (uint256 mintedAmount, uint256 depositPrice) { depositPrice = approxPrice(true); if (shouldPremint(deposi
wiki_exploit_scenario: |
    Per audit finding: When the owner calls preMint(), the function ends up calling this.stake(), the same function that external users can call. function stake( uint256 _minOut ) external payable nonReentrant returns (uint
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PremintingCanStakeFromPremintSupply"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(preMint|nonReentrant|mintedAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "preMint"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
