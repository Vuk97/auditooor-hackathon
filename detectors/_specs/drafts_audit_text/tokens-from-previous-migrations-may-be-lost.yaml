skeleton: "name_match_missing_require"
name: "tokens-from-previous-migrations-may-be-lost"
class_name: "TokensFromPreviousMigrationsMayBeLost"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Level Points Farm - Zellic Audit Report"
help: |
    Inside the migrate function, after verifying the signature, the _migrate function is called to update the user’s balance and grant the migrator contract approval to transfer the us
wiki_title: "Tokens from previous migrations may be lost"
wiki_description: |
    Inside the migrate function, after verifying the signature, the _migrate function is called to update the user’s balance and grant the migrator contract approval to transfer the user’s tokens. function _migrate( address _user, address _destination, address _migratorContract, address[] calldata _toke
wiki_exploit_scenario: |
    Per audit finding: Inside the migrate function, after verifying the signature, the _migrate function is called to update the user’s balance and grant the migrator contract approval to transfer the user’s tokens. functio
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TokensFromPreviousMigrationsMayBeLost"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(eventId).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "eventId"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
