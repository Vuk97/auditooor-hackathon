skeleton: "name_match_missing_call"
name: "inconsistent-usage-of-modifiers"
class_name: "InconsistentUsageOfModifiers"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Valts - Zellic Audit Report"
help: |
    In wipeApproval, the onlyRole(OWNER_ROLE) can be enforced over the current require statement. function wipeApproval(ApprovalType approvalType, address to, uint256 amount) external
wiki_title: "Inconsistent usage of modifiers"
wiki_description: |
    In wipeApproval, the onlyRole(OWNER_ROLE) can be enforced over the current require statement. function wipeApproval(ApprovalType approvalType, address to, uint256 amount) external { require(hasRole(OWNER_ROLE, msg.sender), “Owner required”); cleanupApproval(makeKey(approvalType, to, amount)); }
wiki_exploit_scenario: |
    Per audit finding: In wipeApproval, the onlyRole(OWNER_ROLE) can be enforced over the current require statement. function wipeApproval(ApprovalType approvalType, address to, uint256 amount) external { require(hasRole(OW
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InconsistentUsageOfModifiers"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(wipeApproval|onlyRole|approvalType).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "wipeApproval"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
