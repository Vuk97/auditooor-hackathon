skeleton: "name_match_missing_call"
name: "redos-in-search-filter"
class_name: "RedosInSearchFilter"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Babylon Genesis Chain - Zellic Audit Report"
help: |
    In FinalityProviderState.tsx, the search filter creates a regular expression out of the user's input and uses that to filter finality providers. This is an anti-pattern since it co
wiki_title: "REDOS in search filter"
wiki_description: |
    In FinalityProviderState.tsx, the search filter creates a regular expression out of the user's input and uses that to filter finality providers. This is an anti-pattern since it could lead to regular expression denial of service (REDOS). const FILTERS = { search: (fp: FinalityProvider, filter: Filte
wiki_exploit_scenario: |
    Per audit finding: In FinalityProviderState.tsx, the search filter creates a regular expression out of the user's input and uses that to filter finality providers. This is an anti-pattern since it could lead to regular
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RedosInSearchFilter"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(btcPk|searchParams|useSearchParams).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "btcPk"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
