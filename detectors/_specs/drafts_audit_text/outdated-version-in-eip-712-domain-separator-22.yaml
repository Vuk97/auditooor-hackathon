skeleton: "name_match_missing_require"
name: "outdated-version-in-eip-712-domain-separator-22"
class_name: "OutdatedVersionInEip712DomainSeparator22"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Aori 0.3.1 Upgrade - Zellic Audit Report"
help: |
    The source-chain cancel function allows a solver to cancel an active single-chain order or allows the offerer to cancel the single-chain order when it expires. However, the cancel
wiki_title: "Outdated version in EIP-712 domain separator                                   22"
wiki_description: |
    The source-chain cancel function allows a solver to cancel an active single-chain order or allows the offerer to cancel the single-chain order when it expires. However, the cancel function lacks a nonReentrant modifier, exposing it to potential reentrancy attacks. function cancel(bytes32 orderId) ex
wiki_exploit_scenario: |
    Per audit finding: The source-chain cancel function allows a solver to cancel an active single-chain order or allows the offerer to cancel the single-chain order when it expires. However, the cancel function lacks a non
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OutdatedVersionInEip712DomainSeparator22"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(nonReentrant|orderId|whenNotPaused).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "nonReentrant"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
