skeleton: "name_match_missing_require"
name: "architecture-changes-from-endpoint-v1"
class_name: "ArchitectureChangesFromEndpointV1"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LayerZero Endpoint V2 - Zellic Audit Report"
help: |
    int V1 The following table documents the architectural changes between Endpoint V1 and Endpoint V2. Endpoint V1 Endpoint V2 Validation Uses a relayer and oracle to sub- Abstracts t
wiki_title: "Architecture changes from Endpoint V1"
wiki_description: |
    int V1 The following table documents the architectural changes between Endpoint V1 and Endpoint V2. Endpoint V1 Endpoint V2 Validation Uses a relayer and oracle to sub- Abstracts the entities required to mit the necessary information for validate a message for delivery to delivery. verifiers whose a
wiki_exploit_scenario: |
    Per audit finding: int V1 The following table documents the architectural changes between Endpoint V1 and Endpoint V2. Endpoint V1 Endpoint V2 Validation Uses a relayer and oracle to sub- Abstracts the entities required
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArchitectureChangesFromEndpointV1"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(fierNetwork).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "fierNetwork"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
