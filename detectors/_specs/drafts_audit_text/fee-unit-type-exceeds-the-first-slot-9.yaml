skeleton: "name_match_missing_call"
name: "fee-unit-type-exceeds-the-first-slot-9"
class_name: "FeeUnitTypeExceedsTheFirstSlot9"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Core and Periphery - Zellic Audit Report"
help: |
    The tokenRatioPriority function is used to build token URI for NFT positions. The goal of this func- tion is to reorder the tokens in the URI to always keep some tokens at the firs
wiki_title: "Fee unit type exceeds the first slot                             9"
wiki_description: |
    The tokenRatioPriority function is used to build token URI for NFT positions. The goal of this func- tion is to reorder the tokens in the URI to always keep some tokens at the first position. Sailor made the following change to the function: function tokenRatioPriority(address token, uint256 chainId
wiki_exploit_scenario: |
    Per audit finding: The tokenRatioPriority function is used to build token URI for NFT positions. The goal of this func- tion is to reorder the tokens in the URI to always keep some tokens at the first position. Sailor m
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FeeUnitTypeExceedsTheFirstSlot9"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokenRatioPriority|chainId|chainID).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "tokenRatioPriority"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
