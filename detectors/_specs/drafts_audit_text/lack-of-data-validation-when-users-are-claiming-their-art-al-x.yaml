skeleton: "name_match_missing_require"
name: "lack-of-data-validation-when-users-are-claiming-their-art-al-x"
class_name: "LackOfDataValidationWhenUsersAreClaimingTheirArtAlX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-08-phi"
help: |
    rscodes , also found by gregom MrValioBg McToady , and MrPotatoMagic In the file PhiFactory.sol , the lack of checks in merkleClaim allows malicious users to bypass the hash check
wiki_title: |
    Lack of data validation when users are claiming their art allows malicious user to bypass signature/merkle hash to provide unapproved
wiki_description: |
    rscodes , also found by gregom MrValioBg McToady , and MrPotatoMagic In the file PhiFactory.sol , the lack of checks in merkleClaim allows malicious users to bypass the hash check to pass in their own parameter of choice. The 3 variables that could possibly be manipulated ( referral artId and imageU
wiki_exploit_scenario: |
    Per audit finding: rscodes , also found by gregom MrValioBg McToady , and MrPotatoMagic In the file PhiFactory.sol , the lack of checks in merkleClaim allows malicious users to bypass the hash check to pass in their own
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfDataValidationWhenUsersAreClaimingTheirArtAlX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(merkleClaim|artId|imageURI).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "merkleClaim"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
