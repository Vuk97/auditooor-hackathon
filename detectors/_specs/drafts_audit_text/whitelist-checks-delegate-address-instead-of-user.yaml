skeleton: "name_match_missing_call"
name: "whitelist-checks-delegate-address-instead-of-user"
class_name: "WhitelistChecksDelegateAddressInsteadOfUser"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Bloom Trading - Zellic Audit Report"
help: |
    When the whitelist is turned on by governance, the whitelist only allows whitelisted accounts to use onlyWhitelist functions in Trading: modifier onlyWhitelist() { if (isWhiteliste
wiki_title: "Whitelist checks delegate address instead of user"
wiki_description: |
    When the whitelist is turned on by governance, the whitelist only allows whitelisted accounts to use onlyWhitelist functions in Trading: modifier onlyWhitelist() { if (isWhitelisted) require(whitelistedAddress[msg.sender], "WHITELIST_ONLY"); _; } Most of the external functions, such as openTrade and
wiki_exploit_scenario: |
    Per audit finding: When the whitelist is turned on by governance, the whitelist only allows whitelisted accounts to use onlyWhitelist functions in Trading: modifier onlyWhitelist() { if (isWhitelisted) require(whitelist
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "WhitelistChecksDelegateAddressInsteadOfUser"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(onlyWhitelist|isWhitelisted|whitelistedAddress).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "onlyWhitelist"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
