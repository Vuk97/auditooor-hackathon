skeleton: "name_match_missing_call"
name: "insufficient-test-coverage-18"
class_name: "InsufficientTestCoverage18"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Private Transfer With USX - Zellic Audit Report"
help: |
    In PrivateGatewayScroll::transferToken, the function accepts both usxReceiver and usdcReceiver as parameters. The exact same addresses can be used for both parameters. function tra
wiki_title: "Insufficient test coverage                                                     18"
wiki_description: |
    In PrivateGatewayScroll::transferToken, the function accepts both usxReceiver and usdcReceiver as parameters. The exact same addresses can be used for both parameters. function transferToken( address token, uint256 swapAmount, address swapRouter, bytes memory swapData, EncryptedReceiver memory usxRe
wiki_exploit_scenario: |
    Per audit finding: In PrivateGatewayScroll::transferToken, the function accepts both usxReceiver and usdcReceiver as parameters. The exact same addresses can be used for both parameters. function transferToken( address
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InsufficientTestCoverage18"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(transferToken|usxReceiver|usdcReceiver).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transferToken"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
