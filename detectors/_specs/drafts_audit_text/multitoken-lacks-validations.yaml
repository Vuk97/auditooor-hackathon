skeleton: "highlevelcall_missing_sibling"
name: "multitoken-lacks-validations"
class_name: "MultitokenLacksValidations"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Maia DAO Ulysses Protocol May 2023 - Zellic Audit Report"
help: |
    Several functions within ERC4626MultiToken.sol lack necessary validations, which can result in the loss of funds or broken contracts. This contract is similar to the Solmate implem
wiki_title: "Multitoken lacks validations"
wiki_description: |
    Several functions within ERC4626MultiToken.sol lack necessary validations, which can result in the loss of funds or broken contracts. This contract is similar to the Solmate implementation of ERC4626, but it differs in that it allows for the trading of multiple (weighted) assets instead of just one.
wiki_exploit_scenario: |
    Per audit finding: Several functions within ERC4626MultiToken.sol lack necessary validations, which can result in the loss of funds or broken contracts. This contract is similar to the Solmate implementation of ERC4626,
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MultitokenLacksValidations"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(assetsAmounts|previewDeposit|convertToS).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
