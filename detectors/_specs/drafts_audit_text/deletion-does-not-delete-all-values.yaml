skeleton: "name_match_missing_call"
name: "deletion-does-not-delete-all-values"
class_name: "DeletionDoesNotDeleteAllValues"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Anzen and protocol-v2 - Zellic Audit Report"
help: |
    In VotingControllerStorageUpg, _removePool deletes poolData, but the slopeChanges mapping within poolData is not actually deleted. Currently, since poolData is always checked for a
wiki_title: "Deletion does not delete all values"
wiki_description: |
    In VotingControllerStorageUpg, _removePool deletes poolData, but the slopeChanges mapping within poolData is not actually deleted. Currently, since poolData is always checked for activity when used, this does not pose a security risk.
wiki_exploit_scenario: |
    Per audit finding: In VotingControllerStorageUpg, _removePool deletes poolData, but the slopeChanges mapping within poolData is not actually deleted. Currently, since poolData is always checked for activity when used, t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DeletionDoesNotDeleteAllValues"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(poolData|slopeChanges).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "poolData"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
