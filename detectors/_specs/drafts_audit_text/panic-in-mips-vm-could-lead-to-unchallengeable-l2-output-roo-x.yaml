skeleton: "name_match_missing_require"
name: "panic-in-mips-vm-could-lead-to-unchallengeable-l2-output-roo-x"
class_name: "PanicInMipsVmCouldLeadToUnchallengeableL2OutputRooX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-optimism"
help: |
    Zubat , also found by Femboys and n4nika Impact Suppose we have a situation where the MIPS geth goes wrong and always panics. The vmStatus can only be either UNFINISHED or PANIC .
wiki_title: "Panic in MIPS VM could lead to unchallengeable L2 output root claim"
wiki_description: |
    Zubat , also found by Femboys and n4nika Impact Suppose we have a situation where the MIPS geth goes wrong and always panics. The vmStatus can only be either UNFINISHED or PANIC . In the current execution bisection game system, the UNFINISHED state can not be used as the root claim; the PANIC state
wiki_exploit_scenario: |
    Per audit finding: Zubat , also found by Femboys and n4nika Impact Suppose we have a situation where the MIPS geth goes wrong and always panics. The vmStatus can only be either UNFINISHED or PANIC . In the current execu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PanicInMipsVmCouldLeadToUnchallengeableL2OutputRooX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(vmStatus).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "vmStatus"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
