skeleton: "name_match_missing_require"
name: "the-tradeend"
class_name: "TheTradeend"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-reserve"
help: |
    ether_sky , also found by RadiantLabs and stuart_the_minion Lines of code Impact In the BackingManager , we use the tradeEnd value for each type of trade to prevent next auction fr
wiki_title: "The tradeEnd"
wiki_description: |
    ether_sky , also found by RadiantLabs and stuart_the_minion Lines of code Impact In the BackingManager , we use the tradeEnd value for each type of trade to prevent next auction from occurring within the same block. We can find a comment in the code explaining this in line 114 . function rebalance(T
wiki_exploit_scenario: |
    Per audit finding: ether_sky , also found by RadiantLabs and stuart_the_minion Lines of code Impact In the BackingManager , we use the tradeEnd value for each type of trade to prevent next auction from occurring within
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheTradeend"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tradeEnd|nonReentrant|doTrade).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "tradeEnd"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
