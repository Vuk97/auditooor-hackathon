skeleton: "name_match_missing_call"
name: "gas-optimization-in-the-removesplitter-function"
class_name: "GasOptimizationInTheRemovesplitterFunction"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Polygon Staking - Zellic Audit Report"
help: |
    The removeSplitter function removes a splitter. Since the splitters are recorded in an array, it loops through the array in order to locate the splitter and then replaces it with t
wiki_title: "Gas optimization in the removeSplitter function"
wiki_description: |
    The removeSplitter function removes a splitter. Since the splitters are recorded in an array, it loops through the array in order to locate the splitter and then replaces it with the last splitter: function removeSplitter(address _account) external onlyOwner { // [...] uint256 numAccounts = accounts
wiki_exploit_scenario: |
    Per audit finding: The removeSplitter function removes a splitter. Since the splitters are recorded in an array, it loops through the array in order to locate the splitter and then replaces it with the last splitter: fu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "GasOptimizationInTheRemovesplitterFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(removeSplitter|onlyOwner|numAccounts).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "removeSplitter"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
