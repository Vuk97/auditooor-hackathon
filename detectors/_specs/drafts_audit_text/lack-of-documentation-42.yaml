skeleton: "name_match_missing_require"
name: "lack-of-documentation-42"
class_name: "LackOfDocumentation42"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit GTE Perps - Zellic Audit Report"
help: |
    The processMakerFill function performs an isLiquidatable check to ensure that settling a maker's limit order does not immediately make the maker's account liquidatable. function pr
wiki_title: "Lack of documentation                                                            42"
wiki_description: |
    The processMakerFill function performs an isLiquidatable check to ensure that settling a maker's limit order does not immediately make the maker's account liquidatable. function processMakerFill(ClearingHouse storage self, bytes32 asset, MakerFillParams memory params) internal returns (bool unfillab
wiki_exploit_scenario: |
    Per audit finding: The processMakerFill function performs an isLiquidatable check to ensure that settling a maker's limit order does not immediately make the maker's account liquidatable. function processMakerFill(Clear
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfDocumentation42"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(processMakerFill|isLiquidatable|setPosition).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "processMakerFill"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
