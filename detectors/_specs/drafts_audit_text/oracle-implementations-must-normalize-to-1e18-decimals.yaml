skeleton: "name_match_missing_call"
name: "oracle-implementations-must-normalize-to-1e18-decimals"
class_name: "OracleImplementationsMustNormalizeTo1e18Decimals"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Blackhaven (Core Contracts) - Zellic Audit Report"
help: |
    BackingCalculator assumes all oracle implementations return prices scaled to 1e18: function singleNAV(address _token) public view returns (uint256 _netAssetValue) { // [...] return
wiki_title: "Oracle implementations must normalize to 1e18 decimals"
wiki_description: |
    BackingCalculator assumes all oracle implementations return prices scaled to 1e18: function singleNAV(address _token) public view returns (uint256 _netAssetValue) { // [...] return IERC20(_token).balanceOf(backingStorage) * IOracle(backingTokenDetails.oracle).getPrice() / 1e18; } function FDV() publ
wiki_exploit_scenario: |
    Per audit finding: BackingCalculator assumes all oracle implementations return prices scaled to 1e18: function singleNAV(address _token) public view returns (uint256 _netAssetValue) { // [...] return IERC20(_token).bala
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OracleImplementationsMustNormalizeTo1e18Decimals"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(singleNAV|balanceOf|backingStorage).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "singleNAV"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
