skeleton: "name_match_missing_call"
name: "deposit-is-not-compatible-with-noncompliant-erc20s"
class_name: "DepositIsNotCompatibleWithNoncompliantErc20s"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit ether.fi - Zellic Audit Report"
help: |
    When depositWithERC20 is called to deposit an ERC-20 token, the transferFrom is called like this: bool sent = IERC20(_token).transferFrom(msg.sender, address(this), _amount); requi
wiki_title: "Deposit is not compatible with noncompliant ERC20s"
wiki_description: |
    When depositWithERC20 is called to deposit an ERC-20 token, the transferFrom is called like this: bool sent = IERC20(_token).transferFrom(msg.sender, address(this), _amount); require(sent, "erc20 transfer failed"); However, some noncompliant ERC-20 tokens, such as USDT, do not return a boolean. So,
wiki_exploit_scenario: |
    Per audit finding: When depositWithERC20 is called to deposit an ERC-20 token, the transferFrom is called like this: bool sent = IERC20(_token).transferFrom(msg.sender, address(this), _amount); require(sent, "erc20 tran
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DepositIsNotCompatibleWithNoncompliantErc20s"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(depositWithERC20|transferFrom).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "depositWithERC20"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
