skeleton: "name_match_missing_require"
name: "proposal-vote-extensions-byte-limit"
class_name: "ProposalVoteExtensionsByteLimit"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Babylon Genesis Chain - Zellic Audit Report"
help: |
    When adding vote extensions to the proposal, there are no checks ensuring that the added vote ex- tensions do not push the proposal over the maximum proposal size allowed (the defa
wiki_title: "Proposal vote extensions' byte limit"
wiki_description: |
    When adding vote extensions to the proposal, there are no checks ensuring that the added vote ex- tensions do not push the proposal over the maximum proposal size allowed (the default is 10,000). func (h *ProposalHandler) PrepareProposal() sdk.PrepareProposalHandler { return func(ctx sdk.Context, re
wiki_exploit_scenario: |
    Per audit finding: When adding vote extensions to the proposal, there are no checks ensuring that the added vote ex- tensions do not push the proposal over the maximum proposal size allowed (the default is 10,000). func
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ProposalVoteExtensionsByteLimit"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(injectedCkpt|injectedVoteExtTx|buildInjectedTxBytes).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "injectedCkpt"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
