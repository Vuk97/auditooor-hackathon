skeleton: "name_match_missing_call"
name: "the-addfee-functions-can-repeat-receivers"
class_name: "TheAddfeeFunctionsCanRepeatReceivers"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Polygon Staking - Zellic Audit Report"
help: |
    In the PolygonStrategy contract, the addFee function adds a new receiver with an assigned split of the fee: function addFee(address _receiver, uint256 _feeBasisPoints) external onl
wiki_title: "The addFee functions can repeat receivers"
wiki_description: |
    In the PolygonStrategy contract, the addFee function adds a new receiver with an assigned split of the fee: function addFee(address _receiver, uint256 _feeBasisPoints) external onlyOwner { _updateStrategyRewards(); fees.push(Fee(_receiver, _feeBasisPoints)); if (_totalFeesBasisPoints() > 3000) rever
wiki_exploit_scenario: |
    Per audit finding: In the PolygonStrategy contract, the addFee function adds a new receiver with an assigned split of the fee: function addFee(address _receiver, uint256 _feeBasisPoints) external onlyOwner { _updateStra
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheAddfeeFunctionsCanRepeatReceivers"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(addFee|onlyOwner).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "addFee"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
