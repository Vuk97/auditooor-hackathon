skeleton: "name_match_missing_require"
name: "clob-config-invariant-can-be-broken-by-updating-clobmanager--x"
class_name: "ClobConfigInvariantCanBeBrokenByUpdatingClobmanagerX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2025-07-gte-spot-clob-and-router"
help: |
    [L-05] CLOB config invariant can be broken by updating CLOBManager validates the following invariant before deploying a CLOB: File: contracts/clob/CLOBManager.sol 289 : function _a
wiki_title: |
    CLOB config invariant can be broken by updating CLOBManager validates the following invariant before deploying a CLOB: File: contracts/clob/CLOBManager.sol
wiki_description: |
    [L-05] CLOB config invariant can be broken by updating CLOBManager validates the following invariant before deploying a CLOB: File: contracts/clob/CLOBManager.sol 289 : function _assertValidSettings ( SettingsParams calldata settings uint256 baseSize ) internal pure { 290 :@> if ( settings . tickSiz
wiki_exploit_scenario: |
    Per audit finding: [L-05] CLOB config invariant can be broken by updating CLOBManager validates the following invariant before deploying a CLOB: File: contracts/clob/CLOBManager.sol 289 : function _assertValidSettings (
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ClobConfigInvariantCanBeBrokenByUpdatingClobmanagerX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(baseSize|tickSize|fullMulDiv).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "baseSize"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
