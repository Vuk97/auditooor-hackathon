skeleton: "name_match_missing_call"
name: "mismatched-function-parameter-types-affecting-inheritance"
class_name: "MismatchedFunctionParameterTypesAffectingInheritance"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit InfiniCard Vault - Zellic Audit Report"
help: |
    The interface IStrategyVault defines the functions deposit and redeem with parameters of types uint256 and bytes: function deposit(uint256 _amount, bytes calldata depositInfo) exte
wiki_title: "Mismatched function parameter types affecting inheritance"
wiki_description: |
    The interface IStrategyVault defines the functions deposit and redeem with parameters of types uint256 and bytes: function deposit(uint256 _amount, bytes calldata depositInfo) external; function redeem(uint256 _amount, bytes calldata redeemInfo) external returns (uint256); However, the contract Base
wiki_exploit_scenario: |
    Per audit finding: The interface IStrategyVault defines the functions deposit and redeem with parameters of types uint256 and bytes: function deposit(uint256 _amount, bytes calldata depositInfo) external; function redee
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MismatchedFunctionParameterTypesAffectingInheritance"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(depositInfo|redeemInfo|actualRedeemedAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "depositInfo"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
