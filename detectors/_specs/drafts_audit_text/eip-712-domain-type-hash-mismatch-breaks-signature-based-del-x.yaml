skeleton: "name_match_missing_call"
name: "eip-712-domain-type-hash-mismatch-breaks-signature-based-del-x"
class_name: "Eip712DomainTypeHashMismatchBreaksSignatureBasedDelX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-05-blackhole"
help: |
    newspacexyz Finding description There is a mistake in how the DOMAIN_TYPEHASH constant is defined and used in the VotingEscrow contract. bytes32 public constant DOMAIN_TYPEHASH = k
wiki_title: "EIP-712 domain type hash mismatch breaks signature-based delegation"
wiki_description: |
    newspacexyz Finding description There is a mistake in how the DOMAIN_TYPEHASH constant is defined and used in the VotingEscrow contract. bytes32 public constant DOMAIN_TYPEHASH = keccak256 ( "EIP712Domain(string name,uint256 chainId,address verifyingContract)" ); However, when building the domain se
wiki_exploit_scenario: |
    Per audit finding: newspacexyz Finding description There is a mistake in how the DOMAIN_TYPEHASH constant is defined and used in the VotingEscrow contract. bytes32 public constant DOMAIN_TYPEHASH = keccak256 ( "EIP712Do
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "Eip712DomainTypeHashMismatchBreaksSignatureBasedDelX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(chainId|verifyingContract|domainSeparator).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "chainId"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
