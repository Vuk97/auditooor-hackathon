skeleton: "name_match_missing_call"
name: "incorrect-method-comments"
class_name: "IncorrectMethodComments"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit ZetaChain - 7.12.23 Zellic Audit Report"
help: |
    The TSS keeper was changed so that there is only a single entry instead of storing it based on the index, but all of the comments still refer to the index: /) SetTSS set a specific
wiki_title: "Incorrect method comments"
wiki_description: |
    The TSS keeper was changed so that there is only a single entry instead of storing it based on the index, but all of the comments still refer to the index: /) SetTSS set a specific tSS in the store from its index func (k Keeper) SetTSS(ctx sdk.Context, tss types.TSS) { store :) prefix.NewStore(ctx.K
wiki_exploit_scenario: |
    Per audit finding: The TSS keeper was changed so that there is only a single entry instead of storing it based on the index, but all of the comments still refer to the index: /) SetTSS set a specific tSS in the store fr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectMethodComments"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(storeKey).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "storeKey"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
