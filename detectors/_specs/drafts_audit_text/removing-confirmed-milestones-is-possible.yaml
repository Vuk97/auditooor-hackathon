skeleton: "name_match_missing_require"
name: "removing-confirmed-milestones-is-possible"
class_name: "RemovingConfirmedMilestonesIsPossible"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Metavest - Zellic Audit Report"
help: |
    According to the comments, the function removeMilestone should only remove milestones that have not yet been confirmed. /// @notice removes a milestone from '_grantee''s MetaVesT i
wiki_title: "Removing confirmed milestones is possible"
wiki_description: |
    According to the comments, the function removeMilestone should only remove milestones that have not yet been confirmed. /// @notice removes a milestone from '_grantee''s MetaVesT if such milestone has not yet been confirmed, also making the corresponding 'milestoneAward' tokens withdrawable by contr
wiki_exploit_scenario: |
    Per audit finding: According to the comments, the function removeMilestone should only remove milestones that have not yet been confirmed. /// @notice removes a milestone from '_grantee''s MetaVesT if such milestone has
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RemovingConfirmedMilestonesIsPossible"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(removeMilestone|milestoneAward).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "removeMilestone"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
