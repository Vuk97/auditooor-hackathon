skeleton: "name_match_missing_call"
name: "arbitrary-token-transfers-in-wrapanddistributeerc20amounts"
class_name: "ArbitraryTokenTransfersInWrapanddistributeerc20amounts"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Sushiswap Route Processor - Zellic Audit Report"
help: |
    The wrapAndDistributeERC20Amounts function wraps and then forwards the wrapped tokens from the RouteProcessor contract to the pools that it interacts with. function wrapAndDistribu
wiki_title: "Arbitrary token transfers in wrapAndDistributeERC20Amounts"
wiki_description: |
    The wrapAndDistributeERC20Amounts function wraps and then forwards the wrapped tokens from the RouteProcessor contract to the pools that it interacts with. function wrapAndDistributeERC20Amounts(uint256 stream, address token) private returns (uint256 amountTotal) { wNATIVE.deposit{value: msg.value}(
wiki_exploit_scenario: |
    Per audit finding: The wrapAndDistributeERC20Amounts function wraps and then forwards the wrapped tokens from the RouteProcessor contract to the pools that it interacts with. function wrapAndDistributeERC20Amounts(uint2
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArbitraryTokenTransfersInWrapanddistributeerc20amounts"
state_decl: "uint256 internal token;"
fn_name_regex: ".*(token|wNATIVE).*"
read_var_regex: ".*(token|wNATIVE).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "token"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return token > 0;"
