skeleton: "name_match_missing_require"
name: "function-signatureclaim-does-not-check-if-verification-type--x"
class_name: "FunctionSignatureclaimDoesNotCheckIfVerificationTypeX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-08-phi"
help: |
    [L-11] Function signatureClaim() does not check if verification type is SIGNATURE Unlike merkleClaim() , function signatureClaim() does not check if the verification type of the ar
wiki_title: |
    Function signatureClaim() does not check if verification type is SIGNATURE Unlike merkleClaim() , function signatureClaim() does not check if the verification t
wiki_description: |
    [L-11] Function signatureClaim() does not check if verification type is SIGNATURE Unlike merkleClaim() , function signatureClaim() does not check if the verification type of the artId being minted is SIGNATURE or not. This is in case the function is directly called and not through the claim() functi
wiki_exploit_scenario: |
    Per audit finding: [L-11] Function signatureClaim() does not check if verification type is SIGNATURE Unlike merkleClaim() , function signatureClaim() does not check if the verification type of the artId being minted is
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionSignatureclaimDoesNotCheckIfVerificationTypeX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(signatureClaim|merkleClaim|artId).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "signatureClaim"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
