skeleton: "name_match_missing_require"
name: "special-considerations-for-seed-liquidity"
class_name: "SpecialConsiderationsForSeedLiquidity"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit eBridge Ethereum Bridge - Zellic Audit Report"
help: |
    The team has implemented functions that specifically handle the deposit and withdrawal of seed liquidity. function deposit(bytes32 tokenKey, address token, uint256 amount) external
wiki_title: "Special considerations for seed liquidity"
wiki_description: |
    The team has implemented functions that specifically handle the deposit and withdrawal of seed liquidity. function deposit(bytes32 tokenKey, address token, uint256 amount) external { require(tokenList.contains(tokenKey), "not support"); depositAmount[tokenKey] = depositAmount[tokenKey].add(amount);
wiki_exploit_scenario: |
    Per audit finding: The team has implemented functions that specifically handle the deposit and withdrawal of seed liquidity. function deposit(bytes32 tokenKey, address token, uint256 amount) external { require(tokenList
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SpecialConsiderationsForSeedLiquidity"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokenKey|tokenList|depositAmount).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "tokenKey"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
