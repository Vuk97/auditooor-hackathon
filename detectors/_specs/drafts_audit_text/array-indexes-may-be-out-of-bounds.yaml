skeleton: "name_match_missing_require"
name: "array-indexes-may-be-out-of-bounds"
class_name: "ArrayIndexesMayBeOutOfBounds"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Bond Protocol - Zellic Audit Report"
help: |
    In the batchRedeem function, two arrays are passed as parameters to the function. The two arrays, tokenIds and amounts_, are then accessed in one for loop for the same indices, wit
wiki_title: "Array indexes may be out of bounds"
wiki_description: |
    In the batchRedeem function, two arrays are passed as parameters to the function. The two arrays, tokenIds and amounts_, are then accessed in one for loop for the same indices, without prior checking that their lengths are equal. function batchRedeem(uint256[] calldata tokenIds_, uint256[] calldata
wiki_exploit_scenario: |
    Per audit finding: In the batchRedeem function, two arrays are passed as parameters to the function. The two arrays, tokenIds and amounts_, are then accessed in one for loop for the same indices, without prior checking
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArrayIndexesMayBeOutOfBounds"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(batchRedeem|tokenIds|nonReentrant).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "batchRedeem"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
