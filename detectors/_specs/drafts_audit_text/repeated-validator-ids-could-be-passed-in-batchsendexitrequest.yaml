skeleton: "name_match_missing_require"
name: "repeated-validator-ids-could-be-passed-in-batchsendexitrequest"
class_name: "RepeatedValidatorIdsCouldBePassedInBatchsendexitrequest"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit EtherFi - Zellic Audit Report"
help: |
    The function batchSendExitRequest does not check if the _validatorIds passed to it are re- peated. A malicious TNFT holder could use the same _validatorIds twice in an array, which
wiki_title: "Repeated validator IDs could be passed in batchSendExitRequest"
wiki_description: |
    The function batchSendExitRequest does not check if the _validatorIds passed to it are re- peated. A malicious TNFT holder could use the same _validatorIds twice in an array, which would call IEtherFiNode(etherfiNode).updateNumExitRequests(1, 0); twice and would increase the value of numExitRequests
wiki_exploit_scenario: |
    Per audit finding: The function batchSendExitRequest does not check if the _validatorIds passed to it are re- peated. A malicious TNFT holder could use the same _validatorIds twice in an array, which would call IEtherFi
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RepeatedValidatorIdsCouldBePassedInBatchsendexitrequest"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(batchSendExitRequest|etherfiNode|updateNumExitRequests).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "batchSendExitRequest"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
