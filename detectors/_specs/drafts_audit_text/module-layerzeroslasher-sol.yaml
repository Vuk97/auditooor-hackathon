skeleton: "name_match_missing_call"
name: "module-layerzeroslasher-sol"
class_name: "ModuleLayerzeroslasherSol"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LayerZero EigenLayer DVN - Zellic Audit Report"
help: |
    ule: LayerZeroSlasher.sol Function: queueRequest(string memory _description) This function registers a request and starts the bonding period. The APPROVAL_WINDOW (bonding period du
wiki_title: "Module: LayerZeroSlasher.sol"
wiki_description: |
    ule: LayerZeroSlasher.sol Function: queueRequest(string memory _description) This function registers a request and starts the bonding period. The APPROVAL_WINDOW (bonding period duration), bondToken, and bondAmount are set by the owner in advance. Inputs • _description • Validation: None. This was f
wiki_exploit_scenario: |
    Per audit finding: ule: LayerZeroSlasher.sol Function: queueRequest(string memory _description) This function registers a request and starts the bonding period. The APPROVAL_WINDOW (bonding period duration), bondToken,
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ModuleLayerzeroslasherSol"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(queueRequest|bondToken|bondAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "queueRequest"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
