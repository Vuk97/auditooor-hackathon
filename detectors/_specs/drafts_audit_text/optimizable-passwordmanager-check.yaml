skeleton: "name_match_missing_require"
name: "optimizable-passwordmanager-check"
class_name: "OptimizablePasswordmanagerCheck"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Fuelet - Zellic Audit Report"
help: |
    The Fuelet wallet checks user passwords and PINs by first hashing it with SHA-256 then attempting to decrypt a known value with AES-GCM. This operation is highly optimizable and pa
wiki_title: "Optimizable PasswordManager check"
wiki_description: |
    The Fuelet wallet checks user passwords and PINs by first hashing it with SHA-256 then attempting to decrypt a known value with AES-GCM. This operation is highly optimizable and parallelizable, and the low-entropy PIN can be discovered by an attacker with the ciphertext in seconds. This validation i
wiki_exploit_scenario: |
    Per audit finding: The Fuelet wallet checks user passwords and PINs by first hashing it with SHA-256 then attempting to decrypt a known value with AES-GCM. This operation is highly optimizable and parallelizable, and th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OptimizablePasswordmanagerCheck"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(toString|passwordHash).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "toString"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
