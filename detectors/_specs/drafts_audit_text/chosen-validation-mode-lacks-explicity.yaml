skeleton: "name_match_missing_require"
name: "chosen-validation-mode-lacks-explicity"
class_name: "ChosenValidationModeLacksExplicity"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Biconomy Multichain Validator - Zellic Audit Report"
help: |
    licity When a user of the Smart Account wants to authorize several actions for several chains, and only use one signature to do so, they set the “chosen authorization mod- ule addr
wiki_title: "Chosen validation mode lacks explicity"
wiki_description: |
    licity When a user of the Smart Account wants to authorize several actions for several chains, and only use one signature to do so, they set the “chosen authorization mod- ule address” to the address of a deployed MultichainECDSAValidator module. The module function validateUserOp() will call _verif
wiki_exploit_scenario: |
    Per audit finding: licity When a user of the Smart Account wants to authorize several actions for several chains, and only use one signature to do so, they set the “chosen authorization mod- ule address” to the address
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ChosenValidationModeLacksExplicity"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validateUserOp|userOp|merkleTreeRoot).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validateUserOp"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
