skeleton: "name_match_missing_require"
name: "arrays-lengths-are-not-checked"
class_name: "ArraysLengthsAreNotChecked"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Succinct Telepathy - Zellic Audit Report"
help: |
    The initialize function in the TelepathyRouter contract does not check the length of the sourceChainIds array, which is passed as a parameter. function initialize( uint32[] memory
wiki_title: "Arrays’ lengths are not checked"
wiki_description: |
    The initialize function in the TelepathyRouter contract does not check the length of the sourceChainIds array, which is passed as a parameter. function initialize( uint32[] memory _sourceChainIds, address[] memory _lightClients, address[] memory _broadcasters, address _timelock, address _guardian, b
wiki_exploit_scenario: |
    Per audit finding: The initialize function in the TelepathyRouter contract does not check the length of the sourceChainIds array, which is passed as a parameter. function initialize( uint32[] memory _sourceChainIds, add
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ArraysLengthsAreNotChecked"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(sourceChainIds|lightClients|sendingEnabled).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "sourceChainIds"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
