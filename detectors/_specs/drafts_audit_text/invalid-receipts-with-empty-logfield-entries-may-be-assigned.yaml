skeleton: "name_match_missing_require"
name: "invalid-receipts-with-empty-logfield-entries-may-be-assigned"
class_name: "InvalidReceiptsWithEmptyLogfieldEntriesMayBeAssigned"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Brevis - Zellic Audit Report"
help: |
    The buildLogFields function in sdk/app.go converts log fields given in native types, packaged as LogFieldData, into the LogField type that is usable within the circuits. It is impl
wiki_title: "Invalid receipts with empty LogField entries may be assigned"
wiki_description: |
    The buildLogFields function in sdk/app.go converts log fields given in native types, packaged as LogFieldData, into the LogField type that is usable within the circuits. It is implemented as follows: func buildLogFields(fs [NumMaxLogFields]LogFieldData) (fields [NumMaxLogFields]LogField) { empty :=
wiki_exploit_scenario: |
    Per audit finding: The buildLogFields function in sdk/app.go converts log fields given in native types, packaged as LogFieldData, into the LogField type that is usable within the circuits. It is implemented as follows:
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InvalidReceiptsWithEmptyLogfieldEntriesMayBeAssigned"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(buildLogFields|lastNonEmpty).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "buildLogFields"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
