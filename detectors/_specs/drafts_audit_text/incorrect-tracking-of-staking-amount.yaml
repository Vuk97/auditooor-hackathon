skeleton: "name_match_missing_call"
name: "incorrect-tracking-of-staking-amount"
class_name: "IncorrectTrackingOfStakingAmount"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Smart Vault - Zellic Audit Report"
help: |
    The staking amount is tracked in the _stakingAmount state variable. However, it is not updated when the manager withdraws from the staking vault. Because the contract accounts for
wiki_title: "Incorrect tracking of staking amount"
wiki_description: |
    The staking amount is tracked in the _stakingAmount state variable. However, it is not updated when the manager withdraws from the staking vault. Because the contract accounts for staking using a debt token, the actual staked amount can differ from the value stored in _stakingAmount. function withdr
wiki_exploit_scenario: |
    Per audit finding: The staking amount is tracked in the _stakingAmount state variable. However, it is not updated when the manager withdraws from the staking vault. Because the contract accounts for staking using a debt
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectTrackingOfStakingAmount"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(withdrawFromStakingVault|onlyManager|smartVaultManager).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "withdrawFromStakingVault"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
