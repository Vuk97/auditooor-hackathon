skeleton: "name_match_missing_call"
name: "minimum-amount-of-staking-is-only-checked-in-the-registration"
class_name: "MinimumAmountOfStakingIsOnlyCheckedInTheRegistration"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Omni AVS - Zellic Audit Report"
help: |
    When an operator registers into OmniAVS, it is checked that the operator’s stake is greater than the minimum amount of staking. function registerOperator( bytes calldata pubkey, IS
wiki_title: "Minimum amount of staking is only checked in the registration"
wiki_description: |
    When an operator registers into OmniAVS, it is checked that the operator’s stake is greater than the minimum amount of staking. function registerOperator( bytes calldata pubkey, ISignatureUtils.SignatureWithSaltAndExpiry memory operatorSignature ) external whenNotPaused { address operator = msg.send
wiki_exploit_scenario: |
    Per audit finding: When an operator registers into OmniAVS, it is checked that the operator’s stake is greater than the minimum amount of staking. function registerOperator( bytes calldata pubkey, ISignatureUtils.Signat
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MinimumAmountOfStakingIsOnlyCheckedInTheRegistration"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(registerOperator|operatorSignature|whenNotPaused).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "registerOperator"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
