skeleton: "name_match_missing_call"
name: "incorrect-asset-tracking-in-modifyexistingorder"
class_name: "IncorrectAssetTrackingInModifyexistingorder"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Revest Resonate Pt. 1 - Zellic Audit Report"
help: |
    For cross-asset pools, calling submit submitProducer(...))) always sets shouldFarm = false and order.depositedShares > 0 using the oracle price. Orders are then en- queued with the
wiki_title: "Incorrect asset tracking in modifyExistingOrder"
wiki_description: |
    For cross-asset pools, calling submit submitProducer(...))) always sets shouldFarm = false and order.depositedShares > 0 using the oracle price. Orders are then en- queued with the pool asset: if(shouldFarm) { IERC20(asset).safeTransferFrom(msg.sender, address(this), amount); order.depositedShares =
wiki_exploit_scenario: |
    Per audit finding: For cross-asset pools, calling submit submitProducer(...))) always sets shouldFarm = false and order.depositedShares > 0 using the oracle price. Orders are then en- queued with the pool asset: if(shou
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectAssetTrackingInModifyexistingorder"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(submitProducer|shouldFarm|depositedShares).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "submitProducer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
