skeleton: "name_match_missing_require"
name: "reentrancy-when-incrementing-and-decrementing-gauges"
class_name: "ReentrancyWhenIncrementingAndDecrementingGauges"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Maia DAO February 2023 - Zellic Audit Report"
help: |
    The incrementGauges method takes a list of gauges and a list of weights, iterates through them to increase each supplied gauge with the corresponding weight, and then up- dates the
wiki_title: "Reentrancy when incrementing and decrementing gauges"
wiki_description: |
    The incrementGauges method takes a list of gauges and a list of weights, iterates through them to increase each supplied gauge with the corresponding weight, and then up- dates the global weights for the user. function incrementGauges(address[] calldata gaugeList, uint112[] calldata weights) externa
wiki_exploit_scenario: |
    Per audit finding: The incrementGauges method takes a list of gauges and a list of weights, iterates through them to increase each supplied gauge with the corresponding weight, and then up- dates the global weights for
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ReentrancyWhenIncrementingAndDecrementingGauges"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(incrementGauges|gaugeList|newUserWeight).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "incrementGauges"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
