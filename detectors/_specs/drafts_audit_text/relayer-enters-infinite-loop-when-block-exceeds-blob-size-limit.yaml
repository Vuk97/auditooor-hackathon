skeleton: "name_match_missing_call"
name: "relayer-enters-infinite-loop-when-block-exceeds-blob-size-limit"
class_name: "RelayerEntersInfiniteLoopWhenBlockExceedsBlobSizeLimit"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Jovay Relayer - Zellic Audit Report"
help: |
    In the RollupAggregator.process() function, when a single block at chunk index 0 exceeds the blob size limit (batchCommitBlobSizeLimit), the relayer throws an InvalidBatchException
wiki_title: "Relayer enters infinite loop when block exceeds blob size limit"
wiki_description: |
    In the RollupAggregator.process() function, when a single block at chunk index 0 exceeds the blob size limit (batchCommitBlobSizeLimit), the relayer throws an InvalidBatchException and cannot recover. // RollupAggregator.java } else if (ObjectUtil.equal(currChunkStartBlockNumber, currHeight) && over
wiki_exploit_scenario: |
    Per audit finding: In the RollupAggregator.process() function, when a single block at chunk index 0 exceeds the blob size limit (batchCommitBlobSizeLimit), the relayer throws an InvalidBatchException and cannot recover.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RelayerEntersInfiniteLoopWhenBlockExceedsBlobSizeLimit"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(batchCommitBlobSizeLimit|currChunkStartBlockNumber|currHeight).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "batchCommitBlobSizeLimit"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
