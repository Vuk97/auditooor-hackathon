skeleton: "name_match_missing_call"
name: "incorrect-address-emitted-in-processedredemption-event-for-s-x"
class_name: "IncorrectAddressEmittedInProcessedredemptionEventForSX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Frax - Zellic Audit Report"
help: |
    The processRedemption function is responsible for redeeming USDC tokens via USDCustodian or the RWARedeemer contracts then transferring them to the targetAddress in targetEid using
wiki_title: "Incorrect address emitted in ProcessedRedemption event for Solana"
wiki_description: |
    The processRedemption function is responsible for redeeming USDC tokens via USDCustodian or the RWARedeemer contracts then transferring them to the targetAddress in targetEid using the CCTP protocol and emitting the appropriate event. However, when the targetEid corresponds to 30168 (Solana), the ta
wiki_exploit_scenario: |
    Per audit finding: The processRedemption function is responsible for redeeming USDC tokens via USDCustodian or the RWARedeemer contracts then transferring them to the targetAddress in targetEid using the CCTP protocol a
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectAddressEmittedInProcessedredemptionEventForSX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(processRedemption|targetAddress|targetEid).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "processRedemption"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
