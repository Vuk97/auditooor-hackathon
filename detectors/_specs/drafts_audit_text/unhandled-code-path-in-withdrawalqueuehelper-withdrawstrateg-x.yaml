skeleton: "name_match_missing_require"
name: "unhandled-code-path-in-withdrawalqueuehelper-withdrawstrateg-x"
class_name: "UnhandledCodePathInWithdrawalqueuehelperWithdrawstrategX"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Concrete - Zellic Audit Report"
help: |
    During a user withdrawal from a Concrete Vault, if the withdrawable amount is sufficient for the requested withdrawal, execution proceeds to the WithdrawalQueueHelper._withdrawStra
wiki_title: "Unhandled code path in WithdrawalQueueHelper._withdrawStrategyFunds"
wiki_description: |
    During a user withdrawal from a Concrete Vault, if the withdrawable amount is sufficient for the requested withdrawal, execution proceeds to the WithdrawalQueueHelper._withdrawStrategyFunds function: function processWithdrawal( uint256 assets_, address receiver_, uint256 availableAssets, address ass
wiki_exploit_scenario: |
    Per audit finding: During a user withdrawal from a Concrete Vault, if the withdrawable amount is sufficient for the requested withdrawal, execution proceeds to the WithdrawalQueueHelper._withdrawStrategyFunds function:
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnhandledCodePathInWithdrawalqueuehelperWithdrawstrategX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(processWithdrawal|availableAssets|withdrawalQueue).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "processWithdrawal"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
