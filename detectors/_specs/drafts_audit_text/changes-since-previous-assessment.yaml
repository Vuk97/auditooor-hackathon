skeleton: "name_match_missing_require"
name: "changes-since-previous-assessment"
class_name: "ChangesSincePreviousAssessment"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Hyperliquid November - Zellic Audit Report"
help: |
    nges since previous assessment The following changes were observed in the in-scope commits: Withdrawals support specifying destination Withdrawals now support specifying a destinat
wiki_title: "Changes since previous assessment"
wiki_description: |
    nges since previous assessment The following changes were observed in the in-scope commits: Withdrawals support specifying destination Withdrawals now support specifying a destination, instead of assuming the destination is msg.sender. All the information regarding the withdrawal, including user, de
wiki_exploit_scenario: |
    Per audit finding: nges since previous assessment The following changes were observed in the in-scope commits: Withdrawals support specifying destination Withdrawals now support specifying a destination, instead of assu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ChangesSincePreviousAssessment"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(requestWithdrawal|whenNot).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "requestWithdrawal"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
