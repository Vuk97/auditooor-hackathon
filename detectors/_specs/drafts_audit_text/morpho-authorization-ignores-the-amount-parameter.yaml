skeleton: "name_match_missing_require"
name: "morpho-authorization-ignores-the-amount-parameter"
class_name: "MorphoAuthorizationIgnoresTheAmountParameter"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Hyperbeat Pay - Zellic Audit Report"
help: |
    As shown below, ManagementAccount::authorizeOperatorBorrowing accepts an amount parameter intended to limit the operator's borrowing capacity: function authorizeOperatorBorrowing(
wiki_title: "Morpho authorization ignores the amount parameter"
wiki_description: |
    As shown below, ManagementAccount::authorizeOperatorBorrowing accepts an amount parameter intended to limit the operator's borrowing capacity: function authorizeOperatorBorrowing( address service, IService.AuthorizationType authType, uint256 amount ) external override onlyOwner nonReentrant returns
wiki_exploit_scenario: |
    Per audit finding: As shown below, ManagementAccount::authorizeOperatorBorrowing accepts an amount parameter intended to limit the operator's borrowing capacity: function authorizeOperatorBorrowing( address service, ISe
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MorphoAuthorizationIgnoresTheAmountParameter"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(authorizeOperatorBorrowing|authType|onlyOwner).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "authorizeOperatorBorrowing"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
