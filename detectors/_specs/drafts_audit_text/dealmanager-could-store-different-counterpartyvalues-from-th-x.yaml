skeleton: "highlevelcall_missing_sibling"
name: "dealmanager-could-store-different-counterpartyvalues-from-th-x"
class_name: "DealmanagerCouldStoreDifferentCounterpartyvaluesFromThX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit cyberRaise - Zellic Audit Report"
help: |
    The signAndFinalizeDeal function in DealManager skips calling signContractFor when the signer has already signed in the registry: function signAndFinalizeDeal( address signer, byte
wiki_title: "DealManager could store different counterPartyValues from those in the"
wiki_description: |
    The signAndFinalizeDeal function in DealManager skips calling signContractFor when the signer has already signed in the registry: function signAndFinalizeDeal( address signer, bytes32 agreementId, string[] memory partyValues, bytes memory signature, bool _fillUnallocated, string memory name, string
wiki_exploit_scenario: |
    Per audit finding: The signAndFinalizeDeal function in DealManager skips calling signContractFor when the signer has already signed in the registry: function signAndFinalizeDeal( address signer, bytes32 agreementId, str
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DealmanagerCouldStoreDifferentCounterpartyvaluesFromThX"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(signAndFinalizeDeal|signContractFor|agreementId).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
