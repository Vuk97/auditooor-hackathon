skeleton: "name_match_missing_require"
name: "unimplemented-tls-responses-42"
class_name: "UnimplementedTlsResponses42"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The Reclaim node does not validate that either the server name indication (SNI) extension in the ClientHello message or the Host header in the HTTP request match the expected host.
wiki_title: "Unimplemented TLS responses                                             42"
wiki_description: |
    The Reclaim node does not validate that either the server name indication (SNI) extension in the ClientHello message or the Host header in the HTTP request match the expected host. Most popular websites use a reverse proxy service such as Cloudflare, Fastly, or Akamai to serve their content. These s
wiki_exploit_scenario: |
    Per audit finding: The Reclaim node does not validate that either the server name indication (SNI) extension in the ClientHello message or the Host header in the HTTP request match the expected host. Most popular websit
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnimplementedTlsResponses42"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(createRequest|httpReqHeaderStr|reqLine).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "createRequest"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
