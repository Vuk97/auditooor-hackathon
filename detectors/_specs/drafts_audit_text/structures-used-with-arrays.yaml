skeleton: "name_match_missing_call"
name: "structures-used-with-arrays"
class_name: "StructuresUsedWithArrays"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "hexens audit FastToken_public"
help: |
    Arrays and structs don’t play well together in Solidity, but in most cases using a mapping solves the issue and offers efficiency gains. address[] public owners; mapping(address =>
wiki_title: "STRUCTURES USED WITH ARRAYS"
wiki_description: |
    Arrays and structs don’t play well together in Solidity, but in most cases using a mapping solves the issue and offers efficiency gains. address[] public owners; mapping(address => bool) public isOwner; uint256 public numberOfRequiredConﬁrmations; // mapping from transaction index => owner index =>
wiki_exploit_scenario: |
    Per audit finding: Arrays and structs don’t play well together in Solidity, but in most cases using a mapping solves the issue and offers efficiency gains. address[] public owners; mapping(address => bool) public isOwne
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "StructuresUsedWithArrays"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(isOwner).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "isOwner"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
