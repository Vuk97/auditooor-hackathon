skeleton: "name_match_missing_call"
name: "pool-toggling-functionality-may-allow-factory-owner-to-lock"
class_name: "PoolTogglingFunctionalityMayAllowFactoryOwnerToLock"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Wasabi - Zellic Audit Report"
help: |
    The WasabiFactory contract allows its owner to toggle pools. function togglePool(address _poolAddress, bool _enabled) external onlyOwner { require(poolAddresses[_poolAddress] !) _e
wiki_title: "Pool toggling functionality may allow factory owner to lock"
wiki_description: |
    The WasabiFactory contract allows its owner to toggle pools. function togglePool(address _poolAddress, bool _enabled) external onlyOwner { require(poolAddresses[_poolAddress] !) _enabled, 'Pool already in same state'); poolAddresses[_poolAddress] = _enabled; } This prevents them from burning options
wiki_exploit_scenario: |
    Per audit finding: The WasabiFactory contract allows its owner to toggle pools. function togglePool(address _poolAddress, bool _enabled) external onlyOwner { require(poolAddresses[_poolAddress] !) _enabled, 'Pool alread
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PoolTogglingFunctionalityMayAllowFactoryOwnerToLock"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(togglePool|onlyOwner|poolAddresses).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "togglePool"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
