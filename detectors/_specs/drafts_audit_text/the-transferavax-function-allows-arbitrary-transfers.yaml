skeleton: "name_match_missing_require"
name: "the-transferavax-function-allows-arbitrary-transfers"
class_name: "TheTransferavaxFunctionAllowsArbitraryTransfers"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit GoGoPool - Zellic Audit Report"
help: |
    The transferAVAX function is used to perform transfers of avax between two registered contracts. function transferAVAX( string memory fromContractName, string memory toContractName
wiki_title: "The transferAVAX function allows arbitrary transfers"
wiki_description: |
    The transferAVAX function is used to perform transfers of avax between two registered contracts. function transferAVAX( string memory fromContractName, string memory toContractName, uint256 amount ) external onlyRegisteredNetworkContract { /) Valid Amount? if (amount =) 0) { revert InvalidAmount();
wiki_exploit_scenario: |
    Per audit finding: The transferAVAX function is used to perform transfers of avax between two registered contracts. function transferAVAX( string memory fromContractName, string memory toContractName, uint256 amount ) e
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheTransferavaxFunctionAllowsArbitraryTransfers"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(transferAVAX|fromContractName|toContractName).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "transferAVAX"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
