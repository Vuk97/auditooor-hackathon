skeleton: "name_match_missing_call"
name: "users-can-relist-their-tokens"
class_name: "UsersCanRelistTheirTokens"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "hexens audit Babylon-jan-2023"
help: |
    When a user creates a listing for their token, the listing ID is stored in _ids[token][identiﬁer]. However, this listing ID is never used or checked again, e.g. to check whether th
wiki_title: "USERS CAN RELIST THEIR TOKENS"
wiki_description: |
    When a user creates a listing for their token, the listing ID is stored in _ids[token][identiﬁer]. However, this listing ID is never used or checked again, e.g. to check whether the user has already listed the token. Furthermore, the token is never transferred into the BabylonCore contract, only an
wiki_exploit_scenario: |
    Per audit finding: When a user creates a listing for their token, the listing ID is stored in _ids[token][identiﬁer]. However, this listing ID is never used or checked again, e.g. to check whether the user has already l
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UsersCanRelistTheirTokens"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(startListing|timeStart|totalTickets).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "startListing"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
