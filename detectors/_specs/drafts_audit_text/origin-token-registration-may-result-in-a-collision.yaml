skeleton: "name_match_missing_call"
name: "origin-token-registration-may-result-in-a-collision"
class_name: "OriginTokenRegistrationMayResultInACollision"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Arpeggi_Labs_-_Zellic_Audit_Report"
help: |
    If an origin token t1 is registered and there is an attempt to register another origin token t2, such that t1.contractAddress =) t2.contractAddress and t1.tokenId =) t2.toke nId, a
wiki_title: "Origin token registration may result in a collision"
wiki_description: |
    If an origin token t1 is registered and there is an attempt to register another origin token t2, such that t1.contractAddress =) t2.contractAddress and t1.tokenId =) t2.toke nId, a collision happens: t1 gets overwritten by t2 (in case the caller of registerMedia passes the checks in enforceOnlyOverw
wiki_exploit_scenario: |
    Per audit finding: If an origin token t1 is registered and there is an attempt to register another origin token t2, such that t1.contractAddress =) t2.contractAddress and t1.tokenId =) t2.toke nId, a collision happens:
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OriginTokenRegistrationMayResultInACollision"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(contractAddress|tokenId|registerMedia).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "contractAddress"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
