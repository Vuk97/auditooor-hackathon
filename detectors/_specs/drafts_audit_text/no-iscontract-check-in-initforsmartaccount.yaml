skeleton: "name_match_missing_call"
name: "no-iscontract-check-in-initforsmartaccount"
class_name: "NoIscontractCheckInInitforsmartaccount"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Biconomy Smart Account Patch Review - Zellic Audit Report"
help: |
    In the EcdsaOwnershipRegistryModule smart contract, we noted that the transferOwnership() function checks to ensure the new owner is an EOA. However, the same check does not exist
wiki_title: "No isContract check in initForSmartAccount()"
wiki_description: |
    In the EcdsaOwnershipRegistryModule smart contract, we noted that the transferOwnership() function checks to ensure the new owner is an EOA. However, the same check does not exist in the initForSmartAccount() function, which sets the initial owner.
wiki_exploit_scenario: |
    Per audit finding: In the EcdsaOwnershipRegistryModule smart contract, we noted that the transferOwnership() function checks to ensure the new owner is an EOA. However, the same check does not exist in the initForSmartA
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NoIscontractCheckInInitforsmartaccount"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(transferOwnership|initForSmartAccount|isContract).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "transferOwnership"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
