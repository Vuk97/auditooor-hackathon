skeleton: "name_match_missing_call"
name: "discrepancy-in-average-cumulative-volatility-calculation"
class_name: "DiscrepancyInAverageCumulativeVolatilityCalculation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "hexens audit Algebra_public_11_08"
help: |
    In case where the oldest timepoint is still in the WINDOW, the volatilityCumulative is being diluted as it is divided by the whole time window (WINDOW > time passed), although at t
wiki_title: "DISCREPANCY IN AVERAGE CUMULATIVE VOLATILITY CALCULATION"
wiki_description: |
    In case where the oldest timepoint is still in the WINDOW, the volatilityCumulative is being diluted as it is divided by the whole time window (WINDOW > time passed), although at the same time volumePerLiquidityCumulative represents the metrics for only the time that has passed from the oldest timep
wiki_exploit_scenario: |
    Per audit finding: In case where the oldest timepoint is still in the WINDOW, the volatilityCumulative is being diluted as it is divided by the whole time window (WINDOW > time passed), although at the same time volumeP
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DiscrepancyInAverageCumulativeVolatilityCalculation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(volatilityCumulative|volumePerLiquidityCumulative|startOfWindow).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "volatilityCumulative"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
