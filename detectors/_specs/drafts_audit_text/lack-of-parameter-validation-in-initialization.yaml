skeleton: "name_match_missing_require"
name: "lack-of-parameter-validation-in-initialization"
class_name: "LackOfParameterValidationInInitialization"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Mina Token Bridge EVM - Zellic Audit Report"
help: |
    The initialize function initializes several crucial state variables of the Bridge contract, such as threshold, minAmount, and maxAmount. The minAmount and maxAmount variables defin
wiki_title: "Lack of parameter validation in initialization"
wiki_description: |
    The initialize function initializes several crucial state variables of the Bridge contract, such as threshold, minAmount, and maxAmount. The minAmount and maxAmount variables define the mini- mum and maximum values of funds that can be transferred to another chain via the bridge. However, the initia
wiki_exploit_scenario: |
    Per audit finding: The initialize function initializes several crucial state variables of the Bridge contract, such as threshold, minAmount, and maxAmount. The minAmount and maxAmount variables define the mini- mum and
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfParameterValidationInInitialization"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(minAmount|maxAmount|whitelistTokens).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "minAmount"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
