skeleton: "highlevelcall_missing_sibling"
name: "getperiodreward-function-allows-transfers-to-zero-address-wi-x"
class_name: "GetperiodrewardFunctionAllowsTransfersToZeroAddressWiX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-10-ramses-exchange"
help: |
    [L-10] getPeriodReward() function allows transfers to zero address without receiver address validation The getPeriodReward() function does not enforce a check to ensure the receive
wiki_title: "getPeriodReward() function allows transfers to zero address without"
wiki_description: |
    [L-10] getPeriodReward() function allows transfers to zero address without receiver address validation The getPeriodReward() function does not enforce a check to ensure the receiver is not the zero address (address(0)). The safeTransfer reverts if the recipient is the zero address (address(0)), whic
wiki_exploit_scenario: |
    Per audit finding: [L-10] getPeriodReward() function allows transfers to zero address without receiver address validation The getPeriodReward() function does not enforce a check to ensure the receiver is not the zero ad
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "GetperiodrewardFunctionAllowsTransfersToZeroAddressWiX"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(getPeriodReward|safeTransfer|tokenId).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
