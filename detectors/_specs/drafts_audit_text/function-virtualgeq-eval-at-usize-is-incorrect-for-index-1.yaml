skeleton: "name_match_missing_require"
name: "function-virtualgeq-eval-at-usize-is-incorrect-for-index-1"
class_name: "FunctionVirtualgeqEvalAtUsizeIsIncorrectForIndex1"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Hypercube Protocol - Zellic Audit Report"
help: |
    The function virtual_geq::VirtualGeq<F>::eval_at_usize is expected to return the linear combination of the boolean functions index == self.threshold and index >= self.threshold (in
wiki_title: "Function VirtualGeq::eval_at_usize is incorrect for index == (1 <<"
wiki_description: |
    The function virtual_geq::VirtualGeq<F>::eval_at_usize is expected to return the linear combination of the boolean functions index == self.threshold and index >= self.threshold (interpreted as functions with values {0, 1}) with coefficients self.eq_coefficient and self.geq_coefficient. // slop/crate
wiki_exploit_scenario: |
    Per audit finding: The function virtual_geq::VirtualGeq<F>::eval_at_usize is expected to return the linear combination of the boolean functions index == self.threshold and index >= self.threshold (interpreted as functio
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionVirtualgeqEvalAtUsizeIsIncorrectForIndex1"
state_decl: "uint256 internal geq_coefficient;"
fn_name_regex: ".*(VirtualGeq|threshold|geq_coefficient).*"
write_var_regex: ".*(geq_coefficient|threshold).*"
guard_var_regex: ".*(geq_coefficient|threshold).*"
vuln_fn_name: "VirtualGeq"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "geq_coefficient = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
