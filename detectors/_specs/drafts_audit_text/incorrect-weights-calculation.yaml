skeleton: "name_match_missing_require"
name: "incorrect-weights-calculation"
class_name: "IncorrectWeightsCalculation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Y2K Finance - Zellic Audit Report"
help: |
    The function _thresholdWeight performs a calculation of weights for a set of vaults based on their return on investment (ROI) compared to a threshold value. However, during the pro
wiki_title: "Incorrect weights calculation"
wiki_description: |
    The function _thresholdWeight performs a calculation of weights for a set of vaults based on their return on investment (ROI) compared to a threshold value. However, during the process of identifying valid vaults, the validIds array is populated with both valid indexes and zeros, which leads to unin
wiki_exploit_scenario: |
    Per audit finding: The function _thresholdWeight performs a calculation of weights for a set of vaults based on their return on investment (ROI) compared to a threshold value. However, during the process of identifying
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectWeightsCalculation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validIds|validCount|epochIds).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validIds"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
