skeleton: "name_match_missing_call"
name: "unsafe-random-function"
class_name: "UnsafeRandomFunction"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Babylon Genesis Chain - Zellic Audit Report"
help: |
    In internal/utils/rand.go, the random function was implemented like below: import ( "math/rand" ) // RandomAlphaNum generates random alphanumeric string // in case length <= 0 it r
wiki_title: "Unsafe random function"
wiki_description: |
    In internal/utils/rand.go, the random function was implemented like below: import ( "math/rand" ) // RandomAlphaNum generates random alphanumeric string // in case length <= 0 it returns empty string func RandomAlphaNum(length int) string { const charset = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOP
wiki_exploit_scenario: |
    Per audit finding: In internal/utils/rand.go, the random function was implemented like below: import ( "math/rand" ) // RandomAlphaNum generates random alphanumeric string // in case length <= 0 it returns empty string
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnsafeRandomFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(randomString).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "randomString"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
