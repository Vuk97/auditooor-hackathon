skeleton: "name_match_missing_call"
name: "dust-loss-in-roundmanager-allocation"
class_name: "DustLossInRoundmanagerAllocation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit cyberRaise - Zellic Audit Report"
help: |
    The allocate function in RoundManager computes units and investmentUSD through integer division. Remainders from allocatedAmount that do not divide evenly by pricePerUnit or 10 **
wiki_title: "Dust loss in RoundManager allocation"
wiki_description: |
    The allocate function in RoundManager computes units and investmentUSD through integer division. Remainders from allocatedAmount that do not divide evenly by pricePerUnit or 10 ** paymentDecimals are not processed. The contract records unitsRepresented and investmentAmountUSD based on these truncate
wiki_exploit_scenario: |
    Per audit finding: The allocate function in RoundManager computes units and investmentUSD through integer division. Remainders from allocatedAmount that do not divide evenly by pricePerUnit or 10 ** paymentDecimals are
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DustLossInRoundmanagerAllocation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(investmentUSD|allocatedAmount|pricePerUnit).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "investmentUSD"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
