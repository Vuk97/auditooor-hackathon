skeleton: "name_match_missing_call"
name: "panic-for-messages-with-no-signers"
class_name: "PanicForMessagesWithNoSigners"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Osmosis Authentication Abstraction - Zellic Audit Report"
help: |
    The ante handler for the authenticator authenticates the first signer for every message in a transaction. for msgIndex, msg :) range msgs { /) By default, the first signer is the a
wiki_title: "Panic for messages with no signers"
wiki_description: |
    The ante handler for the authenticator authenticates the first signer for every message in a transaction. for msgIndex, msg :) range msgs { /) By default, the first signer is the account account, err :) utils.GetAccount(msg) if err !) nil { return sdk.Context{}, sdkerrors.Wrap(sdkerrors.ErrUnauthori
wiki_exploit_scenario: |
    Per audit finding: The ante handler for the authenticator authenticates the first signer for every message in a transaction. for msgIndex, msg :) range msgs { /) By default, the first signer is the account account, err
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PanicForMessagesWithNoSigners"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(msgIndex).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "msgIndex"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
