skeleton: "name_match_missing_call"
name: "unused-allowance"
class_name: "UnusedAllowance"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Audius EVM - Zellic Audit Report"
help: |
    The initiateRound functions approves a transfer; however, this allowance is not used by the safeTransfer. function initiateRound() external { ...)) audiusToken.mint(address(this),
wiki_title: "Unused allowance"
wiki_description: |
    The initiateRound functions approves a transfer; however, this allowance is not used by the safeTransfer. function initiateRound() external { ...)) audiusToken.mint(address(this), recurringCommunityFundingAmount); /) Approve transfer to community pool address audiusToken.approve(communityPoolAddress
wiki_exploit_scenario: |
    Per audit finding: The initiateRound functions approves a transfer; however, this allowance is not used by the safeTransfer. function initiateRound() external { ...)) audiusToken.mint(address(this), recurringCommunityFu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnusedAllowance"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(initiateRound|safeTransfer|audiusToken).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "initiateRound"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
