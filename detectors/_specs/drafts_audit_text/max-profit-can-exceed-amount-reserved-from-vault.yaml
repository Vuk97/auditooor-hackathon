skeleton: "name_match_missing_call"
name: "max-profit-can-exceed-amount-reserved-from-vault"
class_name: "MaxProfitCanExceedAmountReservedFromVault"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Avantis - Zellic Audit Report"
help: |
    When a new trade is finalized in _registerTrade, the amount reserved from the vault to ensure that the trade can be closed is the leveraged position size: function _registerTrade(I
wiki_title: "Max profit can exceed amount reserved from vault"
wiki_description: |
    When a new trade is finalized in _registerTrade, the amount reserved from the vault to ensure that the trade can be closed is the leveraged position size: function _registerTrade(ITradingStorage.Trade memory _trade) private returns (ITradingStorage.Trade memory) { //... storageT.vaultManager().reser
wiki_exploit_scenario: |
    Per audit finding: When a new trade is finalized in _registerTrade, the amount reserved from the vault to ensure that the trade can be closed is the leveraged position size: function _registerTrade(ITradingStorage.Trade
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MaxProfitCanExceedAmountReservedFromVault"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(vaultManager|reserveBalance|positionSizeUSDC).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "vaultManager"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
