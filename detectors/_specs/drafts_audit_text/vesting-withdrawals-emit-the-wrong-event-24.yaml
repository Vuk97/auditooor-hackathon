skeleton: "name_match_missing_call"
name: "vesting-withdrawals-emit-the-wrong-event-24"
class_name: "VestingWithdrawalsEmitTheWrongEvent24"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Solera - Zellic Audit Report"
help: |
    The price ratio is the ratio of assets to shares. The following function determines how many assets are in the vault: function totalAssets() public view override returns (uint256)
wiki_title: "Vesting withdrawals emit the wrong event                                    24"
wiki_description: |
    The price ratio is the ratio of assets to shares. The following function determines how many assets are in the vault: function totalAssets() public view override returns (uint256) { return super.totalAssets() - getVestingDeposit() + vestedAmount(block.timestamp.toUint64()); } Since super.totalAssets
wiki_exploit_scenario: |
    Per audit finding: The price ratio is the ratio of assets to shares. The following function determines how many assets are in the vault: function totalAssets() public view override returns (uint256) { return super.total
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "VestingWithdrawalsEmitTheWrongEvent24"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(totalAssets|getVestingDeposit|vestedAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "totalAssets"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
