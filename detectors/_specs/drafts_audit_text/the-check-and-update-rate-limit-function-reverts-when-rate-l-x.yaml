skeleton: "name_match_missing_require"
name: "the-check-and-update-rate-limit-function-reverts-when-rate-l-x"
class_name: "TheCheckAndUpdateRateLimitFunctionRevertsWhenRateLX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit LayerZero Starknet Endpoint V2 - Zellic Audit Report"
help: |
    The comment of the _check_and_update_rate_limit function explains that the function would return true if the rate limit is exceeded and false otherwise. However, it does not return
wiki_title: "The _check_and_update_rate_limit function reverts when rate limit is ex-"
wiki_description: |
    The comment of the _check_and_update_rate_limit function explains that the function would return true if the rate limit is exceeded and false otherwise. However, it does not return any result, but reverts when the rate limit is exceeded: /// Checks and updates the rate limit for the given endpoint I
wiki_exploit_scenario: |
    Per audit finding: The comment of the _check_and_update_rate_limit function explains that the function would return true if the rate limit is exceeded and false otherwise. However, it does not return any result, but rev
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheCheckAndUpdateRateLimitFunctionRevertsWhenRateLX"
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(eid|amount|direction).*"
write_var_regex: ".*(amount|direction|eid).*"
guard_var_regex: ".*(amount|direction|eid).*"
vuln_fn_name: "eid"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "amount = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
