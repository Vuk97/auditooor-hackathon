skeleton: "name_match_missing_call"
name: "each-msg-sender-can-only-have-one-token-each-msg-sender-can--x"
class_name: "EachMsgSenderCanOnlyHaveOneTokenEachMsgSenderCanX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-12-secondswap"
help: |
    [L-05] Each msg.sender can only have one token Each msg.sender can only have one token due to the _tokenOwner[msg.sender] checks. Therefore, if a user or a contract manages multipl
wiki_title: |
    Each msg.sender can only have one token Each msg.sender can only have one token due to the _tokenOwner[msg.sender] checks. Therefore, if a user or a contract ma
wiki_description: |
    [L-05] Each msg.sender can only have one token Each msg.sender can only have one token due to the _tokenOwner[msg.sender] checks. Therefore, if a user or a contract manages multiple tokens then only one of them will be able to have a vesting program. Proof of Concept require ( _tokenOwner [ msg . se
wiki_exploit_scenario: |
    Per audit finding: [L-05] Each msg.sender can only have one token Each msg.sender can only have one token due to the _tokenOwner[msg.sender] checks. Therefore, if a user or a contract manages multiple tokens then only o
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "EachMsgSenderCanOnlyHaveOneTokenEachMsgSenderCanX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tokenAddress|deployVesting).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "tokenAddress"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
