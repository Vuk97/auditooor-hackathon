skeleton: "name_match_missing_call"
name: "storage-root-assignment-missing-in-tree-finalization"
class_name: "StorageRootAssignmentMissingInTreeFinalization"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "code4arena audit 2025-01-initia-rollup-modules"
help: |
    OMEN Summary The handleTree function in the challenger’s child chain processing fails to assign the merkle tree root hash to the storage root return value, causing the output handl
wiki_title: "Storage root assignment missing in tree finalization"
wiki_description: |
    OMEN Summary The handleTree function in the challenger’s child chain processing fails to assign the merkle tree root hash to the storage root return value, causing the output handling logic to never execute. Impact Severity: High The bug prevents proper state synchronization between L1 and L2 chains
wiki_exploit_scenario: |
    Per audit finding: OMEN Summary The handleTree function in the challenger’s child chain processing fails to assign the merkle tree root hash to the storage root return value, causing the output handling logic to never e
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "StorageRootAssignmentMissingInTreeFinalization"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(handleTree|blockHeight|blockHeader).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "handleTree"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
