skeleton: "name_match_missing_require"
name: "ownership-transfer-is-one-step"
class_name: "OwnershipTransferIsOneStep"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit eBridge Ethereum Bridge - Zellic Audit Report"
help: |
    In the RegimentImplementation, the functions ChangeController and TransferRegimentOwner- ship allow a previous controller and owner to transfer control and ownership, respectively,
wiki_title: "Ownership transfer is one-step"
wiki_description: |
    In the RegimentImplementation, the functions ChangeController and TransferRegimentOwner- ship allow a previous controller and owner to transfer control and ownership, respectively, to a new address: function ChangeController( address _controller ) external assertSenderIsController { require(_control
wiki_exploit_scenario: |
    Per audit finding: In the RegimentImplementation, the functions ChangeController and TransferRegimentOwner- ship allow a previous controller and owner to transfer control and ownership, respectively, to a new address: f
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "OwnershipTransferIsOneStep"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(assertSenderIsController|regimentId|newManagerAddress).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "assertSenderIsController"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
