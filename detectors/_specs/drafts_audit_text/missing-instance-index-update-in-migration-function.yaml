skeleton: "name_match_missing_call"
name: "missing-instance-index-update-in-migration-function"
class_name: "MissingInstanceIndexUpdateInMigrationFunction"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Mitosis (May) - Zellic Audit Report"
help: |
    In the EOLVaultFactory contract, the migrate function fails to properly update the instance index mapping for the destination vault type. While the function correctly removes the i
wiki_title: "Missing instance index update in migration function"
wiki_description: |
    In the EOLVaultFactory contract, the migrate function fails to properly update the instance index mapping for the destination vault type. While the function correctly removes the instance from the source vault type's tracking, it does not set the corresponding index in the destination's instanceInde
wiki_exploit_scenario: |
    Per audit finding: In the EOLVaultFactory contract, the migrate function fails to properly update the instance index mapping for the destination vault type. While the function correctly removes the instance from the sou
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingInstanceIndexUpdateInMigrationFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(instanceIndex|onlyOwner).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "instanceIndex"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
