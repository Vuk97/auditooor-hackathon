skeleton: "name_match_missing_call"
name: "lack-of-reentrancy-protection-22"
class_name: "LackOfReentrancyProtection22"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Chateau - Zellic Audit Report"
help: |
    The swap function iterates through the entire array of issues. These issues are created upon per- forming a stake() operation, and they essentially represent a user deposit. Becaus
wiki_title: "Lack of reentrancy protection                                           22"
wiki_description: |
    The swap function iterates through the entire array of issues. These issues are created upon per- forming a stake() operation, and they essentially represent a user deposit. Because there is no minimal cap as to the amount of tokens to stake() in the pool, a malicious user could perform stake() with
wiki_exploit_scenario: |
    Per audit finding: The swap function iterates through the entire array of issues. These issues are created upon per- forming a stake() operation, and they essentially represent a user deposit. Because there is no minima
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfReentrancyProtection22"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(indexEnd|indexStar|issueInfo).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "indexEnd"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
