skeleton: "name_match_missing_call"
name: "manager-should-not-be-removable"
class_name: "ManagerShouldNotBeRemovable"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit eBridge Ethereum Bridge - Zellic Audit Report"
help: |
    The RegimentImplementation contract is used to manage the protocol actions. It adds and removes regiment members, who can then perform particular actions across the bridges and oth
wiki_title: "Manager should not be removable"
wiki_description: |
    The RegimentImplementation contract is used to manage the protocol actions. It adds and removes regiment members, who can then perform particular actions across the bridges and other contracts. Each regiment also has a manager, who can add and remove members. Currently, when removing a particular me
wiki_exploit_scenario: |
    Per audit finding: The RegimentImplementation contract is used to manage the protocol actions. It adds and removes regiment members, who can then perform particular actions across the bridges and other contracts. Each r
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ManagerShouldNotBeRemovable"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(regimentId|leaveMemberAddress|regimentInfo).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "regimentId"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
