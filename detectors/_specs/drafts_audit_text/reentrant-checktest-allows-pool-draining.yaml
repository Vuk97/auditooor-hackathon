skeleton: "highlevelcall_missing_sibling"
name: "reentrant-checktest-allows-pool-draining"
class_name: "ReentrantChecktestAllowsPoolDraining"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Ante - Zellic Audit Report"
help: |
    Because checkTest allows reentrancy, in specific cases, an attacker may be able to drain AntePool by 1. Calling checkTest on a test that returns false (not one that reverts). The t
wiki_title: "Reentrant checkTest allows pool draining"
wiki_description: |
    Because checkTest allows reentrancy, in specific cases, an attacker may be able to drain AntePool by 1. Calling checkTest on a test that returns false (not one that reverts). The test must be written in a way that causes the contract to make an external call to an attacker contract. The attacker con
wiki_exploit_scenario: |
    Per audit finding: Because checkTest allows reentrancy, in specific cases, an attacker may be able to drain AntePool by 1. Calling checkTest on a test that returns false (not one that reverts). The test must be written
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ReentrantChecktestAllowsPoolDraining"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(checkTest|checkTestPasses).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
