skeleton: "name_match_missing_call"
name: "ineffective-short-circuit-in-optimizednoncecommittable"
class_name: "IneffectiveShortCircuitInOptimizednoncecommittable"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit LayerZero TON - Zellic Audit Report"
help: |
    In the function optimizedNonceCommittable, the short circuit condition never triggers before the first commit because the executionQueue is only updated during commit and execution
wiki_title: "Ineffective short circuit in _optimizedNonceCommittable"
wiki_description: |
    In the function optimizedNonceCommittable, the short circuit condition never triggers before the first commit because the executionQueue is only updated during commit and execution phases, where entries are set to ExecutionQueue::committed or ExecutionQueue::executing. Before that, each entry remain
wiki_exploit_scenario: |
    Per audit finding: In the function optimizedNonceCommittable, the short circuit condition never triggers before the first commit because the executionQueue is only updated during commit and execution phases, where entri
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IneffectiveShortCircuitInOptimizednoncecommittable"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(optimizedNonceCommittable|executionQueue|invalidKey).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "optimizedNonceCommittable"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
