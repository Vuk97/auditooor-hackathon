skeleton: "highlevelcall_missing_sibling"
name: "cross-msg-recipient-can-block-checkpoint-submission"
class_name: "CrossMsgRecipientCanBlockCheckpointSubmission"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "code4arena audit 2025-02-recall"
help: |
    SBSecurity As mentioned in the natspec of LibGateway::executeCrossMsg : /// @dev Execute the cross message using low level `call` method. This way ipc will /// catch contract rever
wiki_title: "Cross msg recipient can block checkpoint submission"
wiki_description: |
    SBSecurity As mentioned in the natspec of LibGateway::executeCrossMsg : /// @dev Execute the cross message using low level `call` method. This way ipc will /// catch contract revert messages as well. We need this because in `CrossMsgHelper.execute` /// there are `require` and `revert` calls, without
wiki_exploit_scenario: |
    Per audit finding: SBSecurity As mentioned in the natspec of LibGateway::executeCrossMsg : /// @dev Execute the cross message using low level `call` method. This way ipc will /// catch contract revert messages as well.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CrossMsgRecipientCanBlockCheckpointSubmission"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(call|require|revert).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
