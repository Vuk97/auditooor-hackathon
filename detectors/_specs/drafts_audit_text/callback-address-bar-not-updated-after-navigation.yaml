skeleton: "state_write_without_paired_write"
name: "callback-address-bar-not-updated-after-navigation"
class_name: "CallbackAddressBarNotUpdatedAfterNavigation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The Reclaim app uses a WebView component to display the content of the callback webpage. It displays an address bar above the WebView. But, this address bar does not update wheneve
wiki_title: "Callback address bar not updated after navigation"
wiki_description: |
    The Reclaim app uses a WebView component to display the content of the callback webpage. It displays an address bar above the WebView. But, this address bar does not update whenever the WebView navigates to a new page. This issue occurs in reclaim-app/src/screens/Submit.tsx: const Submit: React.FC<P
wiki_exploit_scenario: |
    Per audit finding: The Reclaim app uses a WebView component to display the content of the callback webpage. It displays an address bar above the WebView. But, this address bar does not update whenever the WebView naviga
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CallbackAddressBarNotUpdatedAfterNavigation"
state_decl: |
    mapping(address => uint256) internal balances;
        mapping(address => bool) internal flags;
fn_name_regex: ".*(numberOfLines|callbackUrl).*"
primary_var_regex: ".*(balance|amount|total|supply|reserve).*"
paired_var_regex: ".*(flag|isLong|cleared|reset|status).*"
vuln_fn_name: "liquidate"
vuln_fn_params: "address user"
primary_write_line: "balances[user] = 0;"
paired_write_line: "flags[user] = false;"
