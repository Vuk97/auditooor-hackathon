skeleton: "name_match_missing_require"
name: "the-ismulticall-flag-is-not-correctly-reset-to"
class_name: "TheIsmulticallFlagIsNotCorrectlyResetTo"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-06-size"
help: |
    [L-04] The isMulticall flag is not correctly reset to false in the multicall function. In the multicall function, we set isMulticall to true at the beginning and reset it to false
wiki_title: "The isMulticall flag is not correctly reset to"
wiki_description: |
    [L-04] The isMulticall flag is not correctly reset to false in the multicall function. In the multicall function, we set isMulticall to true at the beginning and reset it to false at the end. function multicall(State storage state, bytes[] calldata data) internal returns (bytes[] memory results) { s
wiki_exploit_scenario: |
    Per audit finding: [L-04] The isMulticall flag is not correctly reset to false in the multicall function. In the multicall function, we set isMulticall to true at the beginning and reset it to false at the end. function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheIsmulticallFlagIsNotCorrectlyResetTo"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(isMulticall).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "isMulticall"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
