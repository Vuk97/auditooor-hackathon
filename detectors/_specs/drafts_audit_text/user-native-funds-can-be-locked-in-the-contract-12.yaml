skeleton: "name_match_missing_call"
name: "user-native-funds-can-be-locked-in-the-contract-12"
class_name: "UserNativeFundsCanBeLockedInTheContract12"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Ooga Booga - Zellic Audit Report"
help: |
    The _swap function calculates the slippage that occurs during the token swap and uses it to deter- mine the amount of tokens (amountOut) the user will receive. Slippage is calculat
wiki_title: "User native funds can be locked in the contract                                12"
wiki_description: |
    The _swap function calculates the slippage that occurs during the token swap and uses it to deter- mine the amount of tokens (amountOut) the user will receive. Slippage is calculated by subtracting the user-defined quote (tokenInfo.outputQuote) from the fee-adjusted amount of tokens the user will re
wiki_exploit_scenario: |
    Per audit finding: The _swap function calculates the slippage that occurs during the token swap and uses it to deter- mine the amount of tokens (amountOut) the user will receive. Slippage is calculated by subtracting th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UserNativeFundsCanBeLockedInTheContract12"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(amountOut|tokenInfo|outputQuote).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "amountOut"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
