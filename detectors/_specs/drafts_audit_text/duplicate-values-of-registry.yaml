skeleton: "name_match_missing_call"
name: "duplicate-values-of-registry"
class_name: "DuplicateValuesOfRegistry"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Extended ERC20 - Zellic Audit Report"
help: |
    The contract's document-management system contains an error in its hash-removal process. When removing or updating a document hash, the function sets registeredHashes[existingHash]
wiki_title: "Duplicate values of registry"
wiki_description: |
    The contract's document-management system contains an error in its hash-removal process. When removing or updating a document hash, the function sets registeredHashes[existingHash] = false without verifying if other keys still reference that hash. This leads to isHashRegistered() incorrectly returni
wiki_exploit_scenario: |
    Per audit finding: The contract's document-management system contains an error in its hash-removal process. When removing or updating a document hash, the function sets registeredHashes[existingHash] = false without ver
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DuplicateValuesOfRegistry"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(registeredHashes|existingHash|isHashRegistered).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "registeredHashes"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
