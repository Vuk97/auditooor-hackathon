skeleton: "name_match_missing_call"
name: "abi-encoded-inputs-can-mismatch-specified-amount"
class_name: "AbiEncodedInputsCanMismatchSpecifiedAmount"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit STFX - Zellic Audit Report"
help: |
    A manager or admin can execute a swap via Uniswap’s universal router. However, they can potentially cause a mismanagement of funds if they abi.encode a different value in the input
wiki_title: "ABI-encoded inputs can mismatch specified amount"
wiki_description: |
    A manager or admin can execute a swap via Uniswap’s universal router. However, they can potentially cause a mismanagement of funds if they abi.encode a different value in the inputs parameter than what is specified in the amountIn parameter for the swap.
wiki_exploit_scenario: |
    Per audit finding: A manager or admin can execute a swap via Uniswap’s universal router. However, they can potentially cause a mismanagement of funds if they abi.encode a different value in the inputs parameter than wha
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AbiEncodedInputsCanMismatchSpecifiedAmount"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(amountIn).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "amountIn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
