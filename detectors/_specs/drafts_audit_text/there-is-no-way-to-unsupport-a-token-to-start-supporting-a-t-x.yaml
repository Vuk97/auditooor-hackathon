skeleton: "name_match_missing_call"
name: "there-is-no-way-to-unsupport-a-token-to-start-supporting-a-t-x"
class_name: "ThereIsNoWayToUnsupportATokenToStartSupportingATX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-12-secondswap"
help: |
    [L-01] There is no way to unsupport a token To start supporting a token the s2Admin call the SecondSwap_Marketplace::addCoin() function, however, if for any reason the s2Admin deci
wiki_title: |
    There is no way to unsupport a token To start supporting a token the s2Admin call the SecondSwap_Marketplace::addCoin() function, however, if for any reason the
wiki_description: |
    [L-01] There is no way to unsupport a token To start supporting a token the s2Admin call the SecondSwap_Marketplace::addCoin() function, however, if for any reason the s2Admin decide to not support a token anymore, there is no way to remove that token from the isTokenSupport[] list because the value
wiki_exploit_scenario: |
    Per audit finding: [L-01] There is no way to unsupport a token To start supporting a token the s2Admin call the SecondSwap_Marketplace::addCoin() function, however, if for any reason the s2Admin decide to not support a
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ThereIsNoWayToUnsupportATokenToStartSupportingATX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(s2Admin|addCoin|isTokenSupport).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "s2Admin"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
