skeleton: "name_match_missing_require"
name: "liquidation-should-not-be-paused"
class_name: "LiquidationShouldNotBePaused"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2025-03-starknet-perpetual"
help: |
    [L-05] Liquidation should not be paused The liquidate function is currently gated by a pause check via self.pausable.assert_not_paused() . While pausing protocol operations is esse
wiki_title: "Liquidation should not be paused"
wiki_description: |
    [L-05] Liquidation should not be paused The liquidate function is currently gated by a pause check via self.pausable.assert_not_paused() . While pausing protocol operations is essential during emergencies, applying this restriction to liquidation poses a critical risk to protocol solvency. fn liquid
wiki_exploit_scenario: |
    Per audit finding: [L-05] Liquidation should not be paused The liquidate function is currently gated by a pause check via self.pausable.assert_not_paused() . While pausing protocol operations is essential during emergen
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LiquidationShouldNotBePaused"
state_decl: "uint256 internal liquidated_fee_amount;"
fn_name_regex: ".*(liquidated_fee_amount).*"
write_var_regex: ".*(liquidated_fee_amount).*"
guard_var_regex: ".*(liquidated_fee_amount).*"
vuln_fn_name: "liquidated_fee_amount"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "liquidated_fee_amount = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
