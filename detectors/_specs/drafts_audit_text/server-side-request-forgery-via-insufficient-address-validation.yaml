skeleton: "name_match_missing_require"
name: "server-side-request-forgery-via-insufficient-address-validation"
class_name: "ServerSideRequestForgeryViaInsufficientAddressValidation"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The function initializeSession of an externally opened gRPC server allows the user to establish a socket with any desired server. In particular, localhost can be accessed due to an
wiki_title: "Server-side request forgery via insufficient address validation"
wiki_description: |
    The function initializeSession of an externally opened gRPC server allows the user to establish a socket with any desired server. In particular, localhost can be accessed due to an insufficient address check, leading to a server-side request forgery vulnerability. const initialiseSession: RPCPromise
wiki_exploit_scenario: |
    Per audit finding: The function initializeSession of an externally opened gRPC server allows the user to establish a socket with any desired server. In particular, localhost can be accessed due to an insufficient addres
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ServerSideRequestForgeryViaInsufficientAddressValidation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(initializeSession|gRPC|initialiseSession).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "initializeSession"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
