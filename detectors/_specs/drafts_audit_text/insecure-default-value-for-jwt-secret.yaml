skeleton: "name_match_missing_call"
name: "insecure-default-value-for-jwt-secret"
class_name: "InsecureDefaultValueForJwtSecret"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LootRush - Zellic Audit Report"
help: |
    The custodial wallet uses JSON Web Tokens (JWTs) to authenticate a user to the back- end. To prevent a malicious user from tampering with the contents of the JWTs, a secret only kn
wiki_title: "Insecure default value for JWT secret"
wiki_description: |
    The custodial wallet uses JSON Web Tokens (JWTs) to authenticate a user to the back- end. To prevent a malicious user from tampering with the contents of the JWTs, a secret only known to the server is used to sign the token. import * as env from 'env-var' export const ENV_NAME = env.get('ENV_NAME').
wiki_exploit_scenario: |
    Per audit finding: The custodial wallet uses JSON Web Tokens (JWTs) to authenticate a user to the back- end. To prevent a malicious user from tampering with the contents of the JWTs, a secret only known to the server is
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InsecureDefaultValueForJwtSecret"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(asString|asUrlString|asPortNumber).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "asString"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
