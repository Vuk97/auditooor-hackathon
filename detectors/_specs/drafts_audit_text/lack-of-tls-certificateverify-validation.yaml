skeleton: "name_match_missing_call"
name: "lack-of-tls-certificateverify-validation"
class_name: "LackOfTlsCertificateverifyValidation"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Reclaim Protocol - Zellic Audit Report"
help: |
    The Reclaim TLS implementation only validates the server’s certificate chain when it receives a Cer- tificateVerify record. If the record is never received, the certificate chain i
wiki_title: "Lack of TLS CertificateVerify validation"
wiki_description: |
    The Reclaim TLS implementation only validates the server’s certificate chain when it receives a Cer- tificateVerify record. If the record is never received, the certificate chain is never validated and the connection proceeds normally. In particular, for TLS 1.2 connections where the server only use
wiki_exploit_scenario: |
    Per audit finding: The Reclaim TLS implementation only validates the server’s certificate chain when it receives a Cer- tificateVerify record. If the record is never received, the certificate chain is never validated an
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LackOfTlsCertificateverifyValidation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(tificateVerify|createClaim|responseMatches).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "tificateVerify"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
