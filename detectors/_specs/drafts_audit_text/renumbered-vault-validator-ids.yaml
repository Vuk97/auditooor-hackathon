skeleton: "name_match_missing_call"
name: "renumbered-vault-validator-ids"
class_name: "RenumberedVaultValidatorIds"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Polygon Staking - Zellic Audit Report"
help: |
    The finalizeValidatorRemoval function removes a validator by moving every validator after it one slot backwards in the validators array: function finalizeValidatorRemoval() externa
wiki_title: "Renumbered vault/validator IDs"
wiki_description: |
    The finalizeValidatorRemoval function removes a validator by moving every validator after it one slot backwards in the validators array: function finalizeValidatorRemoval() external onlyOwner { // [...] for (uint256 i = validatorId; i < validators.length - 1; ++i) { validators[i] = validators[i + 1]
wiki_exploit_scenario: |
    Per audit finding: The finalizeValidatorRemoval function removes a validator by moving every validator after it one slot backwards in the validators array: function finalizeValidatorRemoval() external onlyOwner { // [..
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RenumberedVaultValidatorIds"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(finalizeValidatorRemoval|onlyOwner|validatorId).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "finalizeValidatorRemoval"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
