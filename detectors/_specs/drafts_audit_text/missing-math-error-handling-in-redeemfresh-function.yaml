skeleton: "name_match_missing_call"
name: "missing-math-error-handling-in-redeemfresh-function"
class_name: "MissingMathErrorHandlingInRedeemfreshFunction"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Takara Lend Contracts - Zellic Audit Report"
help: |
    In the TToken.redeemFresh() function, the lines that call divScalarByExpTruncate() assign a math error to vars.mathErr, but this error is not checked before it is overwritten by th
wiki_title: "Missing math-error handling in redeemFresh function"
wiki_description: |
    In the TToken.redeemFresh() function, the lines that call divScalarByExpTruncate() assign a math error to vars.mathErr, but this error is not checked before it is overwritten by the subsequent math operation: (vars.mathErr, vars.redeemTokens) = divScalarByExpTruncate(redeemAmountIn, Exp({mantissa: v
wiki_exploit_scenario: |
    Per audit finding: In the TToken.redeemFresh() function, the lines that call divScalarByExpTruncate() assign a math error to vars.mathErr, but this error is not checked before it is overwritten by the subsequent math op
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingMathErrorHandlingInRedeemfreshFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(redeemFresh|divScalarByExpTruncate|mathErr).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "redeemFresh"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
