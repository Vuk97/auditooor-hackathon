skeleton: "name_match_missing_call"
name: "denial-of-service-on-pending-requests-13"
class_name: "DenialOfServiceOnPendingRequests13"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Prosper Omnichain Fungible Token - Zellic Audit Report"
help: |
    A user can request to burn some of their tokens by calling the requestBurn function. The function creates or updates the burnRequests in storage with the new amount: function reque
wiki_title: "Denial of service on pending requests                                13"
wiki_description: |
    A user can request to burn some of their tokens by calling the requestBurn function. The function creates or updates the burnRequests in storage with the new amount: function requestBurn(uint256 amount) external { Storage.Layout storage $ = Storage.layout(); if (amount == 0) { revert RequestBurn__Am
wiki_exploit_scenario: |
    Per audit finding: A user can request to burn some of their tokens by calling the requestBurn function. The function creates or updates the burnRequests in storage with the new amount: function requestBurn(uint256 amoun
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DenialOfServiceOnPendingRequests13"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(requestBurn|burnRequests|currentBurnAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "requestBurn"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
