skeleton: "highlevelcall_missing_sibling"
name: "directly-sending-dust-token-amount-will-slow-down-distributi-x"
class_name: "DirectlySendingDustTokenAmountWillSlowDownDistributiX"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-loopfi"
help: |
    novamanbg While sending a small amount and calling getReward is a typical example of griefing which is normally considered Medium. Given the low cost of the attack and that no spec
wiki_title: "Directly sending dust token amount will slow down distribution in MultiFeeDistribution.sol"
wiki_description: |
    novamanbg While sending a small amount and calling getReward is a typical example of griefing which is normally considered Medium. Given the low cost of the attack and that no specific external conditions need to be met as well as the high impact for all the users of the protocol I consider it High
wiki_exploit_scenario: |
    Per audit finding: novamanbg While sending a small amount and calling getReward is a typical example of griefing which is normally considered Medium. Given the low cost of the attack and that no specific external condit
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "DirectlySendingDustTokenAmountWillSlowDownDistributiX"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(getReward|balanceOf|rewardPerSecond).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
