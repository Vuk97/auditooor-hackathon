skeleton: "name_match_missing_call"
name: "rounding-errors-in-computing-fee"
class_name: "RoundingErrorsInComputingFee"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit StakeKit FeeWrapper - Zellic Audit Report"
help: |
    Due to Solidity's lack of native support for floating-point arithmetic, the results of computing fees may be inconsistent. function computeFee(uint256 amount, FeeConfig memory feeC
wiki_title: "Rounding errors in computing fee"
wiki_description: |
    Due to Solidity's lack of native support for floating-point arithmetic, the results of computing fees may be inconsistent. function computeFee(uint256 amount, FeeConfig memory feeConfig) public pure returns (uint256) { uint16 fee = feeConfig.fee; uint256 feeAmount = (amount * fee) / 10_000; return f
wiki_exploit_scenario: |
    Per audit finding: Due to Solidity's lack of native support for floating-point arithmetic, the results of computing fees may be inconsistent. function computeFee(uint256 amount, FeeConfig memory feeConfig) public pure r
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RoundingErrorsInComputingFee"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(computeFee|feeConfig|feeAmount).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "computeFee"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
