skeleton: "name_match_missing_call"
name: "permissionless-factory"
class_name: "PermissionlessFactory"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "hexens audit Azuro_public"
help: |
    Anyone can deploy a Liquidity Pool with any token, and arbitrary fees, even if a pool already exists associated with that token. This seems to be a design decision which fosters he
wiki_title: "PERMISSIONLESS FACTORY"
wiki_description: |
    Anyone can deploy a Liquidity Pool with any token, and arbitrary fees, even if a pool already exists associated with that token. This seems to be a design decision which fosters healthy competition, but it also has the potential to facilitate scam schemes. If this is not intended behaviour, then it
wiki_exploit_scenario: |
    Per audit finding: Anyone can deploy a Liquidity Pool with any token, and arbitrary fees, even if a pool already exists associated with that token. This seems to be a design decision which fosters healthy competition, b
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PermissionlessFactory"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(createPool|daoFee|coreType).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "createPool"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
