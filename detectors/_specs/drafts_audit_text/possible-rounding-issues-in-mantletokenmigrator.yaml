skeleton: "name_match_missing_call"
name: "possible-rounding-issues-in-mantletokenmigrator"
class_name: "PossibleRoundingIssuesInMantletokenmigrator"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Mantle - Zellic Audit Report"
help: |
    The swap calculation in MantleTokenMigrator is done using _tokenSwapCalculation (as shown below). This function uses TOKEN_CONVERSION_NUMERATOR and TOKEN_CONVERSIO N_DENOMINATOR va
wiki_title: "Possible rounding issues in MantleTokenMigrator"
wiki_description: |
    The swap calculation in MantleTokenMigrator is done using _tokenSwapCalculation (as shown below). This function uses TOKEN_CONVERSION_NUMERATOR and TOKEN_CONVERSIO N_DENOMINATOR variables declared in the constructor. function _tokenSwapCalculation(uint256 _amount) internal view returns (uint256) { r
wiki_exploit_scenario: |
    Per audit finding: The swap calculation in MantleTokenMigrator is done using _tokenSwapCalculation (as shown below). This function uses TOKEN_CONVERSION_NUMERATOR and TOKEN_CONVERSIO N_DENOMINATOR variables declared in
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PossibleRoundingIssuesInMantletokenmigrator"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(amountToSwap|safeTransferFrom|safeTransfer).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "amountToSwap"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
