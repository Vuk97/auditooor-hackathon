skeleton: "highlevelcall_missing_sibling"
name: "stealing-eth-using-reentrancy-in-minting-pass"
class_name: "StealingEthUsingReentrancyInMintingPass"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "hexens audit Babylon-jan-2023"
help: |
    In BabylonCore.sol:participate a user can buy tickets, which are minting pass NFTs and get minted to the user. The minting pass contracts use _safeMint, which makes an external cal
wiki_title: "STEALING ETH USING REENTRANCY IN MINTING PASS"
wiki_description: |
    In BabylonCore.sol:participate a user can buy tickets, which are minting pass NFTs and get minted to the user. The minting pass contracts use _safeMint, which makes an external call with onERC721Received to the receiver user and thus that execution is passed to the user upon minting. As a result, a
wiki_exploit_scenario: |
    Per audit finding: In BabylonCore.sol:participate a user can buy tickets, which are minting pass NFTs and get minted to the user. The minting pass contracts use _safeMint, which makes an external call with onERC721Recei
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "StealingEthUsingReentrancyInMintingPass"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(onERC721Received|checkApproval|timeStart).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
