skeleton: "name_match_missing_require"
name: "storage-not-set-properly"
class_name: "StorageNotSetProperly"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Spectral Modelers - Zellic Audit Report"
help: |
    A ZKMLChallenge is one type of challenge that the Spectral protocol supports. The modelers are required to reply to a challenge, by essentially answering with their model’s respons
wiki_title: "Storage not set properly"
wiki_description: |
    A ZKMLChallenge is one type of challenge that the Spectral protocol supports. The modelers are required to reply to a challenge, by essentially answering with their model’s response. The respondToZKMLChallenge function thus allows the modeler to respond to a ZKMLChallenge by providing the IPFS respo
wiki_exploit_scenario: |
    Per audit finding: A ZKMLChallenge is one type of challenge that the Spectral protocol supports. The modelers are required to reply to a challenge, by essentially answering with their model’s response. The respondToZKML
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "StorageNotSetProperly"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(respondToZKMLChallenge|modelerSubmissionBlock|isValidatorRegistered).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "respondToZKMLChallenge"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
