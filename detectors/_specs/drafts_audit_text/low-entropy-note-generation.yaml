skeleton: "name_match_missing_call"
name: "low-entropy-note-generation"
class_name: "LowEntropyNoteGeneration"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Singularity - Zellic Audit Report"
help: |
    If there are multiple notes for the same public key with identical rho, only one of those notes will be spendable. The function generateRho generates this value only with 64 bits o
wiki_title: "Low-entropy note generation"
wiki_description: |
    If there are multiple notes for the same public key with identical rho, only one of those notes will be spendable. The function generateRho generates this value only with 64 bits of entropy according to its implementation: export const generateRho = () => { let ab = new ArrayBuffer(32); return bn_to
wiki_exploit_scenario: |
    Per audit finding: If there are multiple notes for the same public key with identical rho, only one of those notes will be spendable. The function generateRho generates this value only with 64 bits of entropy according
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "LowEntropyNoteGeneration"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(generateRho|getRandomValues|readBigUInt64BE).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "generateRho"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
