skeleton: "name_match_missing_require"
name: "missing-test-coverage"
class_name: "MissingTestCoverage"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Vendor Finance - Zellic Audit Report"
help: |
    While the overall test coverage of the project is very good, there are some critical functionalities that have not been fully tested. Specifically, there is a lack of negative test
wiki_title: "Missing test coverage"
wiki_description: |
    While the overall test coverage of the project is very good, there are some critical functionalities that have not been fully tested. Specifically, there is a lack of negative test cases, which are essential for ensuring the platform’s resilience to unexpected inputs and edge cases. As such, it is r
wiki_exploit_scenario: |
    Per audit finding: While the overall test coverage of the project is very good, there are some critical functionalities that have not been fully tested. Specifically, there is a lack of negative test cases, which are es
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingTestCoverage"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setPoolRates|validateParams|grantOwnership).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "setPoolRates"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
