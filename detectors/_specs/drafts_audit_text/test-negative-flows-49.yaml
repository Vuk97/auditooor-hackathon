skeleton: "name_match_missing_require"
name: "test-negative-flows-49"
class_name: "TestNegativeFlows49"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Metavest - Zellic Audit Report"
help: |
    A set is a collection of MetaVesTs, each representing a specific vesting allocation. Each MetaVesT's grantee within the same set can vote on updates to MetaVesTs in that set. Updat
wiki_title: "Test negative flows                                                           49"
wiki_description: |
    A set is a collection of MetaVesTs, each representing a specific vesting allocation. Each MetaVesT's grantee within the same set can vote on updates to MetaVesTs in that set. Updates include changes to the purchase price, unlocking rate, and other changes to MetaVesT details. However, the function v
wiki_exploit_scenario: |
    Per audit finding: A set is a collection of MetaVesTs, each representing a specific vesting allocation. Each MetaVesT's grantee within the same set can vote on updates to MetaVesTs in that set. Updates include changes t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TestNegativeFlows49"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(voteOnMetavestAmendment|functionToSetMajorityProposal|isPending).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "voteOnMetavestAmendment"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
