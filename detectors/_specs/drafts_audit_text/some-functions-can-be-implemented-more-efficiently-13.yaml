skeleton: "name_match_missing_require"
name: "some-functions-can-be-implemented-more-efficiently-13"
class_name: "SomeFunctionsCanBeImplementedMoreEfficiently13"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Session Token - Zellic Audit Report 10.23.24"
help: |
    In the _contributeFunds function, when the staking requirements are met and manualFinalize is not set, the status is set to Status.WaitForFinalized, awaiting the operator to call t
wiki_title: "Some functions can be implemented more efficiently                    13"
wiki_description: |
    In the _contributeFunds function, when the staking requirements are met and manualFinalize is not set, the status is set to Status.WaitForFinalized, awaiting the operator to call the finalize function. function _contributeFunds(address caller, address beneficiary, uint256 amount) private { [...] if
wiki_exploit_scenario: |
    Per audit finding: In the _contributeFunds function, when the staking requirements are met and manualFinalize is not set, the status is set to Status.WaitForFinalized, awaiting the operator to call the finalize function
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SomeFunctionsCanBeImplementedMoreEfficiently13"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(manualFinalize|currTotalContribution|stakingRequirement).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "manualFinalize"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
