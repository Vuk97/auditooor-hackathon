skeleton: "name_match_missing_require"
name: "function-opentrade-lacks-incorrect-payment-sanity-checks"
class_name: "FunctionOpentradeLacksIncorrectPaymentSanityChecks"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Avantis - Zellic Audit Report"
help: |
    The function openTrade is called in order to open both market and limit orders. Opening a market order requires paying the Pyth oracle fee in ETH and a valid priceUpdateData. Openi
wiki_title: "Function openTrade lacks incorrect-payment sanity checks"
wiki_description: |
    The function openTrade is called in order to open both market and limit orders. Opening a market order requires paying the Pyth oracle fee in ETH and a valid priceUpdateData. Opening a limit order means the caller needs to pay an additional _executionFee. However, for both market and limit orders, t
wiki_exploit_scenario: |
    Per audit finding: The function openTrade is called in order to open both market and limit orders. Opening a market order requires paying the Pyth oracle fee in ETH and a valid priceUpdateData. Opening a limit order mea
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionOpentradeLacksIncorrectPaymentSanityChecks"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(openTrade|priceUpdateData).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "openTrade"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
