skeleton: "name_match_missing_call"
name: "using-transfer-instead-of-call-might-revert"
class_name: "UsingTransferInsteadOfCallMightRevert"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Ostium - Zellic Audit Report"
help: |
    The OstiumPriceUpKeep contract defines a function named withdrawEth that could be called by the governance to withdraw any ETH in this contract: function withdrawEth() external onl
wiki_title: "Using transfer instead of call might revert"
wiki_description: |
    The OstiumPriceUpKeep contract defines a function named withdrawEth that could be called by the governance to withdraw any ETH in this contract: function withdrawEth() external onlyGov { uint256 amount = address(this).balance; if (amount == 0) { revert EmptyBalance(); } emit EthWithdrawn(msg.sender,
wiki_exploit_scenario: |
    Per audit finding: The OstiumPriceUpKeep contract defines a function named withdrawEth that could be called by the governance to withdraw any ETH in this contract: function withdrawEth() external onlyGov { uint256 amoun
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UsingTransferInsteadOfCallMightRevert"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(withdrawEth|onlyGov).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "withdrawEth"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
