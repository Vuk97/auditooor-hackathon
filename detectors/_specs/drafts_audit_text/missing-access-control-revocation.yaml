skeleton: "name_match_missing_call"
name: "missing-access-control-revocation"
class_name: "MissingAccessControlRevocation"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit SpiceFiNFT4626 - Zellic Audit Report"
help: |
    The initialize function assigns the DEFAULT_ADMIN_ROLE to the multisig_ account passed as a parameter to the function. The address is also stored in the aptly named multisig member
wiki_title: "Missing access control revocation"
wiki_description: |
    The initialize function assigns the DEFAULT_ADMIN_ROLE to the multisig_ account passed as a parameter to the function. The address is also stored in the aptly named multisig member variable. The setMultisig function can be used by authorized callers to update the multisi g variable; however, the fun
wiki_exploit_scenario: |
    Per audit finding: The initialize function assigns the DEFAULT_ADMIN_ROLE to the multisig_ account passed as a parameter to the function. The address is also stored in the aptly named multisig member variable. The setMu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingAccessControlRevocation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(setMultisig).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "setMultisig"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
