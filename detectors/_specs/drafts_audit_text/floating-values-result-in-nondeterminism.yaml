skeleton: "name_match_missing_call"
name: "floating-values-result-in-nondeterminism"
class_name: "FloatingValuesResultInNondeterminism"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Babylon Genesis Chain - Zellic Audit Report"
help: |
    The BeforeValidatorSlashed function uses float values. This may result in nondeterministic be- havior of rounding in the floating values. func (h Hooks) BeforeValidatorSlashed(ctx
wiki_title: "Floating values result in nondeterminism"
wiki_description: |
    The BeforeValidatorSlashed function uses float values. This may result in nondeterministic be- havior of rounding in the floating values. func (h Hooks) BeforeValidatorSlashed(ctx context.Context, valAddr sdk.ValAddress, fraction math.LegacyDec) error { ... for _, threshold := range thresholds { //
wiki_exploit_scenario: |
    Per audit finding: The BeforeValidatorSlashed function uses float values. This may result in nondeterministic be- havior of rounding in the floating values. func (h Hooks) BeforeValidatorSlashed(ctx context.Context, val
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FloatingValuesResultInNondeterminism"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(valAddr|slashedVotingPower|totalVotingPower).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "valAddr"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
