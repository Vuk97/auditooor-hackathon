skeleton: "name_match_missing_call"
name: "commit-080a0191ed-18"
class_name: "Commit080a0191ed18"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Flare FAsset Diffs - Zellic Audit Report"
help: |
    To compute the closedWithFeeUBA amount and ensure that it is less than or equal to the _amountUBA input, the selfClose function first scales _amountUBA down to toCloseUBA via mulDi
wiki_title: "Commit 080a0191ed                                                              18"
wiki_description: |
    To compute the closedWithFeeUBA amount and ensure that it is less than or equal to the _amountUBA input, the selfClose function first scales _amountUBA down to toCloseUBA via mulDiv, passes it to Redemptions.closeTickets to obtain closedUBA, and then applies the inverse mulDiv to compute closedWithF
wiki_exploit_scenario: |
    Per audit finding: To compute the closedWithFeeUBA amount and ensure that it is less than or equal to the _amountUBA input, the selfClose function first scales _amountUBA down to toCloseUBA via mulDiv, passes it to Rede
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "Commit080a0191ed18"
state_decl: "uint256 internal closeduba;"
fn_name_regex: ".*(closedUBA|toCloseUBA|closedWithFeeUBA).*"
read_var_regex: ".*(closedUBA|closedWithFeeUBA|factorMul).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "closedUBA"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return closeduba > 0;"
