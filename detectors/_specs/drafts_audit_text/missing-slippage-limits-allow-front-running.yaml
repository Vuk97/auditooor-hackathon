skeleton: "name_match_missing_call"
name: "missing-slippage-limits-allow-front-running"
class_name: "MissingSlippageLimitsAllowFrontRunning"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Rainmaker - Zellic Audit Report"
help: |
    During deposits and withdrawals, the staking manager interacts with the underlying vault for entry and exit. Those functions each have the parameter minAmount, which sets slippage
wiki_title: "Missing slippage limits allow front-running"
wiki_description: |
    During deposits and withdrawals, the staking manager interacts with the underlying vault for entry and exit. Those functions each have the parameter minAmount, which sets slippage limits for the staker actions. However, the minAmount is set to zero in all cases: /** * @dev Withdraw tokens from Defin
wiki_exploit_scenario: |
    Per audit finding: During deposits and withdrawals, the staking manager interacts with the underlying vault for entry and exit. Those functions each have the parameter minAmount, which sets slippage limits for the stake
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingSlippageLimitsAllowFrontRunning"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(minAmount|withdrawFromDefinitive|lpTokens).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "minAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
