skeleton: "name_match_missing_require"
name: "functions-allow-interaction-with-services-removed-from-registry"
class_name: "FunctionsAllowInteractionWithServicesRemovedFromRegistry"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Hyperbeat Pay - Zellic Audit Report"
help: |
    Several functions validate services using only the local _approvedServices[service] mapping without checking the ServiceRegistry, allowing interaction with previously approved serv
wiki_title: "Functions allow interaction with services removed from registry"
wiki_description: |
    Several functions validate services using only the local _approvedServices[service] mapping without checking the ServiceRegistry, allowing interaction with previously approved services that have been removed from the registry: function getBalance(address service, address asset) external view returns
wiki_exploit_scenario: |
    Per audit finding: Several functions validate services using only the local _approvedServices[service] mapping without checking the ServiceRegistry, allowing interaction with previously approved services that have been
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionsAllowInteractionWithServicesRemovedFromRegistry"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getBalance|requestCreditMode|collateralToken).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getBalance"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
