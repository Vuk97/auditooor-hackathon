skeleton: "name_match_missing_call"
name: "protocol-owner-can-drain-pools"
class_name: "ProtocolOwnerCanDrainPools"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Rainmaker - Zellic Audit Report"
help: |
    Each staking manager has an associated token for accounting purposes. When a user accrues rewards, the corresponding tokens are minted. When a user withdraws to- kens, the contract
wiki_title: "Protocol owner can drain pools"
wiki_description: |
    Each staking manager has an associated token for accounting purposes. When a user accrues rewards, the corresponding tokens are minted. When a user withdraws to- kens, the contract uses the sum of their deposits and their reward token balance. From the withdraw function, /) Withdraw from definitive
wiki_exploit_scenario: |
    Per audit finding: Each staking manager has an associated token for accounting purposes. When a user accrues rewards, the corresponding tokens are minted. When a user withdraws to- kens, the contract uses the sum of the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ProtocolOwnerCanDrainPools"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(underlyingAmount|withdrawFromDefinitive|rewardTokenBalance).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "underlyingAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
