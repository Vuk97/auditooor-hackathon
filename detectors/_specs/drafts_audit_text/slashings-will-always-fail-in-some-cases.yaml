skeleton: "name_match_missing_require"
name: "slashings-will-always-fail-in-some-cases"
class_name: "SlashingsWillAlwaysFailInSomeCases"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-karak"
help: |
    0xCiphky The requestSlashing function allows a slashing to be requested for a given operator’s deployed vaults staked to the DSS. The slashing request must pass the SLASHING_VETO_W
wiki_title: "Slashings will always fail in some cases"
wiki_description: |
    0xCiphky The requestSlashing function allows a slashing to be requested for a given operator’s deployed vaults staked to the DSS. The slashing request must pass the SLASHING_VETO_WINDOW (2 days) before it can be confirmed, allowing the veto committee to cancel any unfair queued slashing. This time g
wiki_exploit_scenario: |
    Per audit finding: 0xCiphky The requestSlashing function allows a slashing to be requested for a given operator’s deployed vaults staked to the DSS. The slashing request must pass the SLASHING_VETO_WINDOW (2 days) befor
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "SlashingsWillAlwaysFailInSomeCases"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(requestSlashing|slashAssets|totalAssetsToSlash).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "requestSlashing"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
