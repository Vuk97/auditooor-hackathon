skeleton: "name_match_missing_require"
name: "the-lack-of-token-addresses-verification"
class_name: "TheLackOfTokenAddressesVerification"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Y2K Finance - Zellic Audit Report"
help: |
    The permitSwapAndBridge and swapAndBridge functions allow users to perform the swap, and after that, bridge the resulting tokens to another chain using Stargate, which is a decentr
wiki_title: "The lack of token addresses’ verification"
wiki_description: |
    The permitSwapAndBridge and swapAndBridge functions allow users to perform the swap, and after that, bridge the resulting tokens to another chain using Stargate, which is a decentralised bridge and exchange building on top of the Layer Zero protocol. These functions have the following parameters: sw
wiki_exploit_scenario: |
    Per audit finding: The permitSwapAndBridge and swapAndBridge functions allow users to perform the swap, and after that, bridge the resulting tokens to another chain using Stargate, which is a decentralised bridge and ex
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheLackOfTokenAddressesVerification"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(permitSwapAndBridge|swapAndBridge|swapPayload).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "permitSwapAndBridge"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
