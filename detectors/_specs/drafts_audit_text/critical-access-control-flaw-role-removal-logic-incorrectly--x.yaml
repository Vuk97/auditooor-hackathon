skeleton: "name_match_missing_call"
name: "critical-access-control-flaw-role-removal-logic-incorrectly--x"
class_name: "CriticalAccessControlFlawRoleRemovalLogicIncorrectlyX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-05-blackhole"
help: |
    rayss , also found by KineticsOfWeb3 Finding description The removeRole() function incorrectly updates a user’s role list by replacing a removed role with the last role from the gl
wiki_title: "Critical access control flaw: Role removal logic incorrectly grants unauthorized roles"
wiki_description: |
    rayss , also found by KineticsOfWeb3 Finding description The removeRole() function incorrectly updates a user’s role list by replacing a removed role with the last role from the global_roles array. This results in unintended and unauthorized role assignments to users. The removeRole() function corre
wiki_exploit_scenario: |
    Per audit finding: rayss , also found by KineticsOfWeb3 Finding description The removeRole() function incorrectly updates a user’s role list by replacing a removed role with the last role from the global_roles array. Th
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CriticalAccessControlFlawRoleRemovalLogicIncorrectlyX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(removeRole).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "removeRole"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
