skeleton: "name_match_missing_require"
name: "no-check-that-pool-asset-is-registered"
class_name: "NoCheckThatPoolAssetIsRegistered"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Blackwing - Zellic Audit Report"
help: |
    In the BlackwingVault contract, the updateDeployer() function is responsible for modifying the de- ployer. It lacks a check that a pool is registered.
wiki_title: "No check that pool[asset] is registered"
wiki_description: |
    In the BlackwingVault contract, the updateDeployer() function is responsible for modifying the de- ployer. It lacks a check that a pool is registered.
wiki_exploit_scenario: |
    Per audit finding: In the BlackwingVault contract, the updateDeployer() function is responsible for modifying the de- ployer. It lacks a check that a pool is registered.
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "NoCheckThatPoolAssetIsRegistered"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(updateDeployer).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "updateDeployer"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
