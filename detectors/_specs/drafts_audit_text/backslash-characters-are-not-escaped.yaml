skeleton: "name_match_missing_require"
name: "backslash-characters-are-not-escaped"
class_name: "BackslashCharactersAreNotEscaped"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Cosmos SDK Sign Mode Textual - Zellic Audit Report"
help: |
    The tx_display_translation function is responsible for escaping bytes such as new- lines. The following is a mapping of a byte to the suffix, which is appended after a ’\’ characte
wiki_title: "Backslash characters are not escaped"
wiki_description: |
    The tx_display_translation function is responsible for escaping bytes such as new- lines. The following is a mapping of a byte to the suffix, which is appended after a ’\’ character: static const ascii_subst_t ascii_substitutions[] = { {0x07, 'a'}, {0x08, 'b'}, {0x0C, 'f'}, {0x0A, 'n'}, {0x0D, 'r'},
wiki_exploit_scenario: |
    Per audit finding: The tx_display_translation function is responsible for escaping bytes such as new- lines. The following is a mapping of a byte to the suffix, which is appended after a ’\’ character: static const asci
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "BackslashCharactersAreNotEscaped"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(dstLen).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "dstLen"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
