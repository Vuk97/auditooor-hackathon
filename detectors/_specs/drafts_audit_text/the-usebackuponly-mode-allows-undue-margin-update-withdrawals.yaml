skeleton: "name_match_missing_require"
name: "the-usebackuponly-mode-allows-undue-margin-update-withdrawals"
class_name: "TheUsebackuponlyModeAllowsUndueMarginUpdateWithdrawals"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Avantis - Zellic Audit Report"
help: |
    The function updateMargin allows users to deposit or withdraw USDC from their open positions. When the function is called to withdraw USDC, it first verifies if the new leverage li
wiki_title: "The useBackupOnly mode allows undue margin update withdrawals"
wiki_description: |
    The function updateMargin allows users to deposit or withdraw USDC from their open positions. When the function is called to withdraw USDC, it first verifies if the new leverage lies in the correct range, and then, if it does, passes control to the fulfill function in PriceAggregator. If the value o
wiki_exploit_scenario: |
    Per audit finding: The function updateMargin allows users to deposit or withdraw USDC from their open positions. When the function is called to withdraw USDC, it first verifies if the new leverage lies in the correct ra
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheUsebackuponlyModeAllowsUndueMarginUpdateWithdrawals"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(updateMargin|useBackupOnly|updateMarginCallback).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "updateMargin"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
