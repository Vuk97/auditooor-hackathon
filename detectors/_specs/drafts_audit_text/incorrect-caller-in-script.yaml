skeleton: "name_match_missing_call"
name: "incorrect-caller-in-script"
class_name: "IncorrectCallerInScript"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Frax0 Mesh - Zellic Audit Report"
help: |
    The new version of the implementation contract requires minting permission for the corresponding tokens. The function addMinterRole calls the token contract’s function addMinter to
wiki_title: "Incorrect caller in script"
wiki_description: |
    The new version of the implementation contract requires minting permission for the corresponding tokens. The function addMinterRole calls the token contract’s function addMinter to set the proxy contract address as a minter. However, the privileged accounts that can add new minters differ between th
wiki_exploit_scenario: |
    Per audit finding: The new version of the implementation contract requires minting permission for the corresponding tokens. The function addMinterRole calls the token contract’s function addMinter to set the proxy contr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectCallerInScript"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(addMinterRole|addMinter|frxUsd).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "addMinterRole"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
