skeleton: "highlevelcall_missing_sibling"
name: "apply-oracle-update-doesn-t-check-for-duplicate-validator-votes"
class_name: "ApplyOracleUpdateDoesnTCheckForDuplicateValidatorVotes"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-01-initia-rollup-modules"
help: |
    0xAlix2 When an Oracle update is required, validators cast votes for the change. An UpdateOracle message is then sent from one of the bridge executors. This message invokes the App
wiki_title: "Apply oracle update doesn’t check for duplicate validator votes"
wiki_description: |
    0xAlix2 When an Oracle update is required, validators cast votes for the change. An UpdateOracle message is then sent from one of the bridge executors. This message invokes the ApplyOracleUpdate function, which calls UpdateOracle . In this function, the incoming votes are validated, and if deemed va
wiki_exploit_scenario: |
    Per audit finding: 0xAlix2 When an Oracle update is required, validators cast votes for the change. An UpdateOracle message is then sent from one of the bridge executors. This message invokes the ApplyOracleUpdate funct
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ApplyOracleUpdateDoesnTCheckForDuplicateValidatorVotes"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(valStore|chainID|extCommit).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
