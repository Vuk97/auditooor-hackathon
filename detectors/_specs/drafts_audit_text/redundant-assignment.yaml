skeleton: "name_match_missing_call"
name: "redundant-assignment"
class_name: "RedundantAssignment"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit cyberRaise - Zellic Audit Report"
help: |
    The initialize function in DealManager sets corp and dealRegistry twice — directly through LexScrowStorage.set* and again within __LexScroWLite_init. function initialize(address _a
wiki_title: "Redundant assignment"
wiki_description: |
    The initialize function in DealManager sets corp and dealRegistry twice — directly through LexScrowStorage.set* and again within __LexScroWLite_init. function initialize(address _auth, address _corp, address _dealRegistry, address _issuanceManager, address _upgradeFactory) public initializer { // [.
wiki_exploit_scenario: |
    Per audit finding: The initialize function in DealManager sets corp and dealRegistry twice — directly through LexScrowStorage.set* and again within __LexScroWLite_init. function initialize(address _auth, address _corp,
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RedundantAssignment"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(dealRegistry|setCorp|setDealRegistry).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "dealRegistry"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
