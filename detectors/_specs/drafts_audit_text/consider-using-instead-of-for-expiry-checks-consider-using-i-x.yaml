skeleton: "name_match_missing_require"
name: "consider-using-instead-of-for-expiry-checks-consider-using-i-x"
class_name: "ConsiderUsingInsteadOfForExpiryChecksConsiderUsingIX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-08-phi"
help: |
    [L-09] Consider using < instead of <= for expiry checks Consider using < instead of <= on Line 625 below. From the frontend, we might just want the signature to be valid for the cu
wiki_title: |
    Consider using < instead of <= for expiry checks Consider using < instead of <= on Line 625 below. From the frontend, we might just want the signature to be val
wiki_description: |
    [L-09] Consider using < instead of <= for expiry checks Consider using < instead of <= on Line 625 below. From the frontend, we might just want the signature to be valid for the current block. So we just use block.timestamp but currently it requires expiresIn to be greater than block.timestamp . Fil
wiki_exploit_scenario: |
    Per audit finding: [L-09] Consider using < instead of <= for expiry checks Consider using < instead of <= on Line 625 below. From the frontend, we might just want the signature to be valid for the current block. So we j
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ConsiderUsingInsteadOfForExpiryChecksConsiderUsingIX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(expiresIn|phiSignerAddress|signatureClaim).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "expiresIn"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
