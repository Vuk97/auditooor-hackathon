skeleton: "name_match_missing_call"
name: "events-emitted-by-l2-bridge-hooks-are-discarded"
class_name: "EventsEmittedByL2BridgeHooksAreDiscarded"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-01-initia-rollup-modules"
help: |
    RadiantLabs Finding description and impact The L1 deposit function allows a depositor to send a signed payload to be executed atomically with the deposited tokens, via bridge hooks
wiki_title: "Events emitted by L2 bridge hooks are discarded"
wiki_description: |
    RadiantLabs Finding description and impact The L1 deposit function allows a depositor to send a signed payload to be executed atomically with the deposited tokens, via bridge hooks. These are executed in L2 by routing messages to the default message handler from the app’s router: File: deposit. go 5
wiki_exploit_scenario: |
    Per audit finding: RadiantLabs Finding description and impact The L1 deposit function allows a depositor to send a signed payload to be executed atomically with the deposited tokens, via bridge hooks. These are executed
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "EventsEmittedByL2BridgeHooksAreDiscarded"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(cacheCtx|requestTypeName|writeCache).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "cacheCtx"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
