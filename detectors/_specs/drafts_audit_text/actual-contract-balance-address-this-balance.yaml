skeleton: "name_match_missing_call"
name: "actual-contract-balance-address-this-balance"
class_name: "ActualContractBalanceAddressThisBalance"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LayerZero VeDistributor - Zellic Audit Report"
help: |
    nce (address(this).balance) This value is intended to be incremented when distribute is called. It is never directly used, and accounting for which funds are safe to use is done us
wiki_title: "Actual contract balance (address(this).balance)"
wiki_description: |
    nce (address(this).balance) This value is intended to be incremented when distribute is called. It is never directly used, and accounting for which funds are safe to use is done using the usdcDistributedAmount variable. If the owner called emergencyWithdraw, the contract balance can decrease without
wiki_exploit_scenario: |
    Per audit finding: nce (address(this).balance) This value is intended to be incremented when distribute is called. It is never directly used, and accounting for which funds are safe to use is done using the usdcDistribu
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ActualContractBalanceAddressThisBalance"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(usdcDistributedAmount|emergencyWithdraw).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "usdcDistributedAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
