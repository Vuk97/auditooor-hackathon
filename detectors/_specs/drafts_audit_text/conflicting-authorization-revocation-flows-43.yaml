skeleton: "name_match_missing_call"
name: "conflicting-authorization-revocation-flows-43"
class_name: "ConflictingAuthorizationRevocationFlows43"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Hyperbeat Pay - Zellic Audit Report"
help: |
    There is an inconsistency in how ManagementAccount resolves the operator address. The contract has an operator() function that returns the operator from the factory, but the _check
wiki_title: "Conflicting authorization-revocation flows                                       43"
wiki_description: |
    There is an inconsistency in how ManagementAccount resolves the operator address. The contract has an operator() function that returns the operator from the factory, but the _checkOperator() and _checkOwnerOrOperator() functions use role-based access control instead: function operator() external vie
wiki_exploit_scenario: |
    Per audit finding: There is an inconsistency in how ManagementAccount resolves the operator address. The contract has an operator() function that returns the operator from the factory, but the _checkOperator() and _chec
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ConflictingAuthorizationRevocationFlows43"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(hasRole|executeModeChange|approveWithdrawal).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "hasRole"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
