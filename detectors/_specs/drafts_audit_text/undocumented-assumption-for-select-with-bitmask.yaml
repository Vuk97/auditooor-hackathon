skeleton: "name_match_missing_require"
name: "undocumented-assumption-for-select-with-bitmask"
class_name: "UndocumentedAssumptionForSelectWithBitmask"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Nebra Universal Proof Aggregator - Zellic Audit Report"
help: |
    In circuits/src/utils/hashing.rs, the select_with_bitmask function is implemented as fol- lows: /// Selects the right `elements` using `bitmask`. /// /// # Note /// /// The element
wiki_title: "Undocumented assumption for select_with_bitmask"
wiki_description: |
    In circuits/src/utils/hashing.rs, the select_with_bitmask function is implemented as fol- lows: /// Selects the right `elements` using `bitmask`. /// /// # Note /// /// The elements in `bitmask` must be constrained to be booleans. fn select_with_bitmask<F: EccPrimeField>( ctx: &mut Context<F>, chip:
wiki_exploit_scenario: |
    Per audit finding: In circuits/src/utils/hashing.rs, the select_with_bitmask function is implemented as fol- lows: /// Selects the right `elements` using `bitmask`. /// /// # Note /// /// The elements in `bitmask` must
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UndocumentedAssumptionForSelectWithBitmask"
state_decl: "uint256 internal bitmask;"
fn_name_regex: ".*(elements|bitmask).*"
write_var_regex: ".*(bitmask|elements).*"
guard_var_regex: ".*(bitmask|elements).*"
vuln_fn_name: "elements"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "bitmask = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
