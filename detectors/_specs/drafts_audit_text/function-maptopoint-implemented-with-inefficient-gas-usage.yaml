skeleton: "name_match_missing_require"
name: "function-maptopoint-implemented-with-inefficient-gas-usage"
class_name: "FunctionMaptopointImplementedWithInefficientGasUsage"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit SolBLS - Zellic Audit Report"
help: |
    In the library BLS, the function mapToPoint is implemented as follows: /// @notice Map field element to E using SvdW /// @param u Field element to map /// @return p Point on curve
wiki_title: "Function mapToPoint implemented with inefficient gas usage"
wiki_description: |
    In the library BLS, the function mapToPoint is implemented as follows: /// @notice Map field element to E using SvdW /// @param u Field element to map /// @return p Point on curve function mapToPoint(uint256 u) internal view returns (uint256[2] memory p) { if (u >= N) revert InvalidFieldElement(u);
wiki_exploit_scenario: |
    Per audit finding: In the library BLS, the function mapToPoint is implemented as follows: /// @notice Map field element to E using SvdW /// @param u Field element to map /// @return p Point on curve function mapToPoint(
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionMaptopointImplementedWithInefficientGasUsage"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(mapToPoint|hasRoot).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "mapToPoint"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
