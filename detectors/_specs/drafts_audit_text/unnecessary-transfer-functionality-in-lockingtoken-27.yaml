skeleton: "name_match_missing_require"
name: "unnecessary-transfer-functionality-in-lockingtoken-27"
class_name: "UnnecessaryTransferFunctionalityInLockingtoken27"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Anzen and protocol-v2 - Zellic Audit Report"
help: |
    In VotingResultBroadcaster, the function finalizeAndBroadcast is used to calculate the fees of broadcasting and execute broadcasting. Based on the structure of the contract, it see
wiki_title: "Unnecessary transfer functionality in LockingToken                  27"
wiki_description: |
    In VotingResultBroadcaster, the function finalizeAndBroadcast is used to calculate the fees of broadcasting and execute broadcasting. Based on the structure of the contract, it seems that the owner deposits Ether into the contract and periodically/automatically calls finalizeAndBroadcast to ensure u
wiki_exploit_scenario: |
    Per audit finding: In VotingResultBroadcaster, the function finalizeAndBroadcast is used to calculate the fees of broadcasting and execute broadcasting. Based on the structure of the contract, it seems that the owner de
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnnecessaryTransferFunctionalityInLockingtoken27"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(finalizeAndBroadcast|chainId|chainIds).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "finalizeAndBroadcast"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
