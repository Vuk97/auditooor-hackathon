skeleton: "name_match_missing_call"
name: "the-authority-can-execute-majority-consented-proposals-with--x"
class_name: "TheAuthorityCanExecuteMajorityConsentedProposalsWithX"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Metavest - Zellic Audit Report"
help: |
    Before making amendments to MetaVesT, the modifier consentCheck first checks if the proposal passes. It then checks if the hash of the last 32 bytes of calldata is the same as the
wiki_title: "The authority can execute majority-consented proposals with arbitrary data"
wiki_description: |
    Before making amendments to MetaVesT, the modifier consentCheck first checks if the proposal passes. It then checks if the hash of the last 32 bytes of calldata is the same as the value stored in the proposal. modifier consentCheck(address _grant, bytes calldata _data) { if (isMetavestInSet(_grant))
wiki_exploit_scenario: |
    Per audit finding: Before making amendments to MetaVesT, the modifier consentCheck first checks if the proposal passes. It then checks if the hash of the last 32 bytes of calldata is the same as the value stored in the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheAuthorityCanExecuteMajorityConsentedProposalsWithX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(consentCheck|isMetavestInSet|isPending).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "consentCheck"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
