skeleton: "name_match_missing_require"
name: "addresses-can-be-pre-populated-with-bad-data"
class_name: "AddressesCanBePrePopulatedWithBadData"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-optimism"
help: |
    bronze_pickaxe , also found by Zubat Inside PreimageOracle.sol , anyone can call the loadPrecompilePreimagePart() to load data into the contract. The problem is that anyone is able
wiki_title: "Addresses can be pre-populated with bad data"
wiki_description: |
    bronze_pickaxe , also found by Zubat Inside PreimageOracle.sol , anyone can call the loadPrecompilePreimagePart() to load data into the contract. The problem is that anyone is able to specify the address _precompile parameter. This opens up the door for malicious users to pre-populate data and use i
wiki_exploit_scenario: |
    Per audit finding: bronze_pickaxe , also found by Zubat Inside PreimageOracle.sol , anyone can call the loadPrecompilePreimagePart() to load data into the contract. The problem is that anyone is able to specify the addr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AddressesCanBePrePopulatedWithBadData"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(loadPrecompilePreimagePart|readPreimage|datLen).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "loadPrecompilePreimagePart"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
