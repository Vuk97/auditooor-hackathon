skeleton: "highlevelcall_missing_sibling"
name: "poolaction-balancerexit-returns-wrong-token-out-amount"
class_name: "PoolactionBalancerexitReturnsWrongTokenOutAmount"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-loopfi"
help: |
    0xAlix2 Users use PoolAction::exit to exit a Balancer pool position, it calls _balancerExit to do the job. It is expected to exit the pool and return the amount out of the token; h
wiki_title: "PoolAction::_balancerExit returns wrong token out amount"
wiki_description: |
    0xAlix2 Users use PoolAction::exit to exit a Balancer pool position, it calls _balancerExit to do the job. It is expected to exit the pool and return the amount out of the token; however, it returns the whole recipient’s balance, without considering the case where the recipient is holding an amount
wiki_exploit_scenario: |
    Per audit finding: 0xAlix2 Users use PoolAction::exit to exit a Balancer pool position, it calls _balancerExit to do the job. It is expected to exit the pool and return the amount out of the token; however, it returns t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PoolactionBalancerexitReturnsWrongTokenOutAmount"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(tokenOut|onCreditFlashLoan|withdrawnCollateral).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
