skeleton: "name_match_missing_require"
name: "inaccurate-handling-of-identical-scores-in-findmodelerupperbound"
class_name: "InaccurateHandlingOfIdenticalScoresInFindmodelerupperbound"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Spectral Modelers - Zellic Audit Report"
help: |
    The findModelerUpperBound function in the CompetitionLibrary contract is intended to find the upper bound index for a given element in a sorted array using binary search. However,
wiki_title: "Inaccurate handling of identical scores in findModelerUpperBound"
wiki_description: |
    The findModelerUpperBound function in the CompetitionLibrary contract is intended to find the upper bound index for a given element in a sorted array using binary search. However, the cur- rent implementation does not correctly handle scenarios where multiple identical scores exist in the topNModele
wiki_exploit_scenario: |
    Per audit finding: The findModelerUpperBound function in the CompetitionLibrary contract is intended to find the upper bound index for a given element in a sorted array using binary search. However, the cur- rent implem
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InaccurateHandlingOfIdenticalScoresInFindmodelerupperbound"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(findModelerUpperBound|topNModelers|modelerToMedian).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "findModelerUpperBound"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
