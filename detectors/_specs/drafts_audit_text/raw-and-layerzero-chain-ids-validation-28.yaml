skeleton: "name_match_missing_require"
name: "raw-and-layerzero-chain-ids-validation-28"
class_name: "RawAndLayerzeroChainIdsValidation28"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Orderly Network - Zellic Audit Report"
help: |
    The LayerZero cross-chain messaging application requires paying a fee for each message sent. The fee is usually paid in the native token of the source chain. Currently, the CrossCh
wiki_title: "Raw and LayerZero chain IDs’ validation                             28"
wiki_description: |
    The LayerZero cross-chain messaging application requires paying a fee for each message sent. The fee is usually paid in the native token of the source chain. Currently, the CrossChainRelayUp- gradeable contract does not check the msg.value of the call against the necessary fee required for performin
wiki_exploit_scenario: |
    Per audit finding: The LayerZero cross-chain messaging application requires paying a fee for each message sent. The fee is usually paid in the native token of the source chain. Currently, the CrossChainRelayUp- gradeabl
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "RawAndLayerzeroChainIdsValidation28"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(sendMessage|onlyCaller|lzPayload).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "sendMessage"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
