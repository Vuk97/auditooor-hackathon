skeleton: "name_match_missing_call"
name: "incorrect-protocol-fee-calculation-in-flash"
class_name: "IncorrectProtocolFeeCalculationInFlash"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-10-ramses-exchange"
help: |
    [L-03] Incorrect protocol fee calculation in flash() function The issue is related to how protocol fees are calculated. Specifically, applying fees to the already-paid fees ( fee-o
wiki_title: "Incorrect protocol fee calculation in flash()"
wiki_description: |
    [L-03] Incorrect protocol fee calculation in flash() function The issue is related to how protocol fees are calculated. Specifically, applying fees to the already-paid fees ( fee-on-fee logic ) can create several risks that affect both the protocol’s revenue and user interactions. Problem The flash(
wiki_exploit_scenario: |
    Per audit finding: [L-03] Incorrect protocol fee calculation in flash() function The issue is related to how protocol fees are calculated. Specifically, applying fees to the already-paid fees ( fee-on-fee logic ) can cr
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectProtocolFeeCalculationInFlash"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(balanceAfter|balanceBefore|balance0After).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "balanceAfter"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
