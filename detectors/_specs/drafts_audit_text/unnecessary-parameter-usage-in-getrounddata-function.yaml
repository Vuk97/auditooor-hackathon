skeleton: "name_match_missing_call"
name: "unnecessary-parameter-usage-in-getrounddata-function"
class_name: "UnnecessaryParameterUsageInGetrounddataFunction"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Yei Finance Oracle - Zellic Audit Report"
help: |
    The getRoundData function in the contract includes a parameter _roundId that is not utilized in the function’s logic. Instead, the function retrieves the price data without conside
wiki_title: "Unnecessary parameter usage in getRoundData function"
wiki_description: |
    The getRoundData function in the contract includes a parameter _roundId that is not utilized in the function’s logic. Instead, the function retrieves the price data without considering the provided _roundId. This creates a potential for misuse, as users might expect the function to return data spe-
wiki_exploit_scenario: |
    Per audit finding: The getRoundData function in the contract includes a parameter _roundId that is not utilized in the function’s logic. Instead, the function retrieves the price data without considering the provided _r
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnnecessaryParameterUsageInGetrounddataFunction"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getRoundData|roundId|startedAt).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "getRoundData"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
