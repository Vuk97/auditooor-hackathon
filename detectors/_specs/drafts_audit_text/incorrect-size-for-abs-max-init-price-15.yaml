skeleton: "name_match_missing_call"
name: "incorrect-size-for-abs-max-init-price-15"
class_name: "IncorrectSizeForAbsMaxInitPrice15"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Euler Fee Flow - Zellic Audit Report"
help: |
    The buy function initializes the next auction immediately after it processes the purchase. function buy(address[] calldata assets, address assetsReceiver, uint256 deadline, uint256
wiki_title: "Incorrect size for ABS_MAX_INIT_PRICE                          15"
wiki_description: |
    The buy function initializes the next auction immediately after it processes the purchase. function buy(address[] calldata assets, address assetsReceiver, uint256 deadline, uint256 maxPaymentTokenAmount) external nonReentrant returns(uint256 paymentAmount) { // ... // Setup new auction uint256 newIn
wiki_exploit_scenario: |
    Per audit finding: The buy function initializes the next auction immediately after it processes the purchase. function buy(address[] calldata assets, address assetsReceiver, uint256 deadline, uint256 maxPaymentTokenAmou
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "IncorrectSizeForAbsMaxInitPrice15"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(assetsReceiver|maxPaymentTokenAmount|nonReentrant).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "assetsReceiver"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
