skeleton: "name_match_missing_require"
name: "missing-public-key-validation"
class_name: "MissingPublicKeyValidation"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Mitosis (May) - Zellic Audit Report"
help: |
    There is an issue in Mitosis protocol's LibSecp256k1 library, where proper curve validation is not performed during the public-key decompression process. In the current LibSecp256k
wiki_title: "Missing public-key validation"
wiki_description: |
    There is an issue in Mitosis protocol's LibSecp256k1 library, where proper curve validation is not performed during the public-key decompression process. In the current LibSecp256k1 Solidity code, the uncompressPubkey function is implemented as follows: function verifyCmpPubkey(bytes memory cmpPubke
wiki_exploit_scenario: |
    Per audit finding: There is an issue in Mitosis protocol's LibSecp256k1 library, where proper curve validation is not performed during the public-key decompression process. In the current LibSecp256k1 Solidity code, the
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingPublicKeyValidation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(uncompressPubkey|verifyCmpPubkey|cmpPubkey).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "uncompressPubkey"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
