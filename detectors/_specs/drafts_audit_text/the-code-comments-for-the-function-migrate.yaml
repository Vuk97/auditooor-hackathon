skeleton: "name_match_missing_call"
name: "the-code-comments-for-the-function-migrate"
class_name: "TheCodeCommentsForTheFunctionMigrate"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Migration - Zellic Audit Report"
help: |
    for the function migrate The comments for the function migrate mention that the function follows the checks-effects- interactions pattern (CEI) and list which step each operation c
wiki_title: "The code comments for the function migrate"
wiki_description: |
    for the function migrate The comments for the function migrate mention that the function follows the checks-effects- interactions pattern (CEI) and list which step each operation corresponds to. * @dev This function uses the check-effects-interactions pattern to mitigate reentrancy risks. External c
wiki_exploit_scenario: |
    Per audit finding: for the function migrate The comments for the function migrate mention that the function follows the checks-effects- interactions pattern (CEI) and list which step each operation corresponds to. * @de
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheCodeCommentsForTheFunctionMigrate"
state_decl: "uint256 internal amount;"
fn_name_regex: ".*(amount|Migration).*"
read_var_regex: ".*(amount).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "amount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return amount > 0;"
