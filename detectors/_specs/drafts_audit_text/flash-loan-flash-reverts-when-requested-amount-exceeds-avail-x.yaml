skeleton: "name_match_missing_call"
name: "flash-loan-flash-reverts-when-requested-amount-exceeds-avail-x"
class_name: "FlashLoanFlashRevertsWhenRequestedAmountExceedsAvailX"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "code4arena audit 2024-10-ramses-exchange"
help: |
    [L-05] Flash loan ( flash() ) reverts when requested amount exceeds available token balances In the flash loan function, the transaction will revert if the requested amount of toke
wiki_title: |
    Flash loan ( flash() ) reverts when requested amount exceeds available token balances In the flash loan function, the transaction will revert if the requested a
wiki_description: |
    [L-05] Flash loan ( flash() ) reverts when requested amount exceeds available token balances In the flash loan function, the transaction will revert if the requested amount of token0 or token1 exceeds the available balance held by the contract. FILE: 2024 - 10 - ramses - exchange / contracts / CL /
wiki_exploit_scenario: |
    Per audit finding: [L-05] Flash loan ( flash() ) reverts when requested amount exceeds available token balances In the flash loan function, the transaction will revert if the requested amount of token0 or token1 exceeds
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FlashLoanFlashRevertsWhenRequestedAmountExceedsAvailX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(safeTransfer).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "safeTransfer"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
