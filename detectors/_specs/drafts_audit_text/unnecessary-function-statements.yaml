skeleton: "name_match_missing_call"
name: "unnecessary-function-statements"
class_name: "UnnecessaryFunctionStatements"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Valts - Zellic Audit Report"
help: |
    In the cleanupApproval() function, a delete statement is executed on the _approvals. function cleanupApproval(bytes32 hashKey) private { for(uint8 i=0; i<_oracles.length; i+)) { _a
wiki_title: "Unnecessary function statements"
wiki_description: |
    In the cleanupApproval() function, a delete statement is executed on the _approvals. function cleanupApproval(bytes32 hashKey) private { for(uint8 i=0; i<_oracles.length; i+)) { _approvals[hashKey].approvers[_oracles[i]] = false; } _approvals[hashKey].count = 0; _approvals[hashKey].hasowner = false;
wiki_exploit_scenario: |
    Per audit finding: In the cleanupApproval() function, a delete statement is executed on the _approvals. function cleanupApproval(bytes32 hashKey) private { for(uint8 i=0; i<_oracles.length; i+)) { _approvals[hashKey].ap
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "UnnecessaryFunctionStatements"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(cleanupApproval|hashKey).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "cleanupApproval"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
