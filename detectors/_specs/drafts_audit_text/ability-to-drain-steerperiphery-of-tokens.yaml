skeleton: "name_match_missing_call"
name: "ability-to-drain-steerperiphery-of-tokens"
class_name: "AbilityToDrainSteerperipheryOfTokens"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Steer - Zellic Audit Report"
help: |
    This is the _deposit function of SteerPeriphery: function _deposit( address vaultAddress-, uint256 amount0Desired, uint256 amount1Desired, uint256 amount0Min, uint256 amount1Min, a
wiki_title: "Ability to drain SteerPeriphery of tokens"
wiki_description: |
    This is the _deposit function of SteerPeriphery: function _deposit( address vaultAddress-, uint256 amount0Desired, uint256 amount1Desired, uint256 amount0Min, uint256 amount1Min, address to ) internal returns (uint256) { IMultiPositionManager vaultInstance = IMultiPositionManager( vaultAddress ); IE
wiki_exploit_scenario: |
    Per audit finding: This is the _deposit function of SteerPeriphery: function _deposit( address vaultAddress-, uint256 amount0Desired, uint256 amount1Desired, uint256 amount0Min, uint256 amount1Min, address to ) internal
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "AbilityToDrainSteerperipheryOfTokens"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(vaultAddress|amount0Desired|amount1Desired).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "vaultAddress"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
