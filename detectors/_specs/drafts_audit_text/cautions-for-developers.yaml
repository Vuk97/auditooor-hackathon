skeleton: "name_match_missing_require"
name: "cautions-for-developers"
class_name: "CautionsForDevelopers"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LZ OApp OFT - Zellic Audit Report"
help: |
    . Cautions for developers OApp developers It is important that OApp developers consider the following: • Only override _lzReceive — not lzReceive. Note that overriding _lzReceive (
wiki_title: "Cautions for developers"
wiki_description: |
    . Cautions for developers OApp developers It is important that OApp developers consider the following: • Only override _lzReceive — not lzReceive. Note that overriding _lzReceive (i.e., not lzReceive) would not impact functionality (i.e., it would still pass tests) and would produce no warning but w
wiki_exploit_scenario: |
    Per audit finding: . Cautions for developers OApp developers It is important that OApp developers consider the following: • Only override _lzReceive — not lzReceive. Note that overriding _lzReceive (i.e., not lzReceive)
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "CautionsForDevelopers"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(lzReceive|assertRemoteAddress).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "lzReceive"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
