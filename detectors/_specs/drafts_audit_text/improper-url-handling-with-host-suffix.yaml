skeleton: "name_match_missing_require"
name: "improper-url-handling-with-host-suffix"
class_name: "ImproperUrlHandlingWithHostSuffix"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Family Wallet - Zellic Audit Report"
help: |
    The in-app embedded browser recognizes trusted dApps by downloading a list of well-known hosts. The WCAppMappingsManager class is responsible for matching the URL of the origin of
wiki_title: "Improper URL handling with host suffix"
wiki_description: |
    The in-app embedded browser recognizes trusted dApps by downloading a list of well-known hosts. The WCAppMappingsManager class is responsible for matching the URL of the origin of an RPC re- quest to a known dApp. The match is done by first attempting to match the precise URL against a list of known
wiki_exploit_scenario: |
    Per audit finding: The in-app embedded browser recognizes trusted dApps by downloading a list of well-known hosts. The WCAppMappingsManager class is responsible for matching the URL of the origin of an RPC re- quest to
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ImproperUrlHandlingWithHostSuffix"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(dApps|dApp|getMappedInfo).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "dApps"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
