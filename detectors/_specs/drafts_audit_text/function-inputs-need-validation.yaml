skeleton: "name_match_missing_require"
name: "function-inputs-need-validation"
class_name: "FunctionInputsNeedValidation"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Orderly Network - Zellic Audit Report"
help: |
    The majority of the project’s functions are only callable by privileged accounts, such as the Op- eratorManager. This architectural approach is atypical in the DeFi space and raise
wiki_title: "Function inputs need validation"
wiki_description: |
    The majority of the project’s functions are only callable by privileged accounts, such as the Op- eratorManager. This architectural approach is atypical in the DeFi space and raises concerns about heightened centralization risks. For example, in Ledger, similar to issue 3.3. ↗, there are functions w
wiki_exploit_scenario: |
    Per audit finding: The majority of the project’s functions are only callable by privileged accounts, such as the Op- eratorManager. This architectural approach is atypical in the DeFi space and raises concerns about hei
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionInputsNeedValidation"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(eratorManager).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "eratorManager"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
