skeleton: "name_match_missing_require"
name: "the-validateorder-view-function-has-a-misleading-interface"
class_name: "TheValidateorderViewFunctionHasAMisleadingInterface"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Apyx Stablecoin - Zellic Audit Report"
help: |
    The validateOrder function is a view function that checks if a given order is valid: /** * @notice Validates an order without executing it (useful for off-chain checks) * @param or
wiki_title: "The validateOrder view function has a misleading interface"
wiki_description: |
    The validateOrder function is a view function that checks if a given order is valid: /** * @notice Validates an order without executing it (useful for off-chain checks) * @param order The mint order to validate * @param signature The beneficiary's signature over the order * @return bool True if the
wiki_exploit_scenario: |
    Per audit finding: The validateOrder function is a view function that checks if a given order is valid: /** * @notice Validates an order without executing it (useful for off-chain checks) * @param order The mint order t
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheValidateorderViewFunctionHasAMisleadingInterface"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(validateOrder|notAfter|notBefore).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "validateOrder"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
