skeleton: "highlevelcall_missing_sibling"
name: "zero-rates-on-new-quoted-tokens-allow-an-attacker-to-take-an-x"
class_name: "ZeroRatesOnNewQuotedTokensAllowAnAttackerToTakeAnX"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "code4arena audit 2024-07-loopfi"
help: |
    Bauchibred Take a look here : function addQuotaToken ( address token ) external override gaugeOnly { if ( quotaTokensSet . contains ( token )) { revert TokenAlreadyAddedException (
wiki_title: "Zero rates on new quoted tokens allow an attacker to take an interest free quota"
wiki_description: |
    Bauchibred Take a look here : function addQuotaToken ( address token ) external override gaugeOnly { if ( quotaTokensSet . contains ( token )) { revert TokenAlreadyAddedException (); } quotaTokensSet . add ( token ); //@audit rates here are `0` by default. totalQuotaParams [ token ]. cumulativeIndex
wiki_exploit_scenario: |
    Per audit finding: Bauchibred Take a look here : function addQuotaToken ( address token ) external override gaugeOnly { if ( quotaTokensSet . contains ( token )) { revert TokenAlreadyAddedException (); } quotaTokensSet
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ZeroRatesOnNewQuotedTokensAllowAnAttackerToTakeAnX"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(addQuotaToken|gaugeOnly|quotaTokensSet).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
