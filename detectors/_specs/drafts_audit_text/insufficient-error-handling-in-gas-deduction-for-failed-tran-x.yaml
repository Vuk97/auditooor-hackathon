skeleton: "highlevelcall_missing_sibling"
name: "insufficient-error-handling-in-gas-deduction-for-failed-tran-x"
class_name: "InsufficientErrorHandlingInGasDeductionForFailedTranX"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit Fairyring - Zellic Audit Report"
help: |
    In both processFailedEncryptedTx and decryptAndExecuteTx, there is insufficient error handling when deducting gas fees from an account with an insufficient balance. 1. When a trans
wiki_title: "Insufficient error handling in gas deduction for failed transactions"
wiki_description: |
    In both processFailedEncryptedTx and decryptAndExecuteTx, there is insufficient error handling when deducting gas fees from an account with an insufficient balance. 1. When a transaction fails validation, processFailedEncryptedTx is triggered to account for gas used in operations like decryption. Th
wiki_exploit_scenario: |
    Per audit finding: In both processFailedEncryptedTx and decryptAndExecuteTx, there is insufficient error handling when deducting gas fees from an account with an insufficient balance. 1. When a transaction fails validat
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InsufficientErrorHandlingInGasDeductionForFailedTranX"
state_decl: "mapping(address => uint256) internal balances;"
trigger_sig_regex: ".*(processFailedEncryptedTx|decryptAndExecuteTx|handleGasConsumption).*"
required_sibling_regex: ".*(accrue|update|sync|validate|check|refresh).*"
target_interface_decl: "interface IT { function trigger(uint256) external; function sibling(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doStuff"
vuln_fn_params: "uint256 x"
trigger_call: "target.trigger(x)"
sibling_call: "target.sibling(x)"
post_trigger_body: "balances[msg.sender] = x;"
