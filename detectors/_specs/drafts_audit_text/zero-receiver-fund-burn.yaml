skeleton: "paired_function_divergence"
name: "zero-receiver-fund-burn"
class_name: "ZeroReceiverFundBurn"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "code4arena audit 2025-05-blackhole"
help: |
    0x15 Finding description and impact The RouterV2 implementation has a fundamental flaw where tokens are burned to the zero address instead of being sent to the intended recipient.
wiki_title: "Zero-receiver fund burn"
wiki_description: |
    0x15 Finding description and impact The RouterV2 implementation has a fundamental flaw where tokens are burned to the zero address instead of being sent to the intended recipient. The route struct includes a receiver field that defaults to address(0) when not explicitly set: struct route { address p
wiki_exploit_scenario: |
    Per audit finding: 0x15 Finding description and impact The RouterV2 implementation has a fundamental flaw where tokens are burned to the zero address instead of being sent to the intended recipient. The route struct inc
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "ZeroReceiverFundBurn"
state_decl: |
    mapping(address => uint256) internal balances;
        uint256 internal counter;
forward_verb: "add"
inverse_verb: "remove"
tracking_var_regex: ".*(balance|amount|total|supply|reserve).*"
forward_fn_name: "addThing"
inverse_fn_name: "removeThing"
fn_params: "address account"
forward_body: "balances[account] += 1; counter++;"
inverse_body_no_tracker: "balances[account] -= 1; /* counter missing */"
inverse_body_with_tracker: "balances[account] -= 1; counter--;"
