skeleton: "name_match_missing_call"
name: "insufficient-equilibrium-fee-pool-can-cause-swaps-to-fail"
class_name: "InsufficientEquilibriumFeePoolCanCauseSwapsToFail"
wave: "14"
severity: "MEDIUM"
confidence: "MEDIUM"
source: "zellic audit LayerZero Stargate - Zellic Audit Report"
help: |
    In the function swap, s.eqReward is subtracted from eqFeePool using SafeMath, which will revert if s.eqReward is larger than eqFeePool. This can happen either when eqFeePool is ver
wiki_title: "Insufficient equilibrium fee pool can cause swaps to fail"
wiki_description: |
    In the function swap, s.eqReward is subtracted from eqFeePool using SafeMath, which will revert if s.eqReward is larger than eqFeePool. This can happen either when eqFeePool is very low or a large amount of tokens are being swapped out. require(!stopSwap, "Stargate: swap func stopped"); ChainPath st
wiki_exploit_scenario: |
    Per audit finding: In the function swap, s.eqReward is subtracted from eqFeePool using SafeMath, which will revert if s.eqReward is larger than eqFeePool. This can happen either when eqFeePool is very low or a large amo
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "InsufficientEquilibriumFeePoolCanCauseSwapsToFail"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(eqReward|eqFeePool|stopSwap).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "eqReward"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
