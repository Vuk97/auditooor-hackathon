skeleton: "name_match_missing_require"
name: "function-getvalidatorindex-returns-index-of-first-validator--x"
class_name: "FunctionGetvalidatorindexReturnsIndexOfFirstValidatorX"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "zellic audit Mina Token Bridge EVM - Zellic Audit Report"
help: |
    The getValidatorIndex of the ValidatorManager Mina contract is intended to be used to check whether the passed public key is the public key of one of the three configured legitimat
wiki_title: "Function getValidatorIndex returns index of first validator for every public"
wiki_description: |
    The getValidatorIndex of the ValidatorManager Mina contract is intended to be used to check whether the passed public key is the public key of one of the three configured legitimate validators, and if so, which one. It returns the index of the legitimate validator, starting from 1, or 0 if the publi
wiki_exploit_scenario: |
    Per audit finding: The getValidatorIndex of the ValidatorManager Mina contract is intended to be used to check whether the passed public key is the public key of one of the three configured legitimate validators, and if
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "FunctionGetvalidatorindexReturnsIndexOfFirstValidatorX"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getValidatorIndex|compareValidators|getAndRequireEquals).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getValidatorIndex"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
