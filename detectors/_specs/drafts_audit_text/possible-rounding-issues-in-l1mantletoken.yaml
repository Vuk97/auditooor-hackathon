skeleton: "name_match_missing_call"
name: "possible-rounding-issues-in-l1mantletoken"
class_name: "PossibleRoundingIssuesInL1mantletoken"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Mantle - Zellic Audit Report"
help: |
    The mint function in L1MantleToken calculated the maximumMintAmount using the fol- lowing formula: uint256 maximumMintAmount = (totalSupply() * mintCapNumerator) / MINT_CAP_DENOMIN
wiki_title: "Possible rounding issues in L1MantleToken"
wiki_description: |
    The mint function in L1MantleToken calculated the maximumMintAmount using the fol- lowing formula: uint256 maximumMintAmount = (totalSupply() * mintCapNumerator) / MINT_CAP_DENOMINATOR; Below is the mint function: function mint(address _recipient, uint256 _amount) public onlyOwner { uint256 maximumM
wiki_exploit_scenario: |
    Per audit finding: The mint function in L1MantleToken calculated the maximumMintAmount using the fol- lowing formula: uint256 maximumMintAmount = (totalSupply() * mintCapNumerator) / MINT_CAP_DENOMINATOR; Below is the m
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "PossibleRoundingIssuesInL1mantletoken"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(maximumMintAmount|totalSupply|mintCapNumerator).*"
read_var_regex: ".*(balance|amount|total|supply|reserve).*"
required_call_regex: ".*(accrue|update|sync|validate|check|refresh).*"
guarded_helper_name: "_accrue"
vuln_fn_name: "maximumMintAmount"
vuln_fn_params: ""
vuln_fn_mutability: "internal"
vuln_fn_mutability_clean: "internal"
vuln_fn_return: "bool"
vuln_fn_body: "return balance > 0;"
