skeleton: "name_match_missing_require"
name: "the-signature-may-be-too-short"
class_name: "TheSignatureMayBeTooShort"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Bebop - Zellic Audit Report"
help: |
    There are no checks on the signature length in the getRsv function. This function is responsible for extracting the r/s/v values from a signature. The function is defined as follow
wiki_title: "The signature may be too short"
wiki_description: |
    There are no checks on the signature length in the getRsv function. This function is responsible for extracting the r/s/v values from a signature. The function is defined as follows: function getRsv(bytes memory sig) internal pure returns (bytes32, bytes32, uint8) { bytes32 r; bytes32 s; uint8 v; as
wiki_exploit_scenario: |
    Per audit finding: There are no checks on the signature length in the getRsv function. This function is responsible for extracting the r/s/v values from a signature. The function is defined as follows: function getRsv(b
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "TheSignatureMayBeTooShort"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(getRsv).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "getRsv"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
