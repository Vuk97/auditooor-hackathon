skeleton: "name_match_missing_require"
name: "missing-length-check"
class_name: "MissingLengthCheck"
wave: "14"
severity: "LOW"
confidence: "MEDIUM"
source: "zellic audit Plaza - Zellic Audit Report"
help: |
    In the _deposit and withdraw functions, there is no check that the lengths of the tokens array and the amounts array are equal. function _deposit(address[] memory tokens, uint256[]
wiki_title: "Missing length check"
wiki_description: |
    In the _deposit and withdraw functions, there is no check that the lengths of the tokens array and the amounts array are equal. function _deposit(address[] memory tokens, uint256[] memory amounts, address recipient) private checkDepositStarted checkDepositNotEnded { _checkCap(tokens, amounts); for (
wiki_exploit_scenario: |
    Per audit finding: In the _deposit and withdraw functions, there is no check that the lengths of the tokens array and the amounts array are equal. function _deposit(address[] memory tokens, uint256[] memory amounts, add
wiki_recommendation: "See source audit report for recommended fix."
contract_name: "MissingLengthCheck"
state_decl: "uint256 internal balance;"
fn_name_regex: ".*(checkDepositStarted|checkDepositNotEnded|safeTransferFrom).*"
write_var_regex: ".*(balance|amount|total|supply|reserve).*"
guard_var_regex: ".*(balance|amount|total|supply|reserve).*"
vuln_fn_name: "checkDepositStarted"
vuln_fn_params: "uint256 newVal"
vuln_fn_body_no_require: "balance = newVal;"
guard_require_line: "require(newVal <= 10000, \"cap\");"
