skeleton: "highlevelcall_missing_sibling"
name: "fractalprotocol-2026-05"
class_name: "Fractalprotocol202605"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "DeFiHackLabs PoC 2026-05/FractalProtocol_exp.sol"
help: "FractalProtocol 2026-05 — flashloan exploit (~$13.7K)"
wiki_title: "FractalProtocol (2026-05) — flashloan"
wiki_description: |
    Real-world exploit reproduced by DeFiHackLabs (2026-05): FractalProtocol lost ~$13.7K. Bug class: flashloan. Attack tx: 0x20db78913a51c3b3aece860ea142c240f3f8fa3b5bbf533a3d1d48eed857e10f.
wiki_exploit_scenario: |
    Attacker (0xe2acec13c6d1aaca584f827a22f2e4a02131e39a) exploits FractalProtocol via flashloan: the victim contract 0x80e1a981285181686a3951b05ded454734892a09 exposes a code path that can be triggered without the guard expected by the highlevelcall_missing_sibling skeleton.
wiki_recommendation: |
    Validate msg.sender against the trusted flash-loan provider inside the callback; require `initiator == address(this)`; never trust untrusted callers with flash-loan callback dispatch.
contract_name: "Fractalprotocol202605"
trigger_sig_regex: ".*(executeOperation).*"
required_sibling_regex: ".*(validateInitiator).*"
target_interface_decl: "interface IT { function executeOperation(uint256) external; function validateInitiator(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "doFlash"
vuln_fn_params: "uint256 x"
trigger_call: "target.executeOperation(x)"
sibling_call: "target.validateInitiator(x)"
post_trigger_body: "balances[msg.sender] = x;"
state_decl: "mapping(address => uint256) internal balances;"
