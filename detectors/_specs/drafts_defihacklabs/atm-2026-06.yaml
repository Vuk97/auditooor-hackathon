skeleton: "highlevelcall_missing_sibling"
name: "atm-2026-06"
class_name: "Atm202606"
wave: "14"
severity: "HIGH"
confidence: "MEDIUM"
source: "DeFiHackLabs PoC 2026-06/ATM_exp.sol"
help: "ATM 2026-06 — oracle exploit (~$243,543 USDT (gross drained from the ATM/USDT PancakeSwap pair))"
wiki_title: "ATM (2026-06) — oracle"
wiki_description: |
    Real-world exploit reproduced by DeFiHackLabs (2026-06): ATM lost ~$243,543 USDT (gross drained from the ATM/USDT PancakeSwap pair). Bug class: oracle. Attack tx: unknown.
wiki_exploit_scenario: |
    Attacker (unknown) exploits ATM via oracle: the victim contract (unknown) exposes a code path that can be triggered without the guard expected by the highlevelcall_missing_sibling skeleton.
wiki_recommendation: |
    Replace spot-price reads with a TWAP wrapper (e.g., Uniswap V3 OracleLibrary.consult) and require a minimum observation window; sanity-check prices against a reference oracle.
contract_name: "Atm202606"
trigger_sig_regex: ".*(getPrice).*"
required_sibling_regex: ".*(validatePrice).*"
target_interface_decl: "interface IT { function getPrice(uint256) external; function validatePrice(uint256) external; }"
target_iface_name: "IT"
vuln_fn_name: "readPrice"
vuln_fn_params: "uint256 x"
trigger_call: "target.getPrice(x)"
sibling_call: "target.validatePrice(x)"
post_trigger_body: "balances[msg.sender] = x;"
state_decl: "mapping(address => uint256) internal balances;"
