"""
contract-phinft1155-can-t-be-paused — generated from skeleton `name_match_missing_require`
Ported from: code4arena audit 2024-08-phi
Severity: MEDIUM
Pattern: function whose name matches the target regex writes a state var
matching the write regex, but does NOT contain a `require`/`assert` that
references the guard variable regex. Flag the function.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
Spec: detectors/_specs/contract-phinft1155-can-t-be-paused.yaml
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract, is_leaf_helper
from slither.core.cfg.node import NodeType
from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification


class ContractPhinft1155CanTBePaused(AbstractDetector):
    ARGUMENT = "contract-phinft1155-can-t-be-paused"
    HELP = "kuprum , also found by MrPotatoMagic Rhaydden hail_the_lord Gosho inh3l , and DigiSafe The pausing mechanism of PhiNFT1155 contract is implemented incorrectly and doesn’t work. Use"
    IMPACT = DetectorClassification.MEDIUM
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "Contract PhiNFT1155 can’t be paused"
    WIKI_DESCRIPTION = "kuprum , also found by MrPotatoMagic Rhaydden hail_the_lord Gosho inh3l , and DigiSafe The pausing mechanism of PhiNFT1155 contract is implemented incorrectly and doesn’t work. Users are still able to transfer NFTs in paused state. Summary Contract PhiNFT1155 inherits from the following parent contr"
    WIKI_EXPLOIT_SCENARIO = "Per audit finding: kuprum , also found by MrPotatoMagic Rhaydden hail_the_lord Gosho inh3l , and DigiSafe The pausing mechanism of PhiNFT1155 contract is implemented incorrectly and doesn’t work. Users are still able to"
    WIKI_RECOMMENDATION = "See source audit report for recommended fix."

    _FN_NAME_REGEX = re.compile(r".*(setProtocolFeeDestination|initialize|test_claim_1155_with_ref).*", re.IGNORECASE)
    _WRITE_VAR_REGEX = re.compile(r".*(test_claim_1155_with_ref).*", re.IGNORECASE)
    _GUARD_VAR_REGEX = re.compile(r".*(test_claim_1155_with_ref).*", re.IGNORECASE)

    def _detect(self):
        results = []
        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue
            for f in c.functions_and_modifiers_declared:
                if not self._FN_NAME_REGEX.search(f.name):
                    continue
                if is_leaf_helper(f):
                    continue

                writes_target = False
                for sv in f.state_variables_written:
                    if self._WRITE_VAR_REGEX.search(sv.name):
                        writes_target = True
                        break
                if not writes_target:
                    continue

                has_guard = False
                for node in f.nodes:
                    if node.type not in (NodeType.IF, NodeType.EXPRESSION):
                        continue
                    if not node.contains_require_or_assert():
                        continue
                    expr_text = str(node.expression) if node.expression else ""
                    if self._GUARD_VAR_REGEX.search(expr_text):
                        has_guard = True
                        break

                if has_guard:
                    continue

                info = [
                    f,
                    f" — contract-phinft1155-can-t-be-paused: writes `.*(test_claim_1155_with_ref).*`-matching state without a `require` on `.*(test_claim_1155_with_ref).*`. ",
                    f"Ported from code4arena audit 2024-08-phi.",
                ]
                results.append(self.generate_result(info))
        return results
