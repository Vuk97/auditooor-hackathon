"""
refunds-sent-to-incorrect-addresses-in-certain-cases — generated from skeleton `name_match_missing_require`
Ported from: code4arena audit 2024-08-phi
Severity: MEDIUM
Pattern: function whose name matches the target regex writes a state var
matching the write regex, but does NOT contain a `require`/`assert` that
references the guard variable regex. Flag the function.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
Spec: detectors/_specs/refunds-sent-to-incorrect-addresses-in-certain-cases.yaml
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract, is_leaf_helper
from slither.core.cfg.node import NodeType
from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification


class RefundsSentToIncorrectAddressesInCertainCases(AbstractDetector):
    ARGUMENT = "refunds-sent-to-incorrect-addresses-in-certain-cases"
    HELP = "0xCiphky , also found by MrPotatoMagic pep7siup aldarion hgrano McToady , TECHFUND-inc ( 1 2 ), pipidu83 13u9 , typicalHuman ( 1 2 ), 0xc0ffEE ( 1 2 ), DanielArmstrong ( 1 2 ), Fit"
    IMPACT = DetectorClassification.MEDIUM
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "Refunds sent to incorrect addresses in certain cases"
    WIKI_DESCRIPTION = "0xCiphky , also found by MrPotatoMagic pep7siup aldarion hgrano McToady , TECHFUND-inc ( 1 2 ), pipidu83 13u9 , typicalHuman ( 1 2 ), 0xc0ffEE ( 1 2 ), DanielArmstrong ( 1 2 ), Fitro th3l1ghtd3m0n ironside , farismaulana ( 1 2 ), smaul eLSeR17 federodes robertodf99 , CAUsr ( 1 2 ), OpaBatyo ( 1 2 ),"
    WIKI_EXPLOIT_SCENARIO = "Per audit finding: 0xCiphky , also found by MrPotatoMagic pep7siup aldarion hgrano McToady , TECHFUND-inc ( 1 2 ), pipidu83 13u9 , typicalHuman ( 1 2 ), 0xc0ffEE ( 1 2 ), DanielArmstrong ( 1 2 ), Fitro th3l1ghtd3m0n iro"
    WIKI_RECOMMENDATION = "See source audit report for recommended fix."

    _FN_NAME_REGEX = re.compile(r".*(handleRewardsAndGetValueSent|withdrawFor|updateArtSettings|depositBatch|withdraw|typicalHuman|eLSeR17|claimFromFactory).*", re.IGNORECASE)
    _WRITE_VAR_REGEX = re.compile(r".*(balance|amount|total|supply|reserve).*", re.IGNORECASE)
    _GUARD_VAR_REGEX = re.compile(r".*(balance|amount|total|supply|reserve).*", re.IGNORECASE)

    def _detect(self):
        results = []
        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue
            for f in c.functions_and_modifiers_declared:
                if not self._FN_NAME_REGEX.search(f.name):
                    continue
                if is_leaf_helper(f):
                    continue

                writes_target = False
                for sv in f.state_variables_written:
                    if self._WRITE_VAR_REGEX.search(sv.name):
                        writes_target = True
                        break
                if not writes_target:
                    continue

                has_guard = False
                for node in f.nodes:
                    if node.type not in (NodeType.IF, NodeType.EXPRESSION):
                        continue
                    if not node.contains_require_or_assert():
                        continue
                    expr_text = str(node.expression) if node.expression else ""
                    if self._GUARD_VAR_REGEX.search(expr_text):
                        has_guard = True
                        break

                if has_guard:
                    continue

                info = [
                    f,
                    f" — refunds-sent-to-incorrect-addresses-in-certain-cases: writes `.*(balance|amount|total|supply|reserve).*`-matching state without a `require` on `.*(balance|amount|total|supply|reserve).*`. ",
                    f"Ported from code4arena audit 2024-08-phi.",
                ]
                results.append(self.generate_result(info))
        return results
