"""
input-validation-improvement-in-subnetactormanagerfacet-pref-x — generated from skeleton `name_match_missing_require`
Ported from: code4arena audit 2025-02-recall
Severity: LOW
Pattern: function whose name matches the target regex writes a state var
matching the write regex, but does NOT contain a `require`/`assert` that
references the guard variable regex. Flag the function.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
Spec: detectors/_specs/input-validation-improvement-in-subnetactormanagerfacet-pref-x.yaml
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract, is_leaf_helper
from slither.core.cfg.node import NodeType
from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification


class InputValidationImprovementInSubnetactormanagerfacetPrefX(AbstractDetector):
    ARGUMENT = "input-validation-improvement-in-subnetactormanagerfacet-pref-x"
    HELP = "[L-01] Input validation improvement in @SubnetActorManagerFacet.preFund() preFund in SubnetActorManagerFacet.sol validates that the amount is not zero but does not ensure it matche"
    IMPACT = DetectorClassification.LOW
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "Input validation improvement in @SubnetActorManagerFacet.preFund() preFund"
    WIKI_DESCRIPTION = "[L-01] Input validation improvement in @SubnetActorManagerFacet.preFund() preFund in SubnetActorManagerFacet.sol validates that the amount is not zero but does not ensure it matches the value sent with the transaction (msg.value). Impact This mismatch enables accounting discrepancies where the recor"
    WIKI_EXPLOIT_SCENARIO = "Per audit finding: [L-01] Input validation improvement in @SubnetActorManagerFacet.preFund() preFund in SubnetActorManagerFacet.sol validates that the amount is not zero but does not ensure it matches the value sent wit"
    WIKI_RECOMMENDATION = "See source audit report for recommended fix."

    _FN_NAME_REGEX = re.compile(r".*(leave|submitCheckpoint|preFund|supplySource|genesisBalance).*", re.IGNORECASE)
    _WRITE_VAR_REGEX = re.compile(r".*(balance|amount|total|supply|reserve).*", re.IGNORECASE)
    _GUARD_VAR_REGEX = re.compile(r".*(balance|amount|total|supply|reserve).*", re.IGNORECASE)

    def _detect(self):
        results = []
        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue
            for f in c.functions_and_modifiers_declared:
                if not self._FN_NAME_REGEX.search(f.name):
                    continue
                if is_leaf_helper(f):
                    continue

                writes_target = False
                for sv in f.state_variables_written:
                    if self._WRITE_VAR_REGEX.search(sv.name):
                        writes_target = True
                        break
                if not writes_target:
                    continue

                has_guard = False
                for node in f.nodes:
                    if node.type not in (NodeType.IF, NodeType.EXPRESSION):
                        continue
                    if not node.contains_require_or_assert():
                        continue
                    expr_text = str(node.expression) if node.expression else ""
                    if self._GUARD_VAR_REGEX.search(expr_text):
                        has_guard = True
                        break

                if has_guard:
                    continue

                info = [
                    f,
                    f" — input-validation-improvement-in-subnetactormanagerfacet-pref-x: writes `.*(balance|amount|total|supply|reserve).*`-matching state without a `require` on `.*(balance|amount|total|supply|reserve).*`. ",
                    f"Ported from code4arena audit 2025-02-recall.",
                ]
                results.append(self.generate_result(info))
        return results
