"""
erc-20-allowance-bypass-spender-can-force-sender-to-pay-extr-x — generated from skeleton `highlevelcall_missing_sibling`
Ported from: code4arena audit 2025-01-next-generation
Severity: MEDIUM
Pattern: a function makes a HighLevelCall to an external function matching
the trigger signature regex, but does NOT also make a sibling call
matching the required sibling signature regex in the same function body.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract

from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification
from slither.slithir.operations import HighLevelCall
from slither.core.declarations import Function


class Erc20AllowanceBypassSpenderCanForceSenderToPayExtrX(AbstractDetector):
    ARGUMENT = "erc-20-allowance-bypass-spender-can-force-sender-to-pay-extr-x"
    HELP = "dd0x7e8 , also found by 0xAadi 0xgh0stcybers3c Daniel526 deeney djshan_eden JCN mxteem Roger , and unnamed Finding description and impact The EURFToken contract includes a transact"
    IMPACT = DetectorClassification.MEDIUM
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "ERC-20 allowance bypass: spender can force sender to pay extra fees beyond approved amount"
    WIKI_DESCRIPTION = "dd0x7e8 , also found by 0xAadi 0xgh0stcybers3c Daniel526 deeney djshan_eden JCN mxteem Roger , and unnamed Finding description and impact The EURFToken contract includes a transaction fee mechanism where a percentage of the transferred amount is deducted as a fee. This fee is deducted from the sende"
    WIKI_EXPLOIT_SCENARIO = "Per audit finding: dd0x7e8 , also found by 0xAadi 0xgh0stcybers3c Daniel526 deeney djshan_eden JCN mxteem Roger , and unnamed Finding description and impact The EURFToken contract includes a transaction fee mechanism wh"
    WIKI_RECOMMENDATION = "See source audit report for recommended fix."

    _TRIGGER_SIG_REGEX = re.compile(r".*(transferFrom|transferSanity|adminSanity).*", re.IGNORECASE)
    _REQUIRED_SIBLING_REGEX = re.compile(r".*(accrue|update|sync|validate|check|refresh).*", re.IGNORECASE)

    def _detect(self):
        results = []
        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue
            for f in c.functions_and_modifiers_declared:
                triggers = []
                siblings = []
                for hc_tuple in f.high_level_calls:
                    hc = hc_tuple[1] if isinstance(hc_tuple, tuple) else hc_tuple
                    if not isinstance(hc, HighLevelCall):
                        continue
                    if not isinstance(hc.function, Function):
                        continue
                    sig = hc.function.solidity_signature or hc.function.name
                    if self._TRIGGER_SIG_REGEX.search(sig):
                        triggers.append(sig)
                    if self._REQUIRED_SIBLING_REGEX.search(sig):
                        siblings.append(sig)
                if triggers and not siblings:
                    info = [
                        f,
                        f" — erc-20-allowance-bypass-spender-can-force-sender-to-pay-extr-x: calls `{{{{}}}}` without also calling a `.*(accrue|update|sync|validate|check|refresh).*` sibling. ",
                        f"Ported from code4arena audit 2025-01-next-generation.",
                    ]
                    results.append(self.generate_result(info))
        return results
