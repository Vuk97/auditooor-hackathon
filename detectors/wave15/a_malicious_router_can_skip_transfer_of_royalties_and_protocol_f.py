"""
a-malicious-router-can-skip-transfer-of-royalties-and-protocol-f — generated from skeleton `paired_function_divergence`
Ported from: Solodit #18303 (Spearbit/Sudoswap LSSVM2)
Severity: MEDIUM
Pattern: `add<Stem>` (or verb<Stem>) paired with `remove<Stem>` (or anti<Stem>)
         where the forward verb writes to a tracking state variable
         matching the tracking regex, but the inverse verb does NOT write
         to it. Flag the inverse function.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract

from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification


class AMaliciousRouterCanSkipTransferOfRoyaltiesAndProtocolF(AbstractDetector):
    ARGUMENT = "a-malicious-router-can-skip-transfer-of-royalties-and-protocol-f"
    HELP = "A malicious router can skip transfer of royalties and protocol fee"
    IMPACT = DetectorClassification.MEDIUM
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "A malicious router can skip transfer of royalties and protocol fee"
    WIKI_DESCRIPTION = "## Security Advisory\n\n## Severity: Medium Risk\n\n### Context\n- **File**: LSSVMPairERC20.sol\n- **Lines**: L59-L91\n\n### Description\nA malicious router, if accidentally or intentionally whitelisted by the protocol, may implement `pair-TransferERC20From()` functions which do not actually transfer the num"
    WIKI_EXPLOIT_SCENARIO = "Per Solodit #18303: ## Security Advisory\n\n## Severity: Medium Risk\n\n### Context\n- **File**: LSSVMPairERC20.sol\n- **Lines**: L59-L91\n\n### Description\nA malicious router, if accidentally or intentionally whitelisted by the"
    WIKI_RECOMMENDATION = "See source audit report for recommended fix."

    _FORWARD_VERB = r"add"
    _INVERSE_VERB = r"remove"
    _TRACKING_VAR_REGEX = re.compile(r".*(protocolFee|saleAmount).*", re.IGNORECASE)

    def _detect(self):
        results = []
        fwd_re = re.compile(rf"^{self._FORWARD_VERB}(.*)$", re.IGNORECASE)
        inv_re = re.compile(rf"^{self._INVERSE_VERB}(.*)$", re.IGNORECASE)

        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue

            # Bucket functions by stem (the part after the verb prefix).
            forwards = {}  # stem -> fn
            inverses = {}
            for f in c.functions_and_modifiers_declared:
                m = fwd_re.match(f.name)
                if m:
                    forwards[m.group(1).lower()] = f
                m = inv_re.match(f.name)
                if m:
                    inverses[m.group(1).lower()] = f

            for stem, fwd in forwards.items():
                inv = inverses.get(stem)
                if inv is None:
                    continue

                fwd_writes_tracker = any(
                    self._TRACKING_VAR_REGEX.search(sv.name)
                    for sv in fwd.state_variables_written
                )
                if not fwd_writes_tracker:
                    continue

                inv_writes_tracker = any(
                    self._TRACKING_VAR_REGEX.search(sv.name)
                    for sv in inv.state_variables_written
                )
                if inv_writes_tracker:
                    continue

                info = [
                    inv,
                    f" — a-malicious-router-can-skip-transfer-of-royalties-and-protocol-f: inverse function does not write the tracking state "
                    f"variable that its forward counterpart ({{{{}}}}) writes. ",
                    f"Ported from Solodit #18303 (Spearbit/Sudoswap LSSVM2).",
                ]
                info[1] = info[1].format()
                info = [inv, f" — a-malicious-router-can-skip-transfer-of-royalties-and-protocol-f: inverse `", inv.name,
                        "` does not write tracking var that forward `", fwd.name,
                        "` writes.", ]
                results.append(self.generate_result(info))
        return results
