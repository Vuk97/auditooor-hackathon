"""
a-malicious-proposer-can-update-proposal-past-inattentive-voters — generated from skeleton `state_write_without_paired_write`
Ported from: Solodit #21325 (Spearbit/Nouns DAO)
Severity: MEDIUM
Pattern: a function whose name matches the target regex writes a state
variable matching the primary regex but does NOT write a second state
variable matching the paired regex in the same function body.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract

from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification


class AMaliciousProposerCanUpdateProposalPastInattentiveVoters(AbstractDetector):
    ARGUMENT = "a-malicious-proposer-can-update-proposal-past-inattentive-voters"
    HELP = "A malicious proposer can update proposal past inattentive voters to sneak in otherwise unacceptable details"
    IMPACT = DetectorClassification.MEDIUM
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "A malicious proposer can update proposal past inattentive voters to sneak in otherwise unacceptable details"
    WIKI_DESCRIPTION = "## Medium Risk Security Issue\n\n## Context\n- **Affected Files:**\n  - `NounsDAOV3Proposals.sol` (Lines: 269-423)\n  - `NounsDAOV3Admin.sol` (Line: 118)\n  - `NounsDAOV3Votes.sol` (Lines: 70-293)\n\n## Description\nUpdatable proposal description and transactions is a new feature being introduced in V3 to im"
    WIKI_EXPLOIT_SCENARIO = "Per Solodit #21325: ## Medium Risk Security Issue\n\n## Context\n- **Affected Files:**\n  - `NounsDAOV3Proposals.sol` (Lines: 269-423)\n  - `NounsDAOV3Admin.sol` (Line: 118)\n  - `NounsDAOV3Votes.sol` (Lines: 70-293)\n\n## Descr"
    WIKI_RECOMMENDATION = "See source audit report for recommended fix."

    _FN_NAME_REGEX = re.compile(r".*(updateProposal|proposal).*", re.IGNORECASE)
    _PRIMARY_VAR_REGEX = re.compile(r".*(proposal|payload|hash).*", re.IGNORECASE)
    _PAIRED_VAR_REGEX = re.compile(r".*(notice|invalidate|voter).*", re.IGNORECASE)

    def _detect(self):
        results = []
        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue
            for f in c.functions_and_modifiers_declared:
                if not self._FN_NAME_REGEX.search(f.name):
                    continue

                writes_primary = False
                writes_paired = False
                for sv in f.state_variables_written:
                    if self._PRIMARY_VAR_REGEX.search(sv.name):
                        writes_primary = True
                    if self._PAIRED_VAR_REGEX.search(sv.name):
                        writes_paired = True

                if writes_primary and not writes_paired:
                    info = [
                        f,
                        f" — a-malicious-proposer-can-update-proposal-past-inattentive-voters: writes `.*(proposal|payload|hash).*`-matching var but not `.*(notice|invalidate|voter).*`-matching paired state. ",
                        f"Ported from Solodit #21325 (Spearbit/Nouns DAO).",
                    ]
                    results.append(self.generate_result(info))
        return results
