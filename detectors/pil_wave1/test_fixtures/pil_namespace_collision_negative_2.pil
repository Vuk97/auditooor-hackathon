// Negative fixture 2: single namespace, no collision possible.
namespace SingleChip(2**18);
    col commit input_val;
    col commit output_val;
    col commit carry;
    col fixed step;

    // All columns are unique; no cross-namespace collision.
    carry * (1 - carry) = 0;
    output_val = input_val + carry;
