"""
oracle-timestamp-unchecked — generated from skeleton `name_match_missing_require`
Ported from: corpus slice slice_aa
Severity: HIGH
Pattern: function whose name matches the target regex writes a state var
matching the write regex, but does NOT contain a `require`/`assert` that
references the guard variable regex. Flag the function.

Generated by tools/gen-detector.py — DO NOT EDIT BY HAND.
Spec: detectors/_specs/oracle-timestamp-unchecked.yaml
"""

import re
import sys
from pathlib import Path as _Path

sys.path.insert(0, str(_Path(__file__).resolve().parent.parent))
from _template_utils import is_vendored_or_test_contract, is_leaf_helper
from slither.core.cfg.node import NodeType
from slither.detectors.abstract_detector import AbstractDetector, DetectorClassification


class OracleTimestampUnchecked(AbstractDetector):
    ARGUMENT = "oracle-timestamp-unchecked"
    HELP = "DIA oracle `getValue` returns `(price, timestamp)` but timestamp is ignored; stale prices used for collateral/liquidation."
    IMPACT = DetectorClassification.HIGH
    CONFIDENCE = DetectorClassification.MEDIUM
    WIKI = "https://github.com/Vuk97/auditooor/blob/main/reference/bug_patterns_observed.md"
    WIKI_TITLE = "oracle-timestamp-unchecked"
    WIKI_DESCRIPTION = "DIA oracle `getValue` returns `(price, timestamp)` but timestamp is ignored; stale prices used for collateral/liquidation."
    WIKI_EXPLOIT_SCENARIO = "Attacker exploits oracle-timestamp-unchecked as described: DIA oracle `getValue` returns `(price, timestamp)` but timestamp is ignored; stale prices used for collateral/liquidation."
    WIKI_RECOMMENDATION = "`HighLevelCall` to DIA/Band oracle where return value tuple has second element (timestamp) discarded (tuple unpacking with `(price, )` pattern)."

    _FN_NAME_REGEX = re.compile(r".*(getValue|HighLevelCall).*", re.IGNORECASE)
    _WRITE_VAR_REGEX = re.compile(r".*(getValue).*", re.IGNORECASE)
    _GUARD_VAR_REGEX = re.compile(r".*(getValue).*", re.IGNORECASE)

    def _detect(self):
        results = []
        for c in self.contracts:
            if is_vendored_or_test_contract(c):
                continue
            for f in c.functions_and_modifiers_declared:
                if not self._FN_NAME_REGEX.search(f.name):
                    continue
                if is_leaf_helper(f):
                    continue

                writes_target = False
                for sv in f.state_variables_written:
                    if self._WRITE_VAR_REGEX.search(sv.name):
                        writes_target = True
                        break
                if not writes_target:
                    continue

                has_guard = False
                for node in f.nodes:
                    if node.type not in (NodeType.IF, NodeType.EXPRESSION):
                        continue
                    if not node.contains_require_or_assert():
                        continue
                    expr_text = str(node.expression) if node.expression else ""
                    if self._GUARD_VAR_REGEX.search(expr_text):
                        has_guard = True
                        break

                if has_guard:
                    continue

                info = [
                    f,
                    f" — oracle-timestamp-unchecked: writes `.*(getValue).*`-matching state without a `require` on `.*(getValue).*`. ",
                    f"Ported from corpus slice slice_aa.",
                ]
                results.append(self.generate_result(info))
        return results
